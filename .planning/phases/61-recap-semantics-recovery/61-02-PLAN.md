---
phase: 61-recap-semantics-recovery
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/screens/admin/admin_reports_screen.dart
  - test/screens/admin/admin_reports_policy_recap_test.dart
  - test/screens/admin/rekap_harian_test.dart
autonomous: true
requirements: ["RECAP-05", "RECAP-06", "RECAP-07"]
must_haves:
  truths:
    - "The active Rekap Harian surface shows usable day rows again, not only the payroll matrix."
    - "Pending recap states come from canonical merged recap signals, not raw-log heuristics."
    - "Fallback rows remain visibly honest while strict rows keep their richer signals."
  artifacts:
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Admin recap screen wired to the canonical merged recap dataset service with row filters and compatibility disclosure"
      contains: "_buildRekapHarian"
    - path: "test/screens/admin/admin_reports_policy_recap_test.dart"
      provides: "Helper and row-widget regression coverage for strict and compatibility recap rows"
      contains: "PolicyRecapTile"
    - path: "test/screens/admin/rekap_harian_test.dart"
      provides: "Real recap-tab widget coverage replacing the old placeholder stubs"
      contains: "Rekap Harian"
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/admin_policy_recap_dataset_service.dart"
      via: "the screen must load canonical merged recap rows instead of carrying its own merge contract"
      pattern: "AdminPolicyRecapDatasetService"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/legacy_payroll_recap_fallback_service.dart"
      via: "compatibility disclosure remains driven by actual fallback injection, not hard-coded copy alone"
      pattern: "isCompatibilityMode"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "test/screens/admin/rekap_harian_test.dart"
      via: "the active recap tab must prove row visibility, pending filters, and mixed strict-plus-fallback continuity"
      pattern: "_buildRekapHarian"
---

<objective>
Restore the active admin Rekap Harian experience on top of the canonical merged recap dataset.

Purpose: the repo already has row-level recap helpers, but the active tab is currently payroll-matrix-first and the placeholder recap tests never evolved into real behavior coverage. This plan makes the admin recap day-row surface active again, keeps pending semantics truthful, and preserves strict versus fallback differences visibly.
Output: a row-first Rekap Harian tab driven by merged `AttendancePolicyRecapDay` rows, plus real widget coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-recap-semantics-recovery/61-RESEARCH.md
@.planning/phases/61-recap-semantics-recovery/61-VALIDATION.md
@.planning/phases/61-recap-semantics-recovery/61-01-PLAN.md
@lib/screens/admin/admin_reports_screen.dart
@lib/services/admin_policy_recap_dataset_service.dart
@lib/services/attendance_policy_recap_service.dart
@lib/models/attendance_policy_recap_day.dart
@test/screens/admin/admin_reports_policy_recap_test.dart
@test/screens/admin/rekap_harian_test.dart

<locked_decisions>
D-04: The active Rekap Harian tab must be row-first and recap-first. Spreadsheet and payroll PDF remain available, but they are supporting salary outputs rather than the primary admin recap content in this phase.
D-05: Pending recap semantics must be derived from canonical merged recap signals such as `belum_absen_pulang` and `active_incomplete`, not from raw-log shift heuristics.
D-06: Schedule-gap notifications and outlet follow-up notices remain out of scope for this phase and belong to Phase 62.
</locked_decisions>

<interfaces>
The active recap tab must consume:
```dart
AdminPolicyRecapDatasetResult result = adminPolicyRecapDatasetService.build(...);
```

Required recap filter chips:
```text
Semua
Terlambat
Kurang jam kerja
Istirahat berlebih
Lembur
Tidak Hadir
Manager exempt
Hadir Tanpa Jadwal
Belum absen pulang
```

Required compatibility disclosure:
```text
Mode kompatibilitas aktif
Sebagian hari admin recap dipulihkan dari log absensi dan kontrak karena jadwal belum tersedia.
```
The exact body text above should be used for the recap surface so operators can distinguish it from the salary-facing payroll banner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire the active Rekap Harian tab to the merged recap-row dataset</name>
  <files>lib/screens/admin/admin_reports_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/admin_policy_recap_dataset_service.dart
    - lib/models/attendance_policy_recap_day.dart
    - test/screens/admin/admin_reports_policy_recap_test.dart
    - .planning/phases/61-recap-semantics-recovery/61-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: when merged recap rows exist, the active tab shows day rows rather than only a payroll matrix.
    - Test 2: a mixed strict-plus-fallback dataset shows both rows, with strict rows preserving their richer signals and fallback rows carrying honest no-schedule copy.
    - Test 3: the tab can filter to pending recap rows (`belumAbsenPulang` and `activeIncomplete`) without relying on raw-log open-shift heuristics.
    - Test 4: the compatibility banner appears only when fallback rows were injected.
  </behavior>
  <action>
    Restore the admin recap surface per D-04 through D-06.

    Required behavior:
    1. Update `_loadDailySummaryData()` so the screen stores merged admin recap rows from `AdminPolicyRecapDatasetService` in dedicated state, not just `_payrollMatrixDataset`.
    2. The active `_buildRekapHarian()` path must render merged `AttendancePolicyRecapDay` rows as the primary content for the tab.
    3. Reuse the existing recap primitives already in this file:
       - `PolicyRecapFilter`
       - `filterPolicyRecapRows(...)`
       - `buildPolicyRecapReasonCopy(...)`
       - `PolicyRecapTile`
       Update them only where needed to support the now-active mixed strict-plus-fallback flow.
    4. Add a compact recap-level compatibility banner with this exact copy when fallback rows exist:
       - title: `Mode kompatibilitas aktif`
       - body: `Sebagian hari admin recap dipulihkan dari log absensi dan kontrak karena jadwal belum tersedia.`
    5. Surface pending recap semantics from merged recap rows by using canonical signal-based filters and counts for:
       - `belumAbsenPulang`
       - `activeIncomplete`
       These may share one summary bucket, but the underlying filter behavior must stay distinct.
    6. Preserve access to salary-facing exports, but do not let the payroll matrix remain the only recap content in this tab.
    7. Do not add schedule-gap notice UI, outlet notification feeds, or dashboard alert logic here.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart test/screens/admin/rekap_harian_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `_loadDailySummaryData()` stores merged recap rows from `AdminPolicyRecapDatasetService`
    - `_buildRekapHarian()` renders `AttendancePolicyRecapDay` rows as primary recap content
    - the active recap surface exposes the locked filter chips
    - the recap compatibility banner uses the exact locked copy above
    - pending recap states are derived from recap signals, not `_loadOpenShifts()`
    - mixed strict-plus-fallback rows remain visible together on the active tab
  </acceptance_criteria>
  <done>The active Rekap Harian tab is now a truthful day-row recap surface instead of a payroll-matrix-only shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the placeholder recap tests with real mixed strict-plus-fallback widget coverage</name>
  <files>test/screens/admin/admin_reports_policy_recap_test.dart, test/screens/admin/rekap_harian_test.dart</files>
  <read_first>
    - test/screens/admin/admin_reports_policy_recap_test.dart
    - test/screens/admin/rekap_harian_test.dart
    - lib/screens/admin/admin_reports_screen.dart
    - lib/models/attendance_policy_recap_day.dart
    - .planning/phases/61-recap-semantics-recovery/61-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: row filters still isolate `managerExempt`, `hadirTanpaJadwal`, and `belumAbsenPulang` correctly in a mixed dataset.
    - Test 2: the active recap surface shows the compatibility banner only when fallback rows exist.
    - Test 3: a strict overnight row keeps its strict signal while a fallback row stays honest and no-schedule.
    - Test 4: the pending recap bucket shows rows backed by `belumAbsenPulang` or `activeIncomplete`.
  </behavior>
  <action>
    Turn recap coverage into real behavior tests.

    Required behavior:
    1. Extend `test/screens/admin/admin_reports_policy_recap_test.dart` with mixed strict-plus-fallback helper and widget assertions, including:
       - `hadirTanpaJadwal`
       - `belumAbsenPulang`
       - `activeIncomplete`
       - strict rows that still show richer signals such as `late` or `excessBreak`
    2. Replace the placeholder assertions in `test/screens/admin/rekap_harian_test.dart` with real widget tests that pump the active recap surface and assert:
       - day rows are visible
       - the compatibility banner appears only when fallback rows exist
       - pending recap rows remain visible and filterable
       - no-schedule rows do not claim fabricated lateness or absence
    3. Keep the tests local to the admin recap surface. They must not require live Supabase state.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart test/screens/admin/rekap_harian_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/admin/rekap_harian_test.dart` no longer contains only placeholder `expect(true, isTrue)` coverage
    - `test/screens/admin/admin_reports_policy_recap_test.dart` covers mixed strict-plus-fallback recap rows
    - the widget tests assert the recap compatibility banner copy
    - the widget tests assert pending recap visibility or filtering
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart test/screens/admin/rekap_harian_test.dart` exits 0
  </acceptance_criteria>
  <done>The recap tab now has real regression coverage for the exact mixed strict-plus-fallback behavior Phase 61 promises.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart test/screens/admin/rekap_harian_test.dart` passes.
- the active recap tab renders merged day rows instead of relying on a payroll-matrix-only shell.
- pending recap states and compatibility disclosure are driven by canonical merged recap rows.
</verification>

<success_criteria>
- Admin recap once again shows usable daily rows for legacy no-schedule attendance data.
- Compatibility rows remain honest and visibly distinct.
- Strict rows keep their richer signals in the same recap surface.
</success_criteria>

<output>
After completion, create `.planning/phases/61-recap-semantics-recovery/61-02-SUMMARY.md`
</output>
