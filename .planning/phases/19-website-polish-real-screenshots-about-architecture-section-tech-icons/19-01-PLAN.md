---
phase: 19-website-polish-real-screenshots-about-architecture-section-tech-icons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - absenkok-website/src/assets/images/enakko-hero.png
  - absenkok-website/src/assets/images/login-success.png
  - absenkok-website/src/assets/images/monitoring-screen.png
  - absenkok-website/src/assets/images/attendance-selection.png
  - absenkok-website/src/assets/images/team-hands.png
  - absenkok-website/src/components/Hero.astro
  - absenkok-website/src/components/HowItWorks.astro
autonomous: true
requirements:
  - WEB-P01
  - WEB-P02

must_haves:
  truths:
    - "Hero section shows a real app screenshot (enakko-hero.png optimized to WebP), not the 90-line CSS tablet mockup"
    - "Each HowItWorks step has a companion screenshot from the actual app below its text"
    - "All 5 images are in src/assets/images/ with kebab-case names, originals deleted from public/"
    - "All images served as WebP via Astro <Image> component, no raw PNGs in dist/"
  artifacts:
    - path: "src/assets/images/enakko-hero.png"
      provides: "Hero screenshot source"
    - path: "src/assets/images/login-success.png"
      provides: "HowItWorks step companion screenshot"
    - path: "src/assets/images/monitoring-screen.png"
      provides: "HowItWorks step companion screenshot"
    - path: "src/assets/images/attendance-selection.png"
      provides: "HowItWorks step companion screenshot"
    - path: "src/assets/images/team-hands.png"
      provides: "About section illustration (used in Plan 02)"
    - path: "src/components/Hero.astro"
      provides: "Hero section with real <Image> instead of CSS mockup"
      contains: "import { Image } from 'astro:assets'"
    - path: "src/components/HowItWorks.astro"
      provides: "How it works with screenshot companions per step"
      contains: "import { Image } from 'astro:assets'"
  key_links:
    - from: "src/components/Hero.astro"
      to: "src/assets/images/enakko-hero.png"
      via: "ESM import + Astro <Image>"
      pattern: "import.*enakko-hero"
    - from: "src/components/HowItWorks.astro"
      to: "src/assets/images/*.png"
      via: "ESM import + Astro <Image>"
      pattern: "import.*from.*assets/images"
---

<objective>
Replace the CSS tablet mockup in Hero with a real app screenshot, and add real screenshot companions to each HowItWorks step. Move all images from public/ to src/assets/images/ with kebab-case names for Astro's <Image> optimization (17MB PNG → ~253KB WebP).

Purpose: The website currently uses a CSS-drawn grid mockup and has no real app visuals. Real screenshots make the product tangible and credible.
Output: Optimized hero image, 3 step-companion screenshots in HowItWorks, all images pipeline-ready.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-website-polish-real-screenshots-about-architecture-section-tech-icons/19-RESEARCH.md

**CRITICAL: Website repo is at `C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\`**
This is a SEPARATE repo from the current working directory (Flutter app). ALL file operations must target the website repo path.

<interfaces>
<!-- Key patterns from existing codebase — executor should follow these exactly -->

From src/styles/global.css (Tailwind v4 theme):
```css
--color-brand-navy: #384259;
--color-brand-pink: #F73859;
--color-brand-teal: #7AC7C4;
--color-brand-mint: #C4EDDE;
--color-brand-surface: #F0FAF5;
--animate-float: float 6s ease-in-out infinite;
```

From src/components/Hero.astro (current — to be replaced):
- Lines 46-135: CSS tablet mockup (RIGHT COLUMN) — DELETE entirely
- Lines 14-44: Left column text content — KEEP unchanged
- Lines 5-9: Decorative blurs — KEEP unchanged
- Lines 47-49: Float animation wrapper — KEEP pattern

From src/components/HowItWorks.astro (current — to be enhanced):
- 3 steps array in frontmatter (Pasang Tablet, Tap Kartu NFC, Pantau & Kelola)
- Each step renders: number badge → h3 title → p description
- Section uses bg-white, staggered fade-in-up animations
- Grid: grid-cols-1 md:grid-cols-3 gap-12
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate images + rewrite Hero with real screenshot</name>
  <files>
    absenkok-website/public/enakko hero.png (delete after copy)
    absenkok-website/public/gambar berhasil login.png (delete after copy)
    absenkok-website/public/moonitoing tangan.png (delete after copy)
    absenkok-website/public/pilih absen otmatis tangan.png (delete after copy)
    absenkok-website/public/tangan bersama.png (delete after copy)
    absenkok-website/src/assets/images/enakko-hero.png (create)
    absenkok-website/src/assets/images/login-success.png (create)
    absenkok-website/src/assets/images/monitoring-screen.png (create)
    absenkok-website/src/assets/images/attendance-selection.png (create)
    absenkok-website/src/assets/images/team-hands.png (create)
    absenkok-website/src/components/Hero.astro (rewrite)
  </files>
  <action>
**Step 1: Move and rename images.**
Copy each file from `public/` to `src/assets/images/` with kebab-case names:
- `public/enakko hero.png` → `src/assets/images/enakko-hero.png`
- `public/gambar berhasil login.png` → `src/assets/images/login-success.png`
- `public/moonitoing tangan.png` → `src/assets/images/monitoring-screen.png`
- `public/pilih absen otmatis tangan.png` → `src/assets/images/attendance-selection.png`
- `public/tangan bersama.png` → `src/assets/images/team-hands.png`

After copying, DELETE all 5 PNG files from `public/`. Keep `public/favicon.svg`.

**Step 2: Rewrite Hero.astro.**
Replace the entire file. The new Hero.astro must:

1. Add frontmatter with imports:
```astro
---
import { Image } from 'astro:assets';
import heroImage from '../assets/images/enakko-hero.png';
---
```

2. Keep the LEFT COLUMN text content EXACTLY as-is (lines 14-44 of current Hero.astro — the badge "Sistem Absensi NFC #1", h1, p, and two CTA buttons). Do NOT change any text, links, or classes.

3. Keep the decorative background blurs (mint and teal circles with blur).

4. REPLACE the RIGHT COLUMN (lines 46-135, the entire CSS tablet mockup `<div>` tree) with a real image:
```astro
<!-- Right Column: Real App Screenshot -->
<div class="relative flex justify-center lg:justify-end" style="animation: slide-in-right 0.8s ease-out 0.2s both;">
  <div class="absolute -inset-4 -z-10 rounded-[2.5rem] bg-gradient-to-br from-brand-teal/30 via-brand-mint/20 to-brand-pink/10 blur-2xl"></div>
  <div class="animate-float">
    <Image
      src={heroImage}
      alt="Tangan memegang smartphone menampilkan dashboard admin ABSENKOK"
      width={420}
      class="rounded-2xl shadow-2xl ring-1 ring-black/5"
    />
  </div>
</div>
```

5. Keep the overall section structure: `<section>` → `max-w-7xl` container → 2-column grid (`grid-cols-1 lg:grid-cols-2`).

**Why this approach:** The CSS mockup is 90 lines of fake divs. A single `<Image>` tag is more visually authentic, simpler code, and Astro's sharp pipeline auto-converts to WebP at 420px width (~53KB vs 2.13MB original).
  </action>
  <verify>
    <automated>cd "C:\Users\HYPE R Series\Desktop\projekan\absenkok-website" && npm run build 2>&1 | Select-String -Pattern "(error|Error|✓|dist)" | Select-Object -First 10</automated>
  </verify>
  <done>
    - All 5 images exist in src/assets/images/ with kebab-case names
    - No PNG files remain in public/ (only favicon.svg)
    - Hero.astro uses `<Image>` component with enakko-hero.png import
    - CSS tablet mockup divs completely removed from Hero.astro
    - `npm run build` succeeds, dist/ contains WebP hero image in _astro/
  </done>
</task>

<task type="auto">
  <name>Task 2: Add screenshot companions to HowItWorks steps</name>
  <files>absenkok-website/src/components/HowItWorks.astro</files>
  <action>
**Rewrite HowItWorks.astro** to add a screenshot image below each step's text.

1. Add frontmatter imports:
```astro
---
import { Image } from 'astro:assets';
import attendanceImg from '../assets/images/attendance-selection.png';
import loginImg from '../assets/images/login-success.png';
import monitoringImg from '../assets/images/monitoring-screen.png';
```

2. Update the steps array to include image references and alt text:
```javascript
const steps = [
  {
    number: "1",
    title: "Pasang Tablet",
    description: "Letakkan tablet Android di area masuk restoran. Buka ABSENKOK dalam mode kiosk.",
    accent: "bg-brand-teal text-white",
    image: attendanceImg,
    imageAlt: "Layar pilihan mode absensi otomatis pada ABSENKOK",
  },
  {
    number: "2",
    title: "Tap Kartu NFC",
    description: "Karyawan tap kartu NFC masing-masing ke tablet saat masuk dan pulang kerja.",
    accent: "bg-brand-pink text-white",
    image: loginImg,
    imageAlt: "Konfirmasi berhasil login setelah tap kartu NFC",
  },
  {
    number: "3",
    title: "Pantau & Kelola",
    description: "Admin lihat dashboard kehadiran real-time, atur jadwal shift, dan export laporan PDF.",
    accent: "bg-brand-navy text-white",
    image: monitoringImg,
    imageAlt: "Dashboard monitoring kehadiran karyawan real-time",
  },
];
```

3. In the template, add `<Image>` below each step's description text:
```astro
{steps.map((step, i) => (
  <div class="text-center group" style={`animation: fade-in-up 0.6s ease-out ${0.2 + i * 0.2}s both;`}>
    <div class={`h-16 w-16 rounded-full ${step.accent} text-2xl font-bold shadow-lg mx-auto flex items-center justify-center group-hover:scale-110 transition-transform duration-300`}>
      {step.number}
    </div>
    <h3 class="text-xl font-semibold text-brand-navy mt-6">{step.title}</h3>
    <p class="text-base leading-7 text-brand-text-secondary mt-3 max-w-xs mx-auto">{step.description}</p>
    <div class="mt-6">
      <Image
        src={step.image}
        alt={step.imageAlt}
        width={320}
        class="rounded-xl shadow-md mx-auto ring-1 ring-brand-border"
      />
    </div>
  </div>
))}
```

4. Keep ALL existing structure: section id="cara-kerja", bg-white, section header with "Cara Kerja" title, grid layout, staggered animations. Only ADD the image block — don't change existing text, classes, or animations.

**Image width rationale:** 320px keeps screenshots compact within each column on desktop (3-col grid ~350px per col) and appropriately sized on mobile (full-width). Step 3's monitoring-screen is landscape (4081×3619) so at 320w it will be proportionally shorter — which is fine, it shows the dashboard overview.
  </action>
  <verify>
    <automated>cd "C:\Users\HYPE R Series\Desktop\projekan\absenkok-website" && npm run build 2>&1 | Select-String -Pattern "(error|Error|✓|dist)" | Select-Object -First 10</automated>
  </verify>
  <done>
    - HowItWorks.astro imports 3 screenshot images via Astro <Image>
    - Each step (Pasang Tablet, Tap Kartu NFC, Pantau & Kelola) has a companion screenshot
    - All images have descriptive alt text in Bahasa Indonesia
    - `npm run build` succeeds with no image import errors
    - dist/ contains 3 additional WebP images in _astro/
  </done>
</task>

</tasks>

<verification>
Run from the website repo directory:
```bash
cd "C:\Users\HYPE R Series\Desktop\projekan\absenkok-website"

# 1. Build succeeds
npm run build

# 2. No large PNGs in dist (all should be WebP via _astro/)
Get-ChildItem dist -Recurse -Filter "*.png" | Where-Object { $_.Length -gt 100KB }
# Expected: empty (no PNG > 100KB)

# 3. WebP files exist in dist/_astro/
Get-ChildItem dist/_astro -Filter "*.webp"
# Expected: 4+ WebP files (hero + 3 HowItWorks screenshots)

# 4. No images left in public/ except favicon
Get-ChildItem public
# Expected: only favicon.svg
```
</verification>

<success_criteria>
1. Hero section renders real app screenshot (enakko-hero.png → WebP), CSS mockup completely removed
2. All 3 HowItWorks steps show companion screenshots from the actual app
3. All images optimized through Astro's pipeline (src/assets/ → WebP in dist/_astro/)
4. No unoptimized PNGs in dist/ output (total image payload < 300KB)
5. `npm run build` passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/19-website-polish-real-screenshots-about-architecture-section-tech-icons/19-01-SUMMARY.md`
</output>
