---
phase: 55-schedule-policy-absence-rules
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - sql/phase_55_admin_policy_recap_20260326.sql
  - lib/models/attendance_policy_recap_day.dart
  - lib/services/attendance_policy_recap_service.dart
  - test/services/attendance_policy_recap_service_test.dart
autonomous: true
requirements: [SCHED-02, SCHED-03]
user_setup:
  - service: supabase
    why: "Phase 55 adds a new admin recap RPC and policy helper logic in SQL, so the live database change must be reviewed and applied only after explicit approval."
    dashboard_config:
      - task: "Run sql/phase_55_admin_policy_recap_20260326.sql in Supabase SQL Editor after the user approves the production change."
        location: "Supabase Dashboard -> SQL Editor -> New Query"
must_haves:
  truths:
    - "One additive admin recap path evaluates lateness and absence from typed schedule policy instead of raw screen-only heuristics."
    - "Current-day zero-log rows stay `belum_masuk`, while closed-day zero-log rows become `tidak_hadir`."
    - "Leave states override absence before no-show logic runs."
    - "The recap contract distinguishes break-first eligibility from break-first confirmation so Phase 55 does not invent confirmation data that only Phase 56 can capture."
  artifacts:
    - path: "sql/phase_55_admin_policy_recap_20260326.sql"
      provides: "Additive admin-facing policy recap RPC with typed lateness/no-show fields"
      contains: "get_admin_schedule_policy_recap"
    - path: "lib/models/attendance_policy_recap_day.dart"
      provides: "Typed Dart recap row model and enums for attendance status and late kind"
      exports: ["AttendancePolicyRecapDay"]
    - path: "lib/services/attendance_policy_recap_service.dart"
      provides: "Supabase-facing parser/service for the Phase 55 admin recap RPC"
      exports: ["AttendancePolicyRecapService"]
    - path: "test/services/attendance_policy_recap_service_test.dart"
      provides: "Regression coverage for recap parsing and policy status semantics"
      contains: "AttendancePolicyRecapDay"
  key_links:
    - from: "sql/phase_55_admin_policy_recap_20260326.sql"
      to: "sql/phase_55_schedule_policy_foundation_20260326.sql"
      via: "the recap RPC consumes the same policy keys written into `shift_slot` by Plan 01"
      pattern: "required_work_minutes|band"
    - from: "lib/services/attendance_policy_recap_service.dart"
      to: "lib/models/attendance_policy_recap_day.dart"
      via: "RPC rows parse into typed late/no-show enums instead of raw maps"
      pattern: "LateKind|AttendancePolicyStatus"
    - from: "lib/services/attendance_policy_recap_service.dart"
      to: "sql/phase_55_admin_policy_recap_20260326.sql"
      via: "the service trusts the additive admin recap RPC instead of re-deriving policy rules in widgets"
      pattern: "get_admin_schedule_policy_recap"
---

<objective>
Create one additive admin-facing recap contract for Phase 55 lateness and absence policy.

Purpose: admin recap meaning is currently computed inside the Flutter screen from raw scan rows. Phase 55 needs one typed contract that can express late, break-first-eligible, current-day `belum_masuk`, and closed-day `tidak_hadir` without duplicating business rules in widgets.
Output: an additive Supabase SQL patch that defines an admin policy recap RPC plus a typed Dart model/service pair that parses the new contract safely.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
@.planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
@.planning/phases/55-schedule-policy-absence-rules/55-VALIDATION.md
@.planning/phases/55-schedule-policy-absence-rules/55-01-PLAN.md
@lib/models/daily_summary.dart
@lib/models/attendance_log.dart
@lib/screens/admin/admin_reports_screen.dart
@sql/phase_42_portal_attendance_recap_20260323.sql

<interfaces>
Target admin recap RPC shape for Phase 55:
```text
logical_date
employee_id
employee_name
outlet_id
outlet_name
shift_band
required_work_minutes
late_cutoff_local
break_first_deadline_local
attendance_status
late_kind
is_late
break_first_eligible
break_first_confirmed
first_scan_local
first_break_local
last_pulang_local
notes
```

Target Dart enums:
```dart
enum AttendancePolicyStatus {
  hadir,
  belumMasuk,
  tidakHadir,
  sakit,
  izin,
  cuti,
  libur,
  hadirTanpaJadwal,
}

enum LateKind { none, normal, breakFirstEligible, breakFirstConfirmed }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the additive SQL admin recap RPC for Phase 55 policy states</name>
  <files>sql/phase_55_admin_policy_recap_20260326.sql</files>
  <read_first>
    - sql/phase_42_portal_attendance_recap_20260323.sql
    - sql/phase_55_schedule_policy_foundation_20260326.sql
    - .planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
    - .planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
  </read_first>
  <action>
    Create `sql/phase_55_admin_policy_recap_20260326.sql` as an additive SQL patch.

    Required behavior:
    1. Define one admin-facing RPC named exactly `get_admin_schedule_policy_recap`.
       - inputs:
         - `p_outlet_id uuid`
         - `p_start_date date`
         - `p_end_date date`
       - do not accept caller-controlled employee identity

    2. The RPC must join schedules, schedule entries, employees, outlets, and attendance logs so each scheduled or attendance-relevant logical day can emit:
       - typed schedule policy fields (`shift_band`, `required_work_minutes`, `late_cutoff_local`, `break_first_deadline_local`)
       - typed recap outcome fields (`attendance_status`, `late_kind`, `is_late`)
       - operational booleans (`break_first_eligible`, `break_first_confirmed`)

    3. Attendance-status derivation must follow these exact rules:
       - leave states (`sakit`, `izin`, `cuti`, `libur`) override absence
       - scheduled current logical day with zero logs -> `belum_masuk`
       - scheduled closed logical day with zero logs -> `tidak_hadir`
       - unscheduled logs -> `hadir_tanpa_jadwal`
       - late arrivals remain present; do not convert them to absence

    4. Late-kind derivation must be explicit:
       - `normal` when first relevant scan is after the band cutoff
       - `break_first_eligible` when the first relevant scan is after cutoff but still within the contract window and there is no explicit confirmation yet
       - `break_first_confirmed` only when an explicit future confirmation signal exists
       - `none` when the row is not late

    5. Because Phase 56 has not built the confirmation flow yet, the SQL patch must document in comments that `break_first_confirmed` is expected to remain false until a later phase writes the explicit signal.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'sql/phase_55_admin_policy_recap_20260326.sql' -Pattern 'get_admin_schedule_policy_recap','break_first_eligible','break_first_confirmed','belum_masuk','tidak_hadir','hadir_tanpa_jadwal','late_kind' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `sql/phase_55_admin_policy_recap_20260326.sql` exists
    - the SQL file contains `get_admin_schedule_policy_recap`
    - the SQL file contains `break_first_eligible`
    - the SQL file contains `break_first_confirmed`
    - the SQL file contains `belum_masuk`
    - the SQL file contains `tidak_hadir`
    - the SQL file contains `hadir_tanpa_jadwal`
    - the SQL file contains `late_kind`
    - the SQL file contains `required_work_minutes`
    - the SQL file documents that break-first confirmation is a later-phase signal
  </acceptance_criteria>
  <done>The repo contains one additive admin recap RPC that can express Phase 55 late/no-show policy without inventing scan-confirmation data too early.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the typed Dart recap model and parser service for the new admin policy RPC</name>
  <files>lib/models/attendance_policy_recap_day.dart, lib/services/attendance_policy_recap_service.dart, test/services/attendance_policy_recap_service_test.dart</files>
  <read_first>
    - lib/models/daily_summary.dart
    - lib/models/attendance_log.dart
    - lib/screens/admin/admin_reports_screen.dart
    - .planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
  </read_first>
  <action>
    Implement the Phase 55 Dart parsing contract for the new admin recap RPC.

    Required behavior:
    1. Create `lib/models/attendance_policy_recap_day.dart` with:
       - `AttendancePolicyStatus` enum values:
         - `hadir`
         - `belumMasuk`
         - `tidakHadir`
         - `sakit`
         - `izin`
         - `cuti`
         - `libur`
         - `hadirTanpaJadwal`
       - `LateKind` enum values:
         - `none`
         - `normal`
         - `breakFirstEligible`
         - `breakFirstConfirmed`
       - `AttendancePolicyRecapDay` model carrying the RPC fields needed by admin filters and badges

    2. Create `lib/services/attendance_policy_recap_service.dart` that:
       - calls `get_admin_schedule_policy_recap`
       - parses raw Supabase rows into `AttendancePolicyRecapDay`
       - keeps time parsing resilient for nullable fields
       - does not compute new lateness logic locally beyond enum parsing

    3. Create `test/services/attendance_policy_recap_service_test.dart` and cover:
       - parsing `belum_masuk` vs `tidak_hadir`
       - parsing `late_kind` values into the correct enum
       - parsing `break_first_eligible` without treating it as confirmed
       - preserving required-work minutes and cutoff strings from the RPC payload
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/attendance_policy_recap_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/attendance_policy_recap_day.dart` exists and contains `enum AttendancePolicyStatus`
    - `lib/models/attendance_policy_recap_day.dart` contains `enum LateKind`
    - `lib/models/attendance_policy_recap_day.dart` contains `AttendancePolicyRecapDay`
    - `lib/services/attendance_policy_recap_service.dart` exists and contains `get_admin_schedule_policy_recap`
    - `lib/services/attendance_policy_recap_service.dart` does not implement a second lateness rule engine in widget-only code
    - `test/services/attendance_policy_recap_service_test.dart` exists and contains `breakFirstEligible`
    - `test/services/attendance_policy_recap_service_test.dart` contains `belumMasuk`
    - `test/services/attendance_policy_recap_service_test.dart` contains `tidakHadir`
    - `C:\flutter\bin\flutter.bat test test/services/attendance_policy_recap_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo has one typed Dart recap parsing layer that trusts the additive SQL recap contract instead of re-deriving policy semantics in screens.</done>
</task>

</tasks>

<verification>
- `sql/phase_55_admin_policy_recap_20260326.sql` exists and defines the expected admin policy RPC and fields.
- `C:\flutter\bin\flutter.bat test test/services/attendance_policy_recap_service_test.dart` passes.
- Phase 55 recap logic now has one typed cross-layer contract for lateness and absence states.
</verification>

<success_criteria>
- Admin recap meaning no longer depends on raw widget-local heuristics alone.
- The phase can distinguish `belum_masuk` from `tidak_hadir` safely.
- Break-first eligibility is modeled now without pretending confirmation exists before Phase 56.
- Later admin UI work can filter on typed policy states instead of strings buried in notes.
</success_criteria>

<output>
After completion, create `.planning/phases/55-schedule-policy-absence-rules/55-03-SUMMARY.md`
</output>

