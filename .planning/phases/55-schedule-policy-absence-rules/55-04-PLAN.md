---
phase: 55-schedule-policy-absence-rules
plan: 04
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - lib/widgets/attendance_policy_badge.dart
  - test/widgets/attendance_policy_badge_test.dart
  - lib/screens/admin/admin_reports_screen.dart
autonomous: true
requirements: [SCHED-02, SCHED-03]
must_haves:
  truths:
    - "Admin can filter policy recap rows quickly without losing the existing raw per-scan tab."
    - "Normal late and break-first-related rows are no longer lumped into one unlabeled warning bucket."
    - "Current-day informational states stay calmer than true follow-up gaps like `tidak_hadir`."
    - "No-show rows show explicit operational reason copy instead of a bare badge."
  artifacts:
    - path: "lib/widgets/attendance_policy_badge.dart"
      provides: "Reusable badge and label mapping for policy recap states"
      exports: ["AttendancePolicyBadge"]
    - path: "test/widgets/attendance_policy_badge_test.dart"
      provides: "Widget coverage for late/no-show/leave badge rendering"
      contains: "AttendancePolicyBadge"
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Rekap Harian filter chips, badge rendering, and policy-reason copy powered by the Phase 55 recap service"
      contains: "AttendancePolicyRecapService"
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/attendance_policy_recap_service.dart"
      via: "the Rekap Harian tab consumes the additive policy recap contract"
      pattern: "AttendancePolicyRecapService"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/widgets/attendance_policy_badge.dart"
      via: "daily cards render one reusable policy badge instead of ad hoc colors"
      pattern: "AttendancePolicyBadge"
    - from: "lib/widgets/attendance_policy_badge.dart"
      to: "lib/models/attendance_policy_recap_day.dart"
      via: "the badge uses typed attendance and late-kind enums"
      pattern: "AttendancePolicyStatus|LateKind"
---

<objective>
Turn the new Phase 55 recap contract into a fast manager-facing audit surface inside Admin Reports.

Purpose: the policy engine is not enough on its own. Managers need to see late, break-first-related, `belum_masuk`, and `tidak_hadir` rows as distinct review states without regressing the existing per-scan history.
Output: one reusable policy badge widget plus a Rekap Harian tab that filters and explains policy recap rows from the new service.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
@.planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
@.planning/phases/55-schedule-policy-absence-rules/55-VALIDATION.md
@.planning/phases/55-schedule-policy-absence-rules/55-03-PLAN.md
@lib/models/attendance_policy_recap_day.dart
@lib/services/attendance_policy_recap_service.dart
@lib/screens/admin/admin_reports_screen.dart

<interfaces>
Required Rekap Harian filter set for Phase 55:
```text
Semua
Terlambat
Terlambat Normal
Break-first
Kandidat Break-first
Belum Masuk
Tidak Hadir
Hadir Tanpa Jadwal
```

Required badge labels:
```text
Terlambat
Break-first
Kandidat break-first
Belum masuk
Tidak hadir
Hadir tanpa jadwal
```

Required reason copy:
```text
Tidak ada scan pada hari kerja yang sudah selesai.
Hari kerja masih berjalan dan belum ada scan masuk.
Masih dalam jendela break-first, menunggu konfirmasi.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the reusable policy badge and label mapping for Phase 55 recap states</name>
  <files>lib/widgets/attendance_policy_badge.dart, test/widgets/attendance_policy_badge_test.dart</files>
  <read_first>
    - lib/models/attendance_policy_recap_day.dart
    - .planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
    - .planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
  </read_first>
  <action>
    Create the shared admin badge surface for Phase 55 recap states.

    Required behavior:
    1. Create `lib/widgets/attendance_policy_badge.dart` with one reusable `AttendancePolicyBadge`.
    2. The widget must render distinct labels for:
       - `Terlambat`
       - `Break-first`
       - `Kandidat break-first`
       - `Belum masuk`
       - `Tidak hadir`
       - `Hadir tanpa jadwal`
       - leave states (`Sakit`, `Izin`, `Cuti`, `Libur`)
    3. Tone rules:
       - current-day informational states like `Belum masuk` stay neutral or warning-light
       - follow-up gaps like `Tidak hadir` are stronger danger tone
       - `Kandidat break-first` must not share the exact same label/tone as confirmed `Break-first`

    4. Create `test/widgets/attendance_policy_badge_test.dart` and cover:
       - normal late badge
       - candidate break-first badge
       - confirmed break-first badge
       - no-show badge
       - current-day `Belum masuk` badge
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/attendance_policy_badge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/widgets/attendance_policy_badge.dart` exists and contains `class AttendancePolicyBadge`
    - `lib/widgets/attendance_policy_badge.dart` contains `Kandidat break-first`
    - `lib/widgets/attendance_policy_badge.dart` contains `Tidak hadir`
    - `lib/widgets/attendance_policy_badge.dart` contains `Belum masuk`
    - `test/widgets/attendance_policy_badge_test.dart` exists and contains `Break-first`
    - `test/widgets/attendance_policy_badge_test.dart` contains `Kandidat break-first`
    - `C:\flutter\bin\flutter.bat test test/widgets/attendance_policy_badge_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo contains one reusable policy badge surface that distinguishes late, candidate/confirmed break-first, no-show, and leave states clearly.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade the Rekap Harian tab with policy filters and operational reason copy</name>
  <files>lib/screens/admin/admin_reports_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/attendance_policy_recap_service.dart
    - lib/widgets/attendance_policy_badge.dart
    - .planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
  </read_first>
  <action>
    Update `lib/screens/admin/admin_reports_screen.dart` so the Rekap Harian tab consumes the Phase 55 policy recap service and exposes fast manager filters.

    Required behavior:
    1. Keep the existing Per Scan tab unchanged.
    2. For the Rekap Harian tab, fetch and render `AttendancePolicyRecapDay` rows from `AttendancePolicyRecapService`.
    3. Add filter chips with these exact labels:
       - `Semua`
       - `Terlambat`
       - `Terlambat Normal`
       - `Break-first`
       - `Kandidat Break-first`
       - `Belum Masuk`
       - `Tidak Hadir`
       - `Hadir Tanpa Jadwal`
    4. Each recap card must show:
       - `AttendancePolicyBadge`
       - band + required-hours context if scheduled
       - short reason copy, using the exact UI contract where applicable:
         - `Terlambat: scan pertama HH:mm, batas HH:mm`
         - `Hari kerja masih berjalan dan belum ada scan masuk.`
         - `Tidak ada scan pada hari kerja yang sudah selesai.`
         - `Masih dalam jendela break-first, menunggu konfirmasi.`
         - `Hadir tanpa jadwal`
    5. If the selected filter has no rows, show a filter-aware empty state instead of a blank list.
    6. Do not change CSV/PDF export behavior in Phase 55. Export redesign belongs to later report phases.
    7. Keep all user-facing copy in Bahasa Indonesia.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_reports_screen.dart lib/widgets/attendance_policy_badge.dart lib/services/attendance_policy_recap_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_reports_screen.dart` contains `AttendancePolicyRecapService`
    - `lib/screens/admin/admin_reports_screen.dart` contains `AttendancePolicyBadge`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Terlambat Normal`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Kandidat Break-first`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Belum Masuk`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Tidak Hadir`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Hadir Tanpa Jadwal`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Terlambat: scan pertama`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Tidak ada scan pada hari kerja yang sudah selesai.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Hari kerja masih berjalan dan belum ada scan masuk.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Masih dalam jendela break-first, menunggu konfirmasi.`
    - `C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_reports_screen.dart lib/widgets/attendance_policy_badge.dart lib/services/attendance_policy_recap_service.dart` exits 0
  </acceptance_criteria>
  <done>The Rekap Harian tab now exposes fast policy filters and explicit operational reason copy without regressing the raw scan history tab.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/attendance_policy_badge_test.dart` passes.
- `C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_reports_screen.dart lib/widgets/attendance_policy_badge.dart lib/services/attendance_policy_recap_service.dart` reports no issues.
- Rekap Harian now distinguishes late/no-show/current-day states through reusable badges and filters instead of raw daily-summary heuristics alone.
</verification>

<success_criteria>
- Managers can filter late, break-first-related, no-show, and unscheduled-presence rows quickly.
- Current-day informational states stay distinct from true follow-up gaps.
- The raw per-scan tab remains intact.
- Phase 56 can later populate confirmed break-first cases without forcing a UI redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/55-schedule-policy-absence-rules/55-04-SUMMARY.md`
</output>
