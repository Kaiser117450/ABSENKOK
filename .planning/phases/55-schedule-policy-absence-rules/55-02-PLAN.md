---
phase: 55-schedule-policy-absence-rules
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/screens/admin/shift_scheduler_screen.dart
  - lib/screens/admin/widgets/schedule_table_view.dart
  - lib/screens/admin/widgets/schedule_cells.dart
  - lib/screens/admin/widgets/schedule_policy_summary_card.dart
  - test/widgets/schedule_policy_widgets_test.dart
autonomous: true
requirements: [SCHED-01]
must_haves:
  truths:
    - "The weekly scheduler grid shows band plus required hours as the primary schedule label."
    - "Quick band assignment remains fast; Phase 55 does not replace the current weekly-grid workflow with a heavy form."
    - "Bulk assign adds a short review step before write, so policy-derived defaults are confirmable instead of silent."
    - "Policy details such as lateness cutoff and break-first window live in a compact summary surface, not repeated inside every grid cell."
  artifacts:
    - path: "lib/screens/admin/shift_scheduler_screen.dart"
      provides: "Band-first scheduler interaction flow, bulk review step, and per-day policy editing"
      contains: "requiredWorkMinutes"
    - path: "lib/screens/admin/widgets/schedule_cells.dart"
      provides: "Grid cell chips that render band and required-hours primary labels"
      contains: "requiredWorkMinutes"
    - path: "lib/screens/admin/widgets/schedule_policy_summary_card.dart"
      provides: "Compact policy summary surface for cutoffs and break-first rules"
      exports: ["SchedulePolicySummaryCard"]
    - path: "test/widgets/schedule_policy_widgets_test.dart"
      provides: "Widget coverage for band-first chip rendering and policy review interactions"
      contains: "SchedulePolicySummaryCard"
  key_links:
    - from: "lib/screens/admin/shift_scheduler_screen.dart"
      to: "lib/services/schedule_policy_service.dart"
      via: "assignment and review flows build schedule rows from one policy source"
      pattern: "SchedulePolicyService"
    - from: "lib/screens/admin/widgets/schedule_cells.dart"
      to: "lib/models/shift_schedule.dart"
      via: "grid chips render typed band and required-hours fields"
      pattern: "requiredWorkMinutes|band"
    - from: "lib/screens/admin/shift_scheduler_screen.dart"
      to: "lib/screens/admin/widgets/schedule_policy_summary_card.dart"
      via: "header or review flow shows compact policy guidance outside the dense grid"
      pattern: "SchedulePolicySummaryCard"
---

<objective>
Preserve the familiar weekly scheduling workflow while switching the admin UI to the Phase 55 band-first policy contract.

Purpose: the schedule model can be correct and still fail Phase 55 if kepala gerai keeps seeing stale exact-time labels or silent bulk assignment. The scheduler UI must now reveal band, required hours, and policy review without losing speed.
Output: updated grid cells, picker flows, bulk review step, compact policy summary UI, and widget coverage for the new band-first scheduler contract.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
@.planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
@.planning/phases/55-schedule-policy-absence-rules/55-VALIDATION.md
@.planning/phases/55-schedule-policy-absence-rules/55-01-PLAN.md
@lib/models/shift_schedule.dart
@lib/services/schedule_policy_service.dart
@lib/screens/admin/shift_scheduler_screen.dart
@lib/screens/admin/widgets/schedule_table_view.dart
@lib/screens/admin/widgets/schedule_cells.dart

<interfaces>
Target cell presentation for scheduled workdays:
```text
Pagi - 10j
07:00 batas telat
```

Target lightweight review copy examples:
```text
Tinjau Penugasan
Periksa band, jam wajib, dan batas telat sebelum konfirmasi.
Band: Siang
Jam wajib: 10j
Batas telat: 10:00
Break-first: sampai 12:00 (FULLTIME)
Konfirmasi Penugasan
Batal
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace stale exact-time labels with band-first schedule chips and compact policy summaries</name>
  <files>lib/screens/admin/shift_scheduler_screen.dart, lib/screens/admin/widgets/schedule_table_view.dart, lib/screens/admin/widgets/schedule_cells.dart, lib/screens/admin/widgets/schedule_policy_summary_card.dart, test/widgets/schedule_policy_widgets_test.dart</files>
  <read_first>
    - lib/screens/admin/shift_scheduler_screen.dart
    - lib/screens/admin/widgets/schedule_table_view.dart
    - lib/screens/admin/widgets/schedule_cells.dart
    - lib/models/shift_schedule.dart
    - lib/services/schedule_policy_service.dart
    - .planning/phases/55-schedule-policy-absence-rules/55-01-PLAN.md
  </read_first>
  <action>
    Update the scheduler UI to make the policy payload the visible source of truth.

    Required behavior:
    1. Create `lib/screens/admin/widgets/schedule_policy_summary_card.dart` and render it in the scheduler header or other compact summary location.
       - use the exact title `Aturan Jadwal Minggu Ini`
       - use the exact helper copy `Band dan jam wajib menjadi acuan utama. Jam lama hanya tampil sebagai petunjuk kecil bila masih dibutuhkan.`
       - show the policy rows with labels `Batas telat`, `Jam wajib default`, and `Break-first`
       - show the lateness cutoffs exactly as `Pagi 07:00`, `Siang 10:00`, and `Sore 15:00`
       - show the default required-hours mapping exactly as `FULLTIME 10j` and `PARTTIME 9j`
       - show break-first windows as compact text, not as dense cell copy

    2. Update `lib/screens/admin/widgets/schedule_cells.dart` so assigned cells render:
       - primary label = `Band + required hours` using the UI-contract format `Pagi - 10j`, `Siang - 9j`, or `Sore - 10j`
       - secondary hint = exact cutoff copy such as `07:00 batas telat`, `10:00 batas telat`, or `15:00 batas telat`
       - exact legacy time ranges, if kept at all, must be tertiary hint-only content and never replace the primary label
       - the assigned chip may wrap to 2 lines instead of truncating the policy hint
       - no primary exact-time strings like `09:00-17:00`

    3. Update `lib/screens/admin/shift_scheduler_screen.dart` so picker-generated schedule rows come from `SchedulePolicyService` and the typed `ShiftBand` contract.
       - remove hard-coded primary picker subtitles `09:00 - 17:00`, `12:00 - 20:00`, and `14:00 - 22:00`
       - keep quick-tap assignment interaction
       - keep `Libur` available as a special band/day-off case

    4. Add `test/widgets/schedule_policy_widgets_test.dart` and verify:
       - a scheduled cell renders `Pagi - 10j` as the primary label
       - the summary card renders the locked WITA cutoffs
       - the summary card renders `Aturan Jadwal Minggu Ini`
       - exact-time hints are secondary, not primary
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/schedule_policy_widgets_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/widgets/schedule_policy_summary_card.dart` exists and contains `SchedulePolicySummaryCard`
    - `lib/screens/admin/widgets/schedule_policy_summary_card.dart` contains `Aturan Jadwal Minggu Ini`
    - `lib/screens/admin/widgets/schedule_policy_summary_card.dart` contains `Band dan jam wajib menjadi acuan utama. Jam lama hanya tampil sebagai petunjuk kecil bila masih dibutuhkan.`
    - `lib/screens/admin/widgets/schedule_policy_summary_card.dart` contains `07:00`, `10:00`, and `15:00`
    - `lib/screens/admin/widgets/schedule_policy_summary_card.dart` contains `FULLTIME 10j` and `PARTTIME 9j`
    - `lib/screens/admin/widgets/schedule_cells.dart` contains `requiredWorkMinutes`
    - `lib/screens/admin/widgets/schedule_cells.dart` contains `batas telat`
    - `lib/screens/admin/widgets/schedule_cells.dart` does not use `09:00 - 17:00` as the primary assigned-cell label
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `SchedulePolicyService`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Tinjau Penugasan`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Periksa band, jam wajib, dan batas telat sebelum konfirmasi.`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Konfirmasi Penugasan`
    - `test/widgets/schedule_policy_widgets_test.dart` exists and contains `group('SchedulePolicySummaryCard'`
    - `test/widgets/schedule_policy_widgets_test.dart` contains `Pagi - 10j`
    - `C:\flutter\bin\flutter.bat test test/widgets/schedule_policy_widgets_test.dart` exits 0
  </acceptance_criteria>
  <done>The weekly scheduler renders band-first schedule chips and a compact policy summary without abandoning the existing quick-grid workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Add the bulk review step and per-day required-hours override editor</name>
  <files>lib/screens/admin/shift_scheduler_screen.dart, lib/screens/admin/widgets/schedule_cells.dart</files>
  <read_first>
    - lib/screens/admin/shift_scheduler_screen.dart
    - lib/screens/admin/widgets/schedule_cells.dart
    - lib/services/schedule_policy_service.dart
    - .planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
  </read_first>
  <action>
    Extend the existing scheduler flow with lightweight policy review and override controls.

    Required behavior:
    1. After the operator chooses a band in bulk mode, do not write immediately.
       - open a short review sheet first
       - use the exact title `Tinjau Penugasan`
       - use the exact helper copy `Periksa band, jam wajib, dan batas telat sebelum konfirmasi.`
       - show selected employee count, affected date range, and review rows labelled `Band`, `Jam wajib`, `Batas telat`, and `Break-first`
       - provide a primary action labeled exactly `Konfirmasi Penugasan`
       - provide a secondary action labeled exactly `Batal`

    2. Tapping an assigned cell must open a lightweight day-detail editor instead of deleting immediately.
       - allow changing the band
       - allow overriding `required_work_minutes`
       - keep a remove action available

    3. Required-hours override choices must be concrete and finite in Phase 55:
       - `480`
       - `540`
       - `600`
       - `660`
       - `720`
       Display them as `8j`, `9j`, `10j`, `11j`, and `12j`.

    4. Auto-generate schedule templates must continue to expose the familiar `2 Shift` and `3 Shift` flows, but generated entries must now carry policy metadata from the typed band contract.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/shift_scheduler_screen.dart lib/screens/admin/widgets/schedule_table_view.dart lib/screens/admin/widgets/schedule_cells.dart lib/screens/admin/widgets/schedule_policy_summary_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Tinjau Penugasan`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Periksa band, jam wajib, dan batas telat sebelum konfirmasi.`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Konfirmasi Penugasan`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `Batal`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `required_work_minutes` or `requiredWorkMinutes`
    - `lib/screens/admin/shift_scheduler_screen.dart` contains `480`, `540`, `600`, `660`, and `720`
    - `lib/screens/admin/shift_scheduler_screen.dart` still contains `2 Shift`
    - `lib/screens/admin/shift_scheduler_screen.dart` still contains `3 Shift`
    - assigned-cell interaction no longer removes rows immediately without an edit/review affordance
    - `C:\flutter\bin\flutter.bat analyze lib/screens/admin/shift_scheduler_screen.dart lib/screens/admin/widgets/schedule_table_view.dart lib/screens/admin/widgets/schedule_cells.dart lib/screens/admin/widgets/schedule_policy_summary_card.dart` exits 0
  </acceptance_criteria>
  <done>The scheduler now adds a short review step for bulk writes and supports per-day required-hours overrides without replacing the weekly-grid workflow.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/schedule_policy_widgets_test.dart` passes.
- `C:\flutter\bin\flutter.bat analyze lib/screens/admin/shift_scheduler_screen.dart lib/screens/admin/widgets/schedule_table_view.dart lib/screens/admin/widgets/schedule_cells.dart lib/screens/admin/widgets/schedule_policy_summary_card.dart` reports no issues.
- The scheduler UI leads with band and required-hours copy, not stale exact-time labels.
</verification>

<success_criteria>
- Weekly scheduler cells show `band + required hours`.
- Bulk assign now includes a confirmable policy review step.
- Per-day required-hours overrides exist without forcing a heavy form-driven workflow.
- Policy guidance lives in a compact summary surface instead of inside every cell.
</success_criteria>

<output>
After completion, create `.planning/phases/55-schedule-policy-absence-rules/55-02-SUMMARY.md`
</output>
