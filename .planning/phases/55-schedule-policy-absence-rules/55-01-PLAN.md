---
phase: 55-schedule-policy-absence-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/phase_55_schedule_policy_foundation_20260326.sql
  - lib/models/shift_band.dart
  - lib/models/shift_schedule.dart
  - lib/services/schedule_policy_service.dart
  - test/models/shift_schedule_policy_test.dart
autonomous: true
requirements: [SCHED-01, SCHED-02]
user_setup:
  - service: supabase
    why: "Phase 55 introduces additive schedule-policy SQL that backfills live schedule JSON and adds helper functions, so it must be reviewed and applied only after explicit approval."
    dashboard_config:
      - task: "Run sql/phase_55_schedule_policy_foundation_20260326.sql in Supabase SQL Editor after the user approves the production change."
        location: "Supabase Dashboard -> SQL Editor -> New Query"
must_haves:
  truths:
    - "Stored schedule rows carry explicit band and required-hours policy fields instead of relying on exact clock labels as the primary meaning."
    - "WITA lateness cutoffs and break-first windows are encoded once in a typed policy layer, not re-derived ad hoc in widgets."
    - "Legacy schedule JSON rows and current tests keep loading because the new parser accepts both old and new payload shapes."
    - "The Phase 55 default required-hours mapping is explicit, documented, and still allows per-day overrides later in the scheduler UI."
  artifacts:
    - path: "sql/phase_55_schedule_policy_foundation_20260326.sql"
      provides: "Additive SQL patch that backfills policy keys into stored schedule JSON and adds reusable schedule-policy helpers"
      contains: "resolve_schedule_shift_band"
    - path: "lib/models/shift_band.dart"
      provides: "Typed shift-band enum with canonical band values and labels"
      exports: ["ShiftBand"]
    - path: "lib/services/schedule_policy_service.dart"
      provides: "Single source of truth for required-hours defaults, WITA cutoffs, and break-first deadlines"
      exports: ["SchedulePolicyService"]
    - path: "lib/models/shift_schedule.dart"
      provides: "Policy-aware schedule payload that still parses legacy `shift_slot` JSON"
      contains: "requiredWorkMinutes"
    - path: "test/models/shift_schedule_policy_test.dart"
      provides: "Regression coverage for legacy/new schedule payload parsing and policy math"
      contains: "ShiftSlot"
  key_links:
    - from: "lib/models/shift_schedule.dart"
      to: "lib/models/shift_band.dart"
      via: "ShiftSlot carries typed band information instead of raw name-only meaning"
      pattern: "ShiftBand"
    - from: "lib/services/schedule_policy_service.dart"
      to: "lib/models/shift_schedule.dart"
      via: "policy helpers build and evaluate the richer shift payload"
      pattern: "requiredWorkMinutes|lateCutoffHour"
    - from: "sql/phase_55_schedule_policy_foundation_20260326.sql"
      to: "lib/models/shift_schedule.dart"
      via: "the SQL backfill writes the same policy keys that Dart serializes and parses"
      pattern: "required_work_minutes|band"
---

<objective>
Create the Phase 55 schedule-policy foundation in one additive Dart-plus-SQL contract.

Purpose: the scheduler UI, recap logic, and later scan flow cannot safely build on top of stale exact-time labels. Phase 55 first needs one band-first schedule payload plus reusable WITA policy helpers.
Output: a typed shift-band model, one shared policy service, a policy-aware `ShiftSlot` serializer/parser, and one additive SQL backfill patch for stored schedule JSON.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
@.planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
@.planning/phases/55-schedule-policy-absence-rules/55-VALIDATION.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-01-PLAN.md
@lib/models/shift_schedule.dart
@test/models/shift_schedule_test.dart
@sql/phase_38_employee_schedule_read_model_20260322.sql
@sql/phase_39_portal_read_path_hardening_20260323.sql
@sql/phase_42_portal_attendance_recap_20260323.sql

<interfaces>
Current serialized `shift_slot` shape in Flutter and Supabase:
```json
{
  "name": "Pagi",
  "start_hour": 9,
  "start_minute": 0,
  "end_hour": 17,
  "end_minute": 0,
  "color": 4282098230
}
```

Target Phase 55 policy keys that must exist after this plan:
```json
{
  "band": "PAGI",
  "required_work_minutes": 600,
  "late_cutoff_hour": 7,
  "late_cutoff_minute": 0,
  "break_first_deadline_hour": 9,
  "break_first_deadline_minute": 0
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the typed schedule-policy domain and make `ShiftSlot` backward-compatible</name>
  <files>lib/models/shift_band.dart, lib/models/shift_schedule.dart, lib/services/schedule_policy_service.dart, test/models/shift_schedule_policy_test.dart</files>
  <read_first>
    - lib/models/shift_schedule.dart
    - test/models/shift_schedule_test.dart
    - .planning/phases/54-workforce-contract-outlet-mode-foundation/54-01-PLAN.md
    - .planning/phases/55-schedule-policy-absence-rules/55-CONTEXT.md
  </read_first>
  <action>
    Implement the Phase 55 Dart policy foundation with these exact contracts:

    1. Create `lib/models/shift_band.dart` with a typed `ShiftBand` enum for `PAGI`, `SIANG`, `SORE`, and `LIBUR`.
       - expose canonical storage value, human label, and helper(s) to parse both uppercase values and existing title-case labels like `Pagi`
       - expose WITA lateness cutoff constants:
         - `PAGI` -> `07:00`
         - `SIANG` -> `10:00`
         - `SORE` -> `15:00`
         - `LIBUR` -> no lateness cutoff

    2. Create `lib/services/schedule_policy_service.dart` as the single policy-math source of truth.
       - use the upstream Phase 54 contract names `FULLTIME` and `PARTTIME`
       - implement the inferred default required-work mapping:
         - `FULLTIME` -> `600` minutes
         - `PARTTIME` -> `540` minutes
       - implement break-first extension mapping:
         - `FULLTIME` -> `120` minutes after cutoff
         - `PARTTIME` -> `60` minutes after cutoff
       - expose helpers for:
         - default required minutes by contract
         - late cutoff by band
         - break-first deadline by band + contract
         - minute-precision late comparison for a concrete logical date

    3. Update `lib/models/shift_schedule.dart` so `ShiftSlot` is band-first.
       - add fields for `band`, `requiredWorkMinutes`, `lateCutoffHour`, `lateCutoffMinute`, `breakFirstDeadlineHour`, and `breakFirstDeadlineMinute`
       - keep legacy `startTime` and `endTime` fields as optional hints only
       - `toJson()` must still emit the legacy keys and also emit the new policy keys exactly as:
         - `band`
         - `required_work_minutes`
         - `late_cutoff_hour`
         - `late_cutoff_minute`
         - `break_first_deadline_hour`
         - `break_first_deadline_minute`
       - `fromJson()` must accept both old and new payloads:
         - if policy keys exist, trust them
         - if only the legacy payload exists, infer `band` from `name`
         - if `required_work_minutes` is missing, fall back to `600`
         - keep parsing `color`, `start_hour`, and `end_hour` without throwing

    4. Add `test/models/shift_schedule_policy_test.dart` with focused coverage for:
       - legacy JSON -> new `ShiftSlot` parse
       - new JSON -> round-trip parse
       - `ShiftBand` parsing from legacy title-case and canonical uppercase values
       - `SchedulePolicyService` cutoff helpers returning the exact locked WITA times
       - `SchedulePolicyService` returning `600` for `FULLTIME` and `540` for `PARTTIME`
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/models/shift_schedule_policy_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/shift_band.dart` exists and contains `enum ShiftBand`
    - `lib/services/schedule_policy_service.dart` exists and contains `class SchedulePolicyService`
    - `lib/services/schedule_policy_service.dart` contains `600` and `540`
    - `lib/services/schedule_policy_service.dart` contains `07:00`, `10:00`, and `15:00` in helper logic or comments
    - `lib/models/shift_schedule.dart` contains `requiredWorkMinutes`
    - `lib/models/shift_schedule.dart` contains `late_cutoff_hour`
    - `lib/models/shift_schedule.dart` contains `break_first_deadline_hour`
    - `lib/models/shift_schedule.dart` still contains `start_hour` and `end_hour`
    - `test/models/shift_schedule_policy_test.dart` exists and contains `group('ShiftBand'`
    - `C:\flutter\bin\flutter.bat test test/models/shift_schedule_policy_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo contains one typed schedule-policy domain that can parse both legacy and new schedule payloads without losing WITA cutoff meaning.</done>
</task>

<task type="auto">
  <name>Task 2: Create the additive SQL patch that backfills schedule policy keys for live rows</name>
  <files>sql/phase_55_schedule_policy_foundation_20260326.sql</files>
  <read_first>
    - sql/phase_38_employee_schedule_read_model_20260322.sql
    - sql/phase_39_portal_read_path_hardening_20260323.sql
    - sql/phase_42_portal_attendance_recap_20260323.sql
    - .planning/phases/54-workforce-contract-outlet-mode-foundation/54-01-PLAN.md
    - .planning/phases/55-schedule-policy-absence-rules/55-RESEARCH.md
  </read_first>
  <action>
    Create `sql/phase_55_schedule_policy_foundation_20260326.sql` as an additive SQL patch. Do not drop columns, do not rewrite tables destructively, and do not remove legacy `shift_slot` keys.

    Required content:
    1. Add helper SQL functions with exact purposes:
       - `resolve_schedule_shift_band(shift_slot jsonb)` -> `PAGI`, `SIANG`, `SORE`, or `LIBUR`
       - `resolve_schedule_required_work_minutes(employee_contract public.employee_contract)` -> `600` or `540`
       - `resolve_schedule_late_cutoff_minutes(shift_band text)` -> `420`, `600`, or `900`
       - `resolve_schedule_break_first_deadline_minutes(shift_band text, employee_contract public.employee_contract)` -> cutoff plus `120` or `60`

    2. Backfill active `schedule_entries.shift_slot` JSON with `jsonb_set(...)` so every scheduled row gains these keys:
       - `band`
       - `required_work_minutes`
       - `late_cutoff_hour`
       - `late_cutoff_minute`
       - `break_first_deadline_hour`
       - `break_first_deadline_minute`

    3. Join `schedule_entries.employee_id` to `employees.employment_contract` so contract defaults come from the stored Phase 54 contract field, not ad hoc scan history.

    4. Keep `name`, `start_hour`, `start_minute`, `end_hour`, `end_minute`, and `color` intact in the JSON payload.

    5. Add comments that explicitly document:
       - the patch is additive-only
       - `FULLTIME=600` and `PARTTIME=540` are Phase 55 default-hours assumptions aligned with later recap requirements
       - per-day schedule rows may still override `required_work_minutes` later from the app layer
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'sql/phase_55_schedule_policy_foundation_20260326.sql' -Pattern 'resolve_schedule_shift_band','required_work_minutes','late_cutoff_hour','break_first_deadline_hour','jsonb_set' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `sql/phase_55_schedule_policy_foundation_20260326.sql` exists
    - the SQL file contains `resolve_schedule_shift_band`
    - the SQL file contains `resolve_schedule_required_work_minutes`
    - the SQL file contains `resolve_schedule_late_cutoff_minutes`
    - the SQL file contains `resolve_schedule_break_first_deadline_minutes`
    - the SQL file contains `required_work_minutes`
    - the SQL file contains `late_cutoff_hour`
    - the SQL file contains `break_first_deadline_hour`
    - the SQL file contains `jsonb_set`
    - the SQL file contains `employment_contract`
  </acceptance_criteria>
  <done>The repo contains one additive SQL patch that upgrades stored schedule rows with band, required-hours, and WITA policy metadata without deleting legacy keys.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/models/shift_schedule_policy_test.dart` passes.
- `sql/phase_55_schedule_policy_foundation_20260326.sql` exists and clearly backfills the agreed policy keys into `shift_slot`.
- The only default required-hours values encoded by this plan are `600` and `540`, and that inference is documented explicitly.
</verification>

<success_criteria>
- Schedule payloads are band-first instead of exact-time-first.
- WITA cutoff and break-first math exists in one reusable Dart layer.
- Live stored rows can be upgraded additively through one SQL patch.
- Later Phase 55 plans can consume the same policy keys without inventing a second schedule contract.
</success_criteria>

<output>
After completion, create `.planning/phases/55-schedule-policy-absence-rules/55-01-SUMMARY.md`
</output>

