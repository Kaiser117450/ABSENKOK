---
phase: 35-multi-device-acceptance-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md
autonomous: false
requirements: [HEALTH-01, HEALTH-03]
must_haves:
  truths:
    - "Phase 35 execution refuses to continue if Phase 34 rollout evidence is missing or inconclusive"
    - "The same physical kiosk device keeps one device_uuid across logout and re-setup"
    - "Two physical devices on the same outlet produce two distinct active kiosk_devices rows"
    - "The admin dashboard shows both devices independently for the same outlet instead of collapsing them"
    - "Phase 35 records baseline and post-action evidence in its validation artifact, not just a test intention"
  artifacts:
    - path: ".planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md"
      provides: "Operator-ready checklist and live evidence log for prerequisite, UUID, and dual-device verification"
      contains: "Prerequisite Check"
  key_links:
    - from: "lib/services/device_identity_service.dart"
      to: "lib/services/heartbeat_service.dart"
      via: "DeviceIdentityService.getOrCreateDeviceUuid() cached before heartbeat start"
      pattern: "getOrCreateDeviceUuid"
    - from: "lib/services/heartbeat_service.dart"
      to: "sql/phase_31_kiosk_devices_20260320.sql"
      via: "Supabase.instance.client.rpc('upsert_kiosk_heartbeat')"
      pattern: "upsert_kiosk_heartbeat"
    - from: "lib/screens/admin/admin_dashboard_screen.dart"
      to: "lib/models/kiosk_device.dart"
      via: "from('kiosk_devices') and _kioskDevices list rendering"
      pattern: "from('kiosk_devices')"
---

<objective>
Create the operator packet for Phase 35 and capture the foundational live proof: UUID persistence on one physical device plus two-device same-outlet separation.

Purpose: close the audit gap between phases 31 and 32 with real outlet evidence before any requirements are marked complete.
Output: `35-VALIDATION.md` contains prerequisite checks, executed evidence for UUID persistence, and executed evidence that two devices stay separate on one outlet.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md
@.planning/phases/34-v6-supabase-rollout-evidence/34-VALIDATION.md
@.planning/phases/35-multi-device-acceptance-verification/35-RESEARCH.md
@.planning/phases/31-device-identity-foundation/31-VALIDATION.md
@.planning/phases/32-multi-device-dashboard/32-VALIDATION.md
@lib/services/device_identity_service.dart
@lib/services/heartbeat_service.dart
@lib/screens/admin/admin_dashboard_screen.dart
@sql/phase_31_kiosk_devices_20260320.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the phase-35 acceptance packet</name>
  <files>.planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md</files>
  <read_first>
    - .planning/phases/35-multi-device-acceptance-verification/35-RESEARCH.md
    - .planning/phases/34-v6-supabase-rollout-evidence/34-VALIDATION.md
    - .planning/phases/31-device-identity-foundation/31-VALIDATION.md
    - .planning/phases/32-multi-device-dashboard/32-VALIDATION.md
    - lib/services/device_identity_service.dart
    - lib/services/heartbeat_service.dart
    - lib/screens/admin/admin_dashboard_screen.dart
    - sql/phase_31_kiosk_devices_20260320.sql
  </read_first>
  <action>
    Finalize `35-VALIDATION.md` as the operator-ready checklist before any live verification:

    1. Keep a dedicated prerequisite section that tells the executor to review Phase 34 rollout evidence first.
    2. Ensure the UUID persistence and same-outlet dual-device sections contain the exact SQL queries from `35-RESEARCH.md`.
    3. Keep placeholders for device UUIDs, outlet IDs, timestamps, and blocker notes so the final execution leaves behind auditable proof.
    4. Do not change app code or SQL in this task. This task is only about building the acceptance packet.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md' -Pattern 'Prerequisite Check','UUID Persistence','Dual-Device Same-Outlet','SELECT device_uuid' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>`35-VALIDATION.md` is ready to drive live execution without improvisation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify UUID persistence across logout and re-setup on the same device</name>
  <what-built>
    HEALTH-01 is proven with live evidence showing that one physical kiosk installation keeps the same `device_uuid` across logout and re-setup.
  </what-built>
  <how-to-verify>
    1. Review `.planning/phases/34-v6-supabase-rollout-evidence/34-VALIDATION.md`. If rollout proof is missing or ambiguous, stop and route back to Phase 34.
    2. On physical device A, open or set up a kiosk session for the target outlet and wait for the immediate or next heartbeat.
    3. Run the baseline query from `35-VALIDATION.md` and record the most recent `device_uuid`, outlet, and timestamp for device A.
    4. Log out device A completely, then perform kiosk re-setup again on that same physical device.
    5. Wait for the next heartbeat and run the post re-setup query filtered by the recorded UUID.
    6. Confirm the same `device_uuid` is still being updated rather than a new device row appearing.
    7. Write the before/after evidence summary into `35-VALIDATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded device UUID and the before/after timestamp summary, or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify two devices stay separate on the same outlet</name>
  <what-built>
    HEALTH-03 has live 31 -> 32 integration proof: two different kiosk devices can operate on one outlet without overwriting each other.
  </what-built>
  <how-to-verify>
    1. Using the same outlet from Task 2, sign in or re-open kiosk sessions on two physical devices at once.
    2. Wait for both devices to send recent heartbeats.
    3. Run the same-outlet query from `35-VALIDATION.md` and confirm there are two active rows with distinct `device_uuid` values for the target outlet.
    4. Open the admin dashboard for that outlet and confirm two device cards are visible independently.
    5. Record the outlet ID, both device UUIDs or prefixes, recent timestamps, and dashboard observation in `35-VALIDATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" with the outlet ID and the two device UUIDs or prefixes you observed, or describe blockers.</resume-signal>
</task>

</tasks>

<verification>
- `35-VALIDATION.md` contains the prerequisite review plus the exact UUID and same-outlet queries
- one physical device proves the same `device_uuid` across logout and re-setup
- one outlet shows two distinct active device rows in `kiosk_devices`
- the admin dashboard shows those devices independently
</verification>

<success_criteria>
- Phase 35 has an operator-ready acceptance packet
- HEALTH-01 has live persistence evidence, not just code-level implementation
- HEALTH-03 has live two-device same-outlet evidence, not just draft validation intent
- execution leaves behind auditable notes for the later documentation-sync plan
</success_criteria>

<output>
After completion, create `.planning/phases/35-multi-device-acceptance-verification/35-01-SUMMARY.md`
</output>
