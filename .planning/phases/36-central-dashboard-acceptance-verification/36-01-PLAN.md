---
phase: 36-central-dashboard-acceptance-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md
autonomous: false
requirements: [ADMIN-01]
must_haves:
  truths:
    - "Phase 36 execution refuses to continue if Phase 34 or Phase 35 evidence is missing or inconclusive"
    - "A full admin lands on CentralDashboardScreen at /admin/dashboard in the live app"
    - "Tapping one outlet card opens the outlet dashboard with the correct outlet preselected"
    - "A kepala_gerai account remains outlet-scoped and does not see the central rollup surface"
    - "Phase 36 records role, route, outlet, and timestamp evidence in its validation artifact"
  artifacts:
    - path: ".planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md"
      provides: "Operator-ready checklist and live evidence log for prerequisite, routing, and role-gating verification"
      contains: "Prerequisite Check"
  key_links:
    - from: "lib/app.dart"
      to: "lib/screens/admin/central_dashboard_screen.dart"
      via: "/admin/dashboard builder for full admin"
      pattern: "CentralDashboardScreen"
    - from: "lib/screens/admin/central_dashboard_screen.dart"
      to: "lib/screens/admin/admin_dashboard_screen.dart"
      via: "context.push('/admin/outlet-dashboard?outletId=...')"
      pattern: "/admin/outlet-dashboard"
    - from: "lib/app.dart"
      to: "lib/screens/admin/admin_dashboard_screen.dart"
      via: "AdminDashboardScreen(initialOutletId: outletId)"
      pattern: "initialOutletId"
---

<objective>
Prepare the Phase 36 acceptance packet and capture the routing proof: full-admin central-dashboard access, outlet drilldown, and kepala-gerai outlet scoping.

Purpose: close the role-aware central-dashboard gap from Phase 33 before any KPI values are marked as verified.
Output: `36-VALIDATION.md` contains prerequisite checks, executed role/routing evidence, and an operator-ready checklist for the later KPI comparison work.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md
@.planning/phases/33-multi-outlet-control-center/33-VALIDATION.md
@.planning/phases/34-v6-supabase-rollout-evidence/34-VALIDATION.md
@.planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md
@.planning/phases/36-central-dashboard-acceptance-verification/36-RESEARCH.md
@lib/app.dart
@lib/screens/admin/central_dashboard_screen.dart
@lib/screens/admin/admin_dashboard_screen.dart
@lib/screens/admin/admin_shell.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the phase-36 acceptance packet</name>
  <files>.planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md</files>
  <read_first>
    - .planning/phases/36-central-dashboard-acceptance-verification/36-RESEARCH.md
    - .planning/phases/33-multi-outlet-control-center/33-VALIDATION.md
    - .planning/phases/34-v6-supabase-rollout-evidence/34-VALIDATION.md
    - .planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md
    - lib/app.dart
    - lib/screens/admin/central_dashboard_screen.dart
    - lib/screens/admin/admin_dashboard_screen.dart
    - lib/screens/admin/admin_shell.dart
  </read_first>
  <action>
    Finalize `36-VALIDATION.md` as the operator-ready checklist before any live verification:

    1. Keep a dedicated prerequisite section that tells the executor to review Phase 34 and Phase 35 evidence first.
    2. Ensure the routing/role steps and the direct KPI comparison queries from `36-RESEARCH.md` are present in `36-VALIDATION.md`.
    3. Keep placeholders for role, route, outlet, KPI snapshot, query timestamp, and blocker notes so the final execution leaves behind auditable proof.
    4. Do not change app code or SQL in this task. This task is only about building the acceptance packet.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md' -Pattern 'Prerequisite Check','Direct chain-wide KPI summary query','Direct outlet rollup comparison query','Evidence Log' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>`36-VALIDATION.md` is ready to drive live routing and KPI verification without improvisation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full-admin landing and outlet drilldown</name>
  <what-built>
    ADMIN-01 has live proof that a full admin lands on the central dashboard and can drill into the correct outlet detail dashboard.
  </what-built>
  <how-to-verify>
    1. Review `.planning/phases/34-v6-supabase-rollout-evidence/34-VALIDATION.md` and `.planning/phases/35-multi-device-acceptance-verification/35-VALIDATION.md`. If either prerequisite is missing or ambiguous, stop and route back.
    2. Sign in as a full admin and open `/admin/dashboard`.
    3. Confirm the central KPI cards and outlet rollup cards are visible.
    4. Tap one outlet card and let the app navigate into the outlet dashboard.
    5. Confirm the outlet detail dashboard opens with the tapped outlet preselected instead of an empty or incorrect outlet context.
    6. Record the full-admin account, tapped outlet, preselected outlet result, and visible screen evidence in `36-VALIDATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" with the tapped outlet name or ID and the observed preselected outlet result, or describe blockers.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify kepala-gerai stays outlet-scoped</name>
  <what-built>
    ADMIN-01 has live role-gating proof that `kepala_gerai` does not see the central rollup surface and remains limited to outlet-scoped dashboard behavior.
  </what-built>
  <how-to-verify>
    1. Sign in as a `kepala_gerai` account.
    2. Open `/admin/dashboard`.
    3. Confirm the central summary cards and outlet rollup list are not shown.
    4. Confirm the dashboard behavior remains scoped to the managed outlet rather than the full chain view.
    5. Record the managed outlet, the observed screen, and the role-gating result in `36-VALIDATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" with the managed outlet and the observed role-gating result, or describe blockers.</resume-signal>
</task>

</tasks>

<verification>
- `36-VALIDATION.md` contains the prerequisite review plus the exact routing and KPI comparison checklist
- a full admin lands on the central dashboard in the live app
- tapping one outlet opens the outlet dashboard with the correct outlet selected
- a `kepala_gerai` account remains outlet-scoped and does not see the central rollup surface
</verification>

<success_criteria>
- Phase 36 has an operator-ready acceptance packet
- ADMIN-01 has live role-aware routing proof, not just widget-test coverage
- execution leaves behind auditable notes for the later KPI-comparison plan
</success_criteria>

<output>
After completion, create `.planning/phases/36-central-dashboard-acceptance-verification/36-01-SUMMARY.md`
</output>
