---
phase: 36-central-dashboard-acceptance-verification
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/33-multi-outlet-control-center/33-VALIDATION.md
  - .planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md
autonomous: false
requirements: [ADMIN-01, ADMIN-02]
must_haves:
  truths:
    - "Displayed central-dashboard KPI cards match direct Supabase query results"
    - "Displayed outlet rollup rows match direct per-outlet Supabase rollup results"
    - "Phase 33 validation stops reading like an unsigned draft once Phase 36 evidence exists"
    - "ADMIN-01 and ADMIN-02 are only marked complete if live routing and KPI proof is concrete"
    - "STATE.md records the central-dashboard role and data proof that closes the final v6.0 audit gap"
  artifacts:
    - path: ".planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md"
      provides: "Executed KPI comparison evidence and final phase-36 approval notes"
      contains: "36-02-01 Live KPI Comparison"
    - path: ".planning/phases/33-multi-outlet-control-center/33-VALIDATION.md"
      provides: "Closed manual verification rows for central dashboard routing and live data proof"
      contains: "Full admin lands on central dashboard at `/admin/dashboard`"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Evidence-backed completion state for ADMIN-01 and ADMIN-02"
      contains: "ADMIN-01"
  key_links:
    - from: "lib/screens/admin/central_dashboard_screen.dart"
      to: "lib/services/analytics_service.dart"
      via: "AnalyticsService.instance.getCentralDashboardSummary(date: DateTime.now())"
      pattern: "getCentralDashboardSummary"
    - from: "lib/screens/admin/central_dashboard_screen.dart"
      to: "lib/services/analytics_service.dart"
      via: "AnalyticsService.instance.getOutletControlCenter(date: DateTime.now())"
      pattern: "getOutletControlCenter"
    - from: "lib/services/analytics_service.dart"
      to: "sql/phase_33_central_dashboard_20260322.sql"
      via: "get_central_dashboard_summary and get_outlet_control_center RPCs"
      pattern: "get_central_dashboard_summary|get_outlet_control_center"
---

<objective>
Verify live central-dashboard KPI fidelity against direct Supabase data, then synchronize the planning artifacts so the remaining v6.0 audit gaps become evidence-backed instead of draft-only.

Purpose: close the final Phase 33 acceptance debt for central-dashboard routing and live aggregate data.
Output: KPI comparison proof is recorded, Phase 33 validation is synchronized, and ADMIN-01 plus ADMIN-02 can be marked complete when evidence exists.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v6.0-MILESTONE-AUDIT.md
@.planning/phases/33-multi-outlet-control-center/33-VALIDATION.md
@.planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md
@.planning/phases/36-central-dashboard-acceptance-verification/36-RESEARCH.md
@lib/screens/admin/central_dashboard_screen.dart
@lib/services/analytics_service.dart
@sql/phase_33_central_dashboard_20260322.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify central KPI cards and outlet rows against live Supabase data</name>
  <what-built>
    ADMIN-01 and ADMIN-02 are proven with live evidence showing that the central-dashboard cards and outlet rollups match direct Supabase data, not just local tests.
  </what-built>
  <how-to-verify>
    1. Start from the full-admin session proven in Plan 01.
    2. Capture the visible central-dashboard KPI values and one or more outlet row values in the UI.
    3. Run the direct chain-wide KPI summary query from `36-VALIDATION.md` and compare the results with the visible KPI cards.
    4. Run the direct outlet rollup comparison query from `36-VALIDATION.md` and compare the results with the visible outlet rows.
    5. Allow only normal rounding tolerance for attendance percentages; if the values disagree materially, stop and record the mismatch instead of marking the requirements complete.
    6. Record the UI snapshots, query summaries, timestamp, and comparison result in `36-VALIDATION.md`.
  </how-to-verify>
  <resume-signal>Type "approved" with the compared KPI summary and one compared outlet row, or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Synchronize validation, requirements, and state from the live proof</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/33-multi-outlet-control-center/33-VALIDATION.md, .planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md</files>
  <read_first>
    - .planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md
    - .planning/phases/33-multi-outlet-control-center/33-VALIDATION.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/v6.0-MILESTONE-AUDIT.md
  </read_first>
  <action>
    Update the planning layer only if Plan 01 and Task 1 produced concrete proof:

    1. In `36-VALIDATION.md`, replace pending notes with the actual role, outlet, KPI, query, and timestamp summaries; set `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`, and approval only when the proof is complete.
    2. In `33-VALIDATION.md`, mark the manual central-dashboard verification rows as completed, reference the Phase 36 evidence date, and set the validation frontmatter/sign-off to an approved, non-draft state only if the proof is complete.
    3. In `REQUIREMENTS.md`, mark `ADMIN-01` and `ADMIN-02` complete and update their traceability rows from `Pending` to `Complete`.
    4. In `STATE.md`, add a dated accumulated-context note that Phase 36 captured live central-dashboard routing proof and KPI comparison evidence; note that v6.0 is ready for a fresh audit if earlier phase evidence already exists.
    5. If any proof is missing or inconclusive, record the blocker in `36-VALIDATION.md` and leave `33-VALIDATION.md` plus `REQUIREMENTS.md` pending.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.planning/REQUIREMENTS.md','.planning/STATE.md','.planning/phases/33-multi-outlet-control-center/33-VALIDATION.md','.planning/phases/36-central-dashboard-acceptance-verification/36-VALIDATION.md' -Pattern 'ADMIN-01','ADMIN-02','Approval:','Phase 36' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>The planning layer reflects live central-dashboard evidence instead of draft acceptance intent, and the ADMIN requirements move to complete only if the proof is real.</done>
</task>

</tasks>

<verification>
- live SQL output proves the central KPI values and outlet rows match the UI
- `33-VALIDATION.md` reflects executed manual proof instead of pure draft status
- `REQUIREMENTS.md` and `STATE.md` are synchronized with the central-dashboard evidence
- the project is positioned for a fresh milestone audit without extra manual interpretation
</verification>

<success_criteria>
- ADMIN-01 and ADMIN-02 are backed by live role and data proof
- the old Phase 33 draft validation debt is explicitly closed
- a fresh `$gsd-audit-milestone` can consume the updated evidence cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-central-dashboard-acceptance-verification/36-02-SUMMARY.md`
</output>
