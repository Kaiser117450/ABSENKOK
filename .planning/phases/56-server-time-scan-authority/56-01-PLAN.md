---
phase: 56-server-time-scan-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/phase_56_server_time_scan_authority_20260327.sql
  - lib/models/attendance_log.dart
  - lib/models/kiosk_scan_context.dart
  - lib/services/kiosk_scan_authority_service.dart
  - test/services/kiosk_scan_authority_service_test.dart
  - test/phase56/server_time_scan_sql_contract_test.dart
autonomous: true
requirements: [SCAN-01, SCAN-02]
user_setup:
  - service: supabase
    why: "Phase 56 adds additive attendance-log authority columns and kiosk RPCs, so the SQL must be reviewed and applied only after explicit user approval."
    dashboard_config:
      - task: "Run sql/phase_56_server_time_scan_authority_20260327.sql in Supabase SQL Editor after the user approves the production change."
        location: "Supabase Dashboard -> SQL Editor -> New Query"
must_haves:
  truths:
    - "Live kiosk scans stop trusting the tablet clock and only become authoritative when the server stamps `attendance_logs.scanned_at`."
    - "Break-first confirmation is stored as explicit metadata instead of being reconstructed later from a plain `break` row."
    - "Queued replay provenance stays attached to the attendance log through additive audit fields, so suspicious reconciliation can be reviewed instead of silently rewritten."
    - "Phase 55 policy helpers remain the only source of truth for WITA lateness and break-first windows."
  artifacts:
    - path: "sql/phase_56_server_time_scan_authority_20260327.sql"
      provides: "Additive SQL patch for kiosk scan context/record RPCs, authoritative attendance fields, and recap updates"
      contains: "record_kiosk_scan"
    - path: "lib/models/kiosk_scan_context.dart"
      provides: "Typed DTOs for kiosk scan context, authority record requests, and authority results"
      exports: ["KioskScanContext", "KioskScanRecordRequest", "KioskScanRecordResult"]
    - path: "lib/services/kiosk_scan_authority_service.dart"
      provides: "Single Flutter gateway for `get_kiosk_scan_context` and `record_kiosk_scan`"
      exports: ["KioskScanAuthorityService"]
    - path: "lib/models/attendance_log.dart"
      provides: "Attendance log model with authoritative capture/provenance metadata"
      contains: "captureMode"
    - path: "test/phase56/server_time_scan_sql_contract_test.dart"
      provides: "Regression guard for Phase 56 SQL field names, RPC names, and WITA timestamp semantics"
      contains: "record_kiosk_scan"
  key_links:
    - from: "lib/services/kiosk_scan_authority_service.dart"
      to: "sql/phase_56_server_time_scan_authority_20260327.sql"
      via: "the Flutter authority service must call the exact RPC names defined by the additive SQL patch"
      pattern: "get_kiosk_scan_context|record_kiosk_scan"
    - from: "lib/models/attendance_log.dart"
      to: "sql/phase_56_server_time_scan_authority_20260327.sql"
      via: "new attendance-log fields must parse the same provenance columns written by the RPC"
      pattern: "device_captured_at|capture_mode|queue_order|initial_scan_intent|requires_admin_review"
    - from: "sql/phase_56_server_time_scan_authority_20260327.sql"
      to: "sql/phase_55_admin_policy_recap_20260326.sql"
      via: "Phase 56 must preserve the Phase 55 recap contract while upgrading `break_first_confirmed` from placeholder false to stored truth"
      pattern: "break_first_confirmed"
---

<objective>
Create the authoritative server contract for Phase 56 before any kiosk UI or offline queue changes.

Purpose: the Flutter kiosk cannot decide authoritative time, break-first eligibility, or suspicious replay outcomes from `DateTime.now()` and direct table inserts. Phase 56 first needs one additive SQL contract plus one typed Flutter service/model layer that every later plan can trust.
Output: one additive SQL patch, one typed authority DTO file, one authority service, and focused contract tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-server-time-scan-authority/56-CONTEXT.md
@.planning/phases/56-server-time-scan-authority/56-RESEARCH.md
@.planning/phases/56-server-time-scan-authority/56-VALIDATION.md
@.planning/phases/56-server-time-scan-authority/56-UI-SPEC.md
@.planning/phases/55-schedule-policy-absence-rules/55-01-PLAN.md
@.planning/phases/55-schedule-policy-absence-rules/55-03-PLAN.md
@lib/models/attendance_log.dart
@lib/services/attendance_policy_recap_service.dart
@sql/phase_42_portal_attendance_recap_20260323.sql
@sql/phase_55_schedule_policy_foundation_20260326.sql
@sql/phase_55_admin_policy_recap_20260326.sql

<interfaces>
Phase 56 SQL authority fields that must exist after this plan:
```text
attendance_logs.device_captured_at
attendance_logs.capture_mode
attendance_logs.queue_order
attendance_logs.initial_scan_intent
attendance_logs.requires_admin_review
```

Target RPC names and responsibilities:
```text
get_kiosk_scan_context
- returns server-owned "now"
- returns WITA display time
- returns last authoritative attendance type/timestamp
- returns Phase 55 policy context needed for break-first eligibility

record_kiosk_scan
- stamps scanned_at on the server
- stores local provenance and explicit break-first intent
- returns authoritative WITA success data
- returns whether the replay needs admin review
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the additive SQL authority patch for server-owned scan time and recap truth</name>
  <files>sql/phase_56_server_time_scan_authority_20260327.sql</files>
  <read_first>
    - sql/phase_55_schedule_policy_foundation_20260326.sql
    - sql/phase_55_admin_policy_recap_20260326.sql
    - sql/phase_42_portal_attendance_recap_20260323.sql
    - .planning/STATE.md
    - .planning/phases/56-server-time-scan-authority/56-RESEARCH.md
  </read_first>
  <action>
    Create `sql/phase_56_server_time_scan_authority_20260327.sql` as one additive-only Phase 56 patch. Do not drop columns, rewrite existing rows destructively, or remove Phase 55 helpers.

    Required SQL content:
    1. Additive `attendance_logs` authority/provenance fields:
       - `device_captured_at timestamptz`
       - `capture_mode text NOT NULL DEFAULT 'live' CHECK (capture_mode IN ('live','queued'))`
       - `queue_order bigint`
       - `initial_scan_intent text NOT NULL DEFAULT 'none' CHECK (initial_scan_intent IN ('none','break_first'))`
       - `requires_admin_review boolean NOT NULL DEFAULT false`
    2. Add an idempotent unique index on `attendance_logs.local_id` for non-null values so queued replay stays idempotent.
    3. Define `get_kiosk_scan_context(...)` as a `SECURITY DEFINER` RPC that:
       - accepts only the employee, outlet, and device identifiers needed for kiosk context
       - derives current WITA business time from `statement_timestamp() AT TIME ZONE 'Asia/Makassar'`
       - returns the last authoritative attendance event for the employee/outlet timeline
       - returns Phase 55 policy outputs needed by the UI: `shift_band`, `employment_contract`, `late_cutoff_local`, `break_first_deadline_local`, and `break_first_eligible`
       - returns the display-ready live time label in the exact `HH:MM WITA` format
    4. Define `record_kiosk_scan(...)` as a `SECURITY DEFINER` RPC that:
       - stamps `attendance_logs.scanned_at` from the server with `statement_timestamp()`
       - accepts `p_type`, `p_local_id`, `p_capture_mode`, `p_device_captured_at`, `p_queue_order`, `p_initial_scan_intent`, `p_is_backup`, `p_notes`, `p_lat`, and `p_lng`
       - writes the new provenance fields exactly into the additive attendance-log columns
       - returns the authoritative WITA success label, the stored attendance type, the stored initial intent, and `requires_admin_review`
       - treats queued replay as suspicious when the replay order or timing no longer matches the Phase 55 break-first window, but still keeps the event row instead of deleting it
    5. In the same SQL patch, replace or extend the admin recap contract so `break_first_confirmed` becomes true only when the stored first relevant event has `initial_scan_intent = 'break_first'`. Keep normal late, leave, and absence rules untouched.
    6. Comment clearly that the file is additive-only and still requires explicit user approval before production application.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'sql/phase_56_server_time_scan_authority_20260327.sql' -Pattern 'get_kiosk_scan_context','record_kiosk_scan','device_captured_at','capture_mode','queue_order','initial_scan_intent','requires_admin_review','statement_timestamp()','break_first_confirmed' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `sql/phase_56_server_time_scan_authority_20260327.sql` exists
    - the SQL file contains `get_kiosk_scan_context`
    - the SQL file contains `record_kiosk_scan`
    - the SQL file contains `device_captured_at`
    - the SQL file contains `capture_mode`
    - the SQL file contains `queue_order`
    - the SQL file contains `initial_scan_intent`
    - the SQL file contains `requires_admin_review`
    - the SQL file contains `statement_timestamp()`
    - the SQL file contains `break_first_confirmed`
    - the SQL file contains `HH24:MI "WITA"` or equivalent exact `HH:MM WITA` formatting logic
  </acceptance_criteria>
  <done>The repo contains one additive SQL authority contract that can stamp kiosk scan time on the server, persist break-first intent explicitly, and surface recap truth without trusting the tablet clock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed Flutter authority DTOs and one service that unwraps the Phase 56 RPC contract</name>
  <files>lib/models/attendance_log.dart, lib/models/kiosk_scan_context.dart, lib/services/kiosk_scan_authority_service.dart, test/services/kiosk_scan_authority_service_test.dart, test/phase56/server_time_scan_sql_contract_test.dart</files>
  <read_first>
    - lib/models/attendance_log.dart
    - lib/services/attendance_policy_recap_service.dart
    - .planning/phases/56-server-time-scan-authority/56-RESEARCH.md
    - sql/phase_56_server_time_scan_authority_20260327.sql
  </read_first>
  <action>
    Implement the typed Phase 56 Flutter authority contract with these exact responsibilities:

    1. Create `lib/models/kiosk_scan_context.dart` with DTOs for:
       - `KioskScanContext`
         - `serverNowUtc`
         - `serverNowWitaLabel`
         - `logicalDate`
         - `lastAuthoritativeType`
         - `lastAuthoritativeScannedAt`
         - `shiftBand`
         - `employmentContract`
         - `lateCutoffLocal`
         - `breakFirstDeadlineLocal`
         - `breakFirstEligible`
       - `KioskScanRecordRequest`
         - `employeeId`
         - `outletId`
         - `deviceId`
         - `type`
         - `localId`
         - `captureMode`
         - `deviceCapturedAt`
         - `queueOrder`
         - `initialScanIntent`
         - `isBackup`
         - `notes`
         - `lat`
         - `lng`
       - `KioskScanRecordResult`
         - `authorityState`
         - `scannedAtUtc`
         - `scannedAtWitaLabel`
         - `recordedType`
         - `initialScanIntent`
         - `requiresAdminReview`
    2. Extend `lib/models/attendance_log.dart` to parse and expose the additive authority fields:
       - `deviceCapturedAt`
       - `captureMode`
       - `queueOrder`
       - `initialScanIntent`
       - `requiresAdminReview`
       Keep all existing Phase 55 fields backward-compatible for rows that do not yet have the new columns.
    3. Create `lib/services/kiosk_scan_authority_service.dart` as the only kiosk RPC gateway.
       - `fetchContext(...)` must call `get_kiosk_scan_context`
       - `recordScan(...)` must call `record_kiosk_scan`
       - use the same nested response unwrapping style already established in `AttendancePolicyRecapService`
       - never compute WITA authority locally in Dart
    4. Add `test/services/kiosk_scan_authority_service_test.dart` to prove:
       - nested `data -> data` RPC payloads unwrap safely
       - `KioskScanContext` maps break-first flags and last-type data correctly
       - `KioskScanRecordResult` maps `authorityState`, `scannedAtWitaLabel`, and `requiresAdminReview` correctly
       - missing optional columns degrade safely instead of throwing
    5. Add `test/phase56/server_time_scan_sql_contract_test.dart` to read the SQL file directly and assert the required RPC names and field names from Task 1 remain present.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/kiosk_scan_authority_service_test.dart test/phase56/server_time_scan_sql_contract_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/kiosk_scan_context.dart` exists and contains `class KioskScanContext`
    - `lib/models/kiosk_scan_context.dart` contains `class KioskScanRecordRequest`
    - `lib/models/kiosk_scan_context.dart` contains `class KioskScanRecordResult`
    - `lib/services/kiosk_scan_authority_service.dart` exists and contains `class KioskScanAuthorityService`
    - `lib/services/kiosk_scan_authority_service.dart` contains `get_kiosk_scan_context`
    - `lib/services/kiosk_scan_authority_service.dart` contains `record_kiosk_scan`
    - `lib/models/attendance_log.dart` contains `captureMode`
    - `lib/models/attendance_log.dart` contains `initialScanIntent`
    - `lib/models/attendance_log.dart` contains `requiresAdminReview`
    - `test/services/kiosk_scan_authority_service_test.dart` exists and contains `group('KioskScanAuthorityService'`
    - `test/phase56/server_time_scan_sql_contract_test.dart` exists and contains `record_kiosk_scan`
    - `C:\flutter\bin\flutter.bat test test/services/kiosk_scan_authority_service_test.dart test/phase56/server_time_scan_sql_contract_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo exposes one typed Flutter authority layer that can fetch authoritative kiosk context and submit server-owned scans without hand-rolling WITA logic in widgets.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/kiosk_scan_authority_service_test.dart test/phase56/server_time_scan_sql_contract_test.dart` passes.
- `sql/phase_56_server_time_scan_authority_20260327.sql` contains the additive Phase 56 columns and both kiosk RPC names.
- `lib/services/kiosk_scan_authority_service.dart` becomes the only intended entry point for live kiosk authority calls.
</verification>

<success_criteria>
- Live kiosk submissions can obtain authoritative WITA time from one server-owned contract.
- Break-first intent becomes explicit data, not a later guess from plain `break` rows.
- Queued replay has additive provenance fields ready before the offline queue changes land.
- The recap layer can consume real break-first confirmation truth after this plan instead of hard-coded false.
</success_criteria>

<output>
After completion, create `.planning/phases/56-server-time-scan-authority/56-01-SUMMARY.md`
</output>
