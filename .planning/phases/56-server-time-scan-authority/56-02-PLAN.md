---
phase: 56-server-time-scan-authority
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/core/constants.dart
  - lib/models/pending_log.dart
  - lib/services/sqlite_service.dart
  - lib/services/sync_service.dart
  - lib/screens/admin/sakit_izin_dialog.dart
  - test/services/sync_service_order_test.dart
autonomous: true
requirements: [SCAN-01]
must_haves:
  truths:
    - "Queued attendance events preserve a monotonic device-local order instead of relying on `created_at` or network race timing."
    - "Every pending row carries the same capture-mode and initial-intent metadata that the Phase 56 authority RPC expects."
    - "Sync replays queued events sequentially through the authority RPC, never with `Future.wait` direct inserts."
    - "Shared queue producers such as admin `sakit/izin` fallback continue to work without inventing break-first metadata."
  artifacts:
    - path: "lib/services/sqlite_service.dart"
      provides: "Phase 56 offline queue schema and ordered read/write helpers"
      contains: "queue_order"
    - path: "lib/models/pending_log.dart"
      provides: "Pending queue model with capture metadata required by authority replay"
      contains: "initialScanIntent"
    - path: "lib/services/sync_service.dart"
      provides: "Deterministic authority replay for queued attendance logs"
      contains: "recordScan"
    - path: "test/services/sync_service_order_test.dart"
      provides: "Regression coverage that queued replay stays ordered and idempotent"
      contains: "queueOrder"
  key_links:
    - from: "lib/services/sync_service.dart"
      to: "lib/services/kiosk_scan_authority_service.dart"
      via: "queued replay must use the same Phase 56 RPC contract as live scans"
      pattern: "KioskScanAuthorityService"
    - from: "lib/services/sqlite_service.dart"
      to: "lib/models/pending_log.dart"
      via: "new queue columns must round-trip exactly through the shared pending-log model"
      pattern: "queueOrder|captureMode|initialScanIntent"
    - from: "lib/screens/admin/sakit_izin_dialog.dart"
      to: "lib/services/sqlite_service.dart"
      via: "the shared offline queue producer must still be able to insert queued rows safely with Phase 56 defaults"
      pattern: "insertPendingLog"
---

<objective>
Upgrade the local queue and sync path so queued scans preserve authoritative replay order before the kiosk UI starts depending on them.

Purpose: Phase 56 cannot promise safe offline capture while `pending_logs` still sorts by clock-derived timestamps and `syncPendingLogs()` still uses parallel direct inserts. This plan hardens the queue first so the later kiosk UI can rely on ordered replay.
Output: one SQLite schema bump, one richer pending-log model, one ordered sync service, and targeted replay tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-server-time-scan-authority/56-CONTEXT.md
@.planning/phases/56-server-time-scan-authority/56-RESEARCH.md
@.planning/phases/56-server-time-scan-authority/56-VALIDATION.md
@.planning/phases/56-server-time-scan-authority/56-01-PLAN.md
@lib/core/constants.dart
@lib/models/pending_log.dart
@lib/services/sqlite_service.dart
@lib/services/sync_service.dart
@lib/screens/admin/sakit_izin_dialog.dart

<interfaces>
Target pending queue fields after Phase 56:
```text
local_id
device_id
type
scanned_at
device_captured_at
capture_mode
queue_order
initial_scan_intent
sync_status
```

Required sync rule:
```text
read rows by queue_order ASC
replay each row through KioskScanAuthorityService.recordScan(...)
mark synced only after RPC success or duplicate local_id acknowledgement
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump the SQLite queue contract for monotonic replay and explicit scan intent</name>
  <files>lib/core/constants.dart, lib/models/pending_log.dart, lib/services/sqlite_service.dart, lib/screens/admin/sakit_izin_dialog.dart</files>
  <read_first>
    - lib/core/constants.dart
    - lib/models/pending_log.dart
    - lib/services/sqlite_service.dart
    - lib/screens/admin/sakit_izin_dialog.dart
    - .planning/phases/56-server-time-scan-authority/56-RESEARCH.md
  </read_first>
  <action>
    Upgrade the Phase 56 queue schema with explicit, concrete fields and safe defaults:

    1. Bump `AppConstants.dbVersion` from `5` to `6`.
    2. Extend `pending_logs` in `lib/services/sqlite_service.dart` to carry:
       - `device_captured_at TEXT`
       - `capture_mode TEXT NOT NULL DEFAULT 'queued' CHECK (capture_mode IN ('live','queued'))`
       - `queue_order INTEGER NOT NULL`
       - `initial_scan_intent TEXT NOT NULL DEFAULT 'none' CHECK (initial_scan_intent IN ('none','break_first'))`
    3. Implement a Phase 56 migration path that keeps unsynced rows and backfills concrete defaults:
       - existing unsynced rows become `capture_mode = 'queued'`
       - existing unsynced rows become `initial_scan_intent = 'none'`
       - existing `scanned_at` values copy into `device_captured_at` when the old schema had no separate device-capture field
       - existing unsynced rows get deterministic `queue_order` values in their prior `created_at ASC, local_id ASC` order
    4. Update `insertPendingLog(...)` so it accepts and stores:
       - `deviceCapturedAt`
       - `captureMode`
       - `initialScanIntent`
       while still defaulting admin fallback producers to `captureMode = 'queued'` and `initialScanIntent = 'none'`
    5. Add a helper that generates monotonic queue order from SQLite itself, for example `SELECT COALESCE(MAX(queue_order), 0) + 1`.
    6. Update `getPendingLogs()` to order exactly by `queue_order ASC, created_at ASC, local_id ASC`.
    7. Update `lib/models/pending_log.dart` so the new fields round-trip through `fromMap()` and `toMap()`.
    8. Update `lib/screens/admin/sakit_izin_dialog.dart` only as needed so the shared offline fallback still compiles and keeps explicit Phase 56 defaults instead of missing required arguments.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/core/constants.dart lib/models/pending_log.dart lib/services/sqlite_service.dart lib/screens/admin/sakit_izin_dialog.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/core/constants.dart` contains `dbVersion = 6`
    - `lib/services/sqlite_service.dart` contains `device_captured_at`
    - `lib/services/sqlite_service.dart` contains `capture_mode`
    - `lib/services/sqlite_service.dart` contains `queue_order`
    - `lib/services/sqlite_service.dart` contains `initial_scan_intent`
    - `lib/services/sqlite_service.dart` contains `MAX(queue_order)`
    - `lib/services/sqlite_service.dart` contains `orderBy: 'queue_order ASC`
    - `lib/models/pending_log.dart` contains `deviceCapturedAt`
    - `lib/models/pending_log.dart` contains `captureMode`
    - `lib/models/pending_log.dart` contains `initialScanIntent`
    - `lib/screens/admin/sakit_izin_dialog.dart` still contains `SqliteService.insertPendingLog`
    - `C:\flutter\bin\flutter.bat analyze lib/core/constants.dart lib/models/pending_log.dart lib/services/sqlite_service.dart lib/screens/admin/sakit_izin_dialog.dart` exits 0
  </acceptance_criteria>
  <done>The local queue now stores monotonic replay order and explicit scan-intent metadata without breaking existing shared offline producers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace parallel direct inserts with ordered authority replay</name>
  <files>lib/services/sync_service.dart, test/services/sync_service_order_test.dart</files>
  <read_first>
    - lib/services/sync_service.dart
    - lib/services/kiosk_scan_authority_service.dart
    - lib/services/sqlite_service.dart
    - .planning/phases/56-server-time-scan-authority/56-01-PLAN.md
    - .planning/phases/56-server-time-scan-authority/56-RESEARCH.md
  </read_first>
  <action>
    Rewrite queued replay around the Phase 56 authority service instead of direct table inserts.

    Required behavior:
    1. `SyncService.syncPendingLogs()` must stop calling `client.from('attendance_logs').insert(...)` directly.
    2. Replace the current `Future.wait(...)` replay with deterministic sequential replay:
       - iterate rows in the exact queue order returned by `SqliteService.getPendingLogs()`
       - call `KioskScanAuthorityService.recordScan(...)` once per row
       - await each row before moving to the next row
    3. Preserve idempotency:
       - duplicate `local_id` acknowledgement from the authority layer still marks the row synced
       - only rows that truly fail the authority call increment retry count
    4. Keep the existing pending cleanup behavior after successful syncs.
    5. Do not treat `Connectivity().checkConnectivity()` alone as proof that the row was synced. Connectivity may remain an optimization gate, but success must come from the authority RPC result.
    6. Add `test/services/sync_service_order_test.dart` and prove:
       - queued rows replay in ascending `queueOrder`
       - a failure on row N does not reorder rows N+1 before it
       - duplicate `local_id` paths still mark rows synced
       - `initialScanIntent = 'break_first'` stays attached to the replay request
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/sync_service_order_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/sync_service.dart` contains `KioskScanAuthorityService`
    - `lib/services/sync_service.dart` does not use `Future.wait(` for attendance replay
    - `lib/services/sync_service.dart` contains `for (final log in pending)` or equivalent sequential loop
    - `lib/services/sync_service.dart` contains `recordScan`
    - `lib/services/sync_service.dart` still contains duplicate-id handling for `local_id`
    - `test/services/sync_service_order_test.dart` exists and contains `queueOrder`
    - `test/services/sync_service_order_test.dart` contains `break_first`
    - `C:\flutter\bin\flutter.bat test test/services/sync_service_order_test.dart` exits 0
  </acceptance_criteria>
  <done>Queued attendance replay now preserves capture order and reaches the server through the same authority contract as live scans.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat analyze lib/core/constants.dart lib/models/pending_log.dart lib/services/sqlite_service.dart lib/screens/admin/sakit_izin_dialog.dart` passes.
- `C:\flutter\bin\flutter.bat test test/services/sync_service_order_test.dart` passes.
- `lib/services/sync_service.dart` no longer inserts attendance rows directly in parallel.
</verification>

<success_criteria>
- Offline queue rows have one deterministic replay order per device.
- Shared offline producers stay compatible with the richer queue schema.
- Queued scans replay through the authority RPC instead of bypassing it.
- Later kiosk UI can trust the local queue to represent the next real action.
</success_criteria>

<output>
After completion, create `.planning/phases/56-server-time-scan-authority/56-02-SUMMARY.md`
</output>
