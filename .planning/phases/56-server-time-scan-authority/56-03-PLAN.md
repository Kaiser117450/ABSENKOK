---
phase: 56-server-time-scan-authority
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - lib/services/employee_cache_service.dart
  - lib/screens/kiosk/kiosk_idle_screen.dart
  - lib/screens/kiosk/kiosk_scan_screen.dart
  - lib/services/pattern_detection_service.dart
  - test/screens/kiosk/kiosk_scan_server_time_test.dart
autonomous: true
requirements: [SCAN-01, SCAN-02]
must_haves:
  truths:
    - "The normal `Masuk` path stays one tap, while `ISTIRAHAT DULU` only appears for eligible first scans and always asks for confirmation."
    - "Action buttons come from merged authoritative-plus-pending state, so a queued break scan changes the very next available action."
    - "Live scans show authoritative `HH:MM WITA`, while queued scans use a visibly different pending-success state and point staff back to the idle pending indicators."
    - "Late-pattern follow-up only runs from authoritative live scan time, never from provisional device time."
  artifacts:
    - path: "lib/screens/kiosk/kiosk_scan_screen.dart"
      provides: "Phase 56 break-first action flow, live-vs-queued success states, and authority-aware submission logic"
      contains: "ISTIRAHAT DULU"
    - path: "lib/screens/kiosk/kiosk_idle_screen.dart"
      provides: "Offline uncached fallback messaging, passive sync recovery, and continued pending indicator ownership"
      contains: "Belum Bisa Diproses Offline"
    - path: "lib/services/employee_cache_service.dart"
      provides: "Cached employee scan context needed to allow safe offline scans for known employees only"
      contains: "KioskScanContext"
    - path: "test/screens/kiosk/kiosk_scan_server_time_test.dart"
      provides: "Widget coverage for confirmation dialog, live and queued success branches, and offline uncached behavior"
      contains: "Tersimpan Sementara"
  key_links:
    - from: "lib/screens/kiosk/kiosk_scan_screen.dart"
      to: "lib/services/kiosk_scan_authority_service.dart"
      via: "the scan screen must fetch authoritative context before rendering Phase 56 actions and must submit live scans through the authority RPC"
      pattern: "fetchContext|recordScan"
    - from: "lib/screens/kiosk/kiosk_scan_screen.dart"
      to: "lib/services/sqlite_service.dart"
      via: "queued fallback still uses the local queue, but now only after the authority attempt fails"
      pattern: "insertPendingLog"
    - from: "lib/screens/kiosk/kiosk_scan_screen.dart"
      to: "lib/services/pattern_detection_service.dart"
      via: "late notifications must use authoritative live time only"
      pattern: "checkAndNotifyIfLate"
---

<objective>
Turn the new authority and queue contracts into the actual Phase 56 kiosk experience.

Purpose: the SQL and queue changes are not enough unless the kiosk surfaces them in a fast operator flow. This plan upgrades the idle and scan screens so eligible break-first scans can be confirmed safely, cached employees can keep working offline, and live versus queued success states are unmistakable.
Output: authority-aware context caching, upgraded idle/scan flows, authoritative success copy, and widget coverage for the new UX contract.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-server-time-scan-authority/56-CONTEXT.md
@.planning/phases/56-server-time-scan-authority/56-RESEARCH.md
@.planning/phases/56-server-time-scan-authority/56-VALIDATION.md
@.planning/phases/56-server-time-scan-authority/56-UI-SPEC.md
@.planning/phases/56-server-time-scan-authority/56-01-PLAN.md
@.planning/phases/56-server-time-scan-authority/56-02-PLAN.md
@lib/services/employee_cache_service.dart
@lib/screens/kiosk/kiosk_idle_screen.dart
@lib/screens/kiosk/kiosk_scan_screen.dart
@lib/services/pattern_detection_service.dart
@test/screens/kiosk/kiosk_scan_streak_test.dart

<interfaces>
Locked user-facing copy from the UI contract:
```text
ISTIRAHAT DULU
Mulai dengan istirahat?
Simpan Istirahat Dulu
Kembali ke Pilihan Scan
Waktu WITA tercatat
Istirahat dulu disimpan
Berikutnya tap Selesai Istirahat.
Tersimpan Sementara
Lihat tanda pending di layar utama.
Belum Bisa Diproses Offline
Karyawan ini belum tersimpan di perangkat. Sambungkan internet lalu coba lagi.
```

Required authority-aware result states:
```text
live_confirmed -> shows HH:MM WITA and normal success styling
queued_pending -> amber pending styling, no WITA authority claim, same fast auto-close
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache scan context and build the Phase 56 action stack from authoritative plus pending state</name>
  <files>lib/services/employee_cache_service.dart, lib/screens/kiosk/kiosk_idle_screen.dart, lib/screens/kiosk/kiosk_scan_screen.dart, test/screens/kiosk/kiosk_scan_server_time_test.dart</files>
  <read_first>
    - lib/services/employee_cache_service.dart
    - lib/screens/kiosk/kiosk_idle_screen.dart
    - lib/screens/kiosk/kiosk_scan_screen.dart
    - lib/services/kiosk_scan_authority_service.dart
    - lib/services/sqlite_service.dart
    - lib/models/kiosk_scan_context.dart
    - .planning/phases/56-server-time-scan-authority/56-UI-SPEC.md
  </read_first>
  <action>
    Rework the kiosk entry flow so Phase 56 decisions come from cached authority context, not from `DateTime.now()` or server history alone.

    Required behavior:
    1. Extend `EmployeeCacheService` to cache a per-employee `KioskScanContext` snapshot in memory with the employee cache lifecycle. Do not use `SharedPreferences` for this data.
    2. In `lib/screens/kiosk/kiosk_idle_screen.dart`:
       - after a successful online employee lookup, fetch the employee's authority context and cache it together with the employee identity
       - when connectivity is restored while pending rows exist, trigger passive `SyncService.syncPendingLogs()` and refresh the pending badge count after the attempt
       - if an employee is not available in the local cache while the authority context cannot be reached, show the exact offline result state:
         - title `Belum Bisa Diproses Offline`
         - body `Karyawan ini belum tersimpan di perangkat. Sambungkan internet lalu coba lagi.`
       - do not push uncached-offline employees into `/kiosk/scan`
    3. In `lib/screens/kiosk/kiosk_scan_screen.dart`, replace `_loadLastAttendance(...)` with one merged action-resolution path that combines:
       - the authoritative `KioskScanContext`
       - local pending rows for the same employee/device from `SqliteService`
    4. The merged action resolver must produce these exact outcomes:
       - first scan with no authoritative or pending rows -> `MASUK`
       - eligible first scan with `breakFirstEligible = true` -> show `MASUK` and `ISTIRAHAT DULU`
       - after a queued or live `break_first`-intent break -> next action is `SELESAI ISTIRAHAT`
       - after `masuk` or `kembali` -> `Istirahat` plus `Pulang`
       - after `break` -> `SELESAI ISTIRAHAT` plus `Pulang`
    5. Add widget coverage in `test/screens/kiosk/kiosk_scan_server_time_test.dart` for:
       - the extra `ISTIRAHAT DULU` button appearing only when eligible
       - a queued local `break_first` event changing the next available action to `SELESAI ISTIRAHAT`
       - the uncached offline result state copy
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/kiosk/kiosk_scan_server_time_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/employee_cache_service.dart` contains `KioskScanContext`
    - `lib/screens/kiosk/kiosk_idle_screen.dart` contains `Belum Bisa Diproses Offline`
    - `lib/screens/kiosk/kiosk_idle_screen.dart` contains `Karyawan ini belum tersimpan di perangkat. Sambungkan internet lalu coba lagi.`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `ISTIRAHAT DULU`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `SELESAI ISTIRAHAT`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `fetchContext`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains local pending merge logic instead of only `_lastType` from Supabase history
    - `test/screens/kiosk/kiosk_scan_server_time_test.dart` exists and contains `ISTIRAHAT DULU`
    - `test/screens/kiosk/kiosk_scan_server_time_test.dart` contains `Belum Bisa Diproses Offline`
    - `C:\flutter\bin\flutter.bat test test/screens/kiosk/kiosk_scan_server_time_test.dart` exits 0
  </acceptance_criteria>
  <done>The kiosk action stack now reflects authoritative-plus-pending state, and offline scans are allowed only for employees whose context was actually cached.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace direct-submit success flow with authoritative live and queued pending result states</name>
  <files>lib/screens/kiosk/kiosk_scan_screen.dart, lib/services/pattern_detection_service.dart, test/screens/kiosk/kiosk_scan_server_time_test.dart</files>
  <read_first>
    - lib/screens/kiosk/kiosk_scan_screen.dart
    - lib/services/pattern_detection_service.dart
    - lib/services/kiosk_scan_authority_service.dart
    - lib/services/sqlite_service.dart
    - .planning/phases/56-server-time-scan-authority/56-UI-SPEC.md
    - .planning/phases/56-server-time-scan-authority/56-VALIDATION.md
  </read_first>
  <action>
    Rework the scan submission and success branches around authority-state outcomes.

    Required behavior:
    1. Add the Phase 56 break-first confirmation dialog in `kiosk_scan_screen.dart` with the exact UI-contract copy:
       - title `Mulai dengan istirahat?`
       - body `Scan ini akan dicatat sebagai istirahat lebih dulu. Setelah selesai, tap Selesai Istirahat.`
       - primary button `Simpan Istirahat Dulu`
       - secondary action `Kembali ke Pilihan Scan`
       The dialog must appear every eligible time. Do not remember earlier choices.
    2. Replace the current "queue first, then best-effort sync" submit path with this exact order:
       - capture a device timestamp only as `deviceCapturedAt`
       - try `KioskScanAuthorityService.recordScan(...)` with `captureMode = 'live'`
       - when that succeeds, show the live-confirmed success state
       - when that fails and the employee plus scan context are cached locally, enqueue the row with `captureMode = 'queued'`
       - when that fails and the employee/context are not cached, show the offline uncached error state instead of creating a queue row
    3. Live-confirmed success branch requirements:
       - heading `Berhasil!`
       - time label `Waktu WITA tercatat`
       - authoritative time formatted exactly as `HH:MM WITA`
       - break-first success line `Istirahat dulu disimpan`
       - helper `Berikutnya tap Selesai Istirahat.` only when the confirmed intent was break-first
       - keep the current fast auto-close rhythm and confetti only for this live-confirmed path
    4. Queued-pending success branch requirements:
       - heading `Tersimpan Sementara`
       - body `Scan disimpan di perangkat dan akan dikirim otomatis saat koneksi kembali.`
       - footer `Lihat tanda pending di layar utama.`
       - no `Waktu WITA tercatat` label
       - no confetti
       - keep the existing fast auto-close timing
    5. `PatternDetectionService.checkAndNotifyIfLate(...)` must run only when a live `masuk` scan returns authoritative time from the server. Queued scans must skip immediate late-notification evaluation.
    6. Expand `test/screens/kiosk/kiosk_scan_server_time_test.dart` to cover:
       - break-first dialog copy and confirmation
       - live success showing `Waktu WITA tercatat`
       - queued success showing `Tersimpan Sementara` and not showing `Waktu WITA tercatat`
       - break-first helper copy `Berikutnya tap Selesai Istirahat.`
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/kiosk/kiosk_scan_screen.dart lib/screens/kiosk/kiosk_idle_screen.dart lib/services/employee_cache_service.dart lib/services/pattern_detection_service.dart && C:\flutter\bin\flutter.bat test test/screens/kiosk/kiosk_scan_server_time_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `Mulai dengan istirahat?`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `Simpan Istirahat Dulu`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `Kembali ke Pilihan Scan`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `Waktu WITA tercatat`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `Tersimpan Sementara`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` contains `Lihat tanda pending di layar utama.`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` calls `recordScan`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` calls `insertPendingLog` only in the queued fallback branch
    - `lib/services/pattern_detection_service.dart` still contains `checkAndNotifyIfLate`
    - `lib/screens/kiosk/kiosk_scan_screen.dart` passes authoritative scan time into late detection instead of `DateTime.now()`
    - `test/screens/kiosk/kiosk_scan_server_time_test.dart` contains `Waktu WITA tercatat`
    - `test/screens/kiosk/kiosk_scan_server_time_test.dart` contains `Tersimpan Sementara`
    - `test/screens/kiosk/kiosk_scan_server_time_test.dart` contains `Berikutnya tap Selesai Istirahat.`
  </acceptance_criteria>
  <done>The kiosk now branches cleanly between live authoritative success and queued pending success while preserving the fast scan rhythm and explicit break-first confirmation.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/kiosk/kiosk_scan_server_time_test.dart` passes.
- `lib/screens/kiosk/kiosk_scan_screen.dart` shows the locked Phase 56 dialog/success copy and no longer treats queued scans as final WITA-confirmed events.
- `lib/screens/kiosk/kiosk_idle_screen.dart` keeps the pending badge/banner as the persistent follow-up surface after queued scans.
</verification>

<success_criteria>
- Eligible first scans can confirm `ISTIRAHAT DULU` safely without slowing normal taps.
- Cached employees can still scan when the authority call fails, while uncached offline employees receive an explicit retry message.
- Live success shows authoritative WITA time; queued success is unmistakably provisional.
- The next valid action after queued scans stays correct because the scan screen merges authoritative and local pending history.
</success_criteria>

<output>
After completion, create `.planning/phases/56-server-time-scan-authority/56-03-SUMMARY.md`
</output>
