---
phase: 42-attendance-recap-read-model
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts
autonomous: true
requirements: [ATTN-02, ATTN-03]
must_haves:
  truths:
    - "The website recap helper calls the employee-scoped recap RPC exactly once per load and never accepts an employee identifier from the page"
    - "Month-to-date summary counts and recent history are derived from the same normalized recap dataset"
    - "The typed portal recap model preserves the named attendance timestamps and logical-day status returned by the SQL contract"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts"
      provides: "Typed portal helper for attendance recap rows, summary counts, and recent history"
      contains: "loadPortalAttendanceRecap"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      via: "preserves the existing portal employee identity context alongside the recap dataset"
      pattern: "resolvePortalEmployee"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts"
      via: "reuses the same business-local reference-date rule so schedule and recap agree on the current day"
      pattern: "getPortalReferenceDate"
---

<objective>
Create the website-side read model for the portal attendance recap: one typed helper that normalizes the Phase 42 RPC rows and derives summary counts plus recent history from the same dataset.

Purpose: Phase 42 should finish with a portal-ready recap contract, not only a SQL migration. The website repo needs one typed loader so Phase 43 can focus on UI wiring rather than reinventing data normalization.
Output: A new `attendance-recap.ts` helper in the Astro portal that exposes employee context, summary counts, recent recap days, and raw normalized day rows from one recap fetch.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/42-attendance-recap-read-model/42-RESEARCH.md
@.planning/phases/42-attendance-recap-read-model/42-VALIDATION.md
@.planning/phases/42-attendance-recap-read-model/42-01-PLAN.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/home.ts

<interfaces>
Existing portal helper pattern:
```ts
export async function loadPortalSchedule(Astro: AstroGlobal): Promise<PortalScheduleResult> {
  const employeeResult = await resolvePortalEmployee(Astro);
  const referenceDate = getPortalReferenceDate();
  const { data, error } = await supabase.rpc('get_portal_schedule_overview', {
    reference_date: referenceDate,
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the typed portal attendance recap helper</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/home.ts
    - .planning/phases/42-attendance-recap-read-model/42-RESEARCH.md
    - .planning/phases/42-attendance-recap-read-model/42-01-PLAN.md
  </read_first>
  <action>
    Create `src/lib/portal/attendance-recap.ts` as the single typed read-model helper for the portal attendance recap.

    Required behavior:
    1. Define typed raw-row and normalized-day interfaces that mirror the SQL contract from Plan 01.
    2. Expose a loader such as `loadPortalAttendanceRecap(Astro)` that:
       - resolves the authenticated employee using the existing portal helper pattern
       - derives the business-local `referenceDate` using the same helper used by the schedule path
       - calls `get_portal_attendance_recap` exactly once
       - never accepts an employee identifier from the page
    3. Normalize the row set into a typed recap model that includes at least:
       - `employee`
       - `referenceDate`
       - `monthStart`
       - `summaryCounts`
       - `days`
       - `recentDays`
    4. Derive `summaryCounts` from the same normalized row set rather than a second query:
       - counts must be filtered to the current month window
       - recent-history rows may include the pre-month lookback from the SQL horizon
    5. Preserve the day-explanation fields for later UI work:
       - named attendance timestamps
       - `attendanceStatus`
       - schedule context
       - notes

    Scope guard:
    - do not wire any new portal route or component yet
    - do not duplicate `getPortalReferenceDate()` logic inline
    - do not introduce a second recap query for summary chips
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The website repo contains one typed recap helper that can supply Phase 43 with summary counts and recent history from one authenticated recap dataset.</done>
</task>

</tasks>

<verification>
- `src/lib/portal/attendance-recap.ts` exists and exposes a typed recap loader
- the helper derives month-to-date counts and recent history from the same row set
- the helper preserves the SQL contract's logical-day and named-timestamp fields
</verification>

<success_criteria>
- ATTN-02 is supported by a typed portal recap model that preserves day outcomes and explanatory timestamps
- ATTN-03 is supported by the recap helper consuming the SQL contract without inventing a second logical-day transform
- Phase 43 can build the portal recap UI without redesigning the underlying read model
</success_criteria>

<output>
After completion, create `.planning/phases/42-attendance-recap-read-model/42-02-SUMMARY.md`
</output>
