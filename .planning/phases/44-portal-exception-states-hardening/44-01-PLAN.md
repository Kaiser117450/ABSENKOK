---
phase: 44-portal-exception-states-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap-presentation.ts
autonomous: true
requirements: [ATTN-05]
must_haves:
  truths:
    - "One shared recap exception mapping defines which attendance states need follow-up and which are only current-day informational states"
    - "The SQL source comments and the portal presentation helper describe the same recap status meanings without changing the deployed RPC shape"
    - "Phase 44 can build UI hardening on top of typed helper exports instead of re-deriving exception semantics in each component"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql"
      provides: "Source-of-truth recap status commentary that distinguishes follow-up gaps from current-day neutral states"
      contains: "belum_pulang"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap-presentation.ts"
      provides: "Typed recap day presentation and follow-up helper functions for the portal UI"
      contains: "needsFollowUp"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap-presentation.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts"
      via: "imports the existing AttendanceStatus and PortalRecapDay types rather than inventing a second recap contract"
      pattern: "AttendanceStatus"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap-presentation.ts"
      via: "documents the same exception taxonomy that the portal helper turns into follow-up labels and tone"
      pattern: "tidak_hadir"
---

<objective>
Create one explicit exception-state contract for the recap surface so Phase 44 UI work can stay aligned with the shipped SQL semantics instead of duplicating status logic across the route and components.

Purpose: The current recap path already has the right data. The missing piece is one shared meaning for exception statuses across the SQL source and the website repo.
Output: Source-comment clarification in the recap SQL file plus a typed portal presentation helper that exposes labels, tones, and follow-up metadata for recap day statuses.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/44-portal-exception-states-hardening/44-RESEARCH.md
@.planning/phases/44-portal-exception-states-hardening/44-VALIDATION.md
@.planning/phases/43-portal-attendance-recap-surface/43-03-PLAN.md
@.planning/phases/42-attendance-recap-read-model/42-02-SUMMARY.md
@sql/phase_42_portal_attendance_recap_20260323.sql
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts

<interfaces>
Existing recap status union:
```ts
export type AttendanceStatus =
  | 'hadir'
  | 'sakit'
  | 'izin'
  | 'belum_pulang'
  | 'sedang_bekerja'
  | 'tidak_hadir'
  | 'belum_masuk'
  | 'libur'
  | null;
```

Suggested helper shape:
```ts
type RecapDayPresentation = {
  label: string;
  tone: 'neutral' | 'success' | 'warning' | 'danger' | 'accent';
  needsFollowUp: boolean;
  followUpLabel: string | null;
  supportingCopy: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clarify exception-state semantics in the recap SQL source file</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql
    - .planning/phases/44-portal-exception-states-hardening/44-RESEARCH.md
  </read_first>
  <action>
    Update the source comments around the recap `with_status` derivation so the SQL file explicitly documents which statuses represent follow-up gaps and which are only current-day informational states.

    Required behavior:
    1. Keep the existing runtime SQL behavior unchanged.
    2. Clarify, in comments only, that:
       - `belum_pulang` is a prior-day follow-up gap
       - `tidak_hadir` is a scheduled no-attendance follow-up gap
       - `sedang_bekerja` and `belum_masuk` are current-day states, not follow-up warnings yet
    3. Make the comment wording explicit enough that the website repo can build one shared presentation helper directly from it.

    Scope guard:
    - do not add new SQL columns
    - do not change the RPC signature
    - do not apply a database migration as part of this plan
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql' -Pattern 'belum_pulang','tidak_hadir','sedang_bekerja','belum_masuk' | Measure-Object"</automated>
  </verify>
  <done>The recap SQL source file clearly documents which statuses need follow-up and which remain informational.</done>
</task>

<task type="auto">
  <name>Task 2: Create the typed recap presentation helper in the website repo</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap-presentation.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/sql/phase_42_portal_attendance_recap_20260323.sql
    - .planning/phases/44-portal-exception-states-hardening/44-RESEARCH.md
  </read_first>
  <action>
    Create `src/lib/portal/attendance-recap-presentation.ts` as the single portal-side meaning layer for recap statuses.

    Required behavior:
    1. Import the existing recap types from `attendance-recap.ts` instead of duplicating the contract.
    2. Export helper(s) that provide:
       - employee-facing status label
       - tone / style family
       - `needsFollowUp`
       - `followUpLabel`
       - short explanatory copy for exceptional days
    3. Export one aggregate helper such as `countFollowUpDays(days)` for page framing in later plans.
    4. Treat `belum_pulang` and `tidak_hadir` as follow-up states, while keeping `belum_masuk` and `sedang_bekerja` informational.

    Scope guard:
    - do not fetch data in this helper
    - do not add new recap query paths
    - do not encode mutation or request-submission actions
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The website repo has one typed recap presentation helper that later UI work can reuse without redefining follow-up semantics.</done>
</task>

</tasks>

<verification>
- the SQL source file and portal helper describe the same recap exception taxonomy
- the portal helper imports the existing recap types instead of inventing a second contract
- follow-up classification is explicit and keeps current-day states neutral
</verification>

<success_criteria>
- ATTN-05 is grounded in one typed recap exception mapping instead of raw status badges alone
- the milestone's portal and SQL artifacts describe the same implementation surface before UI hardening begins
- later Phase 44 plans can reuse helper exports instead of hand-rolling exception logic again
</success_criteria>

<output>
After completion, create `.planning/phases/44-portal-exception-states-hardening/44-01-SUMMARY.md`
</output>
