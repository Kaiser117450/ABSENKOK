---
phase: 58-payroll-matrix-spreadsheet-export
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/screens/admin/widgets/payroll_matrix_table.dart
  - lib/screens/admin/widgets/payroll_matrix_day_cell_widget.dart
  - lib/screens/admin/widgets/payroll_matrix_summary_rail.dart
  - lib/screens/admin/admin_reports_screen.dart
  - test/widgets/payroll_matrix_table_test.dart
autonomous: true
requirements: [REPORT-01]
must_haves:
  truths:
    - "The recap tab becomes a payroll matrix, not another filtered card list."
    - "Employee identity stays visible on the left while date columns scroll horizontally."
    - "Summary totals stay readable on wide date ranges instead of disappearing after a long horizontal scroll."
    - "Matrix cells remain read-only in Phase 58."
  artifacts:
    - path: "lib/screens/admin/widgets/payroll_matrix_table.dart"
      provides: "Matrix shell for employee rail, date grid, and summary rail"
      contains: "PayrollMatrixTable"
    - path: "lib/screens/admin/widgets/payroll_matrix_day_cell_widget.dart"
      provides: "Compact payroll day-cell rendering"
      contains: "secondaryTags"
    - path: "lib/screens/admin/widgets/payroll_matrix_summary_rail.dart"
      provides: "Sticky right-side summary columns"
      contains: "Terlambat"
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Recap-tab integration for the payroll matrix surface"
      contains: "Rekap Payroll"
  key_links:
    - from: "lib/screens/admin/widgets/payroll_matrix_table.dart"
      to: "lib/screens/admin/widgets/schedule_table_view.dart"
      via: "Phase 58 should reuse the repo's pinned-grid pattern instead of inventing a new table system"
      pattern: "TableView.builder|pinnedColumnCount"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_matrix_builder.dart"
      via: "screen rendering must consume the shared matrix dataset rather than re-index strict recap rows in widget code"
      pattern: "buildPayrollMatrix"
---

<objective>
Replace the Phase 57 recap card list with the new payroll matrix surface.

Purpose: the payroll matrix is the main Phase 58 user-facing change. This plan builds the pinned-grid UI, preserves the existing admin report shell, and renders the roster-first matrix without coupling UI code to SQL or workbook details.
Output: matrix widgets, recap-tab integration, and widget coverage for the new read-only payroll surface.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-RESEARCH.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-01-PLAN.md
@lib/screens/admin/admin_reports_screen.dart
@lib/screens/admin/widgets/schedule_table_view.dart
@lib/services/payroll_matrix_builder.dart
@lib/services/payroll_matrix_semantics.dart
@lib/widgets/app_empty_state.dart
@lib/core/theme.dart

<interfaces>
Required summary labels in the UI:
```text
Terlambat
Kurang Jam
Break Lebih
Tidak Hadir
Lembur
```

Required recap-tab surface copy:
```text
Rekap Payroll
Pantau hasil harian per karyawan lalu ekspor spreadsheet untuk review gaji.
Belum Ada Data Payroll
Pilih satu outlet untuk menghitung payroll matrix.
```

Required layout constants:
```text
employee rail width: 180 px
day cell min width: 92 px
summary column width: 72 px
header row height: 48 px
data row height: 76 px
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the dedicated payroll matrix widgets around the repo's pinned-grid pattern</name>
  <files>lib/screens/admin/widgets/payroll_matrix_table.dart, lib/screens/admin/widgets/payroll_matrix_day_cell_widget.dart, lib/screens/admin/widgets/payroll_matrix_summary_rail.dart, test/widgets/payroll_matrix_table_test.dart</files>
  <read_first>
    - lib/screens/admin/widgets/schedule_table_view.dart
    - lib/core/theme.dart
    - lib/services/payroll_matrix_semantics.dart
    - lib/models/payroll_matrix_day_cell.dart
    - lib/models/payroll_matrix_row.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
  </read_first>
  <action>
    Create the new payroll matrix widget set with these exact responsibilities:

    1. `lib/screens/admin/widgets/payroll_matrix_table.dart`
       - expose a `PayrollMatrixTable` widget that accepts the shared matrix dataset and ordered date headers
       - render a three-part shell:
         - left employee rail
         - scrollable date grid
         - sticky summary rail
       - use `TableView.builder` for the employee/date grid portion
    2. `lib/screens/admin/widgets/payroll_matrix_day_cell_widget.dart`
       - render compact day cells with:
         - primary line for `masuk / pulang` when available
         - explicit fallback label when no normal pair exists
         - secondary tag row only when tags exist
       - keep cells read-only: no `InkWell`, no detail-panel routing, and no edit handler
    3. `lib/screens/admin/widgets/payroll_matrix_summary_rail.dart`
       - render the fixed right-side summary columns in this exact order:
         - `Terlambat`
         - `Kurang Jam`
         - `Break Lebih`
         - `Tidak Hadir`
         - `Lembur`
       - keep count values visually aligned per row with the day grid
    4. Apply these exact layout constants:
       - employee rail width `180`
       - day cell min width `92`
       - summary column width `72`
       - header row height `48`
       - data row height `76`
    5. Add widget tests that prove:
       - the employee rail stays present
       - summary labels render in the required order
       - cells show tags when present
       - cells remain read-only
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/payroll_matrix_table_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/widgets/payroll_matrix_table.dart` exists and contains `PayrollMatrixTable`
    - `lib/screens/admin/widgets/payroll_matrix_table.dart` contains `TableView.builder`
    - `lib/screens/admin/widgets/payroll_matrix_day_cell_widget.dart` exists and contains `secondaryTags`
    - `lib/screens/admin/widgets/payroll_matrix_summary_rail.dart` exists and contains `Terlambat`
    - `lib/screens/admin/widgets/payroll_matrix_summary_rail.dart` contains `Kurang Jam`
    - `lib/screens/admin/widgets/payroll_matrix_summary_rail.dart` contains `Break Lebih`
    - `lib/screens/admin/widgets/payroll_matrix_summary_rail.dart` contains `Tidak Hadir`
    - `lib/screens/admin/widgets/payroll_matrix_summary_rail.dart` contains `Lembur`
    - `test/widgets/payroll_matrix_table_test.dart` exists and contains `PayrollMatrixTable`
    - `test/widgets/payroll_matrix_table_test.dart` contains `read-only`
    - `C:\flutter\bin\flutter.bat test test/widgets/payroll_matrix_table_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo has a dedicated payroll matrix widget set that matches the pinned-grid and sticky-summary requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate the new payroll matrix surface into the recap tab without disturbing the per-scan tab</name>
  <files>lib/screens/admin/admin_reports_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/attendance_policy_recap_service.dart
    - lib/services/payroll_matrix_builder.dart
    - lib/screens/admin/widgets/payroll_matrix_table.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
  </read_first>
  <action>
    Update `lib/screens/admin/admin_reports_screen.dart` so the `Rekap Harian` tab becomes the Phase 58 payroll surface.

    Required behavior:
    1. Keep the existing AppBar, outlet selector, date-range controls, and `Per Scan` / `Rekap Harian` tabs.
    2. On the recap tab, replace the old filter-chip plus `PolicyRecapTile` list path with:
       - heading `Rekap Payroll`
       - helper copy `Pantau hasil harian per karyawan lalu ekspor spreadsheet untuk review gaji.`
       - the new `PayrollMatrixTable`
    3. Continue to load strict recap rows through `AttendancePolicyRecapService`, but also fetch the active employee roster for the selected outlet and pass both into `buildPayrollMatrix`.
    4. Preserve the existing empty/error states, but add payroll-specific empty guidance:
       - `Belum Ada Data Payroll`
       - `Pilih satu outlet untuk menghitung payroll matrix.`
    5. Keep the per-scan tab and its pagination behavior unchanged.
    6. Do not wire workbook generation yet; only make the recap surface ready to receive an export callback and export state from later plans.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'lib/screens/admin/admin_reports_screen.dart' -Pattern 'Rekap Payroll','PayrollMatrixTable','buildPayrollMatrix','Belum Ada Data Payroll','Pilih satu outlet untuk menghitung payroll matrix.' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_reports_screen.dart` contains `Rekap Payroll`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Pantau hasil harian per karyawan lalu ekspor spreadsheet untuk review gaji.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `PayrollMatrixTable`
    - `lib/screens/admin/admin_reports_screen.dart` contains `buildPayrollMatrix`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Belum Ada Data Payroll`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Pilih satu outlet untuk menghitung payroll matrix.`
    - `lib/screens/admin/admin_reports_screen.dart` still contains `Per Scan`
  </acceptance_criteria>
  <done>The recap tab now renders the payroll matrix surface while the per-scan tab remains stable.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/payroll_matrix_table_test.dart` passes.
- `lib/screens/admin/admin_reports_screen.dart` renders the new payroll matrix copy and uses `buildPayrollMatrix`.
- The old recap card-list path is no longer the primary Phase 58 recap surface.
</verification>

<success_criteria>
- Operators see a real payroll matrix instead of a flat recap card list.
- Employee identity stays pinned while date columns scroll.
- Summary totals stay visible on wide date ranges.
- The recap screen is ready for workbook export wiring without embedding export logic in matrix widgets.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-matrix-spreadsheet-export/58-02-SUMMARY.md`
</output>
