---
phase: 58-payroll-matrix-spreadsheet-export
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/services/payroll_spreadsheet_export_service.dart
  - test/services/payroll_spreadsheet_export_service_test.dart
autonomous: true
requirements: [REPORT-02]
must_haves:
  truths:
    - "The recap workbook is a real `.xlsx` file, not a renamed CSV."
    - "The workbook uses one main sheet named `Rekap Payroll`."
    - "Date cells and summary columns consume the same shared matrix contract as the UI."
    - "Workbook output excludes GPS and low-signal technical scan fields."
  artifacts:
    - path: "lib/services/payroll_spreadsheet_export_service.dart"
      provides: "Dedicated Phase 58 workbook generator"
      contains: "Rekap Payroll"
    - path: "test/services/payroll_spreadsheet_export_service_test.dart"
      provides: "Workbook contract coverage for sheet name, headers, and excluded fields"
      contains: "Rekap Payroll"
  key_links:
    - from: "lib/services/payroll_spreadsheet_export_service.dart"
      to: "lib/services/payroll_matrix_builder.dart"
      via: "workbook export must consume the shared matrix dataset, not build a second shape from raw recap rows"
      pattern: "buildPayrollMatrix"
    - from: "lib/services/payroll_spreadsheet_export_service.dart"
      to: "lib/services/payroll_matrix_semantics.dart"
      via: "workbook colors and tags must stay aligned with the on-screen matrix"
      pattern: "fillColorHex|secondaryTags"
---

<objective>
Create the canonical spreadsheet export path for Phase 58.

Purpose: the workbook export is a first-class product requirement, not a side effect of the UI. This plan builds one dedicated XLSX service that can generate the payroll workbook from the shared matrix dataset and proves the workbook contract with tests.
Output: one spreadsheet export service plus one contract test file.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-RESEARCH.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-VALIDATION.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-01-PLAN.md
@lib/services/payroll_matrix_builder.dart
@lib/services/payroll_matrix_semantics.dart
@lib/models/payroll_matrix_day_cell.dart
@lib/models/payroll_matrix_row.dart

<interfaces>
Required service entry point:
```text
Future<File> exportPayrollSpreadsheet({
  required String outletName,
  required DateTime startDate,
  required DateTime endDate,
  required PayrollMatrixDataset dataset,
})
```

Required workbook structure:
```text
Sheet name: Rekap Payroll
Columns A-B: employee identity
Columns C..: one column per selected date, chronological order
Trailing summary columns: Terlambat, Kurang Jam, Break Lebih, Tidak Hadir, Lembur
Frozen panes: top row + employee identity columns
```

Required filename pattern:
```text
rekap_payroll_<outlet>_<yyyyMMdd>_<yyyyMMdd>.xlsx
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate one salary-facing XLSX workbook from the shared payroll matrix dataset</name>
  <files>lib/services/payroll_spreadsheet_export_service.dart</files>
  <read_first>
    - lib/services/payroll_matrix_builder.dart
    - lib/services/payroll_matrix_semantics.dart
    - lib/models/payroll_matrix_day_cell.dart
    - lib/models/payroll_matrix_row.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
  </read_first>
  <action>
    Create `lib/services/payroll_spreadsheet_export_service.dart` using `syncfusion_flutter_xlsio`.

    Required behavior:
    1. Expose `exportPayrollSpreadsheet(...)` that accepts `outletName`, `startDate`, `endDate`, and the shared payroll matrix dataset, then writes one `.xlsx` file under the temporary directory.
    2. Create one sheet named `Rekap Payroll`.
    3. Populate the sheet with this exact column flow:
       - column A: `Karyawan`
       - column B: `Kontrak`
       - date columns in chronological order
       - trailing summary columns in this exact order:
         - `Terlambat`
         - `Kurang Jam`
         - `Break Lebih`
         - `Tidak Hadir`
         - `Lembur`
    4. Freeze the top row and the first two employee-identification columns.
    5. Write day-cell text from the shared matrix contract:
       - primary label in the cell body
       - inline tags when present
       - explicit non-time labels when no normal pair exists
    6. Apply workbook fill/text colors from the shared payroll semantics helper so the workbook matches the on-screen matrix.
    7. Exclude GPS and technical fields entirely:
       - do not create latitude, longitude, queue-order, capture-mode, requires-admin-review, or raw detail-note columns
    8. Save the workbook with the exact filename pattern `rekap_payroll_<outlet>_<yyyyMMdd>_<yyyyMMdd>.xlsx`
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'lib/services/payroll_spreadsheet_export_service.dart' -Pattern 'Rekap Payroll','Karyawan','Kontrak','Terlambat','Kurang Jam','Break Lebih','Tidak Hadir','Lembur','freezePanes','rekap_payroll_' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/payroll_spreadsheet_export_service.dart` exists
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `exportPayrollSpreadsheet`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Rekap Payroll`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Karyawan`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Kontrak`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Terlambat`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Kurang Jam`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Break Lebih`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Tidak Hadir`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `Lembur`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `freezePanes`
    - `lib/services/payroll_spreadsheet_export_service.dart` contains `rekap_payroll_`
  </acceptance_criteria>
  <done>The repo contains one dedicated XLSX generator that writes the payroll workbook in the required single-sheet format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the workbook contract with one focused export test</name>
  <files>test/services/payroll_spreadsheet_export_service_test.dart</files>
  <read_first>
    - lib/services/payroll_spreadsheet_export_service.dart
    - test/services/attendance_policy_recap_service_test.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-VALIDATION.md
  </read_first>
  <action>
    Create `test/services/payroll_spreadsheet_export_service_test.dart`.

    The test must:
    1. generate one workbook from a small deterministic payroll matrix fixture
    2. read the generated workbook back using the `excel` package declared in `dev_dependencies`
    3. assert:
       - the workbook opens successfully
       - the sheet `Rekap Payroll` exists
       - the headers `Karyawan`, `Kontrak`, `Terlambat`, `Kurang Jam`, `Break Lebih`, `Tidak Hadir`, and `Lembur` exist
       - one day cell preserves compact payroll content and tags
       - the saved file ends with `.xlsx`
    4. also fail loudly if the generated workbook text contains any forbidden field name:
       - `latitude`
       - `longitude`
       - `capture_mode`
       - `queue_order`
       - `requires_admin_review`
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/payroll_spreadsheet_export_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/services/payroll_spreadsheet_export_service_test.dart` exists
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `Rekap Payroll`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `Karyawan`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `Kontrak`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `Terlambat`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `latitude`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `.xlsx`
    - `C:\flutter\bin\flutter.bat test test/services/payroll_spreadsheet_export_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The workbook contract is protected by a focused test that proves the exported file is a real payroll spreadsheet and excludes low-signal technical fields.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/payroll_spreadsheet_export_service_test.dart` passes.
- `lib/services/payroll_spreadsheet_export_service.dart` writes one single-sheet workbook named `Rekap Payroll`.
- The export service source contains the frozen-pane call and the required summary headers.
</verification>

<success_criteria>
- Spreadsheet export is a real `.xlsx` workbook rather than CSV.
- The workbook structure matches the locked Phase 58 matrix contract.
- UI and workbook stay aligned through the shared matrix and semantics layers.
- Technical scan fields stay out of the payroll artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-matrix-spreadsheet-export/58-03-SUMMARY.md`
</output>
