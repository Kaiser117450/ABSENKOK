---
phase: 58-payroll-matrix-spreadsheet-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/models/payroll_matrix_day_cell.dart
  - lib/models/payroll_matrix_row.dart
  - lib/services/payroll_matrix_semantics.dart
  - lib/services/payroll_matrix_builder.dart
  - test/models/payroll_matrix_day_cell_test.dart
  - test/models/payroll_matrix_row_test.dart
  - test/services/payroll_matrix_builder_test.dart
  - test/services/payroll_matrix_semantics_test.dart
autonomous: false
requirements: [REPORT-01, REPORT-02]
user_setup:
  - service: licensing
    why: "The researched workbook path uses `syncfusion_flutter_xlsio: ^33.1.45` because the Phase 58 workbook contract requires documented freeze panes and XLSX formatting."
    dashboard_config:
      - task: "Confirm the project qualifies for the Syncfusion Community License or has commercial approval before adding `syncfusion_flutter_xlsio: ^33.1.45`."
        location: "Project decision -> license approval"
must_haves:
  truths:
    - "The payroll matrix is roster-first: active employees appear even if they have zero activity in the selected range."
    - "Archived employees stay excluded from both the admin matrix and workbook."
    - "One shared semantics layer defines labels, colors, tags, and summary-count rules for both UI and workbook export."
    - "Summary counts are fixed to late, short work, excess break, absence, and overtime in that order."
  artifacts:
    - path: "lib/services/payroll_matrix_builder.dart"
      provides: "Pure roster-first matrix builder that turns active employees plus strict recap rows into UI/export-ready rows"
      contains: "buildPayrollMatrix"
    - path: "lib/services/payroll_matrix_semantics.dart"
      provides: "Canonical matrix/workbook label, tag, and color decisions"
      contains: "TLT"
    - path: "lib/models/payroll_matrix_day_cell.dart"
      provides: "Typed day-cell contract for matrix rendering and workbook export"
      contains: "primaryLabel"
    - path: "lib/models/payroll_matrix_row.dart"
      provides: "Typed employee row plus summary counts"
      contains: "lateCount"
  key_links:
    - from: "lib/services/payroll_matrix_builder.dart"
      to: "lib/models/attendance_policy_recap_day.dart"
      via: "Phase 58 must consume strict recap output rather than recalculating payroll rules"
      pattern: "primaryStatus|detailSignals|logicalDate"
    - from: "lib/services/payroll_matrix_builder.dart"
      to: "lib/models/employee.dart"
      via: "matrix rows must stay active-only and grouped by employment contract"
      pattern: "isActive|archivedAt|employmentContract"
---

<objective>
Create the shared Phase 58 foundation before any widget or workbook work begins.

Purpose: the repo already has strict per-day recap rows, but it has no payroll matrix DTO, no roster-first row builder, and no canonical export semantics. This plan creates the contracts that every later Phase 58 plan will read instead of rebuilding the same decisions in multiple places.
Output: dependency additions, typed payroll-matrix models, one semantics helper, one roster-first matrix builder, and focused unit coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-RESEARCH.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-VALIDATION.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
@lib/models/employee.dart
@lib/models/attendance_policy_recap_day.dart
@lib/widgets/attendance_policy_badge.dart
@lib/widgets/attendance_policy_signal_chip.dart
@pubspec.yaml

<interfaces>
Required package additions after this plan:
```text
dependencies:
  syncfusion_flutter_xlsio: ^33.1.45

dev_dependencies:
  excel: ^4.0.6
```

Required `PayrollMatrixDayCell` fields:
```text
date
primaryLabel
secondaryTags
fillColorHex
textColorHex
primaryStatus
hasData
```

Required `PayrollMatrixRow` fields:
```text
employeeId
employeeName
employmentContract
cells
lateCount
shortWorkCount
excessBreakCount
absenceCount
overtimeCount
```

Required `PayrollMatrixDataset` shape:
```text
dates
rows
```

Required tag vocabulary:
```text
TLT
KJG
BRK
ABS
OT
ME
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the workbook/testing dependencies and codify the shared payroll cell semantics</name>
  <files>pubspec.yaml, lib/models/payroll_matrix_day_cell.dart, lib/models/payroll_matrix_row.dart, lib/services/payroll_matrix_semantics.dart, test/models/payroll_matrix_day_cell_test.dart, test/models/payroll_matrix_row_test.dart, test/services/payroll_matrix_semantics_test.dart</files>
  <read_first>
    - pubspec.yaml
    - lib/models/attendance_policy_recap_day.dart
    - lib/models/employee.dart
    - lib/widgets/attendance_policy_badge.dart
    - lib/widgets/attendance_policy_signal_chip.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
  </read_first>
  <action>
    After the user confirms the Syncfusion license path, update `pubspec.yaml` with:
    - `syncfusion_flutter_xlsio: ^33.1.45` in `dependencies`
    - `excel: ^4.0.6` in `dev_dependencies`

    Then create the shared Phase 58 matrix contracts:
    1. `lib/models/payroll_matrix_day_cell.dart`
       - add a typed model that stores `date`, `primaryLabel`, `secondaryTags`, `fillColorHex`, `textColorHex`, `primaryStatus`, and `hasData`
       - include a factory for placeholder cells so missing roster/date combinations render an explicit `-` label instead of a blank cell
    2. `lib/models/payroll_matrix_row.dart`
       - add a typed model that stores employee identity, `employmentContract`, the ordered list of cells for the selected range, and exact summary counters `lateCount`, `shortWorkCount`, `excessBreakCount`, `absenceCount`, and `overtimeCount`
    3. `lib/services/payroll_matrix_semantics.dart`
       - map strict recap rows into matrix labels, tags, and colors with these exact rules:
         - `late` -> `TLT`
         - `short_work` -> `KJG`
         - `excess_break` -> `BRK`
         - `absence` -> `ABS`
         - `overtime` -> `OT`
         - `exempt_manager` -> `ME`
       - map explicit non-time states to these exact labels when present: `Libur`, `Izin`, `Sakit`, `Cuti`, `Tidak Hadir`, `Hadir Tanpa Jadwal`, `Belum Absen Pulang`, `Belum Masuk`
       - keep summary counting payroll-core only: increment `lateCount`, `shortWorkCount`, `excessBreakCount`, `absenceCount`, and `overtimeCount`; do not add separate counts for `ME`, `hadir_tanpa_jadwal`, or `belum_masuk`
    4. Add focused tests that assert:
       - explicit labels never return blank strings for known non-time states
       - tags use the exact uppercase vocabulary above
       - summary counts stay in the required order and ignore non-payroll audit signals
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/models/payroll_matrix_day_cell_test.dart test/models/payroll_matrix_row_test.dart test/services/payroll_matrix_semantics_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` contains `syncfusion_flutter_xlsio: ^33.1.45`
    - `pubspec.yaml` contains `excel: ^4.0.6`
    - `lib/models/payroll_matrix_day_cell.dart` exists and contains `primaryLabel`
    - `lib/models/payroll_matrix_day_cell.dart` contains `secondaryTags`
    - `lib/models/payroll_matrix_row.dart` exists and contains `lateCount`
    - `lib/models/payroll_matrix_row.dart` contains `overtimeCount`
    - `lib/services/payroll_matrix_semantics.dart` exists and contains `TLT`
    - `lib/services/payroll_matrix_semantics.dart` contains `KJG`
    - `lib/services/payroll_matrix_semantics.dart` contains `BRK`
    - `lib/services/payroll_matrix_semantics.dart` contains `ABS`
    - `lib/services/payroll_matrix_semantics.dart` contains `OT`
    - `lib/services/payroll_matrix_semantics.dart` contains `ME`
    - `C:\flutter\bin\flutter.bat test test/models/payroll_matrix_day_cell_test.dart test/models/payroll_matrix_row_test.dart test/services/payroll_matrix_semantics_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo contains one typed payroll-matrix contract and one canonical semantics helper that later UI and workbook plans can share.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the roster-first payroll matrix builder on top of active employees plus strict recap rows</name>
  <files>lib/services/payroll_matrix_builder.dart, test/services/payroll_matrix_builder_test.dart</files>
  <read_first>
    - lib/services/payroll_matrix_semantics.dart
    - lib/models/payroll_matrix_day_cell.dart
    - lib/models/payroll_matrix_row.dart
    - lib/models/employee.dart
    - lib/models/attendance_policy_recap_day.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
  </read_first>
  <action>
    Create `lib/services/payroll_matrix_builder.dart` with one pure entry point named `buildPayrollMatrix`.

    The builder must:
    1. accept the selected inclusive date range, the active employee roster for the selected outlet, and the strict recap rows returned by `AttendancePolicyRecapService`
    2. filter roster rows to employees where:
       - `isActive == true`
       - `archivedAt == null`
    3. sort rows with this exact order:
       - `FULLTIME` employees first
       - `PARTTIME` employees second
       - `employeeName.toLowerCase()` alphabetical inside each contract group
    4. build one indexed lookup keyed by `employeeId + logicalDate`
    5. produce one `PayrollMatrixRow` per active employee with:
       - one cell for every date in the range, in chronological order
       - explicit placeholder cells for missing roster/date pairs
       - summary counts aggregated across the range from the shared semantics helper
    6. expose the ordered date headers and ordered row list in one return value named `PayrollMatrixDataset` so later UI and export plans can consume it without re-indexing recap rows again
    7. avoid any widget imports, Supabase calls, or file I/O so this builder stays pure and unit-testable
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/payroll_matrix_builder_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/payroll_matrix_builder.dart` exists
    - `lib/services/payroll_matrix_builder.dart` contains `buildPayrollMatrix`
    - `lib/services/payroll_matrix_builder.dart` contains `isActive`
    - `lib/services/payroll_matrix_builder.dart` contains `archivedAt`
    - `lib/services/payroll_matrix_builder.dart` contains `FULLTIME`
    - `lib/services/payroll_matrix_builder.dart` contains `PARTTIME`
    - `lib/services/payroll_matrix_builder.dart` contains `PayrollMatrixDataset`
    - `test/services/payroll_matrix_builder_test.dart` exists and contains `archivedAt`
    - `test/services/payroll_matrix_builder_test.dart` contains `FULLTIME`
    - `test/services/payroll_matrix_builder_test.dart` contains `PARTTIME`
    - `C:\flutter\bin\flutter.bat test test/services/payroll_matrix_builder_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo can now build one deterministic payroll matrix dataset from active employees and strict recap rows without putting payroll logic in widgets.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/models/payroll_matrix_day_cell_test.dart test/models/payroll_matrix_row_test.dart test/services/payroll_matrix_semantics_test.dart test/services/payroll_matrix_builder_test.dart` passes.
- `lib/services/payroll_matrix_builder.dart` filters out archived employees and sorts rows by contract then name.
- `lib/services/payroll_matrix_semantics.dart` exposes the exact Phase 58 tag vocabulary.
</verification>

<success_criteria>
- The repo has one typed payroll matrix contract that both UI and workbook code can share.
- Matrix rows are driven by the active roster instead of activity-only recap data.
- Payroll cell labels, tags, colors, and summary counts are defined once.
- Execution is unblocked for the matrix UI and workbook export plans once the license checkpoint is approved.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-matrix-spreadsheet-export/58-01-SUMMARY.md`
</output>
