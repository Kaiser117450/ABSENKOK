---
phase: 58-payroll-matrix-spreadsheet-export
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - lib/screens/admin/admin_reports_screen.dart
  - test/screens/admin/admin_reports_payroll_matrix_test.dart
autonomous: true
requirements: [REPORT-01, REPORT-02]
must_haves:
  truths:
    - "The recap tab exposes one primary workbook action: `Ekspor Spreadsheet`."
    - "CSV and PDF controls must not appear beside the payroll matrix CTA on the recap tab."
    - "Per-scan export behavior remains intact on the per-scan tab."
    - "Export feedback stays lightweight and non-blocking."
  artifacts:
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Final Phase 58 screen wiring for payroll matrix export"
      contains: "Ekspor Spreadsheet"
    - path: "test/screens/admin/admin_reports_payroll_matrix_test.dart"
      provides: "Screen-level regression coverage for the final payroll matrix surface"
      contains: "Menyiapkan spreadsheet payroll..."
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_spreadsheet_export_service.dart"
      via: "the recap tab must invoke the dedicated workbook service rather than reviving the old CSV path"
      pattern: "exportPayrollSpreadsheet"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/screens/admin/widgets/payroll_matrix_table.dart"
      via: "the final export UX lives around the matrix shell, not instead of it"
      pattern: "PayrollMatrixTable"
---

<objective>
Finish Phase 58 by wiring the workbook export and final recap-tab behavior into the admin screen.

Purpose: the previous plans build the shared matrix contract, the matrix UI, and the XLSX service. This plan joins them together in the actual recap tab so operators can review payroll rows and export the spreadsheet from the same surface without exposing legacy CSV/PDF recap actions.
Output: final screen wiring and screen-level regression coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-CONTEXT.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-02-PLAN.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-03-PLAN.md
@lib/screens/admin/admin_reports_screen.dart
@lib/screens/admin/widgets/payroll_matrix_table.dart
@lib/services/payroll_matrix_builder.dart
@lib/services/payroll_spreadsheet_export_service.dart
@lib/widgets/app_toast.dart

<interfaces>
Required recap-tab export copy:
```text
Ekspor Spreadsheet
Menyiapkan spreadsheet payroll...
Spreadsheet siap dibagikan.
Spreadsheet belum bisa dibuat. Periksa outlet, rentang tanggal, dan koneksi lalu coba lagi.
```

Required recap-tab button rules:
```text
enabled only when one outlet is selected and the date range is valid
disabled while export is running
no CSV button on recap tab
no PDF button on recap tab
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the recap tab to the matrix builder and workbook export service with the final Phase 58 toolbar behavior</name>
  <files>lib/screens/admin/admin_reports_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/screens/admin/widgets/payroll_matrix_table.dart
    - lib/services/payroll_matrix_builder.dart
    - lib/services/payroll_spreadsheet_export_service.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-UI-SPEC.md
  </read_first>
  <action>
    Update `lib/screens/admin/admin_reports_screen.dart` so the recap tab is Phase 58-complete.

    Required behavior:
    1. On the recap tab, replace the old text-button export cluster with one primary action labeled `Ekspor Spreadsheet`.
    2. Keep the per-scan tab's legacy `CSV` / `PDF` behavior unchanged.
    3. When the recap tab export starts:
       - disable the recap export button
       - show `Menyiapkan spreadsheet payroll...`
       - build the shared payroll matrix dataset for the selected outlet/date range
       - pass that dataset into `exportPayrollSpreadsheet(...)`
    4. On successful export:
       - share the generated `.xlsx` file
       - surface `Spreadsheet siap dibagikan.`
    5. On failure:
       - show `Spreadsheet belum bisa dibuat. Periksa outlet, rentang tanggal, dan koneksi lalu coba lagi.`
    6. Keep the recap export button disabled when:
       - no outlet is selected
       - the date range is invalid
       - the matrix dataset is empty
    7. Do not show `CSV` or `PDF` labels beside the recap export CTA.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'lib/screens/admin/admin_reports_screen.dart' -Pattern 'Ekspor Spreadsheet','Menyiapkan spreadsheet payroll...','Spreadsheet siap dibagikan.','Spreadsheet belum bisa dibuat.','exportPayrollSpreadsheet' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_reports_screen.dart` contains `Ekspor Spreadsheet`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Menyiapkan spreadsheet payroll...`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Spreadsheet siap dibagikan.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Spreadsheet belum bisa dibuat.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `exportPayrollSpreadsheet`
    - recap-tab code path does not render `CSV` and `PDF` next to `Ekspor Spreadsheet`
    - `lib/screens/admin/admin_reports_screen.dart` still contains the per-scan `CSV` and `PDF` paths for tab 0
  </acceptance_criteria>
  <done>The admin recap tab is now wired to the shared matrix and the workbook export service with the correct Phase 58 toolbar behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add one screen-level regression test for the final payroll matrix recap surface</name>
  <files>test/screens/admin/admin_reports_payroll_matrix_test.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - test/screens/admin/admin_reports_policy_recap_test.dart
    - .planning/phases/58-payroll-matrix-spreadsheet-export/58-VALIDATION.md
  </read_first>
  <action>
    Create `test/screens/admin/admin_reports_payroll_matrix_test.dart` with screen-level coverage for the final recap surface.

    The test file must cover:
    1. recap-tab copy:
       - `Rekap Payroll`
       - `Ekspor Spreadsheet`
    2. loading feedback:
       - `Menyiapkan spreadsheet payroll...`
    3. success or ready feedback:
       - `Spreadsheet siap dibagikan.`
    4. the recap tab does not render `CSV` and `PDF` beside the payroll export CTA
    5. the no-outlet or no-data payroll empty state remains reachable
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` exists
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Rekap Payroll`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Ekspor Spreadsheet`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Menyiapkan spreadsheet payroll...`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Spreadsheet siap dibagikan.`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `CSV`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `PDF`
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart` exits 0
  </acceptance_criteria>
  <done>The final Phase 58 recap surface is protected by a screen-level regression test covering payroll copy and export behavior.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart` passes.
- `lib/screens/admin/admin_reports_screen.dart` uses `exportPayrollSpreadsheet` on the recap tab.
- The recap tab shows only `Ekspor Spreadsheet` as its payroll export action.
</verification>

<success_criteria>
- Operators can review the payroll matrix and export one workbook from the same recap surface.
- Recap export no longer exposes the old CSV/PDF actions beside the payroll matrix CTA.
- Per-scan exports remain intact.
- The final Phase 58 admin surface is covered by a focused screen test.
</success_criteria>

<output>
After completion, create `.planning/phases/58-payroll-matrix-spreadsheet-export/58-04-SUMMARY.md`
</output>
