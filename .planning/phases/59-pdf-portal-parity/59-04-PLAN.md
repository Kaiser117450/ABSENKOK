---
phase: 59-pdf-portal-parity
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - "lib/screens/admin/admin_reports_screen.dart"
  - "test/screens/admin/admin_reports_payroll_matrix_test.dart"
autonomous: true
requirements: ["SCHED-04", "REPORT-03"]
must_haves:
  truths:
    - "The recap tab exposes `Ekspor PDF Payroll` as the primary payroll export action and keeps `Ekspor Spreadsheet` as the secondary action."
    - "The recap tab keeps the compatibility banner and uses inline loading/success/error feedback for the new PDF path."
    - "Legacy per-scan CSV/PDF actions remain untouched outside the recap tab."
    - "The final screen-level regression test covers the payroll PDF CTA and the recap export shell."
  artifacts:
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Final Phase 59 recap-tab export wiring and compatibility-state messaging"
      contains: "Ekspor PDF Payroll"
    - path: "test/screens/admin/admin_reports_payroll_matrix_test.dart"
      provides: "Screen-level regression coverage for recap-tab PDF and spreadsheet export behavior"
      contains: "Menyiapkan PDF payroll..."
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_pdf_matrix_export_service.dart"
      via: "the recap tab must invoke the dedicated matrix PDF service instead of reviving the old audit PDF path"
      pattern: "PayrollPdfMatrixExportService"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_spreadsheet_export_service.dart"
      via: "the spreadsheet export remains the secondary recap action next to the new PDF CTA"
      pattern: "exportPayrollSpreadsheet"
---

<objective>
Finish Phase 59 by wiring the payroll PDF into the admin recap surface and locking the recap export shell with screen-level regression coverage.

Purpose: after the portal contract and PDF service exist, the actual operator-facing parity gap is the recap screen wiring. This plan adds the payroll PDF CTA, preserves the spreadsheet path, keeps compatibility messaging visible, and guards the final shell with one focused screen test.
Output: recap-tab payroll PDF wiring and final regression coverage for the recap export shell.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-pdf-portal-parity/59-CONTEXT.md
@.planning/phases/59-pdf-portal-parity/59-RESEARCH.md
@.planning/phases/59-pdf-portal-parity/59-VALIDATION.md
@.planning/phases/59-pdf-portal-parity/59-UI-SPEC.md
@.planning/phases/59-pdf-portal-parity/59-PARITY-FIXTURES.md
@lib/screens/admin/admin_reports_screen.dart
@lib/services/payroll_pdf_matrix_export_service.dart
@lib/services/payroll_spreadsheet_export_service.dart
@test/screens/admin/admin_reports_payroll_matrix_test.dart

<interfaces>
Locked recap-tab copy:
```text
Rekap Payroll
Ekspor PDF Payroll
Ekspor Spreadsheet
Menyiapkan PDF payroll...
PDF payroll siap dibagikan.
PDF payroll belum bisa dibuat. Coba lagi. Jika masih gagal, gunakan spreadsheet lalu laporkan ke admin.
Mode kompatibilitas aktif
Sebagian hari payroll dihitung dari log absensi dan kontrak karena jadwal belum tersedia.
```

Locked button order:
```text
primary: Ekspor PDF Payroll
secondary: Ekspor Spreadsheet
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the recap tab to the new payroll PDF service and extend the recap export regression test</name>
  <files>lib/screens/admin/admin_reports_screen.dart, test/screens/admin/admin_reports_payroll_matrix_test.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/payroll_pdf_matrix_export_service.dart
    - lib/services/payroll_spreadsheet_export_service.dart
    - test/screens/admin/admin_reports_payroll_matrix_test.dart
    - .planning/phases/59-pdf-portal-parity/59-UI-SPEC.md
    - .planning/phases/59-pdf-portal-parity/59-PARITY-FIXTURES.md
  </read_first>
  <behavior>
    - The recap tab shows a payroll-facing PDF primary action and keeps spreadsheet export as the secondary action.
    - Export feedback is inline and non-blocking.
    - Legacy per-scan export actions remain on their original tab only.
  </behavior>
  <action>
    Finish the admin recap wiring for Phase 59.

    Required behavior:
    1. Update `lib/screens/admin/admin_reports_screen.dart` so the recap tab export bar shows:
       - title `Rekap Payroll`
       - compatibility banner when fallback rows exist
       - primary filled button `Ekspor PDF Payroll`
       - secondary outlined button `Ekspor Spreadsheet`
       - inline status copy below or beside the buttons
    2. When the PDF export starts:
       - keep the user on the recap tab
       - disable the recap PDF button
       - show `Menyiapkan PDF payroll...`
       - build the same `PayrollRecapDatasetResult` used by the matrix/spreadsheet path
       - invoke `PayrollPdfMatrixExportService` with the dataset and compatibility flag
    3. On success:
       - share the generated PDF file
       - show `PDF payroll siap dibagikan.`
    4. On failure:
       - show `PDF payroll belum bisa dibuat. Coba lagi. Jika masih gagal, gunakan spreadsheet lalu laporkan ke admin.`
    5. Keep the spreadsheet action available beside the PDF action and do not change the per-scan tab's legacy CSV/PDF behavior.
    6. Extend `test/screens/admin/admin_reports_payroll_matrix_test.dart` so it covers:
       - `Ekspor PDF Payroll`
       - `Ekspor Spreadsheet`
       - `Menyiapkan PDF payroll...`
       - `PDF payroll siap dibagikan.`
       - compatibility banner copy
       - recap-tab shell still not showing the old per-scan export cluster beside the payroll actions
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_reports_screen.dart` contains `Ekspor PDF Payroll`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Ekspor Spreadsheet`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Menyiapkan PDF payroll...`
    - `lib/screens/admin/admin_reports_screen.dart` contains `PDF payroll siap dibagikan.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `PDF payroll belum bisa dibuat.`
    - `lib/screens/admin/admin_reports_screen.dart` contains `PayrollPdfMatrixExportService`
    - `lib/screens/admin/admin_reports_screen.dart` still contains the legacy per-scan `CSV` and `PDF` paths outside the recap tab
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Ekspor PDF Payroll`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Menyiapkan PDF payroll...`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Mode kompatibilitas aktif`
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart` exits 0
  </acceptance_criteria>
  <done>The admin recap surface now exposes the payroll PDF path alongside the spreadsheet export and protects the final export shell with regression coverage.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart` passes.
- The recap tab exposes the new payroll PDF action and keeps the spreadsheet export as the secondary recap action.
- Compatibility-mode messaging stays visible when fallback rows are present.
</verification>

<success_criteria>
- Admin recap wiring completes the operator-facing Phase 59 parity path.
- Payroll PDF sharing is non-blocking and stays inside the recap surface.
- The final recap export shell is covered by a focused screen regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/59-pdf-portal-parity/59-04-SUMMARY.md`
</output>
