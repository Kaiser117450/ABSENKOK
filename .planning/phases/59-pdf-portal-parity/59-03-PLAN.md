---
phase: 59-pdf-portal-parity
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - "lib/services/payroll_pdf_matrix_export_service.dart"
  - "lib/services/payroll_matrix_semantics.dart"
  - "lib/services/pdf_service.dart"
  - "test/services/payroll_pdf_matrix_export_service_test.dart"
  - "test/services/pdf_service_color_test.dart"
  - "test/services/payroll_matrix_semantics_test.dart"
autonomous: true
requirements: ["REPORT-03"]
must_haves:
  truths:
    - "Payroll PDF recap is generated from the shipped payroll matrix dataset, not from legacy per-scan or daily-summary rows."
    - "The summary page stays compact and payroll-facing, then hands off to matrix pages as the canonical report body."
    - "PDF summary counts, short tags, and severity colors match the spreadsheet semantics exactly."
  artifacts:
    - path: "lib/services/payroll_pdf_matrix_export_service.dart"
      provides: "Dedicated matrix-driven payroll PDF generator"
      contains: "PayrollPdfMatrixExportService"
    - path: "lib/services/payroll_matrix_semantics.dart"
      provides: "Canonical short tags, summary counts, and palettes reused by the PDF service"
      contains: "lateTag"
    - path: "test/services/payroll_pdf_matrix_export_service_test.dart"
      provides: "Contract coverage for summary page, legend, matrix pages, and forbidden-field exclusions"
      contains: "Rekap Payroll PDF"
  key_links:
    - from: "lib/services/payroll_pdf_matrix_export_service.dart"
      to: "lib/services/payroll_matrix_semantics.dart"
      via: "the PDF service must reuse the shipped semantics helper instead of hard-coding a second tag/color taxonomy"
      pattern: "secondaryTagsFor"
    - from: "lib/services/payroll_pdf_matrix_export_service.dart"
      to: "test/services/payroll_pdf_matrix_export_service_test.dart"
      via: "summary-page structure, legend tags, and forbidden-field exclusions must be locked by tests"
      pattern: "Rekap Payroll PDF"
---

<objective>
Create the dedicated payroll PDF export path that mirrors the spreadsheet matrix contract without reviving the legacy audit report shape.

Purpose: the current PDF services still center legacy row-based or per-scan reports. Phase 59 needs a payroll-facing PDF path that reads the existing `PayrollMatrixDataset`, reuses `PayrollMatrixSemantics`, and keeps audit/GPS data out of the recap artifact.
Output: a dedicated matrix-driven payroll PDF service plus focused service-level contract coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-pdf-portal-parity/59-CONTEXT.md
@.planning/phases/59-pdf-portal-parity/59-RESEARCH.md
@.planning/phases/59-pdf-portal-parity/59-VALIDATION.md
@.planning/phases/59-pdf-portal-parity/59-UI-SPEC.md
@.planning/phases/59-pdf-portal-parity/59-PARITY-FIXTURES.md
@lib/services/payroll_matrix_builder.dart
@lib/services/payroll_matrix_semantics.dart
@lib/services/payroll_spreadsheet_export_service.dart
@lib/services/pdf_service.dart
@lib/services/pdf_report_service.dart
@test/services/payroll_matrix_semantics_test.dart
@test/services/pdf_service_color_test.dart

<interfaces>
Target service API:
```dart
class PayrollPdfMatrixExportService {
  Future<File> buildPayrollPdf({
    required PayrollMatrixDataset dataset,
    required String outletName,
    required DateTime startDate,
    required DateTime endDate,
    required bool isCompatibilityMode,
  })
}
```

Locked PDF summary content order:
```text
Rekap Payroll PDF
Compatibility banner when fallback rows exist
Terlambat
Kurang Jam
Break Lebih
Tidak Hadir
Lembur
Legend: TLT KJG BRK ABS OT ME
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the matrix-driven payroll PDF export service and lock its parity contract with tests</name>
  <files>lib/services/payroll_pdf_matrix_export_service.dart, lib/services/payroll_matrix_semantics.dart, lib/services/pdf_service.dart, test/services/payroll_pdf_matrix_export_service_test.dart, test/services/pdf_service_color_test.dart, test/services/payroll_matrix_semantics_test.dart</files>
  <read_first>
    - lib/services/payroll_matrix_builder.dart
    - lib/services/payroll_matrix_semantics.dart
    - lib/services/payroll_spreadsheet_export_service.dart
    - lib/services/pdf_service.dart
    - lib/services/pdf_report_service.dart
    - test/services/payroll_matrix_semantics_test.dart
    - test/services/pdf_service_color_test.dart
    - .planning/phases/59-pdf-portal-parity/59-UI-SPEC.md
    - .planning/phases/59-pdf-portal-parity/59-PARITY-FIXTURES.md
  </read_first>
  <behavior>
    - The generated payroll PDF starts with a compact summary page and then renders matrix pages as the canonical body.
    - The same overnight/fallback row meaning from the spreadsheet contract appears in the PDF label, short-tag, and color system.
  </behavior>
  <action>
    Implement the dedicated payroll PDF path for `REPORT-03`.

    Required changes:
    1. Create `lib/services/payroll_pdf_matrix_export_service.dart` with a `PayrollPdfMatrixExportService` that accepts `PayrollMatrixDataset` and writes a payroll-facing PDF file.
    2. Build the PDF structure in this order:
       - page 1 summary with `Rekap Payroll PDF`, outlet/date context, compatibility banner when `isCompatibilityMode == true`, five summary metrics in the order `Terlambat`, `Kurang Jam`, `Break Lebih`, `Tidak Hadir`, `Lembur`, and one compact `TLT / KJG / BRK / ABS / OT / ME` legend
       - following landscape matrix pages with employee identity columns on the left, date columns in chronological order, and summary columns on the far right
    3. Reuse `PayrollMatrixSemantics` for all labels, short tags, and severity colors. If small helper methods or label maps are missing there, add them to `payroll_matrix_semantics.dart` instead of hard-coding duplicate logic inside the PDF service.
    4. Keep the legacy per-scan PDF path in `pdf_service.dart` intact. If a thin adapter is needed there, make it forward to `PayrollPdfMatrixExportService` only for the new recap use case.
    5. Explicitly exclude salary-irrelevant fields from the payroll PDF recap pages:
       - `latitude`
       - `longitude`
       - `capture_mode`
       - `queue_order`
       - `requires_admin_review`
       - raw technical provenance notes
    6. Add `test/services/payroll_pdf_matrix_export_service_test.dart` with contract assertions for:
       - summary page title and metric labels
       - legend tags `TLT`, `KJG`, `BRK`, `ABS`, `OT`, `ME`
       - one overnight fixture row and one fallback fixture row
       - forbidden-field exclusion
    7. Extend `test/services/pdf_service_color_test.dart` and `test/services/payroll_matrix_semantics_test.dart` only where needed to keep the PDF palette and summary helpers aligned with the existing spreadsheet semantics.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/payroll_matrix_semantics_test.dart test/services/pdf_service_color_test.dart test/services/payroll_pdf_matrix_export_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/payroll_pdf_matrix_export_service.dart` exists
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `class PayrollPdfMatrixExportService`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `Rekap Payroll PDF`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `TLT`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `KJG`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `BRK`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `ABS`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `OT`
    - `lib/services/payroll_pdf_matrix_export_service.dart` contains `ME`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` exists
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `Rekap Payroll PDF`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `latitude`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `longitude`
    - `C:\flutter\bin\flutter.bat test test/services/payroll_matrix_semantics_test.dart test/services/pdf_service_color_test.dart test/services/payroll_pdf_matrix_export_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The app now has one dedicated payroll PDF export service that mirrors the spreadsheet matrix contract and excludes low-signal audit fields.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/payroll_matrix_semantics_test.dart test/services/pdf_service_color_test.dart test/services/payroll_pdf_matrix_export_service_test.dart` passes.
- The payroll PDF service reuses shipped matrix semantics instead of a second report taxonomy.
- The generated payroll PDF stays matrix-first and salary-facing.
</verification>

<success_criteria>
- Phase 59 has a dedicated payroll PDF generator aligned with the spreadsheet export contract.
- Summary counts, short tags, and colors stay in sync with `PayrollMatrixSemantics`.
- Payroll recap pages do not expose GPS or technical scan metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/59-pdf-portal-parity/59-03-SUMMARY.md`
</output>
