---
phase: 57-strict-recap-evaluation-engine
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - lib/widgets/attendance_policy_badge.dart
  - lib/widgets/attendance_policy_signal_chip.dart
  - lib/screens/admin/admin_reports_screen.dart
  - test/widgets/attendance_policy_badge_test.dart
  - test/screens/admin/admin_reports_policy_recap_test.dart
autonomous: true
requirements: [CONTRACT-03, RECAP-04]
must_haves:
  truths:
    - "The admin recap row shows one primary badge plus separate detail-signal chips instead of collapsing everything into one label."
    - "Admin recap filters can target short work, excess break, overtime, exemption, and missing clock-out cases, not only late/no-show."
    - "Manager-exempt rows still explain why the row is informational rather than red."
    - "The recap list preserves the current screen structure and rollout behavior while consuming the richer Phase 57 model."
  artifacts:
    - path: "lib/widgets/attendance_policy_badge.dart"
      provides: "Primary strict-status badge for Phase 57 recap rows"
      contains: "Manager exempt"
    - path: "lib/widgets/attendance_policy_signal_chip.dart"
      provides: "Secondary signal chip widget for strict detail signals"
      exports: ["AttendancePolicySignalChip"]
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Strict recap filters, row metrics, and explanatory copy"
      contains: "_PolicyRecapFilter"
    - path: "test/widgets/attendance_policy_badge_test.dart"
      provides: "Regression coverage for new primary strict-status badges"
      contains: "Kurang jam kerja"
    - path: "test/screens/admin/admin_reports_policy_recap_test.dart"
      provides: "Widget coverage for strict filter behavior and manager-exemption copy"
      contains: "Manager exempt"
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/models/attendance_policy_recap_day.dart"
      via: "the admin screen must drive filters and copy from the typed strict recap contract instead of raw legacy booleans"
      pattern: "primaryStatus|detailSignals"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/widgets/attendance_policy_signal_chip.dart"
      via: "secondary strict signals need a reusable chip surface"
      pattern: "AttendancePolicySignalChip"
    - from: "lib/widgets/attendance_policy_badge.dart"
      to: "lib/models/attendance_policy_signal.dart"
      via: "primary badge labels and colors now come from strict primary status and severity"
      pattern: "AttendancePolicyPrimaryStatus|AttendancePolicySeverity"
---

<objective>
Upgrade the existing admin recap list so it can actually present the new strict recap contract.

Purpose: the SQL engine and Dart model are not enough unless operators can see primary outcomes, detail signals, manager exemption, and work metrics in the current admin report flow. This plan keeps the existing screen but upgrades it from one badge and a few late filters into a strict recap surface.
Output: one primary badge contract, one detail-signal chip widget, screen/filter updates, and widget coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-strict-recap-evaluation-engine/57-CONTEXT.md
@.planning/phases/57-strict-recap-evaluation-engine/57-RESEARCH.md
@.planning/phases/57-strict-recap-evaluation-engine/57-VALIDATION.md
@.planning/phases/57-strict-recap-evaluation-engine/57-02-PLAN.md
@lib/models/attendance_policy_signal.dart
@lib/models/attendance_policy_recap_day.dart
@lib/widgets/attendance_policy_badge.dart
@lib/screens/admin/admin_reports_screen.dart
@test/widgets/attendance_policy_badge_test.dart

<interfaces>
Required admin recap filters after this plan:
```text
semua
terlambat
kurang_jam_kerja
istirahat_berlebih
lembur
tidak_hadir
manager_exempt
hadir_tanpa_jadwal
belum_absen_pulang
```

Required primary badge labels after this plan:
```text
Terlambat
Kurang jam kerja
Istirahat berlebih
Lembur
Tidak hadir
Manager exempt
Belum absen pulang
Belum masuk
Hadir tanpa jadwal
Hadir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the recap badge layer from one late/no-show pill into primary status plus detail-signal components</name>
  <files>lib/widgets/attendance_policy_badge.dart, lib/widgets/attendance_policy_signal_chip.dart, test/widgets/attendance_policy_badge_test.dart</files>
  <read_first>
    - lib/widgets/attendance_policy_badge.dart
    - test/widgets/attendance_policy_badge_test.dart
    - lib/models/attendance_policy_signal.dart
    - lib/models/attendance_policy_recap_day.dart
  </read_first>
  <action>
    Rework the recap badge widgets with these exact responsibilities:

    1. Update `lib/widgets/attendance_policy_badge.dart` so the primary badge is driven by:
       - `AttendancePolicyPrimaryStatus`
       - `AttendancePolicySeverity`
       instead of only `attendanceStatus + lateKind`
    2. Support these exact primary labels and visual semantics:
       - red: `Terlambat`, `Kurang jam kerja`, `Istirahat berlebih`, `Tidak hadir`, `Belum absen pulang`
       - yellow: `Lembur`
       - info/neutral: `Manager exempt`, `Belum masuk`, `Hadir tanpa jadwal`, `Hadir`
    3. Keep a backward-compatible constructor path so older call sites compiling against `status` and `lateKind` can still work until the screen migration lands.
    4. Create `lib/widgets/attendance_policy_signal_chip.dart` as a reusable secondary signal widget for detail signals such as:
       - `late`
       - `short_work`
       - `excess_break`
       - `overtime`
       - `break_first_confirmed`
       - `exempt_manager`
       - `active_incomplete`
    5. Expand `test/widgets/attendance_policy_badge_test.dart` to cover:
       - `Kurang jam kerja`
       - `Istirahat berlebih`
       - `Lembur`
       - `Manager exempt`
       - `Belum absen pulang`
       - backward-compatible late rendering for the legacy constructor path
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/attendance_policy_badge_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/widgets/attendance_policy_badge.dart` contains `Manager exempt`
    - `lib/widgets/attendance_policy_badge.dart` contains `Kurang jam kerja`
    - `lib/widgets/attendance_policy_badge.dart` contains `Istirahat berlebih`
    - `lib/widgets/attendance_policy_badge.dart` contains `Belum absen pulang`
    - `lib/widgets/attendance_policy_signal_chip.dart` exists and contains `class AttendancePolicySignalChip`
    - `test/widgets/attendance_policy_badge_test.dart` contains `Kurang jam kerja`
    - `test/widgets/attendance_policy_badge_test.dart` contains `Manager exempt`
    - `C:\flutter\bin\flutter.bat test test/widgets/attendance_policy_badge_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo contains one primary strict-status badge and one reusable detail-signal chip surface ready for the admin screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade the admin recap list to filter and explain strict recap outcomes</name>
  <files>lib/screens/admin/admin_reports_screen.dart, test/screens/admin/admin_reports_policy_recap_test.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/models/attendance_policy_recap_day.dart
    - lib/widgets/attendance_policy_badge.dart
    - lib/widgets/attendance_policy_signal_chip.dart
    - .planning/phases/57-strict-recap-evaluation-engine/57-CONTEXT.md
  </read_first>
  <action>
    Update `lib/screens/admin/admin_reports_screen.dart` so the recap list uses the strict contract directly.

    Required behavior:
    1. Replace the current `_PolicyRecapFilter` set with the exact values listed in `<interfaces>`.
    2. Drive row filtering from `primaryStatus` and `detailSignals`, not only from `lateKind` and `attendanceStatus`.
    3. Keep one primary badge in the row header, then render secondary detail chips in a `Wrap` below it using `AttendancePolicySignalChip`.
    4. Add metric chips or inline summaries for:
       - `Kerja {netWorkMinutes}`
       - `Break {totalBreakMinutes}`
       - `Lembur {overtimeMinutes}` when overtime is present
       - `Kurang {shortWorkMinutes}` when short work is present
       - `Lebih break {excessBreakMinutes}` when excess break is present
    5. Update `_reasonCopy()` with explicit strict explanations:
       - manager exemption explains that kepala toko / kepala gerai does not receive a red penalty for late/short/break signals
       - `active_incomplete` explains the day is still running
       - `belum_absen_pulang` explains the chain ended without a matching return/clock-out
       - `hadir_tanpa_jadwal` explains that contract thresholds still applied on an unscheduled day
    6. Keep safe fallback behavior:
       - if `primaryStatus` or `detailSignals` are absent, fall back to the existing legacy badge and copy behavior
    7. Create `test/screens/admin/admin_reports_policy_recap_test.dart` and cover:
       - filter selection for `manager_exempt`
       - filter selection for `belum_absen_pulang`
       - a row showing one primary badge plus at least one detail chip
       - manager-exemption explanatory copy
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_reports_screen.dart` contains `managerExempt`
    - `lib/screens/admin/admin_reports_screen.dart` contains `belumAbsenPulang`
    - `lib/screens/admin/admin_reports_screen.dart` contains `AttendancePolicySignalChip`
    - `lib/screens/admin/admin_reports_screen.dart` contains `Kerja `
    - `lib/screens/admin/admin_reports_screen.dart` contains `Break `
    - `lib/screens/admin/admin_reports_screen.dart` contains fallback logic for legacy rows
    - `test/screens/admin/admin_reports_policy_recap_test.dart` exists and contains `Manager exempt`
    - `test/screens/admin/admin_reports_policy_recap_test.dart` contains `Belum absen pulang`
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart` exits 0
  </acceptance_criteria>
  <done>The admin recap list can now filter, explain, and render strict recap outcomes without redesigning the whole screen.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/attendance_policy_badge_test.dart` passes.
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_policy_recap_test.dart` passes.
- `lib/screens/admin/admin_reports_screen.dart` renders one primary badge plus detail chips and supports the strict Phase 57 filter set.
</verification>

<success_criteria>
- Operators can see primary strict outcomes and secondary payroll signals on the same row.
- Admin recap filters cover strict work/break/exemption cases, not only late/no-show basics.
- Manager-exempt rows remain visible and clearly explained.
- The existing report screen structure is preserved while consuming the richer strict recap contract.
</success_criteria>

<output>
After completion, create `.planning/phases/57-strict-recap-evaluation-engine/57-03-SUMMARY.md`
</output>
