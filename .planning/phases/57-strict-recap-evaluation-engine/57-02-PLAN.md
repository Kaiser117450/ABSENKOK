---
phase: 57-strict-recap-evaluation-engine
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/models/attendance_policy_signal.dart
  - lib/models/attendance_policy_recap_day.dart
  - lib/services/attendance_policy_recap_service.dart
  - test/models/attendance_policy_recap_day_test.dart
  - test/services/attendance_policy_recap_service_test.dart
autonomous: true
requirements: [CONTRACT-03, RECAP-01, RECAP-02, RECAP-03, RECAP-04]
must_haves:
  truths:
    - "The typed recap model preserves one primary status plus detail signals and quantitative work metrics."
    - "Legacy Phase 55/56 fields still parse so staged rollout does not break immediately."
    - "Manager exemption, overtime metrics, and incomplete-chain state are available to widgets without re-deriving SQL logic."
    - "The recap service continues to unwrap nested Supabase RPC responses safely."
  artifacts:
    - path: "lib/models/attendance_policy_signal.dart"
      provides: "Typed strict signal, primary-status, and severity enums for Phase 57 recap output"
      exports: ["AttendancePolicySignal", "AttendancePolicyPrimaryStatus", "AttendancePolicySeverity"]
    - path: "lib/models/attendance_policy_recap_day.dart"
      provides: "Expanded recap row model with strict metrics, manager-exemption fields, and legacy compatibility"
      contains: "primaryStatus"
    - path: "lib/services/attendance_policy_recap_service.dart"
      provides: "Strict recap RPC gateway that still unwraps nested data safely"
      contains: "get_admin_schedule_policy_recap"
    - path: "test/models/attendance_policy_recap_day_test.dart"
      provides: "Focused unit coverage for strict-signal parsing and compatibility behavior"
      contains: "AttendancePolicyPrimaryStatus"
    - path: "test/services/attendance_policy_recap_service_test.dart"
      provides: "Regression coverage for strict recap rows, nested RPC responses, and legacy fallback"
      contains: "detail_signals"
  key_links:
    - from: "lib/models/attendance_policy_recap_day.dart"
      to: "lib/models/attendance_policy_signal.dart"
      via: "Phase 57 strict recap rows must parse and expose typed primary and detail signal values"
      pattern: "AttendancePolicySignal|AttendancePolicyPrimaryStatus"
    - from: "lib/services/attendance_policy_recap_service.dart"
      to: "sql/phase_57_strict_recap_evaluation_engine_20260327.sql"
      via: "the service must keep consuming the same recap RPC name while understanding the widened payload"
      pattern: "get_admin_schedule_policy_recap|primary_status|detail_signals"
---

<objective>
Turn the widened strict recap SQL payload into a stable typed Flutter contract before the admin screen consumes it.

Purpose: the app currently only understands `attendance_status` and `late_kind`. Phase 57 needs one typed recap model that can carry strict signals, manager exemption, metrics, and incomplete-state context without moving business logic back into widgets.
Output: a strict signal model, an expanded recap day model, service compatibility updates, and focused unit tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-strict-recap-evaluation-engine/57-CONTEXT.md
@.planning/phases/57-strict-recap-evaluation-engine/57-RESEARCH.md
@.planning/phases/57-strict-recap-evaluation-engine/57-VALIDATION.md
@.planning/phases/57-strict-recap-evaluation-engine/57-01-PLAN.md
@lib/models/attendance_policy_recap_day.dart
@lib/services/attendance_policy_recap_service.dart
@test/services/attendance_policy_recap_service_test.dart

<interfaces>
Required typed enums after this plan:
```text
AttendancePolicySeverity -> info, yellow, red
AttendancePolicyPrimaryStatus -> hadir, late, short_work, excess_break, overtime, absence, exempt_manager, hadir_tanpa_jadwal, belum_absen_pulang, active_incomplete, belum_masuk, sakit, izin, cuti, libur
AttendancePolicySignal -> late, short_work, excess_break, overtime, absence, exempt_manager, hadir_tanpa_jadwal, belum_absen_pulang, active_incomplete, break_first_confirmed
```

Required recap model fields after this plan:
```text
primaryStatus
primarySeverity
detailSignals
detailNotes
isManagerExempt
managerPosition
logicalDayComplete
incompleteReason
netWorkMinutes
totalBreakMinutes
overtimeMinutes
shortWorkMinutes
excessBreakMinutes
pairedBreakCount
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the typed strict recap signal layer and expand the recap row model</name>
  <files>lib/models/attendance_policy_signal.dart, lib/models/attendance_policy_recap_day.dart, test/models/attendance_policy_recap_day_test.dart</files>
  <read_first>
    - lib/models/attendance_policy_recap_day.dart
    - test/services/attendance_policy_recap_service_test.dart
    - .planning/phases/57-strict-recap-evaluation-engine/57-RESEARCH.md
    - .planning/phases/57-strict-recap-evaluation-engine/57-01-PLAN.md
  </read_first>
  <action>
    Implement the typed Phase 57 recap contract with these exact responsibilities:

    1. Create `lib/models/attendance_policy_signal.dart` with these exact enums:
       - `AttendancePolicySeverity` values: `info`, `yellow`, `red`
       - `AttendancePolicyPrimaryStatus` values: `hadir`, `late`, `shortWork`, `excessBreak`, `overtime`, `absence`, `exemptManager`, `hadirTanpaJadwal`, `belumAbsenPulang`, `activeIncomplete`, `belumMasuk`, `sakit`, `izin`, `cuti`, `libur`
       - `AttendancePolicySignal` values: `late`, `shortWork`, `excessBreak`, `overtime`, `absence`, `exemptManager`, `hadirTanpaJadwal`, `belumAbsenPulang`, `activeIncomplete`, `breakFirstConfirmed`
       Each enum must expose:
       - canonical storage value in snake_case
       - readable admin label
       - parse/tryParse helper from RPC text

    2. Expand `lib/models/attendance_policy_recap_day.dart` so it parses and stores:
       - the exact new fields listed in `<interfaces>`
       - `detailSignals` from `detail_signals` as a typed list
       - `detailNotes` from `detail_notes`
    3. Keep the legacy Phase 55/56 compatibility surface alive:
       - preserve `attendanceStatus`, `lateKind`, `isLate`, `breakFirstEligible`, and `breakFirstConfirmed`
       - when legacy fields are absent but new strict fields exist, derive safe compatibility defaults from `primaryStatus` and `detailSignals`
       - do not break current JSON rows that still only contain the Phase 56 field set
    4. Add convenience helpers on the recap model:
       - `hasSignal(AttendancePolicySignal signal)`
       - `isRedPrimary`
       - `isYellowPrimary`
       - `hasAnyPenaltySignals`
    5. Create `test/models/attendance_policy_recap_day_test.dart` and cover:
       - parsing a full strict row with `primary_status`, `detail_signals`, and all metric fields
       - parsing a manager-exempt row with suppressed penalty context
       - deriving compatibility values from a strict-only row when legacy fields are absent
       - still parsing a legacy Phase 56 row without throwing
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/models/attendance_policy_recap_day_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/attendance_policy_signal.dart` exists and contains `enum AttendancePolicySeverity`
    - `lib/models/attendance_policy_signal.dart` contains `enum AttendancePolicyPrimaryStatus`
    - `lib/models/attendance_policy_signal.dart` contains `enum AttendancePolicySignal`
    - `lib/models/attendance_policy_recap_day.dart` contains `primaryStatus`
    - `lib/models/attendance_policy_recap_day.dart` contains `detailSignals`
    - `lib/models/attendance_policy_recap_day.dart` contains `isManagerExempt`
    - `lib/models/attendance_policy_recap_day.dart` contains `logicalDayComplete`
    - `lib/models/attendance_policy_recap_day.dart` contains `netWorkMinutes`
    - `lib/models/attendance_policy_recap_day.dart` still contains `attendanceStatus`
    - `lib/models/attendance_policy_recap_day.dart` still contains `lateKind`
    - `test/models/attendance_policy_recap_day_test.dart` exists and contains `AttendancePolicyPrimaryStatus`
    - `C:\flutter\bin\flutter.bat test test/models/attendance_policy_recap_day_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo contains one typed recap row model that can represent strict signals and metrics without losing staged-rollout compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the recap service and tests to consume the widened strict recap RPC</name>
  <files>lib/services/attendance_policy_recap_service.dart, test/services/attendance_policy_recap_service_test.dart</files>
  <read_first>
    - lib/services/attendance_policy_recap_service.dart
    - test/services/attendance_policy_recap_service_test.dart
    - lib/models/attendance_policy_recap_day.dart
    - sql/phase_57_strict_recap_evaluation_engine_20260327.sql
  </read_first>
  <action>
    Update the strict recap service layer with these exact requirements:

    1. Keep `AttendancePolicyRecapService.fetchAdminSchedulePolicyRecap(...)` calling `get_admin_schedule_policy_recap`.
    2. Keep the nested `data -> data` unwrapping behavior intact.
    3. Make `parseRows(...)` and `_extractRows(...)` accept both:
       - strict recap rows with `primary_status` and `detail_signals`
       - legacy rows with only `attendance_status` and `late_kind`
    4. Expand `test/services/attendance_policy_recap_service_test.dart` to cover these exact row shapes:
       - a 24-hour overnight row whose `logical_date` stays on the previous date and whose `detail_signals` includes `overtime`
       - a manager-exempt row with `is_manager_exempt = true`, `manager_position = 'Kepala Gerai'`, `primary_status = 'exempt_manager'`, and detail signals that still include `late`
       - a part-time overtime row with `primary_status = 'overtime'`, `detail_signals` including `overtime`, and `excess_break_minutes = 0`
       - a `NORMAL`-outlet missing-clockout row with `primary_status = 'belum_absen_pulang'`
       - a legacy Phase 56 row that still parses and produces one recap object
    5. The tests must prove:
       - strict arrays map into typed enums
       - nested RPC data unwraps safely
       - legacy rows remain parseable
       - metric fields such as `net_work_minutes` and `overtime_minutes` map correctly
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/attendance_policy_recap_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/attendance_policy_recap_service.dart` still contains `get_admin_schedule_policy_recap`
    - `lib/services/attendance_policy_recap_service.dart` still contains `_unwrapData`
    - `test/services/attendance_policy_recap_service_test.dart` contains `detail_signals`
    - `test/services/attendance_policy_recap_service_test.dart` contains `exempt_manager`
    - `test/services/attendance_policy_recap_service_test.dart` contains `belum_absen_pulang`
    - `test/services/attendance_policy_recap_service_test.dart` contains `net_work_minutes`
    - `C:\flutter\bin\flutter.bat test test/services/attendance_policy_recap_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The recap service can now consume the strict recap payload safely while still tolerating legacy rows during staged rollout.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/models/attendance_policy_recap_day_test.dart` passes.
- `C:\flutter\bin\flutter.bat test test/services/attendance_policy_recap_service_test.dart` passes.
- The typed recap contract includes strict signal arrays, manager-exemption fields, and work metrics while retaining legacy compatibility fields.
</verification>

<success_criteria>
- Strict recap data is represented by typed enums and metric fields, not ad-hoc strings in widgets.
- The service continues to unwrap Supabase RPC payloads safely.
- Legacy Phase 56 rows still parse during staged rollout.
- Phase 58 and later can consume the same typed strict recap contract without new parsing layers.
</success_criteria>

<output>
After completion, create `.planning/phases/57-strict-recap-evaluation-engine/57-02-SUMMARY.md`
</output>
