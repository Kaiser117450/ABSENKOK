---
phase: 53-security-rollout-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tool/security_hardening_acceptance.ps1
  - test/phase53/security_hardening_acceptance_contract_test.dart
autonomous: true
requirements: [SECOPS-01]
must_haves:
  truths:
    - "Operators can run one read-only helper to preview or execute the repo-local security acceptance checks for kiosk, admin, and portal hardening."
    - "The acceptance helper never applies SQL, never runs the recovery script, and never pretends live rollout proof exists when it does not."
    - "A focused contract test protects the helper's command surface so future edits cannot silently drop a required security verification step."
  artifacts:
    - path: "tool/security_hardening_acceptance.ps1"
      provides: "Read-only acceptance helper that stages or runs the focused Phase 50-52 Flutter checks plus Astro verification commands."
      contains: "CheckOnly"
    - path: "test/phase53/security_hardening_acceptance_contract_test.dart"
      provides: "Focused regression coverage for the Phase 53 acceptance helper command surface."
      contains: "Phase 53 security hardening acceptance contract"
  key_links:
    - from: "tool/security_hardening_acceptance.ps1"
      to: "test/phase50/kiosk_device_model_test.dart"
      via: "the helper runs the focused kiosk safety regression as part of the acceptance sequence"
      pattern: "phase50/kiosk_device_model_test.dart"
    - from: "tool/security_hardening_acceptance.ps1"
      to: "package.json"
      via: "the helper invokes the existing Astro verification commands instead of inventing new ones"
      pattern: "npm run build"
    - from: "test/phase53/security_hardening_acceptance_contract_test.dart"
      to: "tool/security_hardening_acceptance.ps1"
      via: "the contract test guards the helper's command surface and its no-live-mutation rule"
      pattern: "CheckOnly"
---

<objective>
Create the read-only acceptance automation needed to verify the security hardening without touching the live database.

Purpose: SECOPS-01 requires more than prose. The phase needs one helper that can preview or run the repo-local Flutter and Astro checks tied to the hardening work, while staying explicitly separate from live SQL application.
Output: A PowerShell acceptance helper plus a focused contract test that prevents command-surface drift.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-security-rollout-acceptance/53-RESEARCH.md
@.planning/phases/53-security-rollout-acceptance/53-VALIDATION.md
@test/phase50/kiosk_device_model_test.dart
@test/phase51/admin_session_claims_test.dart
@test/phase51/sql_role_guard_contract_test.dart
@test/phase52/portal_recovery_contract_test.dart
@docs/android-release-runbook.md
@package.json
@pubspec.yaml

<interfaces>
Focused repo-local acceptance commands already implied by research:
```powershell
C:\flutter\bin\flutter.bat test test/phase50/kiosk_device_model_test.dart
C:\flutter\bin\flutter.bat test test/phase51/admin_session_claims_test.dart
C:\flutter\bin\flutter.bat test test/phase51/sql_role_guard_contract_test.dart
C:\flutter\bin\flutter.bat test test/phase52/portal_recovery_contract_test.dart
npm run check
npm run build
```
</interfaces>

<constraints>
- the helper must be read-only with respect to the live database
- the helper must not execute SQL files or call Supabase APIs
- failures and timeouts must remain visible instead of being converted into green output
- acceptance automation should use existing tracked commands and tests, not ad hoc shell snippets
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the read-only security hardening acceptance helper</name>
  <files>tool/security_hardening_acceptance.ps1</files>
  <read_first>
    - .planning/phases/53-security-rollout-acceptance/53-RESEARCH.md
    - docs/android-release-runbook.md
    - package.json
    - pubspec.yaml
    - test/phase50/kiosk_device_model_test.dart
    - test/phase51/admin_session_claims_test.dart
    - test/phase51/sql_role_guard_contract_test.dart
    - test/phase52/portal_recovery_contract_test.dart
  </read_first>
  <action>
    Create `tool/security_hardening_acceptance.ps1` as the Phase 53 read-only verification helper.

    Required behavior:
    1. Support `-CheckOnly` that prints or returns the exact planned command surface without executing it.
    2. When not in `-CheckOnly`, run the focused repo-local commands in this order:
       - `C:\flutter\bin\flutter.bat test test/phase50/kiosk_device_model_test.dart`
       - `C:\flutter\bin\flutter.bat test test/phase51/admin_session_claims_test.dart`
       - `C:\flutter\bin\flutter.bat test test/phase51/sql_role_guard_contract_test.dart`
       - `C:\flutter\bin\flutter.bat test test/phase52/portal_recovery_contract_test.dart`
       - `npm run check`
       - `npm run build`
    3. Run from the repo root and fail fast if a command exits non-zero, but print the failing command clearly.
    4. Include an obvious note in the script header and output that live Supabase rollout confirmation is still manual evidence outside this helper.

    Scope guard:
    - do not execute SQL files
    - do not call the recovery script
    - do not silently skip the Phase 52 test even if it times out in this environment
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File tool/security_hardening_acceptance.ps1 -CheckOnly</automated>
  </verify>
  <done>The repo has one read-only helper that can preview or run the focused security acceptance command surface without mutating live systems.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the acceptance helper contract with a focused regression test</name>
  <files>test/phase53/security_hardening_acceptance_contract_test.dart</files>
  <behavior>
    - Test 1: the helper script file exists at `tool/security_hardening_acceptance.ps1`
    - Test 2: the helper script contains all four focused Flutter test commands plus `npm run check` and `npm run build`
    - Test 3: the helper script exposes `-CheckOnly`
    - Test 4: the helper script does not contain `repair_employee_portal_accounts_20260325.sql` as an executable rollout step
  </behavior>
  <read_first>
    - tool/security_hardening_acceptance.ps1
    - test/phase51/sql_role_guard_contract_test.dart
  </read_first>
  <action>
    Create `test/phase53/security_hardening_acceptance_contract_test.dart` as a lightweight repo-local contract test.

    Required behavior:
    1. Read `tool/security_hardening_acceptance.ps1` from disk.
    2. Assert the script exists and contains the focused Flutter and Astro verification commands required by Phase 53.
    3. Assert the script includes `CheckOnly`.
    4. Assert the script does not treat `sql/repair_employee_portal_accounts_20260325.sql` as part of the normal executable acceptance command surface.

    Keep the test lightweight and string-based. This is contract coverage for the acceptance helper, not a live rollout test.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/phase53/security_hardening_acceptance_contract_test.dart</automated>
  </verify>
  <done>The repo will fail fast if future edits weaken the Phase 53 acceptance helper or silently drop a required command from its contract.</done>
</task>

</tasks>

<verification>
- `tool/security_hardening_acceptance.ps1 -CheckOnly` shows the full focused acceptance command surface without touching live systems.
- The helper names the kiosk, admin, and portal security verification commands explicitly.
- The Phase 53 contract test fails if the helper drops required commands or starts treating the recovery SQL as routine automation.
</verification>

<success_criteria>
- SECOPS-01 has a repeatable repo-local acceptance helper rather than free-form operator memory.
- Acceptance automation remains read-only with respect to production rollout.
- The command surface for kiosk, admin, and portal verification is guarded by a focused regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/53-security-rollout-acceptance/53-02-SUMMARY.md`
</output>
