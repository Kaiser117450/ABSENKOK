---
phase: 58.1-schedule-ux-polish-and-legacy-payroll-fallback
plan: 02
subsystem: reporting
tags: [flutter, payroll, compatibility, legacy, overnight]
requires:
  - phase: 57
    provides: overnight-safe strict recap semantics and contract-aware payroll signals
  - phase: 58
    provides: shared payroll matrix contract and spreadsheet semantics
provides:
  - pure legacy payroll fallback synthesis for missing strict recap keys
  - shared contract-aware break allowance helper for compatibility rows
  - fallback parity coverage for matrix builder and semantics
affects: [admin-reports, payroll-matrix, spreadsheet-export]
tech-stack:
  added: []
  patterns: [strict-first additive merge, overnight-safe source-day grouping, honest no-schedule payroll synthesis]
key-files:
  created:
    - lib/services/legacy_payroll_recap_fallback_service.dart
    - test/services/legacy_payroll_recap_fallback_service_test.dart
  modified:
    - lib/services/schedule_policy_service.dart
    - test/services/payroll_matrix_builder_test.dart
    - test/services/payroll_matrix_semantics_test.dart
key-decisions:
  - "Fallback rows stay additive: they fill only missing employee-date keys and never overwrite strict recap rows."
  - "No-schedule synthesis is intentionally honest and never fabricates lateness, absence, or schedule-only cutoffs."
  - "Compatibility tests use explicit +08:00 timestamps so logical-day grouping matches the app's WITA-local runtime contract."
patterns-established:
  - "LegacyPayrollRecapFallbackService groups raw attendance into source days before synthesizing recap rows, keeping the screen layer free of attendance-chain logic."
  - "SchedulePolicyService.payrollBreakAllowanceMinutes(...) is the shared break-allowance source for overtime-aware compatibility rows."
requirements-completed: [REPORT-01]
duration: ~15m
completed: 2026-03-28
---

# Phase 58.1 Plan 02: Legacy Payroll Fallback Summary

**Legacy attendance logs can now become contract-aware payroll recap rows even when `schedule_entries` are still missing, without forking the matrix or export contract.**

## Performance

- **Duration:** ~15m
- **Started:** 2026-03-28T14:12:00+08:00
- **Completed:** 2026-03-28T14:46:32.4988815+08:00
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added a pure fallback service that groups legacy attendance into logical days and synthesizes only the missing recap rows.
- Reused the shared contract defaults and overtime break allowance so FULLTIME and PARTTIME compatibility rows stay payroll-honest.
- Extended matrix builder and semantics tests so fallback rows render and count through the same Phase 58 contract.

## Task Commits

Atomic task commits were skipped for this execution because the repository already contained extensive unrelated dirty changes. Changes were left uncommitted to avoid bundling user-owned diffs.

## Files Created/Modified

- `lib/services/legacy_payroll_recap_fallback_service.dart` - overnight-safe legacy source-day grouping and compatibility recap synthesis
- `lib/services/schedule_policy_service.dart` - shared break allowance helper for overtime-aware fallback rows
- `test/services/legacy_payroll_recap_fallback_service_test.dart` - pure service coverage for contract defaults, strict-key skips, overtime breaks, and incomplete days
- `test/services/payroll_matrix_builder_test.dart` - fallback recap row rendering and mixed strict+fallback logical-date coverage
- `test/services/payroll_matrix_semantics_test.dart` - payroll-facing fallback label and count parity coverage

## Decisions Made

- Kept fallback synthesis pure and screen-agnostic so admin reports can adapt raw attendance without owning chain logic.
- Preserved strict recap precedence by merging with `putIfAbsent` semantics on `(employeeId, logicalDate)`.
- Matched the live WITA conversion behavior in tests instead of weakening the service around UTC fixture drift.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Replaced unsafe nullable presence-log extraction in fallback row synthesis**
- **Found during:** Task 1
- **Issue:** The first fallback pass used cast-heavy `firstWhere/lastWhere` patterns that crashed on incomplete days when no `pulang` log existed.
- **Fix:** Switched to explicit filtered `masuk` and `pulang` lists before deriving `firstMasuk` and `lastPulang`.
- **Files modified:** `lib/services/legacy_payroll_recap_fallback_service.dart`
- **Verification:** `C:\flutter\bin\flutter.bat test test/services/legacy_payroll_recap_fallback_service_test.dart test/services/payroll_matrix_builder_test.dart test/services/payroll_matrix_semantics_test.dart`

**2. [Rule 3 - Blocking] Normalized fallback test fixtures to explicit `+08:00` offsets**
- **Found during:** Task 1
- **Issue:** Initial UTC `Z` fixtures were converted to WITA-local time, splitting some test logs across unintended calendar days and producing false duplicate-key failures.
- **Fix:** Updated the legacy fallback test timestamps to explicit `+08:00` offsets so logical-day assertions match the production timezone contract.
- **Files modified:** `test/services/legacy_payroll_recap_fallback_service_test.dart`
- **Verification:** `C:\flutter\bin\flutter.bat test test/services/legacy_payroll_recap_fallback_service_test.dart test/services/payroll_matrix_builder_test.dart test/services/payroll_matrix_semantics_test.dart`

---

**Total deviations:** 2 auto-fixed blocking issues
**Impact on plan:** Both fixes were required to make the compatibility engine truthful under the actual WITA-local runtime contract. No scope creep.

## Issues Encountered

- Legacy fallback tests initially hid a timezone-fixture problem rather than a business-rule problem; after aligning fixtures to `+08:00`, the service behavior matched the intended contract.

## User Setup Required

None.

## Next Phase Readiness

- Admin reports can now consume one pure fallback service instead of reimplementing no-schedule payroll logic in the screen.
- Matrix and semantic parity tests already protect the shared contract before UI wiring and workbook export consume the compatibility rows.

---
*Phase: 58.1-schedule-ux-polish-and-legacy-payroll-fallback*
*Completed: 2026-03-28*
