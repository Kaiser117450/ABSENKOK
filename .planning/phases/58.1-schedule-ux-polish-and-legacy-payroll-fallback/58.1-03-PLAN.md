---
phase: 58.1-schedule-ux-polish-and-legacy-payroll-fallback
plan: 03
type: execute
wave: 2
depends_on: ["02"]
files_modified:
  - lib/screens/admin/admin_reports_screen.dart
  - test/screens/admin/admin_reports_payroll_matrix_test.dart
  - test/services/payroll_spreadsheet_export_service_test.dart
autonomous: true
requirements: [REPORT-01, REPORT-02]
must_haves:
  truths:
    - "Payroll recap still shows useful matrix rows when strict recap rows are empty or partially missing but attendance logs exist."
    - "Operators can tell when compatibility mode is filling payroll rows from contract defaults."
    - "Spreadsheet export stays on the same payroll matrix contract under fallback."
  artifacts:
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Admin recap integration of strict rows, fallback rows, compatibility note, and shared export path"
      contains: "_loadDailySummaryData"
    - path: "test/screens/admin/admin_reports_payroll_matrix_test.dart"
      provides: "Screen-level regression coverage for compatibility-mode payroll recap"
      contains: "Mode kompatibilitas aktif"
    - path: "test/services/payroll_spreadsheet_export_service_test.dart"
      provides: "Workbook contract coverage proving fallback-style matrix cells still export cleanly"
      contains: "PayrollSpreadsheetExportService"
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/legacy_payroll_recap_fallback_service.dart"
      via: "admin reports must synthesize only missing recap keys from raw attendance before matrix build"
      pattern: "LegacyPayrollRecapFallbackService"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_matrix_builder.dart"
      via: "strict and fallback rows must merge into one shared matrix dataset"
      pattern: "buildPayrollMatrix"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_spreadsheet_export_service.dart"
      via: "compatibility rows must export through the existing spreadsheet service"
      pattern: "exportPayrollSpreadsheet"
---

<objective>
Integrate the legacy fallback service into the admin payroll recap flow and lock recap/export regressions around the compatibility path.

Purpose: the service from Plan 02 keeps the recap logic honest, but Phase 58.1 is only complete when admin reports actually uses it, keeps matrix/export parity, and stops collapsing into a false empty state for legacy outlets.
Output: merged strict-plus-fallback recap loading in admin reports, a visible compatibility note, and recap/export regression coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-CONTEXT.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-RESEARCH.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-VALIDATION.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-02-PLAN.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-04-SUMMARY.md
@lib/screens/admin/admin_reports_screen.dart
@lib/services/legacy_payroll_recap_fallback_service.dart
@lib/services/payroll_matrix_builder.dart
@lib/services/payroll_spreadsheet_export_service.dart
@test/screens/admin/admin_reports_payroll_matrix_test.dart
@test/services/payroll_spreadsheet_export_service_test.dart

<locked_decisions>
D-03: Legacy payroll fallback must feed the same payroll matrix and spreadsheet export path, must not require migrations/backfills, and must keep legacy attendance visible for outlets still missing `schedule_entries`.
</locked_decisions>

<interfaces>
Plan 02 dependency:
```dart
class LegacyPayrollRecapFallbackService {
  List<AttendancePolicyRecapDay> synthesizeMissingRows({
    required Iterable<LegacyPayrollSourceDay> sourceDays,
    required Iterable<AttendancePolicyRecapDay> strictRows,
    DateTime? now,
  });
}
```

Compatibility disclosure for this plan:
```text
Mode kompatibilitas aktif
Sebagian hari payroll dihitung dari log absensi dan kontrak karena jadwal belum tersedia.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge strict recap rows with fallback rows in admin reports and surface a compatibility note</name>
  <files>lib/screens/admin/admin_reports_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/legacy_payroll_recap_fallback_service.dart
    - lib/services/payroll_matrix_builder.dart
    - .planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: when strict recap is empty but attendance-backed source days exist, the payroll tab still receives a non-empty matrix dataset.
    - Test 2: when strict recap is partial, fallback rows fill only the missing employee-date keys, including overnight legacy keys.
    - Test 3: when compatibility rows are present, the tab shows the locked compatibility note instead of pretending the result is fully scheduled.
    - Test 4: if strict recap errors but fallback rows exist, the matrix stays visible and the old hard empty-state blocker does not win.
    - Test 5: a mixed strict+fallback dataset exports from the same merged matrix the recap tab renders.
  </behavior>
  <action>
    Wire the compatibility engine into admin reports per D-03.

    Required behavior:
    1. In `_loadDailySummaryData()`, keep the existing fetch order of raw attendance rows, payroll roster, and strict recap rows.
    2. Adapt raw attendance into the Plan 02 `LegacyPayrollSourceDay` contract by using the shared pure source-day builder from `LegacyPayrollRecapFallbackService`, not naive local calendar-date grouping from `_ReportRow` or `_computeDailySummaries()`.
    3. Pass the selected outlet's `OutletOperatingMode` into that source-day builder so fallback grouping stays overnight-safe and aligned with strict recap keys for `TWENTY_FOUR_HOUR` outlets.
    4. Call `LegacyPayrollRecapFallbackService.synthesizeMissingRows(...)` after strict rows are fetched and before `buildPayrollMatrix(...)`.
    5. Merge strict and fallback recap rows by `(employeeId, logicalDate)` with `putIfAbsent` semantics so strict rows always win.
    6. Build `_payrollMatrixDataset` from the merged row set and store enough UI state to know whether compatibility mode injected fallback rows.
    7. When fallback rows were injected, render the locked note:
       - title: `Mode kompatibilitas aktif`
       - body: `Sebagian hari payroll dihitung dari log absensi dan kontrak karena jadwal belum tersedia.`
    8. If strict recap returned an error but the merged dataset is still non-empty because fallback rows exist, keep the matrix visible and do not render the old `Rekap Policy Belum Tersedia` blocker.
    9. Keep export parity strict: the same merged dataset shown in the recap tab must be the dataset handed to `PayrollSpreadsheetExportService`.
    10. Do not create a second export path, second matrix DTO, migration, or backfill step.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_reports_screen.dart` imports and uses `LegacyPayrollRecapFallbackService`
    - `_loadDailySummaryData()` uses the shared overnight-safe source-day builder before synthesizing fallback rows
    - `_loadDailySummaryData()` merges strict and fallback rows before `buildPayrollMatrix(...)`
    - the payroll tab can render a matrix even when strict recap rows are missing but fallback rows exist
    - the compatibility note uses the locked copy above
    - mixed strict+fallback overnight legacy rows do not create duplicate or misdated employee-date cells
    - the old `Rekap Policy Belum Tersedia` empty state only appears when both strict and fallback paths truly produce no usable payroll rows
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart` exits 0
  </acceptance_criteria>
  <done>The admin payroll recap path now stays useful for legacy outlets without forking the matrix or export flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend recap and workbook regressions so compatibility-mode output keeps the Phase 58 contract</name>
  <files>test/screens/admin/admin_reports_payroll_matrix_test.dart, test/services/payroll_spreadsheet_export_service_test.dart</files>
  <read_first>
    - test/screens/admin/admin_reports_payroll_matrix_test.dart
    - test/services/payroll_spreadsheet_export_service_test.dart
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/payroll_spreadsheet_export_service.dart
  </read_first>
  <behavior>
    - Test 1: the recap tab shows the compatibility note when fallback rows exist.
    - Test 2: the recap tab does not show the strict-policy empty blocker when fallback rows keep the dataset usable.
    - Test 3: a mixed strict+fallback overnight dataset keeps the same rendered cell/date parity in recap and export.
    - Test 4: a workbook generated from fallback-style matrix cells still uses the same locked headers and excludes the forbidden technical fields.
  </behavior>
  <action>
    Add the regression coverage that proves compatibility mode does not fork the Phase 58 contract.

    Required behavior:
    1. Extend `test/screens/admin/admin_reports_payroll_matrix_test.dart` to cover:
       - compatibility note copy
       - fallback-visible matrix state when strict recap is unavailable or partial
       - mixed strict+fallback overnight logical-date behavior
       - the recap tab still exposing only `Ekspor Spreadsheet`
    2. Extend `test/services/payroll_spreadsheet_export_service_test.dart` with a mixed strict+fallback dataset case, including an overnight fallback-style matrix cell using compatibility labels such as `Hadir Tanpa Jadwal` or incomplete fallback copy, and verify:
       - workbook generation still succeeds
       - locked headers remain present
       - the employee/date cell content aligns with the same merged logical-date dataset used by the recap tab
       - forbidden fields remain absent
    3. Keep the export service contract unchanged; this task proves compatibility-mode data survives the existing contract rather than changing it.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart test/services/payroll_spreadsheet_export_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` asserts `Mode kompatibilitas aktif`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` covers a usable fallback dataset without the strict-policy blocker
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` covers a mixed strict+fallback overnight logical-date case
    - `test/services/payroll_spreadsheet_export_service_test.dart` covers at least one mixed strict+fallback matrix cell value
    - workbook assertions still prove the export contract excludes forbidden fields
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart test/services/payroll_spreadsheet_export_service_test.dart` exits 0
  </acceptance_criteria>
  <done>Compatibility-mode recap and spreadsheet output are both protected by focused regression coverage.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_matrix_test.dart test/services/payroll_spreadsheet_export_service_test.dart` passes.
- `lib/screens/admin/admin_reports_screen.dart` merges fallback rows before matrix construction.
- Spreadsheet export still runs through `PayrollSpreadsheetExportService` with no legacy fork.
</verification>

<success_criteria>
- Rekap Payroll no longer collapses into a misleading blank/blocked state for attendance-backed legacy outlets.
- Operators get a small, truthful compatibility disclosure when fallback rows are in use.
- Spreadsheet export remains aligned with the exact same mixed strict+fallback matrix dataset shown in admin recap, including overnight logical dates.
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-03-SUMMARY.md`
</output>
