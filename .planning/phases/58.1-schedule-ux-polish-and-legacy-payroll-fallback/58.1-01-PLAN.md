---
phase: 58.1-schedule-ux-polish-and-legacy-payroll-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/screens/admin/shift_scheduler_screen.dart
  - lib/screens/admin/widgets/schedule_policy_summary_card.dart
  - test/widgets/schedule_policy_widgets_test.dart
  - test/screens/admin/shift_scheduler_screen_test.dart
autonomous: true
requirements: ["58.1-UX-01", "58.1-UX-02"]
must_haves:
  truths:
    - "Admins can collapse or dismiss the schedule policy notice so the weekly scheduler stays reachable."
    - "The schedule policy guidance remains available on-screen instead of being deleted outright."
    - "Shift-band and required-hours chips stay readable in unselected and disabled light states."
  artifacts:
    - path: "lib/screens/admin/shift_scheduler_screen.dart"
      provides: "Screen-owned notice visibility state and explicit assignment chip styling"
      contains: "SchedulePolicySummaryCard"
    - path: "lib/screens/admin/widgets/schedule_policy_summary_card.dart"
      provides: "Dismissible/minimizable schedule guidance card"
      exports: ["SchedulePolicySummaryCard"]
    - path: "test/widgets/schedule_policy_widgets_test.dart"
      provides: "Widget coverage for notice minimize and dismiss behavior"
      contains: "SchedulePolicySummaryCard"
    - path: "test/screens/admin/shift_scheduler_screen_test.dart"
      provides: "Screen-level coverage for readable assignment chips"
      contains: "ChoiceChip"
  key_links:
    - from: "lib/screens/admin/shift_scheduler_screen.dart"
      to: "lib/screens/admin/widgets/schedule_policy_summary_card.dart"
      via: "screen-owned expanded/dismissed state must drive the reusable notice card"
      pattern: "SchedulePolicySummaryCard"
    - from: "lib/screens/admin/shift_scheduler_screen.dart"
      to: "test/screens/admin/shift_scheduler_screen_test.dart"
      via: "assignment chip contrast must stay enforced by widget coverage at the real bottom-sheet surface"
      pattern: "ChoiceChip"
---

<objective>
Polish the admin scheduler UX so policy guidance stops blocking the work surface and the assignment controls stay legible.

Purpose: Phase 55 introduced the right schedule-policy concepts, but the current notice is too dominant and the assignment chips still fail the basic readability bar. This plan applies the urgent UX patch without redesigning the scheduler.
Output: collapsible/dismissible policy notice, explicit readable chip styling, and focused widget/screen coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-CONTEXT.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-RESEARCH.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-VALIDATION.md
@lib/screens/admin/shift_scheduler_screen.dart
@lib/screens/admin/widgets/schedule_policy_summary_card.dart
@test/widgets/schedule_policy_widgets_test.dart

<locked_decisions>
D-01: The schedule guidance notice must become closable or minimizable so it stops dominating the jadwal screen, but it must remain available.
D-02: The shift-band and required-hours chips must use readable dark foreground text on light and disabled states; this is a visual correctness fix, not a chooser redesign.
</locked_decisions>

<interfaces>
Current scheduler seam:
```dart
const SchedulePolicySummaryCard()
```

Target notice control contract for this plan:
```dart
SchedulePolicySummaryCard(
  isExpanded: ...,
  onToggleExpanded: ...,
  onDismiss: ...,
)
```

Current unreadable chip seam:
```dart
ChoiceChip(
  label: Text(band.label),
  selected: selectedBand == band,
  onSelected: (_) => setSheetState(() => selectedBand = band),
)
```

Locked UX outcome:
```text
Policy notice can be collapsed or dismissed.
Shift-band and required-hours chips always keep dark readable labels on light backgrounds.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the schedule policy notice collapsible and dismissible without removing it</name>
  <files>lib/screens/admin/shift_scheduler_screen.dart, lib/screens/admin/widgets/schedule_policy_summary_card.dart, test/widgets/schedule_policy_widgets_test.dart</files>
  <read_first>
    - lib/screens/admin/shift_scheduler_screen.dart
    - lib/screens/admin/widgets/schedule_policy_summary_card.dart
    - test/widgets/schedule_policy_widgets_test.dart
    - .planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the policy card renders in expanded mode by default and shows the locked guidance copy.
    - Test 2: tapping the minimize control collapses the body while keeping a compact affordance visible.
    - Test 3: tapping the dismiss control removes the card from the scheduler surface for the current screen session.
  </behavior>
  <action>
    Update the scheduler notice per D-01.

    Required behavior:
    1. Move notice state ownership into `ShiftSchedulerScreen` with two local booleans for visible/dismissed and expanded/minimized state. Keep this in-memory only for Phase 58.1.
    2. Change `SchedulePolicySummaryCard` from a static zero-argument widget into a reusable card that accepts explicit `isExpanded`, `onToggleExpanded`, and `onDismiss` callbacks.
    3. Preserve the existing policy copy and rows from Phase 55, but add compact controls so admins can:
       - collapse the card into a lightweight header state
       - dismiss it entirely for the current visit
    4. Keep a compact header/restore affordance visible when minimized so guidance is not lost.
    5. Do not add backend persistence, SharedPreferences storage, or any wider scheduler redesign.
    6. Extend `test/widgets/schedule_policy_widgets_test.dart` to cover expanded, minimized, and dismissed states using the real widget contract.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/schedule_policy_widgets_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/shift_scheduler_screen.dart` owns the policy card visibility state instead of rendering `const SchedulePolicySummaryCard()`
    - `lib/screens/admin/widgets/schedule_policy_summary_card.dart` exports a constructor with explicit control callbacks
    - the notice can be minimized without disappearing entirely
    - the notice can be dismissed for the active screen session
    - `test/widgets/schedule_policy_widgets_test.dart` covers minimize and dismiss interactions
    - `C:\flutter\bin\flutter.bat test test/widgets/schedule_policy_widgets_test.dart` exits 0
  </acceptance_criteria>
  <done>The scheduler notice no longer blocks the work surface and remains available as collapsible guidance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply explicit readable styling to the assignment chips and cover the real screen flow</name>
  <files>lib/screens/admin/shift_scheduler_screen.dart, test/screens/admin/shift_scheduler_screen_test.dart</files>
  <read_first>
    - lib/screens/admin/shift_scheduler_screen.dart
    - test/widgets/schedule_policy_widgets_test.dart
    - .planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: unselected shift-band chips render dark readable labels on light backgrounds.
    - Test 2: required-hours chips keep readable dark labels in unselected state and remain legible when disabled by `Libur`.
    - Test 3: selected chips preserve intentional contrast without relying on theme defaults.
  </behavior>
  <action>
    Fix the assignment bottom-sheet chips per D-02.

    Required behavior:
    1. Update the `ChoiceChip`s inside `_showAssignedEntryEditor(...)` for both the shift-band group and required-hours group to use explicit visual rules:
       - readable dark `labelStyle`
       - explicit light `backgroundColor`
       - explicit selected color
       - explicit border/side
       - disabled visuals that still keep label contrast
    2. Scope the fix to the actual assignment editor. Do not redesign the sheet flow or alter unrelated chip groups unless a small shared helper is the cleanest path.
    3. Keep the `Libur` disabling behavior for required-hours chips, but make those disabled chips readable instead of washed out.
    4. Add `test/screens/admin/shift_scheduler_screen_test.dart` if it does not exist and cover the chip states at the real screen/editor seam.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/shift_scheduler_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/shift_scheduler_screen.dart` sets explicit chip foreground/background styling for both band and hours groups
    - the required-hours chips remain readable when `selectedBand == ShiftBand.libur`
    - `test/screens/admin/shift_scheduler_screen_test.dart` exists
    - `test/screens/admin/shift_scheduler_screen_test.dart` asserts readable chip labels for unselected and disabled states
    - `C:\flutter\bin\flutter.bat test test/screens/admin/shift_scheduler_screen_test.dart` exits 0
  </acceptance_criteria>
  <done>The assignment editor chips meet the urgent readability requirement without changing scheduler behavior.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/schedule_policy_widgets_test.dart` passes.
- `C:\flutter\bin\flutter.bat test test/screens/admin/shift_scheduler_screen_test.dart` passes.
- The scheduler notice can be minimized or dismissed while the assignment controls remain visible.
</verification>

<success_criteria>
- Admins are no longer forced to keep the full schedule notice expanded.
- The schedule guidance remains available in a compact form instead of being deleted.
- Shift-band and required-hours chips are readable in the real assignment editor across selected, unselected, and disabled states.
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-01-SUMMARY.md`
</output>
