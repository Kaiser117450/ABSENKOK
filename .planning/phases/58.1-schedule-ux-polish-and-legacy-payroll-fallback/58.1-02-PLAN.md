---
phase: 58.1-schedule-ux-polish-and-legacy-payroll-fallback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/legacy_payroll_recap_fallback_service.dart
  - lib/services/schedule_policy_service.dart
  - test/services/legacy_payroll_recap_fallback_service_test.dart
  - test/services/payroll_matrix_builder_test.dart
  - test/services/payroll_matrix_semantics_test.dart
autonomous: true
requirements: [REPORT-01]
must_haves:
  truths:
    - "Legacy outlets with attendance logs but no matching strict recap rows still produce payroll recap rows."
    - "Fallback rows stay contract-aware for FULLTIME versus PARTTIME required hours."
    - "Fallback never overwrites an existing strict recap row and never fabricates late or absence signals."
  artifacts:
    - path: "lib/services/legacy_payroll_recap_fallback_service.dart"
      provides: "Pure synthesis of missing recap rows from legacy attendance data"
      contains: "LegacyPayrollRecapFallbackService"
    - path: "lib/services/schedule_policy_service.dart"
      provides: "Shared contract-hour and legacy break-allowance helpers for compatibility rows"
      contains: "defaultRequiredWorkMinutes"
    - path: "test/services/legacy_payroll_recap_fallback_service_test.dart"
      provides: "Pure service coverage for legacy compatibility scenarios"
      contains: "LegacyPayrollRecapFallbackService"
    - path: "test/services/payroll_matrix_semantics_test.dart"
      provides: "Shared matrix semantics coverage for fallback-style recap rows"
      contains: "Hadir Tanpa Jadwal"
  key_links:
    - from: "lib/services/legacy_payroll_recap_fallback_service.dart"
      to: "lib/services/schedule_policy_service.dart"
      via: "fallback required hours and break allowance must reuse one contract-aware policy source"
      pattern: "defaultRequiredWorkMinutes"
    - from: "lib/services/legacy_payroll_recap_fallback_service.dart"
      to: "lib/models/attendance_policy_recap_day.dart"
      via: "compatibility output must stay on the existing recap row contract"
      pattern: "AttendancePolicyRecapDay"
    - from: "lib/services/legacy_payroll_recap_fallback_service.dart"
      to: "lib/services/payroll_matrix_builder.dart"
      via: "the service produces rows for the same matrix builder path instead of a legacy fork"
      pattern: "buildPayrollMatrix"
---

<objective>
Create the pure legacy payroll fallback service that synthesizes only the missing recap rows needed for legacy outlets without `schedule_entries`.

Purpose: the admin reports screen already has strict recap rows, active employees, and raw attendance data. This plan builds the compatibility engine underneath that seam so missing schedule data no longer blanks payroll recap, while keeping the existing matrix/export contract intact.
Output: a pure fallback service, shared policy helper(s), and pure tests proving honest contract-aware compatibility behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-CONTEXT.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-RESEARCH.md
@.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-VALIDATION.md
@.planning/phases/57-strict-recap-evaluation-engine/57-02-SUMMARY.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-01-SUMMARY.md
@lib/models/attendance_log.dart
@lib/models/attendance_policy_recap_day.dart
@lib/models/employee.dart
@lib/models/outlet_operating_mode.dart
@lib/services/schedule_policy_service.dart
@lib/services/payroll_matrix_builder.dart
@test/services/payroll_matrix_builder_test.dart
@test/services/payroll_matrix_semantics_test.dart

<locked_decisions>
D-03: Legacy payroll fallback must synthesize contract-aware required hours for attendance-backed days that lack `schedule_entries`, must not require migrations/backfills, and must feed the existing payroll matrix/export path.
</locked_decisions>

<interfaces>
Implement this service surface in this plan so Plan 03 can consume it directly:
```dart
class LegacyPayrollSourceDay {
  final DateTime logicalDate;
  final Employee employee;
  final String outletId;
  final String outletName;
  final OutletOperatingMode outletOperatingMode;
  final List<AttendanceLog> logs;
}

class LegacyPayrollRecapFallbackService {
  const LegacyPayrollRecapFallbackService();

  List<LegacyPayrollSourceDay> buildSourceDays({
    required Iterable<AttendanceLog> logs,
    required Map<String, Employee> employeesById,
    required String outletId,
    required String outletName,
    required OutletOperatingMode outletOperatingMode,
  });

  List<AttendancePolicyRecapDay> synthesizeMissingRows({
    required Iterable<LegacyPayrollSourceDay> sourceDays,
    required Iterable<AttendancePolicyRecapDay> strictRows,
    DateTime? now,
  });
}
```

Locked honesty constraints:
```text
Allowed fallback outcomes: sakit, izin, hadirTanpaJadwal, shortWork, excessBreak, overtime, belumAbsenPulang, activeIncomplete
Forbidden fabricated outcomes: late, tidak_hadir / absence, schedule-derived cutoffs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure legacy fallback service and shared compatibility helpers</name>
  <files>lib/services/legacy_payroll_recap_fallback_service.dart, lib/services/schedule_policy_service.dart, test/services/legacy_payroll_recap_fallback_service_test.dart</files>
  <read_first>
    - lib/models/attendance_log.dart
    - lib/models/attendance_policy_recap_day.dart
    - lib/models/employee.dart
    - lib/services/schedule_policy_service.dart
    - .planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: PARTTIME and FULLTIME source days derive different `requiredWorkMinutes` from contract defaults.
    - Test 2: overtime days use the correct relaxed break allowance before computing `excessBreak`.
    - Test 3: source-day grouping stays overnight-safe for legacy after-midnight logs by deriving the same logical date contract the strict recap path expects, including `TWENTY_FOUR_HOUR` outlet behavior.
    - Test 4: source days that already have a strict `(employeeId, logicalDate)` row do not emit a fallback duplicate.
    - Test 5: fallback rows never emit `late`, `absence`, or schedule-only cutoff fields.
    - Test 6: incomplete attendance without `pulang` resolves to an honest incomplete fallback status, not a fabricated complete day.
  </behavior>
  <action>
    Implement the compatibility engine per D-03.

    Required behavior:
    1. Create `lib/services/legacy_payroll_recap_fallback_service.dart` as a pure Dart service. It must not call Supabase, mutate UI state, or depend on `_ReportRow`.
    2. Define the explicit source-day input contract in that file so admin reports can adapt raw attendance into it without leaking private screen types.
    3. In the same service, expose one pure source-day builder that groups raw attendance logs into `LegacyPayrollSourceDay` using the same overnight-safe logical-day contract required by Phase 57 parity:
       - accept `OutletOperatingMode`
       - never rely on naive local calendar-date grouping alone
       - handle after-midnight continuation logs for legacy `TWENTY_FOUR_HOUR` outlets so fallback keys line up with strict recap keys
       - stay deterministic and unit-testable with no widget or database dependency
    4. Build fallback rows only for source days with attendance-backed logs whose `(employeeId, logicalDate)` key is absent from `strictRows`.
    5. Reuse `SchedulePolicyService.defaultRequiredWorkMinutes(...)` for `requiredWorkMinutes`, and add one shared helper in `SchedulePolicyService` for the legacy break allowance rule if that keeps the break math honest and reusable.
    6. Populate `AttendancePolicyRecapDay` with:
       - `shiftBand: null`
       - `lateCutoffLocal: null`
       - `breakFirstDeadlineLocal: null`
       - contract-aware `requiredWorkMinutes`
       - metrics that are truthful from logs alone (`netWorkMinutes`, `totalBreakMinutes`, `shortWorkMinutes`, `overtimeMinutes`, `excessBreakMinutes`)
    7. Allowed status synthesis:
       - `sakit`
       - `izin`
       - `hadirTanpaJadwal`
       - `shortWork`
       - `excessBreak`
       - `overtime`
       - `belumAbsenPulang`
       - `activeIncomplete`
    8. Explicitly forbid synthesized `late`, `tidak_hadir`, or other schedule-derived labels.
    9. Add `test/services/legacy_payroll_recap_fallback_service_test.dart` with focused pure service coverage for the locked behaviors above, including overnight mixed-date legacy logs and strict-key parity.
    10. Do not add migrations, backfills, alternate export DTOs, or widget dependencies in this plan.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/legacy_payroll_recap_fallback_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/legacy_payroll_recap_fallback_service.dart` exists and exports `LegacyPayrollRecapFallbackService`
    - the service accepts an explicit source-day contract instead of screen-private `_ReportRow`
    - the service exposes an overnight-safe logical-day source builder for legacy raw attendance logs
    - fallback rows skip keys already present in strict recap rows
    - fallback rows set schedule-derived fields to `null`
    - `test/services/legacy_payroll_recap_fallback_service_test.dart` exists
    - `test/services/legacy_payroll_recap_fallback_service_test.dart` covers a cross-midnight `TWENTY_FOUR_HOUR` legacy case
    - `C:\flutter\bin\flutter.bat test test/services/legacy_payroll_recap_fallback_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The repo now has a pure, contract-aware compatibility engine for missing legacy payroll recap rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pure matrix tests so fallback-style recap rows stay on the shared payroll contract</name>
  <files>test/services/payroll_matrix_builder_test.dart, test/services/payroll_matrix_semantics_test.dart</files>
  <read_first>
    - lib/services/payroll_matrix_builder.dart
    - lib/services/payroll_matrix_semantics.dart
    - test/services/payroll_matrix_builder_test.dart
    - test/services/payroll_matrix_semantics_test.dart
    - lib/services/legacy_payroll_recap_fallback_service.dart
  </read_first>
  <behavior>
    - Test 1: fallback `hadirTanpaJadwal` rows still produce visible matrix cells instead of placeholders.
    - Test 2: fallback overtime/short-work/excess-break rows contribute to the same summary counts used by strict rows.
    - Test 3: fallback labels remain payroll-facing and do not introduce forbidden legacy-only vocabulary.
    - Test 4: a mixed strict+fallback dataset with an overnight legacy row stays keyed to the intended logical date instead of creating duplicate calendar-day cells.
  </behavior>
  <action>
    Protect the shared matrix contract with pure tests after Task 1.

    Required behavior:
    1. Extend `test/services/payroll_matrix_builder_test.dart` so one dataset built from fallback-style recap rows proves:
       - rows are still rendered for active employees
       - fallback recap rows become actual populated cells
       - summary counts still come from the shared builder path
       - mixed strict+fallback rows keyed by the same overnight-safe logical date do not double-render
    2. Extend `test/services/payroll_matrix_semantics_test.dart` so fallback-compatible statuses such as `Hadir Tanpa Jadwal` and incomplete/no-schedule penalty states keep the intended payroll-facing labels and tags.
    3. Keep these tests pure. Do not pull in screen widgets or export services here.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/payroll_matrix_builder_test.dart test/services/payroll_matrix_semantics_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/services/payroll_matrix_builder_test.dart` covers fallback recap rows as non-placeholder cells
    - `test/services/payroll_matrix_builder_test.dart` covers a mixed strict+fallback overnight logical-date case
    - `test/services/payroll_matrix_semantics_test.dart` covers fallback-compatible labels such as `Hadir Tanpa Jadwal`
    - the matrix tests still run without widget dependencies
    - `C:\flutter\bin\flutter.bat test test/services/payroll_matrix_builder_test.dart test/services/payroll_matrix_semantics_test.dart` exits 0
  </acceptance_criteria>
  <done>Fallback recap rows are now guarded by the same pure matrix contract as the strict recap path.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/legacy_payroll_recap_fallback_service_test.dart` passes.
- `C:\flutter\bin\flutter.bat test test/services/payroll_matrix_builder_test.dart test/services/payroll_matrix_semantics_test.dart` passes.
- The fallback service produces contract-aware, honest recap rows without any migration or export fork.
</verification>

<success_criteria>
- Legacy attendance-backed days can be turned into payroll recap rows without `schedule_entries`.
- Overnight legacy attendance at `TWENTY_FOUR_HOUR` outlets stays keyed to the same logical date contract used by strict recap merging.
- FULLTIME and PARTTIME fallback rows preserve different required-hours behavior.
- The fallback stays additive: it never overwrites strict rows and never invents schedule-derived lateness or absence.
</success_criteria>

<output>
After completion, create `.planning/phases/58.1-schedule-ux-polish-and-legacy-payroll-fallback/58.1-02-SUMMARY.md`
</output>
