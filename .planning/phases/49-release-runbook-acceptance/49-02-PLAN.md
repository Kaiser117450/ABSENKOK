---
phase: 49-release-runbook-acceptance
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - .planning/phases/49-release-runbook-acceptance/49-ACCEPTANCE.md
  - build/releases/android/ABSENKOK-v7.0.0+8013/ABSENKOK-v7.0.0+8013.apk
  - build/releases/android/ABSENKOK-v7.0.0+8013/release-manifest.json
  - build/releases/android/ABSENKOK-v7.0.0+8013/smoke-check.txt
autonomous: false
requirements: [OPS-01]
user_setup:
  - service: local-signing-bootstrap
    why: "The tracked repo does not contain the private upload keystore or the real `android/key.properties`, so Phase 49 acceptance needs human-provided local signing inputs."
    dashboard_config:
      - task: "Create `android/key.properties` from `android/key.properties.example` using the private upload-key values."
        location: "Local workspace only; keep the file untracked."
      - task: "Make the referenced upload keystore available at the private local path named by `storeFile`."
        location: "Local machine only; outside source control."
  - service: local-android-target
    why: "Smoke verification requires `adb devices` to show at least one Android device or emulator before the release lane can prove install/launch."
    dashboard_config:
      - task: "Connect an Android device or start an emulator until `adb devices` lists one target in `device` state."
        location: "Local Android SDK / device environment"
must_haves:
  truths:
    - "The documented runbook can be followed on this machine to cut the canonical signed APK release record without guessing missing bootstrap steps."
    - "The staged v7.0.0+8013 release directory contains the canonical APK, `release-manifest.json`, retained symbols, and `smoke-check.txt` from the same smoke-verified run."
    - "Acceptance evidence records whether the runbook passed, what command was executed, and any real blocker encountered without using bypass flags."
  artifacts:
    - path: ".planning/phases/49-release-runbook-acceptance/49-ACCEPTANCE.md"
      provides: "Phase-local acceptance evidence describing the executed runbook lane and resulting release record."
      contains: "ABSENKOK-v7.0.0+8013"
    - path: "build/releases/android/ABSENKOK-v7.0.0+8013/release-manifest.json"
      provides: "Manifest proving the canonical artifact and smoke verification state for the reproduced release."
      contains: "canonicalArtifact"
    - path: "build/releases/android/ABSENKOK-v7.0.0+8013/smoke-check.txt"
      provides: "Smoke verification evidence attached to the same staged release directory as the canonical APK."
      contains: "Status:"
  key_links:
    - from: "docs/android-release-runbook.md"
      to: "build/releases/android/ABSENKOK-v7.0.0+8013/release-manifest.json"
      via: "the documented release command must generate the staged manifest at the exact location the runbook promises"
      pattern: "release-manifest.json"
    - from: "build/releases/android/ABSENKOK-v7.0.0+8013/release-manifest.json"
      to: "build/releases/android/ABSENKOK-v7.0.0+8013/ABSENKOK-v7.0.0+8013.apk"
      via: "manifest canonicalArtifact must point to the staged APK retained from the smoke-verified run"
      pattern: "canonicalArtifact"
    - from: "build/releases/android/ABSENKOK-v7.0.0+8013/smoke-check.txt"
      to: "build/releases/android/ABSENKOK-v7.0.0+8013/ABSENKOK-v7.0.0+8013.apk"
      via: "smoke evidence must describe installation and launch of the same canonical APK"
      pattern: "APK source:"
---

<objective>
Use the documented Phase 49 runbook to reproduce the local v7.0 release record and capture acceptance evidence.

Purpose: OPS-01 is not satisfied by documentation alone. The runbook must survive real operator bootstrap, release execution, and smoke evidence capture without hidden assumptions or bypass flags.
Output: A staged release directory for `ABSENKOK-v7.0.0+8013` plus a `49-ACCEPTANCE.md` note that records the result of the documented release lane on this machine.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-release-runbook-acceptance/49-RESEARCH.md
@.planning/phases/49-release-runbook-acceptance/49-VALIDATION.md
@.planning/phases/49-release-runbook-acceptance/49-01-PLAN.md
@docs/android-release-runbook.md
@docs/android-release-contract.md
@tool/release_env.ps1
@tool/release_preflight.ps1
@tool/release_build.ps1
@android/key.properties.example
@pubspec.yaml

<interfaces>
Current local reality captured before execution:
```text
android/key.properties = missing
private keystore under android/ = not found
smoke verification requires adb-visible Android target
```

Release record expected from a successful acceptance run:
```text
build/releases/android/ABSENKOK-v7.0.0+8013/
  ABSENKOK-v7.0.0+8013.apk
  release-manifest.json
  smoke-check.txt
  symbols/
  mapping.txt (if generated)
  ABSENKOK-v7.0.0+8013.aab (only if -IncludeAppBundle was requested)
```
</interfaces>

<constraints>
- do not use `--android-skip-build-dependency-validation` or any equivalent bypass
- acceptance must run through the documented `powershell.exe` helper lane, not an ad hoc shell variant
- if secrets/bootstrap or preflight are still missing/red, record that truthfully in acceptance evidence and fail rather than fabricating a passing release
- default acceptance proves the APK-first lane; optional bundle retention remains non-required unless intentionally requested
</constraints>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Bootstrap the private signing inputs and smoke target required by the runbook</name>
  <files>android/key.properties</files>
  <action>
    Complete the local-only prerequisites that the repo cannot supply automatically.

    Required human actions:
    1. Create `android/key.properties` from `android/key.properties.example` with the real upload-keystore values.
    2. Ensure the private keystore file referenced by `storeFile` is present on the local machine and readable from the workspace.
    3. Connect a real Android device or start an emulator until `adb devices` shows at least one target in `device` state.
    4. Do not add the real `android/key.properties` or keystore file to git.

    Resume only after the above prerequisites are complete.
  </action>
  <verify>User confirms `android/key.properties` exists locally, the referenced keystore path is valid, and `adb devices` lists at least one Android target.</verify>
  <done>The private signing bootstrap and smoke target required by the runbook are present locally without committing secrets to the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Run the documented release lane and capture acceptance evidence for the staged release record</name>
  <files>.planning/phases/49-release-runbook-acceptance/49-ACCEPTANCE.md, build/releases/android/ABSENKOK-v7.0.0+8013/ABSENKOK-v7.0.0+8013.apk, build/releases/android/ABSENKOK-v7.0.0+8013/release-manifest.json, build/releases/android/ABSENKOK-v7.0.0+8013/smoke-check.txt</files>
  <read_first>
    - docs/android-release-runbook.md
    - tool/release_env.ps1
    - tool/release_preflight.ps1
    - tool/release_build.ps1
    - pubspec.yaml
  </read_first>
  <action>
    Execute the exact local release sequence documented in `docs/android-release-runbook.md` and record what happened.

    Required behavior:
    1. Follow the runbook through the tracked helper chain using `powershell.exe`, culminating in `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -SmokeVerify`.
    2. Do not skip or weaken `tool/release_preflight.ps1`. If the run fails because preflight is red, signing inputs are wrong, or no device is visible, capture that blocker in `49-ACCEPTANCE.md` and let the task fail rather than using a bypass.
    3. On success, inspect the staged release directory and write `49-ACCEPTANCE.md` with:
       - executed command surface
       - release label and directory
       - canonical artifact path
       - smoke verification status
       - whether optional bundle retention was omitted
       - whether `mapping.txt` exists
       - any noteworthy operator friction discovered while following the runbook
    4. Keep the acceptance evidence scoped to the default APK-first lane. Optional `.aab` retention is not required for a passing Phase 49 acceptance run.

    Scope guard:
    - do not edit helper scripts or the runbook in this plan unless acceptance uncovers a blocker that belongs in a later gap-closure phase
    - do not publish a GitHub Release or add CI/CD automation here
  </action>
  <verify>
    <automated>powershell -Command "$label = 'ABSENKOK-v7.0.0+8013'; $dir = Join-Path 'build/releases/android' $label; $apk = Join-Path $dir ($label + '.apk'); $manifestPath = Join-Path $dir 'release-manifest.json'; $smokePath = Join-Path $dir 'smoke-check.txt'; $acceptancePath = '.planning/phases/49-release-runbook-acceptance/49-ACCEPTANCE.md'; if (-not (Test-Path $apk)) { throw 'Canonical APK missing' }; if (-not (Test-Path $manifestPath)) { throw 'Manifest missing' }; if (-not (Test-Path $smokePath)) { throw 'Smoke evidence missing' }; if (-not (Test-Path $acceptancePath)) { throw 'Acceptance note missing' }; $manifest = Get-Content $manifestPath -Raw | ConvertFrom-Json; if ($manifest.canonicalArtifact -notmatch '\\.apk$') { throw 'Manifest canonicalArtifact is not APK' }; if ($manifest.smokeVerification.status -ne 'passed') { throw ('Smoke status was ' + $manifest.smokeVerification.status) }"</automated>
  </verify>
  <done>The documented runbook has been exercised on this machine and produced a staged APK-first release record plus a phase-local acceptance note, or it failed transparently on a real blocker without bypassing the contract.</done>
</task>

</tasks>

<verification>
- the runbook was followed via `powershell.exe` and the helper chain, not via ad hoc commands
- the staged release directory contains the canonical APK, `release-manifest.json`, and `smoke-check.txt` from the same acceptance run
- `49-ACCEPTANCE.md` records the real operator outcome and any blocker truthfully
</verification>

<success_criteria>
- OPS-01 is proven by a real local runbook execution, not by undocumented operator memory
- a successful acceptance run leaves one self-contained staged release record under `build/releases/android/ABSENKOK-v7.0.0+8013/`
- if acceptance fails, the failure is attributable to a concrete blocker that can drive a later gap-closure plan
</success_criteria>

<output>
After completion, create `.planning/phases/49-release-runbook-acceptance/49-02-SUMMARY.md`
</output>
