---
phase: 49-release-runbook-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/android-release-runbook.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "A second operator can see the exact Windows PowerShell release sequence without guessing the helper order, shell runtime, or expected staged outputs."
    - "The runbook documents private signing bootstrap as placeholders only, with the real keystore and `android/key.properties` remaining machine-local."
    - "The runbook describes the APK-first release record truthfully: canonical signed APK, retained symbols, optional mapping, optional bundle, and smoke evidence in one versioned directory."
  artifacts:
    - path: "docs/android-release-runbook.md"
      provides: "Operator-facing local release runbook for the v7.0 APK-first release lane."
      contains: "tool/release_build.ps1 -SmokeVerify"
  key_links:
    - from: "docs/android-release-runbook.md"
      to: "tool/release_env.ps1"
      via: "the runbook starts with the tracked environment contract before any release command"
      pattern: "tool/release_env.ps1"
    - from: "docs/android-release-runbook.md"
      to: "tool/release_preflight.ps1"
      via: "the runbook preserves the mandatory fail-fast preflight gate"
      pattern: "tool/release_preflight.ps1"
    - from: "docs/android-release-runbook.md"
      to: "tool/release_build.ps1"
      via: "the runbook uses the existing APK-first build helper instead of inventing a new release surface"
      pattern: "tool/release_build.ps1"
---

<objective>
Write the tracked local release runbook that Phase 49 needs for operator reproducibility.

Purpose: Phase 48.1 aligned the helper and contract to one APK-first truth, but the repo still lacks the exact step-by-step operator instructions that another person can follow without tribal knowledge.
Output: A runbook under `docs/` that explains prerequisites, private bootstrap placeholders, exact `powershell.exe` commands, expected staged outputs, and acceptance checkpoints for the local v7.0 release lane.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/49-release-runbook-acceptance/49-RESEARCH.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-01-SUMMARY.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-02-SUMMARY.md
@docs/android-release-contract.md
@tool/release_env.ps1
@tool/release_preflight.ps1
@tool/release_build.ps1
@android/key.properties.example
@pubspec.yaml

<interfaces>
Current tracked command surface to preserve in the runbook:
```powershell
powershell -ExecutionPolicy Bypass -File tool/release_env.ps1 -CheckOnly
powershell -ExecutionPolicy Bypass -File tool/release_preflight.ps1
powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly
powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -SmokeVerify
```

Current tracked release label from pubspec:
```text
version: 7.0.0+8013
release directory: build/releases/android/ABSENKOK-v7.0.0+8013/
```

Private signing bootstrap schema to describe without committing secrets:
```text
storePassword=REPLACE_WITH_UPLOAD_KEYSTORE_PASSWORD
keyPassword=REPLACE_WITH_UPLOAD_KEY_PASSWORD
keyAlias=REPLACE_WITH_UPLOAD_KEY_ALIAS
storeFile=REPLACE_WITH_PRIVATE_UPLOAD_KEYSTORE_FILE
```
</interfaces>

<constraints>
- keep the runbook aligned with `powershell.exe` / Windows PowerShell 5.1, not `pwsh`-only wording
- do not invent new release helpers, CI/CD steps, GitHub Release automation, or Play Store flow
- document placeholder/bootstrap expectations without exposing real secret values or keystore paths committed to git
- keep the release contract APK-first, with `.aab` called out as optional via `-IncludeAppBundle`
- do not pretend the runbook can bypass a red preflight gate or missing signing/bootstrap state
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the operator runbook with prerequisites, bootstrap placeholders, and exact command order</name>
  <files>docs/android-release-runbook.md</files>
  <read_first>
    - docs/android-release-contract.md
    - tool/release_env.ps1
    - tool/release_preflight.ps1
    - tool/release_build.ps1
    - android/key.properties.example
    - .planning/phases/49-release-runbook-acceptance/49-RESEARCH.md
  </read_first>
  <action>
    Create `docs/android-release-runbook.md` as the tracked Phase 49 operator guide for the local v7.0 release lane.

    Required content:
    1. State the supported shell/runtime explicitly: `powershell.exe`, Java 21 JBR, canonical Flutter CLI, and the APK-first contract inherited from `docs/android-release-contract.md`.
    2. Document prerequisites and private bootstrap clearly, including:
       - `android/key.properties` stays untracked and is derived from `android/key.properties.example`
       - the upload keystore file stays private and machine-local
       - smoke verification needs an Android target visible to `adb devices`
    3. Present the exact helper order and commands another operator should run:
       - `tool/release_env.ps1 -CheckOnly`
       - `tool/release_preflight.ps1`
       - `tool/release_build.ps1 -CheckOnly`
       - `tool/release_build.ps1 -SmokeVerify`
       - optional `-IncludeAppBundle` path described as non-default
    4. Explain the expected release directory contents for `ABSENKOK-v7.0.0+8013`, including the canonical APK, `release-manifest.json`, `symbols/`, optional `mapping.txt`, optional `.aab`, and `smoke-check.txt`.

    Scope guard:
    - keep this as a local release runbook, not a product release note or CI/CD design
    - do not tell operators to edit tracked files beyond the new runbook
    - do not hide current blockers such as missing secret bootstrap or a failing preflight lane
  </action>
  <verify>
    <automated>powershell -Command '&amp; { $content = Get-Content "docs/android-release-runbook.md" -Raw; $markers = @("powershell -ExecutionPolicy Bypass -File tool/release_env.ps1 -CheckOnly","powershell -ExecutionPolicy Bypass -File tool/release_preflight.ps1","powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly","powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -SmokeVerify","android/key.properties.example","ABSENKOK-v7.0.0+8013","-IncludeAppBundle"); foreach ($marker in $markers) { if ($content -notmatch [regex]::Escape($marker)) { throw ("Missing runbook marker: " + $marker) } } }'</automated>
  </verify>
  <done>The repo has a new tracked runbook that documents the exact local release sequence, bootstrap placeholders, and expected APK-first outputs without exposing secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-check the runbook against the live helper contract and expected outputs</name>
  <files>docs/android-release-runbook.md</files>
  <read_first>
    - docs/android-release-runbook.md
    - docs/android-release-contract.md
    - tool/release_build.ps1
    - pubspec.yaml
  </read_first>
  <action>
    Tighten the runbook so it stays truthful to the live helper contract instead of drifting into hand-written release folklore.

    Required behavior:
    1. Re-run the helper in check-only mode and make sure the runbook wording matches the current staged output shape and release label.
    2. Make the runbook distinguish clearly between:
       - check-only contract inspection
       - real release build with `-SmokeVerify`
       - optional `.aab` retention via `-IncludeAppBundle`
    3. Include an operator-facing checklist for what must be true before marking a local release as ready for distribution.
    4. Keep any known-blocker notes factual: no bypass flags, no pretending a failed preflight still counts as a reproducible release.

    Scope guard:
    - do not modify the helper scripts in this plan
    - do not add acceptance evidence or runtime results yet; that belongs to Plan 49-02
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify</automated>
  </verify>
  <done>The runbook wording matches the live helper contract and distinguishes bootstrap inspection from the real smoke-verified release lane.</done>
</task>

</tasks>

<verification>
- `docs/android-release-runbook.md` contains the exact PowerShell helper sequence and the private bootstrap placeholders another operator needs
- the runbook keeps the canonical artifact APK-first and calls `.aab` retention optional via `-IncludeAppBundle`
- the runbook matches the current check-only helper surface for release label, staged directory, and smoke evidence expectations
</verification>

<success_criteria>
- OPS-01 is documented as one repeatable local operator flow rather than scattered helper knowledge
- the runbook names every hidden prerequisite another operator would otherwise guess
- the repo has one tracked runbook that can be used directly in Phase 49 acceptance without reinterpretation
</success_criteria>

<output>
After completion, create `.planning/phases/49-release-runbook-acceptance/49-01-SUMMARY.md`
</output>
