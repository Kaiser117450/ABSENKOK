---
phase: 43-portal-attendance-recap-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
autonomous: true
requirements: [ATTN-01, PORT-03]
must_haves:
  truths:
    - "The existing portal shell exposes attendance recap as a first-class destination without leaving the employee-facing flow"
    - "The portal home gives employees a clear path from schedule home to the recap page"
    - "The new entry affordances fit the current phone-first shell without regressing logout or the existing schedule home"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro"
      provides: "Portal shell navigation state for home and attendance pages"
      contains: "activeSection"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro"
      provides: "Home-page attendance recap entry point"
      contains: "/portal/attendance"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/attendance.astro"
      via: "renders the shell-level route entry to the recap surface"
      pattern: "/portal/attendance"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro"
      via: "passes the active shell state and surfaces the recap CTA from the existing home page"
      pattern: "activeSection"
---

<objective>
Add the entry path for the attendance recap inside the current employee portal shell so employees can discover and open the recap without leaving the existing flow.

Purpose: Phase 43 needs a clear entry point before the recap page is wired. The shell and home page should communicate that attendance recap is now part of the portal, not a detached destination.
Output: A shell-level navigation state for attendance recap and a home-page CTA that points employees to the new recap route.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-portal-attendance-recap-surface/43-RESEARCH.md
@.planning/phases/43-portal-attendance-recap-surface/43-VALIDATION.md
@.planning/phases/39-employee-portal-schedule-ux/39-02-SUMMARY.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro

<interfaces>
Current shell signature:
```astro
interface Props {
  title: string;
  employee: PortalEmployee;
}
```

Current home route:
```astro
<PortalLayout title="Beranda" employee={state.schedule.employee}>
  ...
</PortalLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add attendance navigation state to the portal shell</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
    - .planning/phases/43-portal-attendance-recap-surface/43-RESEARCH.md
  </read_first>
  <action>
    Update `src/layouts/PortalLayout.astro` so the shell can represent both the home and attendance recap destinations.

    Required behavior:
    1. Extend the layout props with a small active-section signal such as `home | attendance`.
    2. Render a compact, phone-safe shell nav that links to:
       - `/portal`
       - `/portal/attendance`
    3. Style the active section clearly while preserving the current sticky header, logout action, and pending affordance.
    4. Keep the shell server-render-friendly and avoid client-framework state.

    Scope guard:
    - do not redesign the brand header or logout behavior
    - do not add more than the two employee-facing portal destinations needed in this milestone
    - do not move the shell nav outside `PortalLayout.astro`
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro' -Pattern '/portal/attendance','activeSection','Kehadiran' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>The portal shell can represent and link to both Beranda and Kehadiran while preserving the current phone-first header and logout affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Add the recap entry point to the portal home</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
    - .planning/phases/43-portal-attendance-recap-surface/43-RESEARCH.md
  </read_first>
  <action>
    Update `src/pages/portal/index.astro` so the existing home page becomes the entry point to the new recap route without losing its schedule-home role.

    Required behavior:
    1. Pass the shell's active-section prop for the home state.
    2. Add one clear entry affordance to `/portal/attendance`, for example:
       - a compact CTA card below the greeting
       - or an equivalent in-page action that fits the current portal language
    3. Keep the schedule surface as the primary home content after the entry affordance.
    4. Preserve the existing authenticated-state routing and avoid duplicating shell navigation logic outside the layout.

    Scope guard:
    - do not replace the schedule home with the recap
    - do not add recap data loading to the home page
    - do not introduce a second detached navigation pattern that conflicts with the shell nav
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The portal home clearly points employees to the recap route while remaining the schedule-focused landing page.</done>
</task>

</tasks>

<verification>
- the portal shell exposes a recap destination alongside the existing home route
- the home page has a clear, in-flow path to the recap route
- the new entry affordances remain mobile-safe and consistent with the current shell
</verification>

<success_criteria>
- ATTN-01 is supported by a discoverable, in-shell route entry to the attendance recap
- PORT-03 is supported by shell and home changes that preserve the current portal's mobile behavior
- the final recap page can rely on an existing shell navigation state instead of inventing its own entry point
</success_criteria>

<output>
After completion, create `.planning/phases/43-portal-attendance-recap-surface/43-02-SUMMARY.md`
</output>
