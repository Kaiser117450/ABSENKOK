---
phase: 43-portal-attendance-recap-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceSummary.astro
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceHistorySection.astro
autonomous: true
requirements: [ATTN-04, PORT-03]
must_haves:
  truths:
    - "Month-to-date summary counts render directly from the trusted recap dataset without a second query"
    - "Recent attendance history shows day-by-day recap cards with the status and timestamps needed to explain each workday"
    - "Both recap components stay readable on a phone-sized browser and visually match the current employee portal language"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceSummary.astro"
      provides: "Shared month-summary card component for the portal recap"
      contains: "summaryCounts"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceHistorySection.astro"
      provides: "Shared recent-history card list for the portal recap"
      contains: "recentDays"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceSummary.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts"
      via: "renders the existing month summary contract directly from the typed recap model"
      pattern: "summaryCounts"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceHistorySection.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts"
      via: "renders recent logical-day history from the typed recap model without a second transform boundary"
      pattern: "recentDays"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceHistorySection.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalScheduleSection.astro"
      via: "reuses the shipped stacked-card visual density and metadata treatment"
      pattern: "rounded-2xl"
---

<objective>
Create the shared ready-state recap components for the employee portal: one month-summary surface and one recent-history surface built directly on the Phase 42 recap model.

Purpose: Phase 43 should introduce reusable recap UI primitives before route wiring so the final page stays small, consistent, and ready for Phase 44 hardening without another markup rewrite.
Output: Two shared Astro components that render summary counts and recent day-by-day recap cards from the existing typed portal recap helper.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-portal-attendance-recap-surface/43-RESEARCH.md
@.planning/phases/43-portal-attendance-recap-surface/43-VALIDATION.md
@.planning/phases/42-attendance-recap-read-model/42-02-SUMMARY.md
@.planning/phases/39-employee-portal-schedule-ux/39-01-SUMMARY.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalScheduleSection.astro

<interfaces>
Relevant recap contract:
```ts
type PortalAttendanceRecapModel = {
  employee: PortalEmployee;
  referenceDate: string;
  monthStart: string;
  summaryCounts: {
    hadir: number;
    sakit: number;
    izin: number;
    tidakHadir: number;
    belumPulang: number;
    libur: number;
  };
  days: PortalRecapDay[];
  recentDays: PortalRecapDay[];
};
```

Recent-history rows already include:
```ts
type PortalRecapDay = {
  logicalDate: string;
  attendanceStatus: AttendanceStatus;
  outletName: string | null;
  shiftName: string | null;
  firstMasukAt: string | null;
  firstBreakAt: string | null;
  lastKembaliAt: string | null;
  lastPulangAt: string | null;
  notes: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the month-summary recap component</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceSummary.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalScheduleSection.astro
    - .planning/phases/43-portal-attendance-recap-surface/43-RESEARCH.md
  </read_first>
  <action>
    Create `src/components/portal/PortalAttendanceSummary.astro` as the shared summary-card surface for the recap page.

    Required behavior:
    1. Accept the typed recap model or a typed `summaryCounts` payload from `attendance-recap.ts`.
    2. Render the month-to-date statuses already exposed by the recap helper:
       - hadir
       - sakit
       - izin
       - tidakHadir
       - belumPulang
       - libur
    3. Use a compact, phone-friendly layout:
       - stacked heading plus wrapped summary cards
       - no table/grid that assumes desktop width
       - keep the visual language aligned with the existing portal cards
    4. Keep labels in employee-facing Indonesian copy and keep the component purely presentational.

    Scope guard:
    - do not fetch data or derive counts inside the component
    - do not add follow-up warnings or exception explainer copy reserved for Phase 44
    - do not introduce a client framework or new dependencies
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The portal has a reusable summary component that renders the typed month-to-date recap counts in a mobile-first card layout.</done>
</task>

<task type="auto">
  <name>Task 2: Create the recent-history recap component</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalAttendanceHistorySection.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/attendance-recap.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/portal/PortalScheduleSection.astro
    - .planning/phases/43-portal-attendance-recap-surface/43-RESEARCH.md
  </read_first>
  <action>
    Create `src/components/portal/PortalAttendanceHistorySection.astro` as the shared recent-history surface for the recap page.

    Required behavior:
    1. Accept `recentDays` (or the typed recap model) from the Phase 42 helper.
    2. Render descending logical-day cards that show:
       - logical date
       - attendance status badge
       - outlet and shift context when present
       - schedule time range when present
       - named attendance timestamps when present
       - notes when present
    3. Keep the layout phone-first and easy to scan:
       - one-column stacked cards
       - compact metadata rows
       - no wide desktop table treatment
    4. Reuse the existing portal status-color tone where it already exists, but keep potentially exceptional statuses neutral enough that Phase 44 can harden them later.

    Scope guard:
    - do not add recap-specific loading, empty, or error states in this component
    - do not add follow-up-day badges or exception explanations reserved for Phase 44
    - do not re-derive logical-day grouping or summary counts in the component
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The portal has a reusable recent-history component that can render typed recap day cards without extra data fetching or a desktop-only layout.</done>
</task>

</tasks>

<verification>
- shared summary and history components exist under `src/components/portal/`
- summary cards consume the existing recap summary-count contract rather than a new query shape
- recent history cards remain readable on phone widths and preserve the explanatory timestamps from the recap model
</verification>

<success_criteria>
- ATTN-04 is supported by a reusable summary component that renders month-to-date recap counts
- PORT-03 is supported by shared recap components that stay within the existing portal mobile design language
- Phase 43 route wiring can compose the recap page from shared components instead of another one-off markup block
</success_criteria>

<output>
After completion, create `.planning/phases/43-portal-attendance-recap-surface/43-01-SUMMARY.md`
</output>
