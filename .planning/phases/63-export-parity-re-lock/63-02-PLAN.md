---
phase: 63-export-parity-re-lock
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - test/services/payroll_spreadsheet_export_service_test.dart
  - test/services/payroll_pdf_matrix_export_service_test.dart
autonomous: true
requirements: ["REPORT-04", "REPORT-05"]
must_haves:
  truths:
    - "Payroll admins can read the spreadsheet and see the same status meaning and color emphasis they already use in Rekap Harian."
    - "Payroll admins can read the PDF and see the same day outcomes, legend meaning, and compatibility notice as the spreadsheet and recap."
    - "Salary-facing exports stay compact and continue to hide raw technical scan details."
  artifacts:
    - path: "test/fixtures/report_export_parity_fixture.dart"
      provides: "Shared parity fixture bundle reused by both export service tests"
      contains: "buildReportExportParityFixtureBundle"
    - path: "test/services/payroll_spreadsheet_export_service_test.dart"
      provides: "Workbook contract coverage derived from the shared merged-row fixture bundle"
      contains: "PayrollSpreadsheetExportService"
    - path: "test/services/payroll_pdf_matrix_export_service_test.dart"
      provides: "PDF preview and file contract coverage derived from the shared merged-row fixture bundle"
      contains: "PayrollPdfMatrixExportService"
  key_links:
    - from: "test/services/payroll_spreadsheet_export_service_test.dart"
      to: "test/fixtures/report_export_parity_fixture.dart"
      via: "spreadsheet assertions must build their dataset from the shared parity bundle"
      pattern: "buildReportExportParityFixtureBundle"
    - from: "test/services/payroll_pdf_matrix_export_service_test.dart"
      to: "test/fixtures/report_export_parity_fixture.dart"
      via: "PDF assertions must build their dataset from the shared parity bundle"
      pattern: "buildReportExportParityFixtureBundle"
    - from: "test/services/payroll_pdf_matrix_export_service_test.dart"
      to: "lib/services/payroll_pdf_matrix_export_service.dart"
      via: "preview assertions must lock compatibility title, legend tags, and forbidden-field exclusion on the real service interface"
      pattern: "buildPreview"
---

<objective>
Retarget the spreadsheet and payroll PDF service contracts onto the shared parity fixtures created in Plan 01.

Purpose: the production export services already use the correct matrix-driven path, but their tests still prove formatting with synthetic matrix literals. This plan replaces those stale fixtures with the shared merged-row parity bundle so REPORT-04 and REPORT-05 are enforced at the salary-facing outputs.
Output: fixture-driven workbook and PDF contract tests that prove shared dataset parity, locked summary order, compatibility flow, and forbidden-field exclusion.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/63-export-parity-re-lock/63-RESEARCH.md
@.planning/phases/63-export-parity-re-lock/63-VALIDATION.md
@.planning/phases/58-payroll-matrix-spreadsheet-export/58-03-SUMMARY.md
@.planning/phases/59-pdf-portal-parity/59-03-SUMMARY.md
@.planning/phases/59-pdf-portal-parity/59-PARITY-FIXTURES.md
@.planning/phases/60-rollout-payroll-acceptance/60-ACCEPTANCE-FIXTURES.md
@test/fixtures/report_export_parity_fixture.dart
@test/services/report_export_parity_test.dart
@lib/services/payroll_spreadsheet_export_service.dart
@lib/services/payroll_pdf_matrix_export_service.dart
@lib/services/payroll_matrix_semantics.dart
@lib/models/payroll_matrix_day_cell.dart
@lib/models/payroll_matrix_row.dart
@test/services/payroll_spreadsheet_export_service_test.dart
@test/services/payroll_pdf_matrix_export_service_test.dart

<locked_decisions>
D-01: Keep both export services on the existing `PayrollMatrixDataset` contract; Phase 63 must not add a second workbook-only or PDF-only semantics layer.
D-02: Spreadsheet output must preserve `Rekap Payroll`, compact cell text, and summary order `Terlambat`, `Kurang Jam`, `Break Lebih`, `Tidak Hadir`, `Lembur`.
D-03: Payroll PDF output must preserve `Mode kompatibilitas aktif`, preview-first testing, legend tags from `PayrollMatrixSemantics`, and the same forbidden-field boundary.
D-04: Both focused test commands must pre-clean `build/unit_test_assets` and `build/native_assets` before `flutter test`.
</locked_decisions>

<interfaces>
Locked spreadsheet service interface:
```dart
class PayrollSpreadsheetExportService {
  static const String sheetName = 'Rekap Payroll';
  static const List<String> forbiddenFields = <String>[
    'latitude',
    'longitude',
    'capture_mode',
    'queue_order',
    'requires_admin_review',
    'detail_note',
  ];

  Future<File> exportPayrollSpreadsheet({
    required String outletName,
    required DateTime startDate,
    required DateTime endDate,
    required PayrollMatrixDataset dataset,
  });
}
```

Locked PDF service interface:
```dart
class PayrollPdfMatrixExportService {
  static const String reportTitle = 'Rekap Payroll PDF';
  static const String compatibilityModeTitle = 'Mode kompatibilitas aktif';
  static const List<String> forbiddenFields = <String>[
    'latitude',
    'longitude',
    'capture_mode',
    'queue_order',
    'requires_admin_review',
    'detail_note',
  ];

  Future<File> buildPayrollPdf({
    required PayrollMatrixDataset dataset,
    required String outletName,
    required DateTime startDate,
    required DateTime endDate,
    required bool isCompatibilityMode,
  });

  PayrollPdfDocumentPreview buildPreview({
    required PayrollMatrixDataset dataset,
    required String outletName,
    required DateTime startDate,
    required DateTime endDate,
    required bool isCompatibilityMode,
  });
}
```

Shared fixture dependency from Plan 01:
```dart
ReportExportParityFixtureBundle buildReportExportParityFixtureBundle();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework the spreadsheet export test onto the shared merged-row parity bundle</name>
  <files>test/services/payroll_spreadsheet_export_service_test.dart</files>
  <read_first>
    - test/services/payroll_spreadsheet_export_service_test.dart
    - test/fixtures/report_export_parity_fixture.dart
    - test/services/report_export_parity_test.dart
    - lib/services/payroll_spreadsheet_export_service.dart
    - lib/services/payroll_matrix_semantics.dart
    - lib/services/admin_policy_recap_dataset_service.dart
    - lib/services/payroll_matrix_builder.dart
    - .planning/phases/63-export-parity-re-lock/63-VALIDATION.md
  </read_first>
  <action>
    Move workbook contract coverage to the shared parity fixture source.

    Required implementation:
    1. In `test/services/payroll_spreadsheet_export_service_test.dart`, replace the inline `PayrollMatrixDataset(...)` fixture with a dataset built from:
       - `buildReportExportParityFixtureBundle()`
       - `AdminPolicyRecapDatasetService.build(...)`
       - `buildPayrollMatrix(...)`
    2. Keep the workbook assertions on the locked spreadsheet contract:
       - sheet name `Rekap Payroll`
       - leading headers `Karyawan` and `Kontrak`
       - summary headers from `PayrollMatrixRow.summaryLabels`
       - compact multiline cell text from `PayrollMatrixDayCell.exportText`
       - parity-driven fill and text colors resolved from the parity-generated matrix cells or `PayrollMatrixSemantics`, not from a second hard-coded palette
       - forbidden-field exclusion via `PayrollSpreadsheetExportService.forbiddenFields`
    3. Assert that the workbook includes parity-driven cells for at least:
       - `Hadir tanpa jadwal`
       - `TLT`
       - `OT`
       - `ABS`
    4. Inspect `xl/styles.xml` plus the relevant worksheet cell style indices, or equivalent workbook style metadata, so the test proves the spreadsheet cells for `TLT`, `OT`, `ABS`, and `Hadir tanpa jadwal` preserve the expected `fillColorHex` and `textColorHex` coming from the parity-generated dataset.
    5. Keep the round-trip workbook reopen step with the `excel` package and the zipped XML scan.
    6. Do not modify `PayrollSpreadsheetExportService` production code unless the new fixture-driven test exposes a real contract bug.
  </action>
  <verify>
    <automated>powershell -Command "Remove-Item -LiteralPath build\unit_test_assets -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item -LiteralPath build\native_assets -Recurse -Force -ErrorAction SilentlyContinue; C:\flutter\bin\flutter.bat test test/services/payroll_spreadsheet_export_service_test.dart"</automated>
  </verify>
  <acceptance_criteria>
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `buildReportExportParityFixtureBundle`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `AdminPolicyRecapDatasetService`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `buildPayrollMatrix`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `styles.xml`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `fillColorHex`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `textColorHex`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `Rekap Payroll`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `Hadir tanpa jadwal`
    - `test/services/payroll_spreadsheet_export_service_test.dart` contains `PayrollSpreadsheetExportService.forbiddenFields`
    - `test/services/payroll_spreadsheet_export_service_test.dart` does not contain `PayrollMatrixDataset(`
  </acceptance_criteria>
  <done>The spreadsheet contract is now proven against the same merged recap fixture bundle that drives admin recap parity.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the payroll PDF export test onto the shared merged-row parity bundle</name>
  <files>test/services/payroll_pdf_matrix_export_service_test.dart</files>
  <read_first>
    - test/services/payroll_pdf_matrix_export_service_test.dart
    - test/fixtures/report_export_parity_fixture.dart
    - test/services/report_export_parity_test.dart
    - lib/services/payroll_pdf_matrix_export_service.dart
    - lib/services/payroll_matrix_semantics.dart
    - lib/services/admin_policy_recap_dataset_service.dart
    - lib/services/payroll_matrix_builder.dart
    - .planning/phases/63-export-parity-re-lock/63-VALIDATION.md
  </read_first>
  <action>
    Move the PDF preview/file contract to the shared parity fixture source.

    Required implementation:
    1. In `test/services/payroll_pdf_matrix_export_service_test.dart`, replace the inline `PayrollMatrixDataset(...)` fixture with a dataset built from:
       - `buildReportExportParityFixtureBundle()`
       - `AdminPolicyRecapDatasetService.build(...)`
       - `buildPayrollMatrix(...)`
    2. Keep the test centered on `PayrollPdfMatrixExportService.buildPreview(...)` plus one real file generation call.
    3. Assert these locked PDF contracts:
       - `preview.title == 'Rekap Payroll PDF'`
       - `preview.compatibilityTitle == 'Mode kompatibilitas aktif'` when fallback rows exist
       - `preview.legendTags` comes from `PayrollMatrixSemantics`
       - summary metric order matches `PayrollMatrixRow.summaryLabels`
       - serialized preview and final file exclude every field in `PayrollPdfMatrixExportService.forbiddenFields`
    4. Assert the preview rows carry parity-driven cells for at least:
       - `Hadir tanpa jadwal`
       - `TLT`
       - `OT`
       - `ABS`
    5. Do not add a PDF-only status mapping or a second compatibility flag path.
  </action>
  <verify>
    <automated>powershell -Command "Remove-Item -LiteralPath build\unit_test_assets -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item -LiteralPath build\native_assets -Recurse -Force -ErrorAction SilentlyContinue; C:\flutter\bin\flutter.bat test test/services/payroll_pdf_matrix_export_service_test.dart"</automated>
  </verify>
  <acceptance_criteria>
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `buildReportExportParityFixtureBundle`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `AdminPolicyRecapDatasetService`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `buildPayrollMatrix`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `Rekap Payroll PDF`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `Mode kompatibilitas aktif`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` contains `PayrollPdfMatrixExportService.forbiddenFields`
    - `test/services/payroll_pdf_matrix_export_service_test.dart` does not contain `PayrollMatrixDataset(`
  </acceptance_criteria>
  <done>The payroll PDF contract is now proven against the same merged recap fixture bundle and compatibility truth as the spreadsheet and admin recap paths.</done>
</task>

</tasks>

<verification>
- `powershell -Command "Remove-Item -LiteralPath build\unit_test_assets -Recurse -Force -ErrorAction SilentlyContinue; Remove-Item -LiteralPath build\native_assets -Recurse -Force -ErrorAction SilentlyContinue; C:\flutter\bin\flutter.bat test test/services/report_export_parity_test.dart test/services/payroll_spreadsheet_export_service_test.dart test/services/payroll_pdf_matrix_export_service_test.dart test/screens/admin/admin_reports_payroll_matrix_test.dart test/screens/admin/rekap_harian_test.dart"` passes.
- Both export service tests build their datasets from `buildReportExportParityFixtureBundle()`, not inline `PayrollMatrixDataset(...)` literals.
- Workbook assertions preserve the locked payroll color semantics, and PDF assertions preserve the forbidden-field boundary and locked payroll summary ordering.
</verification>

<success_criteria>
- Spreadsheet coverage now proves REPORT-04 against the shared merged recap parity bundle.
- PDF coverage now proves REPORT-05 against the same shared merged recap parity bundle.
- Admin recap, spreadsheet, and payroll PDF are locked to one fixture-driven reporting meaning.
</success_criteria>

<output>
After completion, create `.planning/phases/63-export-parity-re-lock/63-02-SUMMARY.md`
</output>
