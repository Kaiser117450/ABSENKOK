---
phase: 27-foundation-kiosk-heartbeat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/phase_27_heartbeat_columns_20260320.sql
  - lib/models/outlet.dart
  - pubspec.yaml
autonomous: true
requirements: [HLTH-02, SYNC-01]
must_haves:
  truths:
    - "Supabase outlets table has 5 new nullable columns for heartbeat data"
    - "Outlet model parses all heartbeat fields from JSON including null values"
    - "battery_plus ^6.2.3 and package_info_plus ^8.1.0 are in pubspec.yaml"
  artifacts:
    - path: "sql/phase_27_heartbeat_columns_20260320.sql"
      provides: "Additive migration for outlets heartbeat columns"
      contains: "ALTER TABLE outlets ADD COLUMN IF NOT EXISTS last_heartbeat_at"
    - path: "lib/models/outlet.dart"
      provides: "Outlet model with 5 heartbeat fields"
      contains: "lastHeartbeatAt"
    - path: "pubspec.yaml"
      provides: "New dependencies for battery and package info"
      contains: "battery_plus"
  key_links:
    - from: "lib/models/outlet.dart"
      to: "outlets table"
      via: "fromJson/toJson field mapping"
      pattern: "battery_level.*int\\?"
---

<objective>
Create the database migration, expand the Outlet model with heartbeat fields, and add battery_plus + package_info_plus dependencies.

Purpose: Foundation layer that Plan 02 (HeartbeatService) builds on.
Output: SQL migration file, updated Outlet model, updated pubspec.yaml.
</objective>

<execution_context>
@C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-foundation-kiosk-heartbeat/27-CONTEXT.md
@.planning/phases/27-foundation-kiosk-heartbeat/27-RESEARCH.md

<interfaces>
<!-- Current Outlet model (lib/models/outlet.dart) — 43 lines -->
class Outlet {
  final String id;
  final String name;
  final String? address;
  final double? lat;
  final double? lng;
  final String? deviceId;
  final bool isActive;
  final String createdAt;

  factory Outlet.fromJson(Map<String, dynamic> json) => Outlet(...);
  Map<String, dynamic> toJson() => {...};
}

<!-- SqliteService.countPendingLogs() at lib/services/sqlite_service.dart:158 -->
static Future<int> countPendingLogs() async {
  // counts pending_logs WHERE sync_status IN ('pending','failed')
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SQL migration + Outlet model + pubspec dependencies</name>
  <files>sql/phase_27_heartbeat_columns_20260320.sql, lib/models/outlet.dart, pubspec.yaml</files>
  <read_first>
    - lib/models/outlet.dart
    - pubspec.yaml
  </read_first>
  <action>
1. Create `sql/phase_27_heartbeat_columns_20260320.sql` with these 5 ALTER TABLE statements (all nullable, no DEFAULT):
```sql
ALTER TABLE outlets ADD COLUMN IF NOT EXISTS last_heartbeat_at  TIMESTAMPTZ;
ALTER TABLE outlets ADD COLUMN IF NOT EXISTS battery_level      SMALLINT;
ALTER TABLE outlets ADD COLUMN IF NOT EXISTS is_charging        BOOLEAN;
ALTER TABLE outlets ADD COLUMN IF NOT EXISTS pending_sync_count INTEGER;
ALTER TABLE outlets ADD COLUMN IF NOT EXISTS app_version        TEXT;
```

2. Expand `lib/models/outlet.dart` — add 5 new nullable fields to the Outlet class:
   - `final DateTime? lastHeartbeatAt;`
   - `final int? batteryLevel;`
   - `final bool? isCharging;`
   - `final int? pendingSyncCount;`
   - `final String? appVersion;`

   Add them to the constructor as optional named parameters.

   In `fromJson`:
   - `lastHeartbeatAt: json['last_heartbeat_at'] != null ? DateTime.parse(json['last_heartbeat_at'] as String) : null,`
   - `batteryLevel: json['battery_level'] as int?,`
   - `isCharging: json['is_charging'] as bool?,`
   - `pendingSyncCount: json['pending_sync_count'] as int?,`
   - `appVersion: json['app_version'] as String?,`

   In `toJson`:
   - `'last_heartbeat_at': lastHeartbeatAt?.toIso8601String(),`
   - `'battery_level': batteryLevel,`
   - `'is_charging': isCharging,`
   - `'pending_sync_count': pendingSyncCount,`
   - `'app_version': appVersion,`

3. Add to `pubspec.yaml` dependencies section:
   - `battery_plus: ^6.2.3`
   - `package_info_plus: ^8.1.0`
   Do NOT use ^7.x for battery_plus (requires Kotlin 2.2.0, breaks nfc_manager).

4. Run `flutter pub get` to resolve dependencies.
  </action>
  <verify>
    <automated>cd "C:\Users\HYPE R Series\Desktop\projekan\absensi apk\absensi_enakko_flutter" && C:\flutter\bin\flutter.bat pub get && C:\flutter\bin\flutter.bat analyze --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - sql/phase_27_heartbeat_columns_20260320.sql exists and contains exactly 5 ALTER TABLE statements
    - lib/models/outlet.dart contains `final DateTime? lastHeartbeatAt;`
    - lib/models/outlet.dart contains `final int? batteryLevel;`
    - lib/models/outlet.dart contains `final bool? isCharging;`
    - lib/models/outlet.dart contains `final int? pendingSyncCount;`
    - lib/models/outlet.dart contains `final String? appVersion;`
    - lib/models/outlet.dart fromJson contains `json['battery_level'] as int?`
    - lib/models/outlet.dart toJson contains `'app_version': appVersion`
    - pubspec.yaml contains `battery_plus: ^6.2.3`
    - pubspec.yaml contains `package_info_plus: ^8.1.0`
    - `flutter pub get` succeeds
    - `flutter analyze --no-fatal-infos` shows no errors
  </acceptance_criteria>
  <done>SQL migration file created with 5 nullable columns. Outlet model has all 5 heartbeat fields with correct fromJson/toJson. battery_plus ^6.2.3 and package_info_plus ^8.1.0 in pubspec. Project compiles cleanly.</done>
</task>

</tasks>

<verification>
- SQL file has exactly 5 `ADD COLUMN IF NOT EXISTS` statements
- Outlet model compiles with all existing code (no breaking changes — fields are optional)
- `flutter pub get` resolves without version conflicts
- `flutter analyze` shows no errors
</verification>

<success_criteria>
- Foundation layer complete: migration ready, model expanded, dependencies added
- No compile errors anywhere in project
</success_criteria>

<output>
After completion, create `.planning/phases/27-foundation-kiosk-heartbeat/27-01-SUMMARY.md`
</output>
