---
phase: 48.1-apk-first-release-artifact-alignment
plan: 02
type: execute
wave: 2
depends_on: [48.1-01]
files_modified:
  - tool/release_build.ps1
  - docs/android-release-contract.md
autonomous: true
requirements: [REL-03]
must_haves:
  truths:
    - "Smoke verification and retained symbols stay attached to the same APK-first release record instead of a special smoke-only side path."
    - "The documented `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 ...` commands work in Windows PowerShell 5.1 as written."
    - "Optional `.aab` generation, when requested, remains supplementary and does not weaken the default APK-first evidence bundle."
  artifacts:
    - path: "tool/release_build.ps1"
      provides: "PowerShell-host-compatible smoke and evidence lane aligned to the canonical retained APK."
      contains: "smoke-check.txt"
    - path: "docs/android-release-contract.md"
      provides: "Tracked evidence contract for symbols, mapping, smoke verification, and optional bundle follow-up."
      contains: "SmokeVerify"
  key_links:
    - from: "tool/release_build.ps1"
      to: "tool/release_preflight.ps1"
      via: "smoke and evidence flow must still sit behind the validated release preflight lane"
      pattern: "release_preflight"
    - from: "tool/release_build.ps1"
      to: "docs/android-release-contract.md"
      via: "the documented powershell commands and the helper's actual host compatibility must match"
      pattern: "SmokeVerify"
---

<objective>
Make the APK-first evidence lane reliable in the same shell runtime the repo tells operators to use.

Purpose: Phase 48.1 is not complete if the helper flips to APK-first but the documented `powershell.exe` smoke lane still breaks or keeps old AAB-first evidence semantics.
Output: A Windows-PowerShell-compatible smoke/evidence lane whose symbols, mapping, and `smoke-check.txt` all stay tied to the canonical retained APK record.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-RESEARCH.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-VALIDATION.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-01-PLAN.md
@tool/release_build.ps1
@tool/release_env.ps1
@tool/release_preflight.ps1
@docs/android-release-contract.md

<interfaces>
Measured host mismatch on 2026-03-23:
```text
Get-Command powershell -> C:\WINDOWS\System32\WindowsPowerShell\v1.0\powershell.exe
powershell -NoLogo -NoProfile -Command '$PSVersionTable.PSVersion.ToString()' -> 5.1.26100.7920
pwsh -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify -> passes
powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify -> MethodNotFound on [System.IO.Path]::GetRelativePath
```

Evidence surfaces that must survive the artifact flip:
```text
symbols/
mapping.txt
smoke-check.txt
release-manifest.json
```
</interfaces>

<constraints>
- keep smoke verification tied to the canonical retained APK, not to a temporary or undistributed side path
- prefer making the helper compatible with the documented `powershell.exe` lane over silently switching the whole repo to `pwsh`
- keep `--split-debug-info` enabled and `--obfuscate` disabled for v7.0
- do not widen this plan into CI/CD, Play Console, or full runbook work
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the smoke and manifest path handling work in the documented powershell host</name>
  <files>tool/release_build.ps1</files>
  <read_first>
    - tool/release_build.ps1
    - .planning/phases/48.1-apk-first-release-artifact-alignment/48.1-RESEARCH.md
    - .planning/phases/48.1-apk-first-release-artifact-alignment/48.1-01-PLAN.md
  </read_first>
  <action>
    Remove the current shell-runtime mismatch in the tracked helper.

    Required behavior:
    1. Replace or wrap any PowerShell-7-only relative-path logic so `powershell.exe` 5.1 can run `-CheckOnly` and `-CheckOnly -SmokeVerify` successfully.
    2. Keep release-manifest path serialization stable enough for later Phase 49 runbook and acceptance evidence.
    3. Verify that the documented `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify` command succeeds after the change.
    4. Do not require operators to infer that `pwsh` is mandatory unless the repo docs are intentionally rewritten everywhere.

    Scope guard:
    - do not convert the whole release toolchain to a `pwsh`-only contract
    - do not remove check-only behavior just to hide the incompatibility
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify</automated>
  </verify>
  <done>The documented powershell smoke lane now works as written instead of failing on PowerShell-7-only path APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor symbols, mapping, and smoke evidence around the canonical APK record</name>
  <files>tool/release_build.ps1, docs/android-release-contract.md</files>
  <read_first>
    - tool/release_build.ps1
    - docs/android-release-contract.md
    - .planning/phases/48.1-apk-first-release-artifact-alignment/48.1-RESEARCH.md
  </read_first>
  <action>
    Finalize the APK-first evidence contract now that the helper and shell runtime agree.

    Required behavior:
    1. Smoke verification installs and launches the retained canonical APK path instead of relying on a `smoke-only` APK state.
    2. `release-manifest.json` and the tracked doc remove stale `smoke-only` semantics that only existed to support the Phase 48 AAB-first policy.
    3. The evidence bundle stays `apk + symbols/ + optional mapping.txt + smoke-check.txt`, with optional `.aab` called out as supplementary only when explicitly requested.
    4. The tracked contract still states that symbol retention and smoke evidence are both required before distribution.

    Scope guard:
    - do not enable obfuscation
    - do not let optional bundle generation quietly become the default distributed artifact again
    - do not add Phase 49 runbook detail or real release acceptance evidence yet
  </action>
  <verify>
    <automated>powershell -Command "$content = Get-Content 'tool/release_build.ps1' -Raw; if ($content -match 'smoke-only') { throw 'stale smoke-only APK state remains' }; $matches = Select-String -Path 'tool/release_build.ps1','docs/android-release-contract.md' -Pattern 'smoke-check','mapping.txt','symbols','bundleRetentionState'; if (-not $matches) { throw 'APK-first evidence markers missing' }"</automated>
  </verify>
  <done>The APK-first release record now keeps its debugging artifacts and smoke evidence on the canonical path without stale AAB-first manifest semantics.</done>
</task>

</tasks>

<verification>
- `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify` succeeds in the documented shell runtime
- the retained release directory and manifest now center on the canonical APK plus symbols, mapping, and smoke evidence
- optional `.aab` output remains explicit and supplementary instead of reintroducing contradictory artifact semantics
</verification>

<success_criteria>
- REL-03 is satisfied in the same shell/runtime and evidence bundle the repo asks operators to use
- the release lane can match a shipped APK back to its symbols, mapping, and smoke evidence without a side-path workaround
- Phase 49 can document one truthful APK-first operator flow
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-02-SUMMARY.md`
</output>
