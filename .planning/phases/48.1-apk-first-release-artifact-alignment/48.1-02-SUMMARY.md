---
phase: 48.1-apk-first-release-artifact-alignment
plan: 02
subsystem: infra
tags: [android, flutter, release, smoke-verification, symbols, powershell]
requires:
  - phase: 48.1-01
    provides: apk-first canonical artifact policy and matching operator contract
provides:
  - documented Windows PowerShell 5.1 smoke lane that stays aligned to the canonical APK
  - helper and doc wording that tie smoke-check evidence to canonicalArtifact instead of a side path
  - cleaned helper response surface without stale IncludeSideLoadApk naming
affects: [49-01, 49-02, android-release-lane]
tech-stack:
  added: []
  patterns:
    - smoke verification always installs the same staged APK recorded as canonicalArtifact
    - tracked release commands must stay truthful in powershell.exe 5.1, not only in pwsh
key-files:
  created: []
  modified:
    - tool/release_build.ps1
    - docs/android-release-contract.md
key-decisions:
  - "The documented release contract stays on powershell.exe 5.1 compatibility instead of silently switching the repo to pwsh-only instructions."
  - "Smoke evidence belongs to the canonical APK record; there is no smoke-only artifact state in the APK-first contract."
patterns-established:
  - "Smoke evidence contract: canonicalArtifact + symbols/ + optional mapping.txt + smoke-check.txt live under one staged release directory"
  - "Helper response vocabulary: MappingPath and BundleRetentionState mirror the tracked manifest language"
requirements-completed: [REL-03]
duration: 8 min
completed: 2026-03-23
---

# Phase 48.1 Plan 02: APK-First Evidence Summary

**The documented Windows PowerShell smoke lane now resolves cleanly against the same canonical APK record that retains symbols, mapping output, and smoke evidence for v7.0 releases**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-23T23:10:00+08:00
- **Completed:** 2026-03-23T23:16:00+08:00
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Confirmed `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify` succeeds in the documented Windows PowerShell 5.1 host.
- Removed the stale helper aliasing around `IncludeSideLoadApk` and aligned the check-only response naming with the APK-first manifest contract.
- Updated helper and doc wording so smoke verification explicitly installs the canonical retained APK and records evidence next to symbols, mapping, and optional bundle output.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make the smoke and manifest path handling work in the documented powershell host** - `fb67869` (fix)
2. **Task 2: Re-anchor symbols, mapping, and smoke evidence around the canonical APK record** - `c0cd565` (docs)

## Files Created/Modified
- `tool/release_build.ps1` - removes stale flag naming, keeps the PowerShell 5.1-safe smoke path contract, and records smoke evidence as part of the canonical APK release notes.
- `docs/android-release-contract.md` - states that `powershell.exe` 5.1 is the supported command surface and that `smoke-check.txt` comes from installing the same canonical APK named in the manifest.

## Decisions Made
- Keep the tracked release instructions on `powershell.exe`; the fix belongs in the helper rather than forcing Phase 49 to rewrite the runbook around `pwsh`.
- Treat smoke verification as evidence on the canonical APK record, not as justification for a separate retained artifact state.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The main APK-first helper refactor already touched the same path and manifest surfaces that were failing in PowerShell 5.1, so wave 2 focused on tightening the remaining live contract language and helper response names after the validation commands passed.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 49 can document one truthful operator flow: `powershell.exe` commands, canonical APK staging, optional bundle retention, and smoke evidence on the same release record.
- The acceptance phase no longer needs to explain or defend a `smoke-only` APK workaround.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-02-SUMMARY.md`.
- Task commit `fb67869` is present in git history.
- Task commit `c0cd565` is present in git history.

---
*Phase: 48.1-apk-first-release-artifact-alignment*
*Completed: 2026-03-23*
