---
phase: 48.1-apk-first-release-artifact-alignment
plan: 01
subsystem: infra
tags: [android, flutter, release, apk, artifacts, powershell]
requires:
  - phase: 48-02
    provides: staged release directory, release-manifest.json contract, and tracked release helper entrypoint
  - phase: 48-03
    provides: symbol retention and smoke-verification scaffolding that had to be re-anchored to the canonical artifact
provides:
  - apk-first release_build.ps1 packaging lane with explicit bundle opt-in
  - tracked operator contract aligned to a canonical signed APK
  - bundleRetentionState manifest vocabulary replacing the old apkRetentionState contract
affects: [48.1-02, 49-01, 49-02, android-release-lane]
tech-stack:
  added: []
  patterns:
    - canonical Android release artifact is always the staged signed APK
    - optional app bundle output is explicit via -IncludeAppBundle instead of being implied by the default lane
key-files:
  created: []
  modified:
    - tool/release_build.ps1
    - docs/android-release-contract.md
key-decisions:
  - "The signed optimized APK is the canonical retained Android release artifact for v7.0."
  - "Optional .aab generation stays available only behind -IncludeAppBundle so the default lane no longer drifts back to AAB-first."
patterns-established:
  - "APK-first release flow: release_env -> release_preflight -> flutter build apk --release --split-debug-info -> optional flutter build appbundle -> stage release-manifest.json"
  - "Manifest contract: canonicalArtifact points at the staged APK and bundleRetentionState tracks whether a supplementary bundle was requested"
requirements-completed: [REL-02]
duration: 14 min
completed: 2026-03-23
---

# Phase 48.1 Plan 01: APK-First Contract Summary

**The tracked Android release helper and contract doc now keep the signed optimized APK as the canonical retained artifact, with optional bundle output only when an operator explicitly asks for it**

## Performance

- **Duration:** 14 min
- **Started:** 2026-03-23T23:02:00+08:00
- **Completed:** 2026-03-23T23:10:00+08:00
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Flipped `tool/release_build.ps1` from AAB-first retention to APK-first retention while preserving release preflight, staging, symbols, mapping capture, and smoke-evidence surfaces.
- Replaced `apkRetentionState` semantics with `bundleRetentionState` and made the canonical artifact path point at the staged signed APK.
- Updated `docs/android-release-contract.md` so the tracked operator contract now matches the live APK-first helper surface and `-IncludeAppBundle` opt-in.

## Task Commits

Each task was committed atomically:

1. **Task 1: Flip the tracked packaging helper to an APK-first contract with optional bundle output** - `3326eda` (feat)
2. **Task 2: Update the tracked release contract doc to match the APK-first helper** - `4ad0bb4` (docs)

## Files Created/Modified
- `tool/release_build.ps1` - makes the staged signed APK canonical, stages a bundle only when `-IncludeAppBundle` is used, and serializes the APK-first manifest contract.
- `docs/android-release-contract.md` - documents the APK-first release policy, the updated helper switch, and the manifest vocabulary that Phase 49 should inherit.

## Decisions Made
- The v7.0 shipped product remains an optimized signed APK, so the tracked release lane must retain that APK by default.
- Optional `.aab` output is supplementary only and should never be implied by the default packaging lane.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The release helper still had PowerShell-7-only relative-path logic from Phase 48; wave 2 tightened the same helper surface so the documented `powershell.exe` smoke lane remained truthful after the APK-first flip.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 48.1-02 can focus on smoke evidence and Windows PowerShell host truthfulness without reopening the artifact policy itself.
- Phase 49 can now inherit a single APK-first contract instead of contradictory APK-vs-AAB guidance.

## Self-Check: PASSED

- Summary file exists at `.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-01-SUMMARY.md`.
- Task commit `3326eda` is present in git history.
- Task commit `4ad0bb4` is present in git history.

---
*Phase: 48.1-apk-first-release-artifact-alignment*
*Completed: 2026-03-23*
