---
phase: 48.1-apk-first-release-artifact-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tool/release_build.ps1
  - docs/android-release-contract.md
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "The tracked release lane retains a signed optimized `.apk` as the canonical Android release artifact by default."
    - "Optional `.aab` output is no longer built or retained implicitly; it only exists when the operator explicitly requests the bundle path."
    - "The staged release manifest and tracked contract doc describe the same APK-first artifact policy instead of the Phase 48 AAB-first policy."
  artifacts:
    - path: "tool/release_build.ps1"
      provides: "Release helper that stages a canonical signed APK first and exposes explicit opt-in bundle behavior."
      contains: "build apk"
    - path: "docs/android-release-contract.md"
      provides: "Operator-facing APK-first release contract for v7.0."
      contains: "signed `.apk`"
  key_links:
    - from: "tool/release_build.ps1"
      to: "tool/release_preflight.ps1"
      via: "APK-first packaging must still stop immediately when the validated preflight lane is red"
      pattern: "release_preflight"
    - from: "tool/release_build.ps1"
      to: "docs/android-release-contract.md"
      via: "the helper interface and the tracked contract must agree that APK is canonical and the bundle path is opt-in"
      pattern: "IncludeAppBundle"
---

<objective>
Re-center the tracked Android artifact contract on the actual v7.0 shipped product: a signed optimized APK.

Purpose: Phase 48 secured signing and evidence capture, but it locked the retained artifact contract to an AAB-first policy that now conflicts with the roadmap and requirements.
Output: An APK-first packaging helper plus a matching operator contract doc, with optional bundle output only when explicitly requested.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-RESEARCH.md
@.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-VALIDATION.md
@.planning/phases/48-secure-signing-artifact-discipline/48-02-SUMMARY.md
@.planning/phases/48-secure-signing-artifact-discipline/48-03-SUMMARY.md
@tool/release_build.ps1
@tool/release_env.ps1
@tool/release_preflight.ps1
@docs/android-release-contract.md
@pubspec.yaml

<interfaces>
Measured current contract on 2026-03-23:
```text
powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly
  Canonical .aab  : ...\ABSENKOK-v7.0.0+8013.aab
  APK retention   : omitted
```

Current tracked doc language:
```text
Signed `.aab` adalah artifact Android release yang canonical dan selalu dipertahankan.
Signed release `.apk` hanya dipertahankan bila side-loading masih dibutuhkan secara sengaja.
```
</interfaces>

<constraints>
- do not reopen private signing or toolchain-pin decisions from phases 47-48
- keep the versioned release directory, release-manifest generation, symbol retention, and smoke-verification surfaces introduced in Phase 48
- keep the default operator lane APK-first; `.aab` must stay optional, not implicit
- do not turn this plan into a full operator runbook or Play Store / CI/CD work
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the tracked packaging helper to an APK-first contract with optional bundle output</name>
  <files>tool/release_build.ps1</files>
  <read_first>
    - tool/release_build.ps1
    - tool/release_env.ps1
    - tool/release_preflight.ps1
    - .planning/phases/48.1-apk-first-release-artifact-alignment/48.1-RESEARCH.md
  </read_first>
  <action>
    Update `tool/release_build.ps1` so the canonical retained artifact matches the current milestone decision.

    Required behavior:
    1. Real builds always produce and stage the signed release `.apk` as the canonical retained Android artifact.
    2. Optional `.aab` generation/retention is moved behind an explicit bundle-oriented switch such as `-IncludeAppBundle`.
    3. `-CheckOnly` output describes the canonical APK path plus bundle retention state, not canonical AAB plus APK retention state.
    4. `release-manifest.json` records the staged APK as `canonicalArtifact` and replaces `apkRetentionState` / `smoke-only` semantics with explicit bundle-retention semantics.
    5. Preflight handoff, versioned release directory naming, `--split-debug-info`, mapping capture hooks, and smoke-verification switches remain intact.

    Scope guard:
    - do not remove `release_env.ps1` or `release_preflight.ps1`
    - do not make the optional bundle path run by default
    - do not widen the change into release-note publishing, CI/CD, or Play Store automation
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly</automated>
  </verify>
  <done>The tracked helper now reports and stages an APK-first canonical artifact lane, with optional bundle output only when explicitly requested.</done>
</task>

<task type="auto">
  <name>Task 2: Update the tracked release contract doc to match the APK-first helper</name>
  <files>docs/android-release-contract.md</files>
  <read_first>
    - docs/android-release-contract.md
    - tool/release_build.ps1
    - .planning/phases/48.1-apk-first-release-artifact-alignment/48.1-RESEARCH.md
  </read_first>
  <action>
    Align the operator-facing doc with the corrected helper contract.

    Required behavior:
    1. State that the signed optimized `.apk` is the canonical retained and distributed Android artifact for v7.0.
    2. State that `.aab` output exists only when a later distribution path explicitly asks for it, via the helper's bundle-oriented opt-in switch.
    3. Document the updated manifest vocabulary and staged release-directory contents for the APK-first lane.
    4. Remove stale AAB-first guidance such as `-IncludeSideLoadApk`, canonical `.aab`, or APK-as-exception language from the live operator doc.

    Scope guard:
    - keep the doc at contract level; detailed step-by-step runbook content belongs to Phase 49
    - do not rewrite completed Phase 48 summary files just to hide the historical drift
  </action>
  <verify>
    <automated>powershell -Command "$matches = Select-String -Path 'tool/release_build.ps1','docs/android-release-contract.md' -Pattern 'IncludeAppBundle','bundleRetentionState','canonical','\\.apk'; if (-not $matches) { throw 'APK-first contract markers missing' }"</automated>
  </verify>
  <done>The tracked operator contract now matches the APK-first packaging helper instead of the obsolete AAB-first policy.</done>
</task>

</tasks>

<verification>
- `tool/release_build.ps1 -CheckOnly` reports a canonical staged APK and optional bundle retention instead of a canonical AAB and omitted APK
- the manifest vocabulary and operator doc both describe the same APK-first contract
- Phase 49 can inherit one artifact policy without contradictory live operator guidance
</verification>

<success_criteria>
- REL-02 is satisfied with one tracked, validated APK-first packaging entrypoint
- the repo no longer treats the final release APK as an exceptional side path
- optional `.aab` output is preserved only as an explicit follow-up distribution path
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-apk-first-release-artifact-alignment/48.1-01-SUMMARY.md`
</output>
