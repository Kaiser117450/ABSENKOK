---
phase: 48.1
slug: apk-first-release-artifact-alignment
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-23
---

# Phase 48.1 - Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | PowerShell release helpers plus Flutter CLI metadata inspection and adb smoke-lane discovery |
| **Config file** | `tool/release_build.ps1`, `docs/android-release-contract.md`, `tool/release_env.ps1`, `tool/release_preflight.ps1` |
| **Quick run command** | `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly` |
| **Full suite command** | `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -IncludeAppBundle -SmokeVerify` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** run the narrowest matching helper/doc command for the touched files
- **After every plan wave:** run `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly`
- **Before final phase verification:** run `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -IncludeAppBundle -SmokeVerify`
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 48.1-01-01 | 01 | 1 | REL-02 | APK-first lane contract | `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly` | Planned | pending |
| 48.1-01-02 | 01 | 1 | REL-02 | helper/doc contract grep | `powershell -Command "$matches = Select-String -Path 'tool/release_build.ps1','docs/android-release-contract.md' -Pattern 'IncludeAppBundle','bundleRetentionState','canonical','\\.apk'; if (-not $matches) { throw 'APK-first contract markers missing' }"` | Planned | pending |
| 48.1-02-01 | 02 | 2 | REL-03 | documented shell compatibility | `powershell -ExecutionPolicy Bypass -File tool/release_build.ps1 -CheckOnly -SmokeVerify` | Planned | pending |
| 48.1-02-02 | 02 | 2 | REL-03 | APK-first evidence bundle grep | `powershell -Command "$content = Get-Content 'tool/release_build.ps1' -Raw; if ($content -match 'smoke-only') { throw 'stale smoke-only APK state remains' }; $matches = Select-String -Path 'tool/release_build.ps1','docs/android-release-contract.md' -Pattern 'smoke-check','mapping.txt','symbols','bundleRetentionState'; if (-not $matches) { throw 'APK-first evidence markers missing' }"` | Planned | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements. No new test framework is required before execution; the main missing behavior is correcting the tracked release helper and contract so the documented `powershell.exe` lane matches the APK-first milestone policy.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| The private upload keystore and real `android/key.properties` stay outside tracked source control during a real build | REL-02, REL-03 | Secret handling cannot be fully proven from tracked code alone | Confirm the real signing inputs are local-only and not staged in git before cutting a release. |
| The canonical signed APK installs and reaches the first usable screen on Android hardware | REL-03 | Real smoke verification needs an attached device or emulator plus private signing inputs | Run the non-check-only smoke lane against a real Android target and retain the generated `smoke-check.txt`. |
| If a `.aab` is retained, it is because a later distribution path explicitly asked for it | REL-02 | Distribution intent is a human release decision, not just syntax | Inspect the run command and release notes to confirm the optional bundle path was invoked deliberately. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or existing helper coverage
- [ ] Sampling continuity: no 3 consecutive implementation tasks without automated verify
- [ ] Wave 0 covers all missing references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30 seconds
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
