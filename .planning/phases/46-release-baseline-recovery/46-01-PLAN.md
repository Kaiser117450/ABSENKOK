---
phase: 46-release-baseline-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/local.properties
  - android/settings.gradle.kts
autonomous: true
requirements: [BUILD-01]
must_haves:
  truths:
    - "The canonical Android release command runs from this checkout without the current native-assets path truncation failure."
    - "The release pipeline reaches Android packaging with the pinned v7.0 baseline toolchain instead of failing inside the `objective_c` hook compile step."
    - "Phase 46 restores a repeatable release baseline without upgrading Kotlin, AGP, Gradle, or changing release signing behavior."
  artifacts:
    - path: "android/local.properties"
      provides: "Resolved Flutter SDK and Java runtime path used by the release build."
      contains: "flutter.sdk="
    - path: "android/settings.gradle.kts"
      provides: "Pinned Flutter plugin loader and Android/Kotlin plugin versions that must remain unchanged during baseline recovery."
      contains: "8.11.1"
    - path: "build/app/outputs/apk/release/ABSENKOK-v6.2.0.apk"
      provides: "Successful release packaging artifact from the restored pre-version-bump baseline."
      contains: "ABSENKOK-v6.2.0.apk"
  key_links:
    - from: "android/local.properties"
      to: "android/settings.gradle.kts"
      via: "the Flutter plugin loader resolves the SDK path from local.properties during the Android release build"
      pattern: "flutter.sdk"
    - from: "android/local.properties"
      to: "build/app/outputs/apk/release/ABSENKOK-v6.2.0.apk"
      via: "the space-safe Flutter SDK path and Android Studio JBR baseline enable the release command to reach packaging"
      pattern: "flutter.sdk"
---

<objective>
Restore one repeatable Android release build path for the current checkout before any version bump happens.

Purpose: Phase 46 cannot realign v7.0 metadata until the release pipeline can package an APK again with the pinned milestone toolchain.
Output: A space-safe release baseline plus a successful current-version release APK at the standard Android output path.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/RETROSPECTIVE.md
@.planning/phases/46-release-baseline-recovery/46-RESEARCH.md
@.planning/phases/46-release-baseline-recovery/46-VALIDATION.md
@.planning/phases/22-production-release/22-01-SUMMARY.md
@pubspec.yaml
@pubspec.lock
@android/local.properties
@android/settings.gradle.kts
@android/app/build.gradle.kts

<interfaces>
Current release-sensitive paths and pins:
```properties
flutter.sdk=C:\\Users\\HYPE R Series\\Desktop\\projekan\\absensi apk\\flutter
java.home=C:\\Program Files\\Android\\Android Studio\\jbr
flutter.versionName=6.2.0
flutter.versionCode=8012
```

```kotlin
id("com.android.application") version "8.11.1" apply false
id("org.jetbrains.kotlin.android") version "1.9.25" apply false
```

Known good prior pattern from Phase 22:
```powershell
C:\flutter\bin\flutter.bat build apk --release
```
</interfaces>

<constraints>
- keep Flutter 3.41.1, AGP 8.11.1, Gradle 8.14, Kotlin 1.9.25, and Java 21 fixed in this plan
- do not change signing config, version numbers, or release publication flow in this plan
- solve the native-assets/path failure with the smallest repeatable baseline change
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore a space-safe Flutter SDK baseline for release builds</name>
  <files>android/local.properties, android/settings.gradle.kts</files>
  <read_first>
    - android/local.properties
    - android/settings.gradle.kts
    - .planning/phases/46-release-baseline-recovery/46-RESEARCH.md
    - .planning/phases/22-production-release/22-01-SUMMARY.md
  </read_first>
  <action>
    Replace the current space-sensitive Flutter SDK resolution with one canonical, repeatable baseline that the release build can execute without truncating command paths on Windows.

    Required behavior:
    1. Prefer a stable, space-safe SDK alias at `C:\flutter`:
       - if `C:\flutter\bin\flutter.bat` already exists, use it
       - if it does not exist, create a junction or equivalent CLI-managed alias that points to the current Flutter SDK
    2. Update `android/local.properties` so `flutter.sdk` resolves to `C:\flutter`.
    3. Keep `java.home` on Android Studio JBR / Java 21.
    4. Only touch `android/settings.gradle.kts` if the current loader logic needs a narrow supporting adjustment for the new canonical SDK path. Do not change AGP or Kotlin versions.

    Scope guard:
    - do not change dependency versions
    - do not bump `pubspec.yaml` version yet
    - do not add a new build script unless the baseline cannot be made repeatable through the existing config path
  </action>
  <verify>
    <automated>powershell -Command "if (-not (Test-Path 'C:\flutter\bin\flutter.bat')) { throw 'missing C:\\flutter alias' }; $matches = Select-String -Path 'android/local.properties' -Pattern '^flutter.sdk=C:\\\\flutter$','^java.home='; if ($matches.Count -lt 2) { throw 'local.properties baseline not updated' }"</automated>
  </verify>
  <done>The repo resolves Flutter and Java through a stable baseline that no longer points release builds at the current space-sensitive SDK path.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the current release artifact with the restored baseline</name>
  <files>android/local.properties</files>
  <read_first>
    - android/local.properties
    - android/app/build.gradle.kts
    - .planning/phases/46-release-baseline-recovery/46-VALIDATION.md
  </read_first>
  <action>
    Use the restored baseline to rerun the real Android release build and confirm it now reaches packaging for the current version before any metadata changes are introduced.

    Required behavior:
    1. Run the canonical release command from repo root using the space-safe SDK baseline:
       - `C:\flutter\bin\flutter.bat build apk --release`
    2. If the build still fails on the same path/native-assets class of error, keep iterating on the baseline-path fix only until that failure is removed.
    3. Do not upgrade Kotlin, AGP, Gradle, or release signing in response to this task.
    4. Stop once the build produces the current-version APK packaging artifact.

    Scope guard:
    - do not modify `pubspec.yaml`
    - do not accept a workaround that bypasses the real release build
    - do not chase unrelated analyzer or feature debt unless it directly blocks this release command after the path fix
  </action>
  <verify>
    <automated>powershell -Command "if (-not (Test-Path 'build/app/outputs/apk/release/ABSENKOK-v6.2.0.apk')) { throw 'release APK missing' }"</automated>
  </verify>
  <done>The canonical release build reaches Android packaging and emits `build/app/outputs/apk/release/ABSENKOK-v6.2.0.apk` without the native-assets path truncation failure.</done>
</task>

</tasks>

<verification>
- the release build no longer dies in `objective_c` hook compilation with `'C:\Users\HYPE' is not recognized`
- the current-version APK packages successfully with the pinned milestone toolchain
- baseline recovery is achieved without mixing in signing or versioning changes
</verification>

<success_criteria>
- BUILD-01 is satisfied for the current checkout before any version bump occurs
- `android/local.properties` points the release build at a repeatable, space-safe Flutter SDK path
- `C:\flutter\bin\flutter.bat build apk --release` produces the current-version APK in `build/app/outputs/apk/release/`
</success_criteria>

<output>
After completion, create `.planning/phases/46-release-baseline-recovery/46-01-SUMMARY.md`
</output>
