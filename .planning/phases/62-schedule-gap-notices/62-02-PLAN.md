---
phase: 62-schedule-gap-notices
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/screens/admin/admin_dashboard_screen.dart
  - lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart
  - test/screens/admin/admin_dashboard_schedule_gap_test.dart
autonomous: true
requirements: ["SCHED-05"]
must_haves:
  truths:
    - "Kepala gerai sees a lightweight outlet-scoped schedule-gap notice surface directly on the dashboard."
    - "The dashboard notice remains operational only and does not alter recap, pending, or export semantics."
    - "The notice surface reuses the existing scheduler route as the resolution path."
  artifacts:
    - path: "lib/screens/admin/admin_dashboard_screen.dart"
      provides: "Dashboard state wiring for schedule-gap notice counts and quick-action visibility"
      contains: "_loadScheduleGapNotices"
    - path: "lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart"
      provides: "Presentational notice sheet with typed rows and scheduler CTA"
      contains: "AdminScheduleGapNoticeSheet"
    - path: "test/screens/admin/admin_dashboard_schedule_gap_test.dart"
      provides: "Widget regression coverage for dashboard notice visibility, outlet scope, and CTA copy"
      contains: "Jadwal Kosong"
  key_links:
    - from: "lib/screens/admin/admin_dashboard_screen.dart"
      to: "lib/services/schedule_gap_notice_service.dart"
      via: "dashboard notice counts must be sourced from the canonical notice service"
      pattern: "ScheduleGapNoticeService"
    - from: "lib/screens/admin/admin_dashboard_screen.dart"
      to: "lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart"
      via: "the dashboard opens a dedicated lightweight detail sheet instead of embedding large inline UI"
      pattern: "AdminScheduleGapNoticeSheet"
    - from: "lib/screens/admin/admin_dashboard_screen.dart"
      to: "lib/screens/admin/shift_scheduler_screen.dart"
      via: "notice follow-up resolves through the existing scheduler route"
      pattern: "_openShiftScheduler"
---

<objective>
Wire the schedule-gap notice read model into the kepala gerai dashboard as a lightweight, non-blocking follow-up surface.

Purpose: the data contract from Plan 01 should become a small operational affordance on `AdminDashboardScreen`, not another recap-side enforcement path. The dashboard already has outlet-scoped quick actions and a bottom-sheet follow-up pattern; this plan plugs schedule-gap notices into that existing operator flow.
Output: dashboard notice state, one presentational sheet, and focused widget coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-schedule-gap-notices/62-RESEARCH.md
@.planning/phases/62-schedule-gap-notices/62-VALIDATION.md
@.planning/phases/62-schedule-gap-notices/62-01-PLAN.md
@lib/screens/admin/admin_dashboard_screen.dart
@lib/screens/admin/shift_scheduler_screen.dart
@lib/services/schedule_gap_notice_service.dart
@lib/services/attendance_policy_recap_service.dart
@lib/services/admin_policy_recap_dataset_service.dart
@lib/providers/app_provider.dart
@test/services/schedule_gap_notice_service_test.dart

<locked_decisions>
D-04: Dashboard notices are only visible when one outlet is resolved: kepala gerai uses `managedOutletId`; full admin requires a selected outlet.
D-05: The quick action label is `Jadwal\nKosong`, and the detail sheet title is `Jadwal Kosong`.
D-06: The notice surface must never change recap filters, pending counts, export buttons, or payroll compatibility banners.
</locked_decisions>

<interfaces>
The dashboard state added in this plan must include:
```dart
final ScheduleGapNoticeService _scheduleGapNoticeService =
    const ScheduleGapNoticeService();

ScheduleGapNoticeResult _scheduleGapNotices = const ScheduleGapNoticeResult(
  entries: <ScheduleGapNoticeEntry>[],
);
bool _loadingScheduleGapNotices = false;
```

Locked dashboard copy:
```text
quick action label: Jadwal\nKosong
sheet title: Jadwal Kosong
sheet CTA: Buka Jadwal
row helper: Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.
```

Locked visibility rules:
```text
- kepala gerai: compute notices only for managedOutletId
- full admin: compute notices only when _selectedOutletId is not null
- quick action visible only when _scheduleGapNotices.hasNotices is true
- follow-up action opens the existing scheduler route for the active outlet
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add outlet-scoped schedule-gap notice state and quick-action wiring to the dashboard</name>
  <files>lib/screens/admin/admin_dashboard_screen.dart, lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart</files>
  <read_first>
    - lib/screens/admin/admin_dashboard_screen.dart
    - lib/services/schedule_gap_notice_service.dart
    - lib/providers/app_provider.dart
    - lib/screens/admin/shift_scheduler_screen.dart
    - .planning/phases/62-schedule-gap-notices/62-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: kepala gerai with notice rows sees a `Jadwal Kosong` quick action and count badge.
    - Test 2: full admin without a selected outlet does not see the notice affordance.
    - Test 3: opening the notice affordance shows a lightweight sheet with employee/date rows and the locked CTA.
    - Test 4: existing `Belum Pulang` quick action remains untouched.
  </behavior>
  <action>
    Wire the new notice domain into the dashboard per D-04 through D-06.

    Required behavior:
    1. Update `lib/screens/admin/admin_dashboard_screen.dart` to store:
       - `ScheduleGapNoticeService`
       - `ScheduleGapNoticeResult`
       - loading state for notice refreshes
    2. Add `_loadScheduleGapNotices()` that:
       - resolves the active outlet from `managedOutletId` or `_selectedOutletId`
       - skips notice loading when no outlet is resolved
       - fetches the same minimum inputs the notice service needs for a fixed recent window
       - calls `ScheduleGapNoticeService.build(...)`
       - stores the result without mutating `_openShifts`, recap state, or export state
    3. Call `_loadScheduleGapNotices()` from the dashboard refresh flow together with `_loadLogs()` and `_loadOpenShifts()`.
    4. Add a new quick action that is only visible when `_scheduleGapNotices.hasNotices` is true:
       - icon: `Icons.event_note_rounded`
       - label: `Jadwal\nKosong`
       - on tap: open a dedicated schedule-gap notice sheet
    5. Create `lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart` with a presentational widget that renders:
       - title `Jadwal Kosong`
       - count badge from `ScheduleGapNoticeResult.count`
       - one row per `ScheduleGapNoticeEntry`
       - helper text from the entry
       - CTA button text `Buka Jadwal`
    6. The CTA must reuse the existing scheduler navigation path through `_openShiftScheduler()`.
    7. Keep the notice tone informational and lightweight. Do not add blocking dialogs, required acknowledgements, or recap/status mutations.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_dashboard_schedule_gap_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/admin_dashboard_screen.dart` contains `_loadScheduleGapNotices`
    - `lib/screens/admin/admin_dashboard_screen.dart` contains `ScheduleGapNoticeService`
    - `lib/screens/admin/admin_dashboard_screen.dart` contains `Jadwal\\nKosong`
    - `lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart` exists
    - `lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart` contains `Jadwal Kosong`
    - `lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart` contains `Buka Jadwal`
    - `lib/screens/admin/admin_dashboard_screen.dart` still contains `Belum\\nPulang`
    - the dashboard notice flow does not change recap or export code paths
  </acceptance_criteria>
  <done>The dashboard now exposes one lightweight outlet-scoped schedule-gap notice affordance without reopening recap semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused widget coverage for dashboard notice visibility, outlet scope, and scheduler CTA</name>
  <files>test/screens/admin/admin_dashboard_schedule_gap_test.dart</files>
  <read_first>
    - lib/screens/admin/admin_dashboard_screen.dart
    - lib/screens/admin/widgets/admin_schedule_gap_notice_sheet.dart
    - lib/services/schedule_gap_notice_service.dart
    - test/services/schedule_gap_notice_service_test.dart
    - .planning/phases/62-schedule-gap-notices/62-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: quick action renders with a count when notice entries exist.
    - Test 2: quick action is hidden when notice entries are empty.
    - Test 3: full admin without a selected outlet does not render the notice affordance.
    - Test 4: the sheet shows `Jadwal Kosong`, the locked helper copy, and `Buka Jadwal`.
  </behavior>
  <action>
    Protect the dashboard notice surface with focused widget tests.

    Required behavior:
    1. Create `test/screens/admin/admin_dashboard_schedule_gap_test.dart`.
    2. Cover these exact scenarios:
       - `ScheduleGapNoticeResult.count > 0` -> quick action visible
       - `ScheduleGapNoticeResult.count == 0` -> quick action hidden
       - kepala gerai outlet scope shows only scoped notice content
       - full admin with no selected outlet does not show the quick action
       - opening the sheet shows `Jadwal Kosong` and `Buka Jadwal`
    3. Assert the exact helper copy:
       - `Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.`
    4. Keep the tests local to the dashboard notice surface. Do not depend on live Supabase data.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_dashboard_schedule_gap_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/admin/admin_dashboard_schedule_gap_test.dart` exists
    - the test file contains `Jadwal Kosong`
    - the test file contains `Buka Jadwal`
    - the test file contains `Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.`
    - the test file asserts the quick action hides when notice entries are empty
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_dashboard_schedule_gap_test.dart` exits 0
  </acceptance_criteria>
  <done>The lightweight dashboard notice flow is now protected against scope, copy, and visibility regressions.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_dashboard_schedule_gap_test.dart` passes.
- the dashboard notice flow is only visible for one resolved outlet at a time.
- the notice surface opens the existing scheduler path and leaves recap/export semantics unchanged.
</verification>

<success_criteria>
- Kepala gerai can see outlet-scoped schedule-gap notices directly on the dashboard.
- The UI stays lightweight and non-blocking.
- Operators get a clear route to fill schedules without mixing this follow-up into recap rules.
</success_criteria>

<output>
After completion, create `.planning/phases/62-schedule-gap-notices/62-02-SUMMARY.md`
</output>
