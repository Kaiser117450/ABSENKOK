---
phase: 62-schedule-gap-notices
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/schedule_gap_notice.dart
  - lib/services/schedule_gap_notice_service.dart
  - test/services/schedule_gap_notice_service_test.dart
autonomous: true
requirements: ["SCHED-05"]
must_haves:
  truths:
    - "Schedule-gap notices are derived from canonical compatibility rows, not from a second raw-log-only heuristic."
    - "Notice rows stay single-outlet scoped and describe employee/date follow-up without mutating recap or pending semantics."
    - "The notice domain is pure and reusable so dashboard UI only renders counts and rows."
  artifacts:
    - path: "lib/models/schedule_gap_notice.dart"
      provides: "Typed schedule-gap notice row and summary contract for dashboard follow-up"
      exports: ["ScheduleGapNoticeEntry", "ScheduleGapNoticeResult"]
    - path: "lib/services/schedule_gap_notice_service.dart"
      provides: "Pure service that builds outlet-scoped notice rows from recap compatibility data"
      contains: "ScheduleGapNoticeService"
    - path: "test/services/schedule_gap_notice_service_test.dart"
      provides: "Regression guard for outlet scoping, date ordering, strict exclusion, and non-punitive copy"
      contains: "ScheduleGapNoticeService"
  key_links:
    - from: "lib/services/schedule_gap_notice_service.dart"
      to: "lib/services/admin_policy_recap_dataset_service.dart"
      via: "schedule-gap notices must read from canonical fallbackRows rather than a new heuristic"
      pattern: "AdminPolicyRecapDatasetService"
    - from: "lib/services/schedule_gap_notice_service.dart"
      to: "lib/models/schedule_gap_notice.dart"
      via: "the service returns a typed notice contract instead of unstructured maps"
      pattern: "ScheduleGapNoticeResult"
    - from: "lib/services/schedule_gap_notice_service.dart"
      to: "test/services/schedule_gap_notice_service_test.dart"
      via: "the pure notice domain stays locked by unit coverage before dashboard wiring begins"
      pattern: "ScheduleGapNoticeService"
---

<objective>
Create the canonical schedule-gap notice read model before touching dashboard UI.

Purpose: Phase 61 already exposes honest compatibility rows for attendance-backed missing schedule days, but that signal is trapped inside recap workflows. Phase 62 starts by extracting one pure typed service that converts the same compatibility signal into outlet-scoped employee/date follow-up notices.
Output: typed notice models, a pure notice service, and direct unit tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-schedule-gap-notices/62-RESEARCH.md
@.planning/phases/62-schedule-gap-notices/62-VALIDATION.md
@.planning/phases/61-recap-semantics-recovery/61-RESEARCH.md
@lib/services/attendance_policy_recap_service.dart
@lib/services/admin_policy_recap_dataset_service.dart
@lib/models/attendance_log.dart
@lib/models/attendance_policy_recap_day.dart
@lib/models/employee.dart
@lib/models/outlet_operating_mode.dart
@lib/screens/admin/admin_reports_screen.dart

<locked_decisions>
D-01: Phase 62 must source notices from the canonical compatibility path exposed by `AdminPolicyRecapDatasetService`, not from a second raw-log-only heuristic.
D-02: Notice rows stay operational only: they may describe employee/date follow-up and compatibility status, but they must not assign new penalties, pending states, or recap colors.
D-03: The service contract must stay single-outlet scoped and pure so `AdminDashboardScreen` can reuse it without embedding grouping logic in widget state.
</locked_decisions>

<interfaces>
Create this notice contract in this plan:
```dart
class ScheduleGapNoticeEntry {
  final String employeeId;
  final String employeeName;
  final String outletId;
  final String outletName;
  final DateTime logicalDate;
  final String statusLabel;
  final String helperText;
}

class ScheduleGapNoticeResult {
  final List<ScheduleGapNoticeEntry> entries;
  bool get hasNotices;
  int get count;
}

class ScheduleGapNoticeService {
  const ScheduleGapNoticeService({
    AdminPolicyRecapDatasetService datasetService =
        const AdminPolicyRecapDatasetService(),
  });

  ScheduleGapNoticeResult build({
    required Iterable<Employee> employees,
    required Iterable<AttendancePolicyRecapDay> strictRows,
    required Iterable<AttendanceLog> attendanceLogs,
    required String outletId,
    required String outletName,
    required OutletOperatingMode outletOperatingMode,
    DateTime? now,
  });
}
```

Locked notice copy:
```text
statusLabel: Hadir tanpa jadwal
helperText: Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.
```

Locked source rule:
```text
- build the recap dataset through AdminPolicyRecapDatasetService
- derive notices only from recapDataset.fallbackRows
- one notice per (employeeId, logicalDate)
- sort by logicalDate descending, then employeeName ascending
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a pure schedule-gap notice domain from canonical compatibility rows</name>
  <files>lib/models/schedule_gap_notice.dart, lib/services/schedule_gap_notice_service.dart</files>
  <read_first>
    - lib/services/admin_policy_recap_dataset_service.dart
    - lib/services/attendance_policy_recap_service.dart
    - lib/screens/admin/admin_reports_screen.dart
    - lib/models/attendance_policy_recap_day.dart
    - .planning/phases/62-schedule-gap-notices/62-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: fallback rows from the recap dataset become one notice entry per `(employeeId, logicalDate)`.
    - Test 2: strict-only rows never appear in notice results.
    - Test 3: notice rows stay filtered to the requested outlet only.
    - Test 4: notice rows use the locked non-punitive copy instead of recap-penalty wording.
  </behavior>
  <action>
    Extract the schedule-gap notice domain per D-01 through D-03.

    Required behavior:
    1. Create `lib/models/schedule_gap_notice.dart` with:
       - `ScheduleGapNoticeEntry`
       - `ScheduleGapNoticeResult`
       - `hasNotices` and `count` convenience getters
    2. Create `lib/services/schedule_gap_notice_service.dart` as a pure Dart service that depends on `AdminPolicyRecapDatasetService`.
    3. The service `build(...)` method must accept:
       - `Iterable<Employee> employees`
       - `Iterable<AttendancePolicyRecapDay> strictRows`
       - `Iterable<AttendanceLog> attendanceLogs`
       - `String outletId`
       - `String outletName`
       - `OutletOperatingMode outletOperatingMode`
       - optional `DateTime now`
    4. Inside `build(...)`, call `datasetService.build(...)` with the same arguments so notices are derived from the canonical merged recap path.
    5. Build notices from `recapDataset.fallbackRows` only.
    6. Each notice entry must carry:
       - `employeeId`
       - `employeeName`
       - `outletId`
       - `outletName`
       - `logicalDate`
       - `statusLabel` set to `Hadir tanpa jadwal`
       - `helperText` set to `Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.`
    7. Deduplicate by `(employeeId, logicalDate)` so duplicate fallback rows cannot create repeated notices.
    8. Sort results by `logicalDate` descending, then `employeeName` ascending.
    9. Do not calculate lateness, absence, pending counts, payroll colors, or export cells in this service.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/schedule_gap_notice_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/schedule_gap_notice.dart` exists
    - `lib/models/schedule_gap_notice.dart` exports `ScheduleGapNoticeEntry`
    - `lib/models/schedule_gap_notice.dart` exports `ScheduleGapNoticeResult`
    - `lib/services/schedule_gap_notice_service.dart` exists
    - `lib/services/schedule_gap_notice_service.dart` contains `ScheduleGapNoticeService`
    - `lib/services/schedule_gap_notice_service.dart` contains `AdminPolicyRecapDatasetService`
    - `lib/services/schedule_gap_notice_service.dart` contains `fallbackRows`
    - `lib/services/schedule_gap_notice_service.dart` contains `Hadir tanpa jadwal`
    - `lib/services/schedule_gap_notice_service.dart` contains `Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.`
  </acceptance_criteria>
  <done>The repo now has one typed schedule-gap notice domain driven by the same compatibility signal as the recovered recap flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add direct regression coverage for notice grouping, outlet scope, and non-punitive copy</name>
  <files>test/services/schedule_gap_notice_service_test.dart</files>
  <read_first>
    - lib/models/schedule_gap_notice.dart
    - lib/services/schedule_gap_notice_service.dart
    - lib/services/admin_policy_recap_dataset_service.dart
    - test/services/admin_policy_recap_dataset_service_test.dart
    - .planning/phases/62-schedule-gap-notices/62-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: empty fallback rows produce `hasNotices == false`.
    - Test 2: one fallback row produces one notice with the locked copy.
    - Test 3: mixed strict-plus-fallback data excludes strict rows from notices.
    - Test 4: out-of-outlet attendance logs do not leak into the notice result.
  </behavior>
  <action>
    Protect the new notice domain with focused pure tests.

    Required behavior:
    1. Create `test/services/schedule_gap_notice_service_test.dart`.
    2. Cover these exact scenarios:
       - no fallback rows -> `count == 0`
       - one fallback row -> one `ScheduleGapNoticeEntry`
       - strict row plus fallback row -> only the fallback-backed date becomes a notice
       - attendance logs from another outlet -> no leaked notice rows
    3. Use concrete employee IDs, outlet IDs, and local dates in the assertions.
    4. Assert the exact locked copy strings:
       - `Hadir tanpa jadwal`
       - `Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.`
    5. Keep the suite pure. Do not pump widgets or call Supabase.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/schedule_gap_notice_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/services/schedule_gap_notice_service_test.dart` exists
    - the test file contains `ScheduleGapNoticeService`
    - the test file asserts `count == 0` for empty notices
    - the test file asserts `Hadir tanpa jadwal`
    - the test file asserts `Jadwal untuk tanggal ini belum terisi. Isi jadwal agar tindak lanjut operasional tetap jelas.`
    - `C:\flutter\bin\flutter.bat test test/services/schedule_gap_notice_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The schedule-gap notice read model is now protected against outlet-scope drift and punitive wording regressions.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/schedule_gap_notice_service_test.dart` passes.
- notice rows come only from `fallbackRows` and never from strict recap rows alone.
- the typed notice contract is reusable without widget-specific logic.
</verification>

<success_criteria>
- Phase 62 has one canonical schedule-gap notice read model driven by compatibility rows.
- The notice domain remains operational and non-punitive.
- Dashboard UI can consume notice counts and rows without re-deriving recap semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/62-schedule-gap-notices/62-01-SUMMARY.md`
</output>
