---
phase: 47-toolchain-contract-preflight-gates
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - tool/release_preflight.ps1
  - docs/android-release-contract.md
autonomous: true
requirements: [BUILD-02]
must_haves:
  truths:
    - "There is one release preflight command operators can run before signing or packaging work begins."
    - "The preflight lane fails fast on analyzer, test, or release-only compile breakage."
    - "The release-only compile stage exercises real release compilation without calling package or sign tasks."
  artifacts:
    - path: "tool/release_preflight.ps1"
      provides: "One fail-fast preflight entrypoint for analyze, test, and release-only compile checks."
      contains: "compileReleaseSources"
    - path: "docs/android-release-contract.md"
      provides: "Operator-facing instructions for the preflight lane and its fail-fast expectations."
      contains: "release_preflight"
  key_links:
    - from: "tool/release_preflight.ps1"
      to: "tool/release_env.ps1"
      via: "the preflight lane must inherit the explicit Java 21 JBR contract before running Flutter or Gradle"
      pattern: "release_env"
    - from: "tool/release_preflight.ps1"
      to: "android/settings.gradle.kts"
      via: "the release-only compile step relies on the tracked Kotlin/AGP baseline when running Gradle"
      pattern: "compileReleaseSources"
---

<objective>
Add one fail-fast preflight lane that proves release work stops before signing when the repo is red.

Purpose: BUILD-02 is about a cheap, deterministic gate before packaging, not about making every check green inside this phase.
Output: A PowerShell preflight script that runs analyze, test, and release-only compile in order, exiting on the first failing stage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-VALIDATION.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-01-PLAN.md
@tool/release_env.ps1
@android/settings.gradle.kts
@android/app/build.gradle.kts
@README.md

<interfaces>
Measured command state on 2026-03-23:
```text
flutter analyze -> exits non-zero with 152 issues
flutter test -> exits non-zero after 206 tests with 26 failures
android\gradlew.bat :app:compileReleaseSources -> BUILD SUCCESSFUL under Java 21 JBR
```

Measured Gradle task graph:
```text
compileFlutterBuildRelease
compileReleaseKotlin
compileReleaseJavaWithJavac
compileReleaseSources
packageRelease
bundleRelease
signReleaseBundle
```
</interfaces>

<constraints>
- always enter through `tool/release_env.ps1`
- use `:app:compileReleaseSources` as the release-only compile stage
- do not call `assembleRelease`, `bundleRelease`, `packageRelease`, or signing tasks in this plan
- do not fix the existing analyzer/test failures in this phase; expose them earlier instead
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a single preflight entrypoint for analyze, test, and release-only compile</name>
  <files>tool/release_preflight.ps1</files>
  <read_first>
    - tool/release_env.ps1
    - .planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
    - android/app/build.gradle.kts
    - android/settings.gradle.kts
  </read_first>
  <action>
    Create `tool/release_preflight.ps1` as the only preflight command operators need before signed packaging.

    Required behavior:
    1. Source or invoke `tool/release_env.ps1` first so the lane always runs on Java 21 JBR.
    2. Run these stages in order:
       - `C:\flutter\bin\flutter.bat analyze`
       - `C:\flutter\bin\flutter.bat test`
       - `android\gradlew.bat :app:compileReleaseSources`
    3. Print clear stage banners and exit immediately on the first non-zero command.
    4. Return the failing stage's exit code to the shell.
    5. Make the script readable enough that operators can see it never enters packaging or signing.

    Scope guard:
    - do not invoke `flutter build apk --release`
    - do not run bundle/sign/package tasks
    - do not add repo-specific bypass flags to make the lane pass artificially
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'tool/release_preflight.ps1' -Pattern 'flutter analyze','flutter test','compileReleaseSources'"</automated>
  </verify>
  <done>The repo has one tracked preflight command that exercises analyzer, tests, and release-only compilation in the right order.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the preflight lane fails before signing on the current red repo state</name>
  <files>tool/release_preflight.ps1, docs/android-release-contract.md</files>
  <read_first>
    - tool/release_preflight.ps1
    - docs/android-release-contract.md
    - .planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
  </read_first>
  <action>
    Run the new preflight lane against the current checkout and document what it proves.

    Required behavior:
    1. Execute `powershell -ExecutionPolicy Bypass -File tool/release_preflight.ps1`.
    2. Capture that the script exits non-zero because the repo is currently red in analyze/test.
    3. Document in `docs/android-release-contract.md` that this is the expected fail-fast behavior for Phase 47, not a reason to jump to signing/package commands.
    4. Make it clear that the release-only compile stage remains part of the lane for when analyze/test become green.

    Scope guard:
    - do not fix unrelated code debt while proving the lane
    - do not weaken the lane to make it pass
    - do not add packaging fallbacks after a failed preflight run
  </action>
  <verify>
    <automated>powershell -Command "& { powershell -ExecutionPolicy Bypass -File tool/release_preflight.ps1; if ($LASTEXITCODE -eq 0) { throw 'preflight unexpectedly passed despite known analyzer/test debt' } }"</automated>
  </verify>
  <done>The preflight lane proves BUILD-02 by failing before packaging on the current broken analyze/test state.</done>
</task>

</tasks>

<verification>
- one command now runs analyze, test, and release-only compile in order
- the preflight lane exits non-zero before any package/sign task would begin
- current repo red checks are surfaced earlier than full `build apk --release`
</verification>

<success_criteria>
- BUILD-02 is satisfied with a single tracked preflight command
- operators no longer need to use full release packaging to discover analyzer/test regressions
- the lane is strict enough to fail on current debt and cheap enough to run before signing work
</success_criteria>

<output>
After completion, create `.planning/phases/47-toolchain-contract-preflight-gates/47-02-SUMMARY.md`
</output>
