---
phase: 47-toolchain-contract-preflight-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tool/release_env.ps1
  - docs/android-release-contract.md
  - README.md
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "The supported Gradle runtime is explicit and tracked as Java 21 from Android Studio JBR, not whatever `java.exe` is first on PATH."
    - "Another operator can discover the v7.0 release contract from a clean checkout without opening planning files."
    - "`android/local.properties` remains machine-local evidence, while the tracked repo contract lives in scripts and docs."
  artifacts:
    - path: "tool/release_env.ps1"
      provides: "One Windows entrypoint that resolves and validates the supported Java runtime plus canonical Flutter path."
      contains: "JAVA_HOME"
    - path: "docs/android-release-contract.md"
      provides: "Tracked v7.0 release contract covering Java 21 JBR, Flutter 3.41.1, AGP 8.11.1, Gradle 8.14, and Kotlin 1.9.25."
      contains: "Java 21"
    - path: "README.md"
      provides: "Repo-root discoverability for the Android release contract."
      contains: "android-release-contract"
  key_links:
    - from: "tool/release_env.ps1"
      to: "docs/android-release-contract.md"
      via: "the helper script enforces the same Java and Flutter contract documented for operators"
      pattern: "JAVA_HOME"
    - from: "README.md"
      to: "docs/android-release-contract.md"
      via: "repo-root instructions point operators to the tracked release contract"
      pattern: "android-release-contract"
---

<objective>
Make the supported release runtime explicit and discoverable before adding any new preflight automation.

Purpose: Phase 47 cannot add a trustworthy fail-fast lane until the repo has one tracked answer for which Java runtime and Flutter baseline operators are supposed to use.
Output: A tracked release-contract document plus a PowerShell helper that resolves Java 21 JBR and the canonical `C:\flutter` lane on Windows.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-VALIDATION.md
@README.md
@pubspec.yaml
@android/local.properties
@android/settings.gradle.kts
@android/gradle/wrapper/gradle-wrapper.properties
@android/app/build.gradle.kts

<interfaces>
Measured runtime drift on 2026-03-23:
```text
java -version -> Temurin 25.0.1
gradlew -version without JAVA_HOME -> Launcher JVM 25.0.1
gradlew -version with JAVA_HOME=C:\Program Files\Android\Android Studio\jbr -> Launcher JVM 21.0.9
```

Tracked Android pins:
```kotlin
id("com.android.application") version "8.11.1" apply false
id("org.jetbrains.kotlin.android") version "1.9.25" apply false
```

```properties
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14-all.zip
```
</interfaces>

<constraints>
- do not change Kotlin, AGP, Gradle, Flutter, or app-module Java target versions in this plan
- do not promote `android/local.properties` into a tracked repo file
- keep the helper Windows-specific and fail-fast; do not broaden scope into cross-platform scripting
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a tracked PowerShell helper for the supported release runtime</name>
  <files>tool/release_env.ps1</files>
  <read_first>
    - .planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
    - android/local.properties
    - android/settings.gradle.kts
    - android/gradle/wrapper/gradle-wrapper.properties
  </read_first>
  <action>
    Create `tool/release_env.ps1` as the canonical Windows entrypoint for release-related commands.

    Required behavior:
    1. Resolve the supported Java runtime from `ABSENKOK_JAVA_HOME` if set, otherwise fall back to `C:\Program Files\Android\Android Studio\jbr`.
    2. Validate that the resolved runtime is Java 21 and exit non-zero with a clear message if not.
    3. Export `JAVA_HOME` and prepend `$JAVA_HOME\bin` for the current process.
    4. Validate that `C:\flutter\bin\flutter.bat` exists and report a clear error if the canonical Flutter lane is missing.
    5. Support a `-CheckOnly` mode that prints the resolved contract without running downstream commands.

    Scope guard:
    - do not edit `android/local.properties`
    - do not hardcode machine-specific Android SDK paths beyond the documented JBR default
    - do not run packaging or signing work in this task
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File tool/release_env.ps1 -CheckOnly</automated>
  </verify>
  <done>The repo has one tracked helper that resolves Java 21 JBR and the canonical Flutter CLI path for release work.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the v7.0 release contract where operators will actually find it</name>
  <files>docs/android-release-contract.md, README.md</files>
  <read_first>
    - README.md
    - pubspec.yaml
    - android/settings.gradle.kts
    - android/gradle/wrapper/gradle-wrapper.properties
    - .planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
  </read_first>
  <action>
    Add a tracked release-contract document and link it from the repo root.

    Required behavior:
    1. Create `docs/android-release-contract.md` with:
       - supported Java runtime: Android Studio JBR Java 21
       - canonical Flutter CLI path: `C:\flutter\bin\flutter.bat`
       - pinned versions: Flutter 3.41.1, AGP 8.11.1, Gradle 8.14, Kotlin 1.9.25
       - note that `android/local.properties` is machine-local evidence, not the tracked contract
       - a short "how to verify the environment" section that points to `tool/release_env.ps1 -CheckOnly`
    2. Update `README.md` so a repo-root operator can discover that document quickly.

    Scope guard:
    - do not add signing or packaging instructions yet
    - do not add CI/CD content
    - keep the doc focused on the runtime/toolchain contract, not Phase 48 or Phase 49 work
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'README.md','docs/android-release-contract.md' -Pattern 'Java 21','Flutter 3.41.1','Gradle 8.14','Kotlin 1.9.25','tool/release_env.ps1'"</automated>
  </verify>
  <done>The release contract is tracked and discoverable from the repo root instead of living only in planning memory.</done>
</task>

</tasks>

<verification>
- `tool/release_env.ps1 -CheckOnly` resolves Java 21 JBR and the canonical `C:\flutter` lane
- the tracked contract doc and README link expose the exact supported runtime/toolchain baseline
- no tracked file now depends on whichever `java.exe` happens to be first on PATH
</verification>

<success_criteria>
- TOOL-01 is satisfied through a tracked helper and tracked documentation
- another operator can identify the supported Java runtime and build baseline from the repo root
- the repo contract is explicit without force-tracking `android/local.properties`
</success_criteria>

<output>
After completion, create `.planning/phases/47-toolchain-contract-preflight-gates/47-01-SUMMARY.md`
</output>
