---
phase: 47-toolchain-contract-preflight-gates
plan: 03
type: execute
wave: 3
depends_on: [47-02]
files_modified:
  - android/settings.gradle.kts
  - docs/android-release-contract.md
  - tool/release_preflight.ps1
autonomous: true
requirements: [TOOL-02]
must_haves:
  truths:
    - "The v7.0 compatibility position is explicit: Flutter 3.41.1, AGP 8.11.1, Gradle 8.14, Kotlin 1.9.25, and Java 21 JBR."
    - "Operators are told not to use `--android-skip-build-dependency-validation` or surprise Kotlin upgrades in the canonical v7.0 lane."
    - "Version drift in the tracked Kotlin/Gradle contract becomes visible in docs and preflight behavior instead of being implicit."
  artifacts:
    - path: "android/settings.gradle.kts"
      provides: "Tracked Kotlin plugin pin and local rationale close to the build definition."
      contains: "1.9.25"
    - path: "docs/android-release-contract.md"
      provides: "Operator-facing explanation of why v7.0 intentionally holds the current compatibility line."
      contains: "android-skip-build-dependency-validation"
    - path: "tool/release_preflight.ps1"
      provides: "Guardrails that reject bypass-flag usage or silent drift from the pinned compatibility line."
      contains: "1.9.25"
  key_links:
    - from: "android/settings.gradle.kts"
      to: "docs/android-release-contract.md"
      via: "the build file and contract doc must express the same Kotlin pin and rationale"
      pattern: "1.9.25"
    - from: "tool/release_preflight.ps1"
      to: "android/settings.gradle.kts"
      via: "preflight should assert or at least surface drift from the tracked Kotlin plugin version"
      pattern: "1.9.25"
---

<objective>
Turn the current Kotlin warning into a tracked v7.0 compatibility decision with guardrails.

Purpose: Phase 47 should prevent operators from interpreting Flutter's warning as license to use bypass flags or local toolchain upgrades during release hardening.
Output: A documented and lightly enforced compatibility contract that pins the current versions for v7.0 and forbids skip-flag usage in the canonical lane.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-VALIDATION.md
@.planning/phases/47-toolchain-contract-preflight-gates/47-02-PLAN.md
@tool/release_preflight.ps1
@docs/android-release-contract.md
@android/settings.gradle.kts
@android/gradle/wrapper/gradle-wrapper.properties
@pubspec.yaml

<interfaces>
Current Kotlin pin and warning context:
```kotlin
id("org.jetbrains.kotlin.android") version "1.9.25" apply false
// Kotlin 1.9.25 — nfc_manager tidak kompatibel dengan Kotlin 2.x
```

Measured Flutter warning:
```text
Flutter support for your project's Kotlin version (1.9.25) will soon be dropped.
Alternatively, use the flag "--android-skip-build-dependency-validation" to bypass this check.
```
</interfaces>

<constraints>
- do not upgrade Kotlin, AGP, Gradle, Flutter, or `nfc_manager` in this plan
- do not add `--android-skip-build-dependency-validation` to any tracked script or document
- keep the enforcement lightweight and operator-facing; Phase 47 is about contract clarity, not a full custom version-management system
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the v7.0 compatibility decision in tracked docs and build comments</name>
  <files>android/settings.gradle.kts, docs/android-release-contract.md</files>
  <read_first>
    - android/settings.gradle.kts
    - docs/android-release-contract.md
    - .planning/phases/47-toolchain-contract-preflight-gates/47-RESEARCH.md
  </read_first>
  <action>
    Make the v7.0 compatibility line explicit in the tracked repo surface.

    Required behavior:
    1. Expand the Kotlin note in `android/settings.gradle.kts` only as much as needed to explain that v7.0 intentionally holds Kotlin 1.9.25.
    2. Update `docs/android-release-contract.md` with a short decision section covering:
       - Flutter 3.41.1
       - AGP 8.11.1
       - Gradle 8.14
       - Kotlin 1.9.25
       - Java 21 JBR
       - no surprise upgrades during v7.0
    3. Explain why the warning is acknowledged but not bypassed or upgraded in this milestone.

    Scope guard:
    - do not rewrite the whole build file
    - do not turn the decision section into future-milestone planning for Kotlin 2.x
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'docs/android-release-contract.md','android/settings.gradle.kts' -Pattern '1\\.9\\.25','8\\.11\\.1','8\\.14','3\\.41\\.1','Java 21'"</automated>
  </verify>
  <done>The tracked build file and tracked operator doc now express the same pinned v7.0 compatibility decision.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the canonical preflight lane against bypass flags and silent version drift</name>
  <files>tool/release_preflight.ps1, docs/android-release-contract.md</files>
  <read_first>
    - tool/release_preflight.ps1
    - docs/android-release-contract.md
    - android/settings.gradle.kts
  </read_first>
  <action>
    Add a small but explicit guardrail to the preflight lane so operators cannot silently drift away from the v7.0 contract.

    Required behavior:
    1. Ensure `tool/release_preflight.ps1` does not include `--android-skip-build-dependency-validation`.
    2. Add a guard or explicit check that surfaces an error if the tracked Kotlin plugin pin is no longer `1.9.25` without the contract being intentionally updated.
    3. Document in `docs/android-release-contract.md` that bypass flags are not part of the canonical v7.0 flow.
    4. Keep the guard simple: string/regex checks and clear operator messaging are enough for this phase.

    Scope guard:
    - do not build a general-purpose version parser
    - do not introduce CI-only enforcement
    - do not weaken the preflight lane to suppress Flutter's warning
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'tool/release_preflight.ps1','docs/android-release-contract.md' -Pattern 'android-skip-build-dependency-validation','1\\.9\\.25','surprise upgrades'"</automated>
  </verify>
  <done>The canonical preflight lane and operator docs now make bypass-flag usage and silent Kotlin drift an explicit contract violation.</done>
</task>

</tasks>

<verification>
- the build file, operator doc, and preflight lane all express the same v7.0 compatibility decision
- bypass flags are explicitly excluded from the canonical flow
- operators can tell the difference between "warning acknowledged" and "upgrade now" without guessing
</verification>

<success_criteria>
- TOOL-02 is satisfied with a tracked compatibility decision and lightweight guardrails
- no operator needs to invent a local Kotlin workaround during v7.0 release prep
- the v7.0 lane stays intentionally pinned until a later dedicated upgrade spike changes it
</success_criteria>

<output>
After completion, create `.planning/phases/47-toolchain-contract-preflight-gates/47-03-SUMMARY.md`
</output>
