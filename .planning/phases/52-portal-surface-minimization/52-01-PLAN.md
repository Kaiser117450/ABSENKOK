---
phase: 52-portal-surface-minimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
autonomous: true
requirements: [SECPORT-01]
must_haves:
  truths:
    - "Protected /portal requests are redirected before the downstream page or endpoint handler runs when there is no verified portal session."
    - "Middleware remains the single place that refreshes cookies and caches the verified portal user for the request."
    - "Protected portal loaders stop performing fallback auth fetches once middleware defines the request contract."
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts"
      provides: "Pre-handler protected-route gate that resolves the verified user before `next()`."
      contains: "isProtectedPortalRoute"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      provides: "Portal employee resolver that trusts the middleware-cached verified user boundary."
      contains: "portalUser"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts"
      via: "protected-route enforcement continues using the shared route-classification helper"
      pattern: "isProtectedPortalRoute"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts"
      via: "protected page loaders consume the verified portal user from `Astro.locals` instead of re-checking auth"
      pattern: "portalUser"
---

<objective>
Complete the protected-route auth gate before any sensitive portal route logic runs.

Purpose: SECPORT-01 is about sequencing, not a new auth model. The protected `/portal` boundary must be decided in middleware before page loaders or RPC helpers can execute.
Output: A middleware-first protected-route redirect plus a portal employee resolver that no longer performs fallback auth fetches for protected routes.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-portal-surface-minimization/52-RESEARCH.md
@.planning/phases/52-portal-surface-minimization/52-VALIDATION.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/attendance.astro

<interfaces>
Current protected-route flow:
```ts
const {
  data: { user },
} = await supabase.auth.getUser();

(context.locals as Record<string, unknown>).portalUser = user ?? null;

const response = await next();

if (isProtectedPortalRoute(pathname) && !user) {
  return redirect('/portal/login', 302);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move protected-route enforcement ahead of `next()` in middleware</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
    - .planning/phases/52-portal-surface-minimization/52-RESEARCH.md
  </read_first>
  <action>
    Update `src/middleware.ts` so protected portal auth is decided before the route handler runs.

    Required behavior:
    1. Keep the existing `/portal`-only scope and the existing `getUser()`-based verification.
    2. Continue caching the verified user on `context.locals.portalUser`.
    3. For protected routes (`isProtectedPortalRoute(pathname) === true`):
       - if `user` is missing, return `redirect('/portal/login', 302)` before `await next()`
       - make sure any refreshed `Set-Cookie` headers from the SSR client are still attached to the redirect response
    4. For public portal routes (`/portal/login`, `/portal/auth/search`, `/portal/auth/sign-in`, `/portal/auth/sign-out`), continue through to `next()` after verification/cookie refresh.
    5. Preserve the existing "marketing routes skip portal logic entirely" behavior.

    Scope guard:
    - do not redesign route classification
    - do not move public portal routes behind auth
    - do not change the Supabase env helper contract
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run build"</automated>
  </verify>
  <done>The middleware decides protected portal auth before route handlers execute and still propagates refreshed cookies correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Make `resolvePortalEmployee()` trust only the middleware-established request boundary</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts
    - .planning/phases/52-portal-surface-minimization/52-RESEARCH.md
  </read_first>
  <action>
    Tighten `resolvePortalEmployee(Astro)` so protected portal loaders stop doing fallback auth work.

    Required behavior:
    1. Treat `Astro.locals.portalUser` as the only verified protected-route session source.
    2. If `portalUser` is `null` or absent, return the `unauthenticated` result immediately.
    3. Call `supabase.rpc('resolve_portal_employee')` only after a verified `portalUser` is present.
    4. Preserve the existing typed result union:
       - `ok: true`
       - `reason: 'unauthenticated' | 'no_mapping' | 'rpc_error'`
    5. Keep the helper non-throwing.

    Scope guard:
    - do not change the `resolve_portal_employee()` SQL contract
    - do not add caller-supplied employee identifiers
    - do not reintroduce a second `supabase.auth.getUser()` fallback in page code
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts','C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts' -Pattern 'portalUser','redirect','resolve_portal_employee' | Measure-Object"</automated>
  </verify>
  <done>Protected portal helpers now trust the middleware-established verified-user boundary instead of performing fallback auth fetches.</done>
</task>

</tasks>

<verification>
- Protected `/portal` requests redirect before protected page loaders can begin when there is no verified portal session.
- Middleware still refreshes Supabase cookies and exposes the verified user on `Astro.locals.portalUser`.
- `resolvePortalEmployee()` returns `unauthenticated` without a fallback auth fetch when the middleware session boundary is absent.
</verification>

<success_criteria>
- SECPORT-01 is satisfied by sequencing, not by adding another layer of page-local redirects.
- Unauthenticated protected portal requests no longer execute sensitive route logic before the auth guard resolves.
- The request-local verified user contract remains explicit and reusable for the existing portal loaders.
</success_criteria>

<output>
After completion, create `.planning/phases/52-portal-surface-minimization/52-01-SUMMARY.md`
</output>
