---
phase: 52-portal-surface-minimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/phase_52_portal_search_minimization_20260325.sql
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro
autonomous: true
requirements: [SECPORT-02]
user_setup:
  - service: supabase
    why: "Phase 52 adds an additive SQL hardening patch that redefines the public portal search RPC and must be reviewed before it is applied."
    dashboard_config:
      - task: "Run sql/phase_52_portal_search_minimization_20260325.sql in Supabase SQL Editor after the user approves the database change."
        location: "Supabase Dashboard -> SQL Editor -> New Query"
must_haves:
  truths:
    - "Public portal search returns only the chooser-safe fields the login UI actually renders."
    - "Enumeration-friendly behavior is reduced through stricter minimum-length, maximum-length, result-cap, and fuzzy-search thresholds."
    - "The accepted passwordless chooser flow still works: the employee can search, select a card, and auto-submit using the hidden employee id."
  artifacts:
    - path: "sql/phase_52_portal_search_minimization_20260325.sql"
      provides: "Additive SQL patch that redefines `search_portal_employees(...)` with a minimal chooser DTO and tighter caps."
      contains: "search_portal_employees"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts"
      provides: "Centralized search thresholds and normalization helpers shared by the public portal search path."
      contains: "SEARCH_MIN_LENGTH"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      provides: "Public search endpoint that enforces the tightened chooser contract and hides unnecessary fields."
      contains: "EmployeeSearchResult"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro"
      provides: "Chooser copy and client-side search behavior aligned with the tighter public search contract."
      contains: "search-status"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      via: "shared search constants enforce the same minimum-length and result-cap on the server route"
      pattern: "SEARCH_MAX_RESULTS"
    - from: "sql/phase_52_portal_search_minimization_20260325.sql"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      via: "the endpoint consumes the tightened `search_portal_employees(...)` response contract"
      pattern: "home_outlet_name"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      via: "chooser UI expectations stay aligned with the narrowed public DTO"
      pattern: "employee_name"
---

<objective>
Reduce the public chooser surface without removing the accepted passwordless portal entry flow.

Purpose: SECPORT-02 is not asking for a new auth model. It is asking for the current public chooser to reveal less data and to be harder to abuse for broad employee enumeration.
Output: An additive SQL patch plus website-side threshold and DTO updates that keep the chooser usable while shrinking the exposed surface.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
@.planning/phases/52-portal-surface-minimization/52-RESEARCH.md
@.planning/phases/52-portal-surface-minimization/52-VALIDATION.md
@sql/phase_39_portal_read_path_hardening_20260323.sql
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro

<interfaces>
Current public search result:
```ts
interface EmployeeSearchResult {
  employee_id: string;
  employee_name: string;
  home_outlet_id: string | null;
  home_outlet_name: string | null;
  position: string | null;
  photo_url: string | null;
  active_badge_id: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create an additive SQL hardening patch for the public chooser RPC</name>
  <files>sql/phase_52_portal_search_minimization_20260325.sql</files>
  <read_first>
    - sql/phase_39_portal_read_path_hardening_20260323.sql
    - .planning/phases/52-portal-surface-minimization/52-RESEARCH.md
    - .planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
  </read_first>
  <action>
    Create `sql/phase_52_portal_search_minimization_20260325.sql` as one additive SQL patch that redefines `search_portal_employees(search_text text, limit_count integer)` using `CREATE OR REPLACE FUNCTION`.

    Required behavior:
    1. Keep the chooser public:
       - `GRANT EXECUTE` remains available to `anon` and `authenticated`
       - no caller auth requirement is introduced for search
    2. Narrow the returned columns to the chooser-safe DTO only:
       - `employee_id`
       - `employee_name`
       - `home_outlet_name`
       - `position`
       - `photo_url`
    3. Tighten query/result behavior inside SQL:
       - return early when normalized query length is below the new threshold
       - cap the effective limit to the new maximum even if a higher `limit_count` is passed
       - keep prefix search first
       - delay trigram fallback until the stricter threshold (do not run it for the shortest allowed public queries)
    4. Keep the existing active/archived employee filters.
    5. Add a short file header explaining that the patch minimizes public chooser exposure while preserving passwordless entry.

    Scope guard:
    - do not remove public chooser search
    - do not remove `employee_id` from the result contract, because the hidden portal sign-in flow still needs it
    - do not create a second search RPC name
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path sql/phase_52_portal_search_minimization_20260325.sql -Pattern 'search_portal_employees','employee_name','home_outlet_name','photo_url','LIMIT 5','char_length','similarity' | Measure-Object"</automated>
  </verify>
  <done>The repo contains an additive SQL patch that tightens public chooser search without removing the accepted passwordless entry contract.</done>
</task>

<task type="auto">
  <name>Task 2: Align the website search boundary and chooser UI with the tightened public contract</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro
    - sql/phase_52_portal_search_minimization_20260325.sql
    - .planning/phases/52-portal-surface-minimization/52-RESEARCH.md
  </read_first>
  <action>
    Update the website-side public chooser boundary to match the new SQL contract exactly.

    Required behavior:
    1. `src/lib/portal/auth.ts`
       - centralize the tightened search constants
       - add an explicit maximum normalized query length constant
       - keep the existing normalization helper pure
    2. `src/pages/portal/auth/search.ts`
       - reject or short-circuit queries outside the new minimum/maximum bounds
       - request only the tightened result count from SQL
       - expose only the minimal chooser DTO fields
       - prefer `Cache-Control: no-store` or equally strict non-persistent caching for this public endpoint
       - keep errors fail-closed (`results: []`)
    3. `src/pages/portal/login.astro`
       - update any chooser guidance/status copy to match the new minimum-character threshold
       - keep the chooser card UI on the same passwordless flow
       - make the client-side `EmployeeCard` interface match the narrowed DTO exactly
    4. Preserve the existing auto-submit after card selection.

    Scope guard:
    - do not add a new employee-facing password field
    - do not change the hidden employee-id submission contract
    - do not add a client framework
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The website repo now exposes only the chooser-safe public search contract and communicates the tighter search boundary without breaking the accepted passwordless chooser flow.</done>
</task>

</tasks>

<verification>
- The public portal search SQL and Astro endpoint return only chooser-safe fields.
- The public search path enforces stricter minimum-length, maximum-length, and result-cap behavior on the server side.
- The passwordless chooser still auto-submits the selected employee id into the existing hidden sign-in flow.
</verification>

<success_criteria>
- SECPORT-02 is satisfied without removing public chooser search entirely.
- Public search reveals less data and is less useful for broad employee enumeration.
- The login UI and SQL/endpoint contracts stay aligned, avoiding hidden field drift.
</success_criteria>

<output>
After completion, create `.planning/phases/52-portal-surface-minimization/52-02-SUMMARY.md`
</output>
