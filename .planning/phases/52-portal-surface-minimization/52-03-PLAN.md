---
phase: 52-portal-surface-minimization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/repair_employee_portal_accounts_20260325.sql
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts
  - test/phase52/portal_recovery_contract_test.dart
autonomous: true
requirements: [SECPORT-03]
user_setup:
  - service: supabase
    why: "The new recovery script is an operator-run database repair tool and must only be executed after explicit approval in the right recovery scenario."
    dashboard_config:
      - task: "Run sql/repair_employee_portal_accounts_20260325.sql in Supabase SQL Editor only when portal-account recovery is needed and the user approves the database change."
        location: "Supabase Dashboard -> SQL Editor -> New Query"
must_haves:
  truths:
    - "Portal-account repair restores only confirmed `employee_portal` auth users with an explicit employee binding."
    - "Recovery paths never repoint an existing employee mapping to a different auth user opportunistically."
    - "Passwordless sign-in still auto-provisions missing hidden auth users for active employees, but it fails closed when an existing hidden-email user has conflicting metadata or identity."
  artifacts:
    - path: "sql/repair_employee_portal_accounts_20260325.sql"
      provides: "Fail-closed portal-account recovery script with explicit binding checks and non-destructive conflict handling."
      contains: "employee_portal_accounts"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts"
      provides: "Passwordless recovery helper that validates an existing hidden-email user before reuse."
      contains: "employee_portal"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts"
      provides: "Portal sign-in path that preserves passwordless entry but fails closed on unsafe recovery conflicts."
      contains: "ensurePortalPasswordlessAccount"
    - path: "test/phase52/portal_recovery_contract_test.dart"
      provides: "Focused contract coverage for the Phase 52 recovery SQL safety rules."
      contains: "Phase 52 portal recovery contract"
  key_links:
    - from: "sql/repair_employee_portal_accounts_20260325.sql"
      to: "sql/phase_37_employee_portal_foundation_20260322.sql"
      via: "reuses the original employee-portal binding model instead of inventing a new mapping schema"
      pattern: "employee_portal_accounts"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts"
      via: "the public sign-in route keeps passwordless entry but delegates safe hidden-user reuse to the hardened helper"
      pattern: "ensurePortalPasswordlessAccount"
    - from: "test/phase52/portal_recovery_contract_test.dart"
      to: "sql/repair_employee_portal_accounts_20260325.sql"
      via: "focused regression coverage for the fail-closed recovery script contract"
      pattern: "employee_portal"
---

<objective>
Harden portal repair and recovery so recovery only restores proven employee-portal identities and never repoints mappings opportunistically.

Purpose: SECPORT-03 is about recovery correctness. Passwordless entry can stay, but recovery tools must stop inferring identity from weak signals once something is already broken.
Output: A new fail-closed repair script, a safer hidden-auth reuse helper, and focused contract coverage for the recovery SQL.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
@.planning/phases/52-portal-surface-minimization/52-RESEARCH.md
@.planning/phases/52-portal-surface-minimization/52-VALIDATION.md
@sql/phase_37_employee_portal_foundation_20260322.sql
@sql/repair_employee_portal_accounts_20260323.sql
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts

<interfaces>
Current repair conflict handling:
```sql
ON CONFLICT (employee_id) DO UPDATE
SET
  auth_user_id = EXCLUDED.auth_user_id,
  auth_email = EXCLUDED.auth_email,
  updated_at = now();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a fail-closed portal-account recovery script</name>
  <files>sql/repair_employee_portal_accounts_20260325.sql</files>
  <read_first>
    - sql/repair_employee_portal_accounts_20260323.sql
    - sql/phase_37_employee_portal_foundation_20260322.sql
    - .planning/phases/52-portal-surface-minimization/52-RESEARCH.md
  </read_first>
  <action>
    Create `sql/repair_employee_portal_accounts_20260325.sql` as the new operator-facing recovery script for `employee_portal_accounts`.

    Required behavior:
    1. Keep the table/index/policy setup idempotent, as in the existing repair script.
    2. Restore mappings only from auth users that already prove they are the right portal identity:
       - hidden email matches the `employee+<uuid>@portal.absenkok.internal` pattern
       - auth email is confirmed
       - `raw_app_meta_data.app_role = 'employee_portal'`
       - `raw_app_meta_data.employee_id` exists, parses as UUID, and matches the hidden-email-derived employee id
       - target employee remains active and not archived
    3. Conflict handling must be non-destructive:
       - do not replace a different `auth_user_id` on an existing `employee_id`
       - only refresh fields when the existing mapping already agrees, or skip the row entirely
    4. Add clear comments that the script intentionally skips ambiguous or conflicting rows for manual review.

    Scope guard:
    - do not mutate the core `employee_portal_accounts` schema beyond idempotent restoration needs
    - do not try to "fix" ambiguous mappings automatically
    - do not re-open the portal search or login flow here
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/phase52/portal_recovery_contract_test.dart</automated>
  </verify>
  <done>The repo contains a new fail-closed repair script that restores only proven employee-portal bindings and skips destructive conflict repair.</done>
</task>

<task type="auto">
  <name>Task 2: Harden hidden-auth recovery in the passwordless sign-in path</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts
    - .planning/phases/52-portal-surface-minimization/52-RESEARCH.md
  </read_first>
  <action>
    Tighten the website-side passwordless recovery helper so it only reuses safe hidden-email users.

    Required behavior:
    1. In `src/lib/portal/provision.ts`:
       - keep the existing create-user path for active employees with no hidden auth user
       - when a hidden-email auth user already exists, treat it as reusable only if it is already a confirmed `employee_portal` user with the same explicit `employee_id` binding
       - if the existing user is unconfirmed, has a different role, or has conflicting `employee_id` metadata, throw a fail-closed error instead of overwriting it
    2. In `src/pages/portal/auth/sign-in.ts`:
       - preserve the existing passwordless employee-facing flow
       - keep failures user-safe (`/portal/login?error=invalid`)
       - log the internal recovery failure for operators without leaking details to the employee
    3. Do not add a manual password prompt or new employee-facing recovery UX.

    Scope guard:
    - do not remove passwordless sign-in
    - do not add service-role logic to the browser
    - do not auto-repair mapping conflicts from the public sign-in endpoint
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/provision.ts','C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts' -Pattern 'employee_portal','employee_id','invalid','passwordless provisioning error' | Measure-Object"</automated>
  </verify>
  <done>The website passwordless recovery helper now fails closed on conflicting hidden-auth identities while preserving the accepted employee-facing flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused contract coverage for the Phase 52 recovery SQL</name>
  <files>test/phase52/portal_recovery_contract_test.dart</files>
  <read_first>
    - sql/repair_employee_portal_accounts_20260325.sql
    - test/phase51/sql_role_guard_contract_test.dart
  </read_first>
  <action>
    Create `test/phase52/portal_recovery_contract_test.dart` with group name `Phase 52 portal recovery contract`.

    The test should read `sql/repair_employee_portal_accounts_20260325.sql` from disk and assert:
    - the new recovery script exists
    - the script requires confirmed portal auth users
    - the script checks for `employee_portal` role metadata
    - the script requires explicit `employee_id` binding metadata
    - the script does not contain an unconditional conflict overwrite that swaps `auth_user_id`

    Keep the test lightweight and repo-local. This is a contract regression, not a live database recovery test.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/phase52/portal_recovery_contract_test.dart</automated>
  </verify>
  <done>The repo has a focused regression test that will fail if future edits weaken the fail-closed Phase 52 recovery SQL contract.</done>
</task>

</tasks>

<verification>
- `sql/repair_employee_portal_accounts_20260325.sql` exists and restores only proven employee-portal identities.
- `C:\flutter\bin\flutter.bat test test/phase52/portal_recovery_contract_test.dart` passes.
- The passwordless sign-in path still works for safe new-user provisioning but fails closed when a conflicting hidden-auth identity already exists.
</verification>

<success_criteria>
- SECPORT-03 is satisfied without removing passwordless portal entry for valid active employees.
- Recovery SQL never repoints an existing employee mapping to a different auth user opportunistically.
- Existing hidden-email auth users must already prove the right `employee_portal` binding before the website reuses them.
</success_criteria>

<output>
After completion, create `.planning/phases/52-portal-surface-minimization/52-03-SUMMARY.md`
</output>
