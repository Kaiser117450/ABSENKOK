---
phase: 07-admin-ui-system-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/widgets/app_card.dart
  - lib/widgets/shimmer_skeleton.dart
  - lib/widgets/app_empty_state.dart
  - lib/widgets/app_badge.dart
  - lib/widgets/app_toast.dart
  - lib/core/theme.dart
autonomous: true
requirements:
  - PHASE7-WIDGETS
  - PHASE7-TOAST

must_haves:
  truths:
    - "AppCard, ShimmerSkeleton, AppEmptyState, AppBadge widgets exist and are importable"
    - "AppToast.success/error/info wraps toastification with brand-consistent defaults"
    - "AppBadge has named constructors for hadir, sakit, izin, belumPulang statuses"
    - "ShimmerSkeleton animates without external packages"
    - "AppColors has badge color constants for sakit/izin/belumPulang"
  artifacts:
    - path: "lib/widgets/app_card.dart"
      provides: "Consistent card container with shadow + border radius"
      exports: ["AppCard"]
    - path: "lib/widgets/shimmer_skeleton.dart"
      provides: "Animated loading placeholder"
      exports: ["ShimmerSkeleton"]
    - path: "lib/widgets/app_empty_state.dart"
      provides: "Empty state widget with icon + heading + subtext"
      exports: ["AppEmptyState"]
    - path: "lib/widgets/app_badge.dart"
      provides: "Colored status chip with named constructors"
      exports: ["AppBadge"]
    - path: "lib/widgets/app_toast.dart"
      provides: "Centralized toast helper wrapping toastification"
      exports: ["AppToast"]
  key_links:
    - from: "lib/widgets/app_card.dart"
      to: "lib/core/theme.dart"
      via: "import AppColors for border/shadow colors"
      pattern: "import.*theme\\.dart"
    - from: "lib/widgets/app_badge.dart"
      to: "lib/core/theme.dart"
      via: "import AppColors for badge color constants"
      pattern: "AppColors\\.badge"
    - from: "lib/widgets/app_toast.dart"
      to: "package:toastification"
      via: "import toastification for toast display"
      pattern: "import.*toastification"
---

<objective>
Create the reusable widget library (AppCard, ShimmerSkeleton, AppEmptyState, AppBadge, AppToast) and extend AppColors with missing badge color constants.

Purpose: Establish the foundational widget primitives that Plans 02 and 03 will apply across all admin screens. Zero new packages needed -- uses Flutter built-ins + already-installed toastification.

Output: 5 widget files in lib/widgets/ + updated theme.dart with sakit/izin/belumPulang badge colors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-ui-system-polish/07-RESEARCH.md
@lib/core/theme.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create widget library files + extend AppColors badge palette</name>
  <files>lib/widgets/app_card.dart, lib/widgets/shimmer_skeleton.dart, lib/widgets/app_empty_state.dart, lib/widgets/app_badge.dart, lib/widgets/app_toast.dart, lib/core/theme.dart</files>
  <action>
Create `lib/widgets/` directory with 5 widget files:

**app_card.dart** -- `AppCard` StatelessWidget:
- Props: `child` (required), `padding` (nullable, default EdgeInsets.all(16)), `onTap` (nullable VoidCallback), `margin` (nullable)
- Decoration: white background, BorderRadius.circular(12), Border.all(color: AppColors.border, width: 1), BoxShadow(color: Color(0x08000000), blurRadius: 8, offset: Offset(0, 2))
- Wrap in GestureDetector if onTap provided
- Import from `../core/theme.dart`

**shimmer_skeleton.dart** -- `ShimmerSkeleton` StatefulWidget with SingleTickerProviderStateMixin:
- Props: `width` (default double.infinity), `height` (default 16), `borderRadius` (default 8)
- AnimationController: 1500ms duration, repeat()
- Build: AnimatedBuilder (NOTE: the research has a typo -- use `AnimatedBuilder`), Container with LinearGradient that slides based on _ctrl.value
- Gradient colors: Color(0xFFE5E7EB), Color(0xFFF3F4F6), Color(0xFFE5E7EB)
- CRITICAL: dispose() must call _ctrl.dispose()

**app_empty_state.dart** -- `AppEmptyState` StatelessWidget:
- Props: `icon` (IconData, required), `heading` (String, required), `subtext` (String?, optional)
- Center > Column(mainAxisSize: min) with icon (56px, AppColors.textMuted), heading (16px, w600, AppColors.textSecondary), optional subtext (13px, AppColors.textMuted, textAlign center)

**app_badge.dart** -- `AppBadge` StatelessWidget:
- Props: `label`, `backgroundColor`, `textColor`
- Container with horizontal padding 10, vertical 4, borderRadius 20
- Text: fontSize 11, fontWeight w700
- Named factory constructors: `AppBadge.hadir()`, `AppBadge.sakit()`, `AppBadge.izin()`, `AppBadge.belumPulang()`
  - hadir: badgeMasukBg/badgeMasukText, label 'Hadir'
  - sakit: badgeSakitBg/badgeSakitText, label 'Sakit'
  - izin: badgeIzinBg/badgeIzinText, label 'Izin'
  - belumPulang: badgeBelumPulangBg/badgeBelumPulangText, label 'Belum Pulang'

**app_toast.dart** -- `AppToast` static helper class:
- Static methods: `success(context, message)`, `error(context, message)`, `info(context, message)`
- Each calls `toastification.show()` with: type (success/error/info), style: ToastificationStyle.flat, title: Text(message, fontWeight w600), autoCloseDuration 3s (4s for error), showProgressBar: false
- Import `package:toastification/toastification.dart`

**theme.dart** -- Add missing badge color constants to AppColors (after existing badgePulang entries):
```dart
// Sakit badge
static const Color badgeSakitBg = Color(0xFFFEF3C7);     // Warm amber light
static const Color badgeSakitText = Color(0xFF92400E);    // Dark amber

// Izin badge
static const Color badgeIzinBg = Color(0xFFDBEAFE);      // Light blue
static const Color badgeIzinText = Color(0xFF1E40AF);     // Dark blue

// Belum Pulang badge
static const Color badgeBelumPulangBg = Color(0xFFFEF9C3); // Light yellow
static const Color badgeBelumPulangText = Color(0xFF854D0E); // Dark yellow-brown
```

Use `Color.withValues()` not `withOpacity()` per project convention. All widgets must import from `../core/theme.dart` for AppColors.
  </action>
  <verify>
    <automated>cd "C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter" && C:/flutter/bin/flutter.bat analyze lib/widgets/ lib/core/theme.dart 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 widget files exist in lib/widgets/, import correctly, and pass flutter analyze with no errors. AppColors has sakit/izin/belumPulang badge constants. ShimmerSkeleton properly disposes AnimationController.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/widgets/ lib/core/theme.dart` passes with no errors
- All 5 files export their primary widget class
- AppBadge has 4 named factory constructors
- ShimmerSkeleton uses SingleTickerProviderStateMixin and disposes controller
</verification>

<success_criteria>
- lib/widgets/ directory exists with 5 .dart files
- Each widget imports AppColors from theme.dart (no hardcoded colors)
- AppToast wraps toastification (already in pubspec.yaml)
- flutter analyze shows no errors on new files
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-ui-system-polish/07-01-SUMMARY.md`
</output>
