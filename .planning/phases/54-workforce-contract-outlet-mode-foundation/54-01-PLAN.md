---
phase: 54-workforce-contract-outlet-mode-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/phase_54_workforce_contract_outlet_mode_20260326.sql
  - lib/models/employee_contract.dart
  - lib/models/outlet_operating_mode.dart
  - lib/models/employee.dart
  - lib/models/outlet.dart
  - test/models/workforce_metadata_test.dart
autonomous: true
requirements: [CONTRACT-01, CONTRACT-02]
user_setup:
  - service: supabase
    why: "Phase 54 introduces additive live-database columns on employees and outlets, so the SQL must be reviewed and applied only after explicit user approval."
    dashboard_config:
      - task: "Run sql/phase_54_workforce_contract_outlet_mode_20260326.sql in Supabase SQL Editor after the user approves the production change."
        location: "Supabase Dashboard -> SQL Editor -> New Query"
must_haves:
  truths:
    - "Existing live employees resolve to one stored contract value without null states leaking into the app."
    - "Existing live outlets resolve to one stored operating mode value without changing current active/inactive behavior."
    - "Joined employee and outlet consumers can read typed contract and outlet-mode metadata from the shared model layer instead of raw string heuristics."
    - "The migration stays additive and production-safe for the existing 4-outlet deployment."
  artifacts:
    - path: "sql/phase_54_workforce_contract_outlet_mode_20260326.sql"
      provides: "Additive Supabase migration for canonical employee contract and outlet operating mode storage"
      contains: "ALTER TABLE public.employees"
    - path: "lib/models/employee_contract.dart"
      provides: "Typed employee contract enum and parse helpers"
      exports: ["EmployeeContract"]
    - path: "lib/models/outlet_operating_mode.dart"
      provides: "Typed outlet operating mode enum and parse helpers"
      exports: ["OutletOperatingMode"]
    - path: "lib/models/employee.dart"
      provides: "Employee model with typed contract field"
      contains: "employmentContract"
    - path: "lib/models/outlet.dart"
      provides: "Outlet model with typed operating mode field"
      contains: "operatingMode"
    - path: "test/models/workforce_metadata_test.dart"
      provides: "Regression coverage for typed workforce metadata parsing and round-trips"
      contains: "workforce metadata"
  key_links:
    - from: "lib/models/employee.dart"
      to: "lib/models/employee_contract.dart"
      via: "Employee.fromJson / toJson / copyWith"
      pattern: "employmentContract|EmployeeContract"
    - from: "lib/models/outlet.dart"
      to: "lib/models/outlet_operating_mode.dart"
      via: "Outlet.fromJson / toJson"
      pattern: "operatingMode|OutletOperatingMode"
    - from: "lib/models/attendance_log.dart"
      to: "lib/models/employee.dart"
      via: "joined employee parsing already flows through Employee.fromJson"
      pattern: "Employee.fromJson"
    - from: "lib/models/attendance_log.dart"
      to: "lib/models/outlet.dart"
      via: "joined outlet parsing already flows through Outlet.fromJson"
      pattern: "Outlet.fromJson"
---

<objective>
Create the additive storage contract and shared Dart model layer for workforce metadata.

Purpose: Phase 54 cannot safely add admin surfaces, CSV enforcement, or later recap logic until the database and model layer expose canonical contract and outlet-mode values.
Output: One additive SQL migration, typed enum helpers, extended employee/outlet models, and focused regression tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-CONTEXT.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-RESEARCH.md
@lib/models/employee.dart
@lib/models/outlet.dart
@lib/models/attendance_log.dart
@sql/phase_37_employee_portal_foundation_20260322.sql
@sql/phase_39_portal_read_path_hardening_20260323.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed workforce metadata contracts and extend the shared employee/outlet models</name>
  <files>lib/models/employee_contract.dart, lib/models/outlet_operating_mode.dart, lib/models/employee.dart, lib/models/outlet.dart, test/models/workforce_metadata_test.dart</files>
  <behavior>
    - `EmployeeContract` exposes canonical DB values `FULLTIME` and `PARTTIME`, plus safe normalization for null, mixed-case, and common alias inputs.
    - `OutletOperatingMode` exposes canonical DB values `NORMAL` and `TWENTY_FOUR_HOUR`, plus safe normalization for null and mixed-case inputs.
    - `Employee.fromJson(...)` and `Outlet.fromJson(...)` parse the new fields without throwing and preserve them through `toJson()` / `copyWith()`.
    - Existing joined consumers such as `AttendanceLog.fromJson(...)` continue to inherit the new metadata through the existing model parse path without needing a second parsing branch.
  </behavior>
  <action>
    1. Create `lib/models/employee_contract.dart` with:
       - a typed `EmployeeContract` enum
       - `dbValue`, `label`, and any compact display helper needed by downstream UI
       - one parse helper that normalizes user- and CSV-facing variants (`FULLTIME`, `fulltime`, `PARTIME`, `part-time`, etc.) into canonical enum values
       - safe fallback to `EmployeeContract.fulltime` for null/unknown values so the app can survive staged rollout while the SQL migration is being applied
    2. Create `lib/models/outlet_operating_mode.dart` with:
       - a typed `OutletOperatingMode` enum
       - canonical DB values `NORMAL` and `TWENTY_FOUR_HOUR`
       - human labels such as `Normal` and `24 Jam`
       - safe fallback to `OutletOperatingMode.normal` for null/unknown values
    3. Extend `lib/models/employee.dart`:
       - add an `employmentContract` field of type `EmployeeContract`
       - parse from `employment_contract`
       - emit `employment_contract` from `toJson()`
       - update `copyWith()` so downstream editors can change the contract without rebuilding the whole object manually
    4. Extend `lib/models/outlet.dart`:
       - add an `operatingMode` field of type `OutletOperatingMode`
       - parse from `operating_mode`
       - emit `operating_mode` from `toJson()`
    5. Create `test/models/workforce_metadata_test.dart` and prove:
       - enum parse helpers normalize canonical uppercase and common alias inputs
       - `Employee.fromJson()` and `Outlet.fromJson()` round-trip the new fields through `toJson()`
       - null or unknown incoming values degrade safely to `FULLTIME` / `NORMAL`
       - existing required fields still parse exactly as before
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/models/workforce_metadata_test.dart</automated>
  </verify>
  <done>The repo contains typed workforce metadata helpers and the shared employee/outlet models expose canonical contract and operating-mode fields without breaking existing joined consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Create the additive SQL migration for employee contract and outlet operating mode</name>
  <files>sql/phase_54_workforce_contract_outlet_mode_20260326.sql</files>
  <read_first>
    - sql/phase_37_employee_portal_foundation_20260322.sql (repo SQL style and public object naming)
    - sql/phase_39_portal_read_path_hardening_20260323.sql (recent additive SQL patch conventions)
    - .planning/STATE.md (production safety guard: explicit approval before DB changes)
  </read_first>
  <action>
    Create `sql/phase_54_workforce_contract_outlet_mode_20260326.sql` as an additive migration. Do not drop columns, rewrite tables destructively, or remove existing data.

    The migration should:
    - create enum types for employee contract and outlet operating mode if they do not already exist
    - add `employment_contract` to `public.employees` with the canonical enum type
    - add `operating_mode` to `public.outlets` with the canonical enum type
    - set safe constant defaults so existing rows read `FULLTIME` and `NORMAL` respectively
    - backfill any pre-existing null rows explicitly before tightening constraints
    - set both columns to `NOT NULL` only after the backfill step is safe
    - leave existing RLS, portal functions, and other unrelated SQL contracts untouched in Phase 54
    - keep the file easy to review in Supabase SQL Editor, with comments noting that it is additive-only and requires explicit approval before production execution
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'sql/phase_54_workforce_contract_outlet_mode_20260326.sql' -Pattern 'CREATE TYPE public.employee_contract','CREATE TYPE public.outlet_operating_mode','ALTER TABLE public.employees','ALTER TABLE public.outlets','SET NOT NULL' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <done>The repository contains one additive SQL migration that introduces canonical workforce metadata columns with safe defaults and no destructive schema changes.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/models/workforce_metadata_test.dart` passes.
- `sql/phase_54_workforce_contract_outlet_mode_20260326.sql` exists and clearly shows additive enum creation, column creation, backfill, and `NOT NULL` tightening.
- The only canonical DB values introduced by this plan are `FULLTIME`, `PARTTIME`, `NORMAL`, and `TWENTY_FOUR_HOUR`.
</verification>

<success_criteria>
- Employee rows have one typed, canonical contract value with a live-safe fallback path during rollout.
- Outlet rows have one typed, canonical operating mode value with a live-safe fallback path during rollout.
- Joined employee/outlet consumers can read those fields through the shared model layer without inventing a second parse contract.
- The production SQL change remains additive and awaits explicit user approval before application.
</success_criteria>

<output>
After completion, create `.planning/phases/54-workforce-contract-outlet-mode-foundation/54-01-SUMMARY.md`
</output>
