---
phase: 54-workforce-contract-outlet-mode-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/models/csv_import_result.dart
  - lib/services/csv_import_service.dart
  - test/services/csv_import_service_test.dart
  - lib/screens/admin/csv_import_screen.dart
autonomous: true
requirements: [CONTRACT-01]
must_haves:
  truths:
    - "Employee CSV import refuses legacy files that do not include a contract column."
    - "Accepted contract aliases normalize to canonical `FULLTIME` / `PARTTIME` values before insert."
    - "The preview and upload instructions make the contract requirement visible to operators before import."
    - "Batch-insert payloads persist `employment_contract` for every imported employee."
  artifacts:
    - path: "lib/models/csv_import_result.dart"
      provides: "CSV row models that carry raw and normalized contract data"
      contains: "contract"
    - path: "lib/services/csv_import_service.dart"
      provides: "Contract-aware CSV parse, validate, payload, and template generation"
      contains: "employment_contract"
    - path: "test/services/csv_import_service_test.dart"
      provides: "Regression coverage for contract header enforcement and alias normalization"
      contains: "contract"
    - path: "lib/screens/admin/csv_import_screen.dart"
      provides: "Operator-facing CSV wizard updated for the required contract column"
      contains: "Kontrak"
  key_links:
    - from: "lib/screens/admin/csv_import_screen.dart"
      to: "lib/services/csv_import_service.dart"
      via: "upload, preview, and template download all flow through the shared service"
      pattern: "CsvImportService"
    - from: "lib/services/csv_import_service.dart"
      to: "lib/models/csv_import_result.dart"
      via: "parsed rows and validation results carry contract state"
      pattern: "CsvRow|CsvRowValidation"
---

<objective>
Make the employee CSV onboarding flow contract-aware from template to insert payload.

Purpose: CSV import is an existing employee-creation path, so Phase 54 is incomplete if batch onboarding can still create employees without a stored contract.
Output: Updated CSV row models, contract-aware parsing/validation/template generation, stronger service tests, and operator-facing wizard copy/preview updates.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-CONTEXT.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-RESEARCH.md
@lib/models/employee_contract.dart
@lib/models/csv_import_result.dart
@lib/services/csv_import_service.dart
@lib/screens/admin/csv_import_screen.dart
@test/services/csv_import_service_test.dart
@lib/app.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the CSV import service contract to require, normalize, and persist employee contract values</name>
  <files>lib/models/csv_import_result.dart, lib/services/csv_import_service.dart, test/services/csv_import_service_test.dart</files>
  <behavior>
    - CSV headers require a fifth `kontrak` column; the old 4-column template is rejected clearly.
    - Contract values normalize common aliases such as `PARTIME`, `part-time`, and mixed-case inputs into canonical `FULLTIME` / `PARTTIME`.
    - Invalid contract values surface row-level validation errors naming the accepted choices.
    - Insert payloads include `employment_contract` for every valid row.
    - Template generation includes the contract column and valid sample values.
  </behavior>
  <action>
    1. Extend `lib/models/csv_import_result.dart` so parsed rows and validation results can carry:
       - the raw contract cell from the CSV
       - the normalized canonical contract when validation succeeds
    2. Update `lib/services/csv_import_service.dart`:
       - change expected headers from `nama,jabatan,gerai,foto_url` to `nama,jabatan,gerai,kontrak,foto_url`
       - parse the new `kontrak` cell into `CsvRow`
       - validate that every row provides a contract
       - normalize allowed aliases into canonical DB values using `EmployeeContract`
       - include `'employment_contract': normalizedContract.dbValue` in `buildInsertPayloads(...)`
       - update `generateTemplateCsv()` to include the new column and sample values
       - keep the service pure except for `insertAll(...)`
    3. Expand `test/services/csv_import_service_test.dart` with focused coverage for:
       - valid 5-column parsing
       - legacy 4-column header rejection
       - alias normalization to canonical contract values
       - invalid contract error reporting
       - payload generation with `employment_contract`
       - updated template header content
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/csv_import_service_test.dart</automated>
  </verify>
  <done>The CSV service now enforces a required contract column, normalizes aliases, and emits canonical `employment_contract` payloads for every valid row.</done>
</task>

<task type="auto">
  <name>Task 2: Update the CSV import wizard so operators can see and verify the new contract requirement</name>
  <files>lib/screens/admin/csv_import_screen.dart</files>
  <read_first>
    - lib/screens/admin/csv_import_screen.dart (upload copy, preview data table, template download)
    - lib/services/csv_import_service.dart (updated parse/validate/template contract from Task 1)
    - lib/app.dart (route stays admin-only)
  </read_first>
  <action>
    Update `lib/screens/admin/csv_import_screen.dart` so the operator-facing workflow matches the new service contract:
    - change the upload-step format text to advertise the required `kontrak` column
    - add a `Kontrak` column to the preview table
    - show normalized contract labels for valid rows and raw contract text for invalid rows
    - keep the existing error panel flow, but ensure header/contract errors remain clear when the CSV must be fixed and re-uploaded
    - keep template download routed through `CsvImportService.generateTemplateCsv()`
    - do not weaken the existing admin-only route gate for `/admin/csv-import`
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/csv_import_screen.dart</automated>
  </verify>
  <done>The CSV import wizard visibly requires the contract column and lets operators verify normalized contract values before import.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/csv_import_service_test.dart` passes.
- `C:\flutter\bin\flutter.bat analyze lib/screens/admin/csv_import_screen.dart` reports no issues.
- The service and screen both refer to the same 5-column contract-aware CSV schema.
</verification>

<success_criteria>
- Legacy CSV templates without the contract column fail clearly instead of silently defaulting values.
- Accepted contract aliases normalize into canonical `FULLTIME` / `PARTTIME` insert payloads.
- The preview table and upload instructions expose the contract requirement before import.
- Every imported employee row persists `employment_contract`.
</success_criteria>

<output>
After completion, create `.planning/phases/54-workforce-contract-outlet-mode-foundation/54-03-SUMMARY.md`
</output>
