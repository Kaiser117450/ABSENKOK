---
phase: 54-workforce-contract-outlet-mode-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/widgets/outlet_mode_badge.dart
  - test/widgets/outlet_mode_badge_test.dart
  - lib/screens/admin/admin_outlets_screen.dart
autonomous: true
requirements: [CONTRACT-02]
must_haves:
  truths:
    - "The outlet list shows each outlet's stored operating mode as a compact badge beside the existing active/non-active state."
    - "The add/edit outlet sheet always saves one explicit operating mode and defaults new outlets to `NORMAL`."
    - "Outlet mode remains a full-admin-only edit path because the existing route gate still blocks kepala gerai from `/admin/outlets`."
  artifacts:
    - path: "lib/widgets/outlet_mode_badge.dart"
      provides: "Reusable compact badge widget for outlet operating mode"
      exports: ["OutletModeBadge"]
    - path: "test/widgets/outlet_mode_badge_test.dart"
      provides: "Focused widget coverage for `NORMAL` and `TWENTY_FOUR_HOUR` badge rendering"
      contains: "OutletModeBadge"
    - path: "lib/screens/admin/admin_outlets_screen.dart"
      provides: "Outlet list and bottom sheet wired to the typed operating mode field"
      contains: "operating_mode"
  key_links:
    - from: "lib/screens/admin/admin_outlets_screen.dart"
      to: "lib/widgets/outlet_mode_badge.dart"
      via: "list-card mode badge rendering"
      pattern: "OutletModeBadge"
    - from: "lib/screens/admin/admin_outlets_screen.dart"
      to: "lib/models/outlet_operating_mode.dart"
      via: "sheet state and `operating_mode` save payload"
      pattern: "operating_mode|OutletOperatingMode"
    - from: "lib/app.dart"
      to: "lib/screens/admin/admin_outlets_screen.dart"
      via: "existing admin-only route guard for /admin/outlets"
      pattern: "/admin/outlets"
---

<objective>
Expose outlet operating mode through the existing admin outlet-management surfaces.

Purpose: Later overnight-safe attendance grouping needs one stored outlet mode, and operators need to audit or correct that value without leaving the current outlet admin flow.
Output: One reusable outlet-mode badge widget plus outlet list/sheet save wiring.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-CONTEXT.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-RESEARCH.md
@lib/models/outlet_operating_mode.dart
@lib/models/outlet.dart
@lib/screens/admin/admin_outlets_screen.dart
@lib/app.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a reusable outlet-mode badge widget and prove its two-state rendering contract</name>
  <files>lib/widgets/outlet_mode_badge.dart, test/widgets/outlet_mode_badge_test.dart</files>
  <behavior>
    - `OutletModeBadge` renders readable labels for both `NORMAL` and `TWENTY_FOUR_HOUR`.
    - The compact badge fits inside the current outlet card header beside the active-status pill.
    - The badge styling is informative and admin-facing, not a danger/warning signal.
  </behavior>
  <action>
    1. Create `lib/widgets/outlet_mode_badge.dart` as the single reusable outlet-mode badge surface.
    2. Create `test/widgets/outlet_mode_badge_test.dart` and cover:
       - `OutletOperatingMode.normal` renders a compact normal-mode label
       - `OutletOperatingMode.twentyFourHour` renders a compact 24-hour label
       - the widget uses the typed enum instead of raw DB strings
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/outlet_mode_badge_test.dart</automated>
  </verify>
  <done>The repository contains one shared outlet-mode badge widget with focused widget coverage for both supported mode states.</done>
</task>

<task type="auto">
  <name>Task 2: Update the outlet list and sheet so operating mode is visible and always persisted</name>
  <files>lib/screens/admin/admin_outlets_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_outlets_screen.dart (_OutletCard and _OutletSheet save flow)
    - lib/app.dart (existing admin-only route guard for /admin/outlets)
    - lib/models/outlet_operating_mode.dart
    - lib/widgets/outlet_mode_badge.dart
  </read_first>
  <action>
    In `lib/screens/admin/admin_outlets_screen.dart`:
    - render `OutletModeBadge` in the outlet card header so operators can audit `NORMAL` vs `24 Jam` from the list
    - add one direct segmented control to `_OutletSheet` for `NORMAL` vs `TWENTY_FOUR_HOUR`
    - default new outlets to `NORMAL`
    - initialize edited outlets from the stored `operatingMode`
    - include `'operating_mode': _selectedOperatingMode.dbValue` in both insert and update payloads
    - keep the existing password, active toggle, and reset-password flows intact
    - add only minimal helper copy clarifying that this mode will drive later overnight attendance grouping
    - do not add any kepala gerai edit path here; rely on the existing admin-only `/admin/outlets` route gate
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_outlets_screen.dart lib/widgets/outlet_mode_badge.dart</automated>
  </verify>
  <done>The outlet admin page shows and saves one explicit operating mode per outlet while preserving the existing full-admin-only access boundary.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/outlet_mode_badge_test.dart` passes.
- `C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_outlets_screen.dart lib/widgets/outlet_mode_badge.dart` reports no issues.
- The outlet list and sheet both use the same typed operating-mode source of truth.
</verification>

<success_criteria>
- Outlet cards visibly show `NORMAL` vs `24 Jam` for rollout review.
- The outlet sheet persists one explicit operating mode and defaults new outlets to `NORMAL`.
- Outlet mode editing stays full-admin only through the existing route gate.
</success_criteria>

<output>
After completion, create `.planning/phases/54-workforce-contract-outlet-mode-foundation/54-04-SUMMARY.md`
</output>
