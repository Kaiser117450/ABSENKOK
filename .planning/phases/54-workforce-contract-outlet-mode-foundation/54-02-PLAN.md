---
phase: 54-workforce-contract-outlet-mode-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - lib/widgets/employee_contract_badge.dart
  - test/widgets/employee_contract_badge_test.dart
  - lib/screens/admin/admin_employees_screen.dart
  - lib/screens/admin/archived_employees_screen.dart
autonomous: true
requirements: [CONTRACT-01]
must_haves:
  truths:
    - "The active employee list shows each employee's stored contract with a neutral, compact badge."
    - "The employee list can filter by all contracts, full-time only, or part-time only without disturbing the existing outlet filter behavior."
    - "The add/edit employee sheet always saves one contract value and defaults new employees to `PARTTIME` while existing employees keep their stored contract."
    - "Archived employee history keeps showing the last stored contract value for audit consistency."
  artifacts:
    - path: "lib/widgets/employee_contract_badge.dart"
      provides: "Reusable neutral contract badge widget"
      exports: ["EmployeeContractBadge"]
    - path: "test/widgets/employee_contract_badge_test.dart"
      provides: "Focused widget coverage for contract badge rendering"
      contains: "EmployeeContractBadge"
    - path: "lib/screens/admin/admin_employees_screen.dart"
      provides: "Employee list/filter/form contract UI and save wiring"
      contains: "employment_contract"
    - path: "lib/screens/admin/archived_employees_screen.dart"
      provides: "Archived employee audit surface that still shows the stored contract"
      contains: "EmployeeContractBadge"
  key_links:
    - from: "lib/screens/admin/admin_employees_screen.dart"
      to: "lib/widgets/employee_contract_badge.dart"
      via: "list-card contract badge rendering"
      pattern: "EmployeeContractBadge"
    - from: "lib/screens/admin/admin_employees_screen.dart"
      to: "lib/models/employee_contract.dart"
      via: "sheet state and `employment_contract` payload save path"
      pattern: "employment_contract|EmployeeContract"
    - from: "lib/screens/admin/archived_employees_screen.dart"
      to: "lib/widgets/employee_contract_badge.dart"
      via: "archived audit badge reuse"
      pattern: "EmployeeContractBadge"
---

<objective>
Expose the employee contract field through the existing employee-management and archive surfaces.

Purpose: Operators need to create, edit, review, and audit workforce contracts in the same employee flows they already use today.
Output: One reusable contract badge widget, contract-aware employee list/filter/form wiring, and archived-history visibility.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-CONTEXT.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-RESEARCH.md
@.planning/phases/54-workforce-contract-outlet-mode-foundation/54-VALIDATION.md
@lib/models/employee_contract.dart
@lib/models/employee.dart
@lib/screens/admin/admin_employees_screen.dart
@lib/screens/admin/archived_employees_screen.dart
@lib/app.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a reusable neutral contract badge widget and prove its rendering contract</name>
  <files>lib/widgets/employee_contract_badge.dart, test/widgets/employee_contract_badge_test.dart</files>
  <behavior>
    - `EmployeeContractBadge` renders `Full-time` and `Part-time` labels from the typed enum rather than raw DB strings.
    - The badge styling stays neutral and informative, not red/yellow exception styling.
    - The widget can render in compact list contexts without clipping the label text.
  </behavior>
  <action>
    1. Create `lib/widgets/employee_contract_badge.dart` as the single reusable badge surface for employee contract state.
    2. Keep the visual language compact and admin-facing:
       - neutral but strong tone
       - no danger/warning semantics
       - works inside list rows and archive cards
    3. Create `test/widgets/employee_contract_badge_test.dart` and cover:
       - `EmployeeContract.fulltime` renders a readable full-time label
       - `EmployeeContract.parttime` renders a readable part-time label
       - the widget does not depend on raw uppercase DB strings being passed in from the caller
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/widgets/employee_contract_badge_test.dart</automated>
  </verify>
  <done>The repository contains one shared contract badge widget with focused widget coverage for both contract states.</done>
</task>

<task type="auto">
  <name>Task 2: Update the active employee list and sheet so contract is visible, filterable, and always persisted</name>
  <files>lib/screens/admin/admin_employees_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_employees_screen.dart (_filtered, _EmployeeCard, _EmployeeSheet, save payload)
    - lib/app.dart (current admin vs kepala gerai route gates)
    - lib/models/employee_contract.dart (typed source of truth from Plan 01)
    - lib/widgets/employee_contract_badge.dart (badge contract from Task 1)
  </read_first>
  <action>
    In `lib/screens/admin/admin_employees_screen.dart`:
    - add contract-filter state with default "all"
    - extend `_filtered` so contract filtering composes cleanly with the existing outlet filter and search query
    - render `EmployeeContractBadge` on each employee card in a visible, compact position
    - keep the existing outlet and NFC behavior untouched
    - in `_EmployeeSheet`, add one direct segmented control for `FULLTIME` vs `PARTTIME` (do not use a dropdown)
    - when creating a new employee, initialize the sheet to `PARTTIME`
    - when editing an existing employee, initialize the sheet from the stored contract value
    - include `'employment_contract': _selectedContract.dbValue` in both insert and update payloads
    - preserve the current kepala gerai outlet lock and allow kepala gerai to edit the contract for employees within their existing outlet scope
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_employees_screen.dart lib/widgets/employee_contract_badge.dart</automated>
  </verify>
  <done>The active employee page shows contract badges, supports contract filtering, and the employee sheet always persists one explicit contract value.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the archived employee audit surface contract-aware</name>
  <files>lib/screens/admin/archived_employees_screen.dart</files>
  <read_first>
    - lib/screens/admin/archived_employees_screen.dart (archived card layout + restore flow)
    - lib/widgets/employee_contract_badge.dart
  </read_first>
  <action>
    Update the archived employee list to render the shared `EmployeeContractBadge` for each archived employee.

    Requirements:
    - keep the existing restore flow exactly as it is
    - do not change archive filtering or role checks
    - place the contract badge near the employee name/outlet metadata so audit review stays fast
    - ensure archived cards continue to show the stored contract even when the employee is inactive
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/archived_employees_screen.dart lib/widgets/employee_contract_badge.dart</automated>
  </verify>
  <done>The archived employee screen now preserves contract visibility for history and audit without changing the restore workflow.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/widgets/employee_contract_badge_test.dart` passes.
- `C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_employees_screen.dart lib/widgets/employee_contract_badge.dart` reports no issues.
- `C:\flutter\bin\flutter.bat analyze lib/screens/admin/archived_employees_screen.dart lib/widgets/employee_contract_badge.dart` reports no issues.
</verification>

<success_criteria>
- Employee cards show the stored contract with neutral badge styling.
- The employee page can filter all employees by contract state without losing the existing outlet-scoped behavior.
- The employee sheet always saves one explicit contract and defaults new records to `PARTTIME`.
- Archived employee history retains the stored contract value for audit review.
</success_criteria>

<output>
After completion, create `.planning/phases/54-workforce-contract-outlet-mode-foundation/54-02-SUMMARY.md`
</output>
