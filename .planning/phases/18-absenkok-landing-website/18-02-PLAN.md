---
phase: 18-absenkok-landing-website
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/components/Header.astro
  - src/components/Hero.astro
  - src/components/Features.astro
  - src/components/HowItWorks.astro
  - src/components/Download.astro
  - src/components/Footer.astro
  - src/pages/index.astro
autonomous: false
requirements: [WEB-01, WEB-02, WEB-03, WEB-04, WEB-05, WEB-06, WEB-10]
must_haves:
  truths:
    - "Hero section visible with CSS tablet mockup frame, tagline, and download CTA button"
    - "Feature showcase displays 4 features with inline SVG icons and Bahasa Indonesia descriptions"
    - "How It Works section shows 3 numbered steps explaining app usage"
    - "Download APK button links to GitHub Releases URL"
    - "Footer shows 'Akmal' developer credit"
    - "All sections responsive across mobile (375px), tablet (768px), and desktop (1440px)"
  artifacts:
    - path: "src/components/Hero.astro"
      provides: "Hero section with tablet mockup frame, tagline, download CTA"
      min_lines: 30
    - path: "src/components/Features.astro"
      provides: "4 feature cards with inline SVG icons"
      min_lines: 40
    - path: "src/components/HowItWorks.astro"
      provides: "3 numbered steps"
      min_lines: 25
    - path: "src/components/Download.astro"
      provides: "Download CTA section with GitHub Releases link"
      min_lines: 15
    - path: "src/components/Footer.astro"
      provides: "Footer with Akmal credit and copyright"
      contains: "Akmal"
    - path: "src/components/Header.astro"
      provides: "Top navigation with logo and anchor links"
    - path: "src/pages/index.astro"
      provides: "Page assembly importing all section components"
  key_links:
    - from: "src/pages/index.astro"
      to: "all components"
      via: "import statements"
      pattern: "import.*from.*components/"
    - from: "src/components/Hero.astro"
      to: "GitHub Releases"
      via: "anchor href"
      pattern: "github\\.com.*releases"
    - from: "src/components/Footer.astro"
      to: "developer credit"
      via: "text content"
      pattern: "Akmal"
    - from: "src/components/Download.astro"
      to: "GitHub Releases"
      via: "anchor href"
      pattern: "github\\.com.*releases"
---

<objective>
Create all content section components (Header, Hero, Features, How It Works, Download CTA, Footer) with full Bahasa Indonesia copy, assemble them into the index page, verify the build ships zero JS, and visually verify the responsive design.

Purpose: Deliver the complete marketing landing page content that visitors will see — the hero with tablet mockup, feature showcase, usage steps, download button, and developer credit.
Output: A complete, buildable, single-page marketing website with all 5 sections, ready for Vercel deployment.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-absenkok-landing-website/18-RESEARCH.md
@.planning/phases/18-absenkok-landing-website/18-01-SUMMARY.md

**CRITICAL: All file operations happen in `C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\`**
NOT in the Flutter project directory. cd to the website directory before every command.

<interfaces>
<!-- From Plan 18-01: BaseLayout contract the executor must use -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description: string;
}
```
Usage: Wrap page content in `<BaseLayout title="..." description="...">` with `<slot />` for children.

From src/styles/global.css — available Tailwind brand classes:
```
bg-brand-primary (#DC2626)    text-brand-primary
bg-brand-dark (#B91C1C)       text-brand-dark
bg-brand-light (#FEE2E2)      text-brand-light
bg-brand-accent (#F59E0B)     text-brand-accent
bg-brand-accent-dark          text-brand-accent-dark
bg-brand-accent-light         text-brand-accent-light
bg-brand-surface (#F9FAFB)    border-brand-border (#E5E7EB)
text-brand-text (#111827)     text-brand-text-secondary (#6B7280)
text-brand-text-muted (#9CA3AF)
font-sans (Inter)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all section components + assemble page + verify build</name>
  <files>
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\components\Header.astro
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\components\Hero.astro
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\components\Features.astro
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\components\HowItWorks.astro
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\components\Download.astro
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\components\Footer.astro
    C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\pages\index.astro
  </files>
  <action>
Create all 6 section components and update index.astro to assemble the full page. All files in `C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\src\`.

**ALL copy MUST be in Bahasa Indonesia.** Use the exact copy from the research, or improve it — but never English.

**Design language:** Clean white minimalist (Apple/Stripe style). Use brand colors sparingly — primary red for CTAs and accents, white/gray for backgrounds. Generous whitespace, large typography, subtle shadows.

---

**Component 1: `src/components/Header.astro`**

Sticky top navigation bar with:
- ABSENKOK brand text/logo on the left (bold, text-brand-primary for "ABSEN", text-brand-text for "KOK")
- Anchor links on the right: "Fitur", "Cara Kerja", "Download" — these link to `#fitur`, `#cara-kerja`, `#download` section IDs
- White background with subtle bottom border, `sticky top-0 z-50`
- Mobile: Hide nav links (they can scroll naturally), show only logo
- Desktop: Show all nav links
- Use `max-w-7xl mx-auto px-6` for consistent page width

---

**Component 2: `src/components/Hero.astro`** (WEB-01)

Two-column layout (stacked on mobile):
- LEFT column: Headline + subtitle + CTA button
  - Headline: `"Absensi NFC untuk Restoran Modern"` — use `text-4xl font-extrabold sm:text-5xl lg:text-6xl`
  - Highlight "Restoran Modern" in `text-brand-primary`
  - Subtitle: `"Tap kartu, hadir tercatat. ABSENKOK mengelola kehadiran karyawan dengan NFC, jadwal shift otomatis, dan laporan real-time."`
  - CTA button: `"Download APK"` — `rounded-full bg-brand-primary text-white px-8 py-3.5 font-semibold shadow-lg hover:bg-brand-dark transition-colors`
  - CTA links to: `https://github.com/user/absenkok/releases/latest` (placeholder URL)
  - Add a download arrow SVG icon inline next to button text

- RIGHT column: CSS-only tablet mockup frame
  - Outer frame: `rounded-[2rem] bg-gray-900 p-3 shadow-2xl ring-1 ring-white/10`
  - Inner screen: `rounded-[1.5rem] overflow-hidden bg-brand-surface` with a visual placeholder
  - Inside the screen: Create a mini representation of the app UI using colored divs — a header bar (bg-brand-primary), a grid of colored cells representing the schedule (small rounded rectangles in brand colors), and a bottom nav bar. This is more visually interesting than a blank placeholder.
  - Decorative gradient glow behind tablet: `absolute -inset-4 -z-10 rounded-[2.5rem] bg-gradient-to-br from-brand-primary/20 via-brand-accent/10 to-transparent blur-2xl`

- Section: `py-20 lg:py-32`, grid: `grid-cols-1 lg:grid-cols-2 gap-12 lg:items-center`
- Container: `max-w-7xl mx-auto px-6 lg:px-8`

---

**Component 3: `src/components/Features.astro`** (WEB-02)

Section with `id="fitur"` (for anchor link from Header).

Define 4 features as a const array in the frontmatter:
```
1. "Absensi NFC Otomatis" — "Karyawan cukup tap kartu NFC di tablet — hadir langsung tercatat. Tanpa username, tanpa password."
2. "Jadwal Shift Cerdas" — "Atur jadwal Pagi, Siang, Sore untuk semua karyawan dalam satu tampilan grid mingguan."
3. "Laporan Real-Time" — "Pantau siapa yang sudah masuk, sedang break, atau sudah pulang — langsung dari dashboard admin."
4. "Mode Kiosk 24/7" — "Tablet berjalan non-stop sebagai kiosk absensi. Tidak perlu pengawasan, tidak perlu restart."
```

Each feature card:
- Icon: Create a unique inline SVG icon for each feature (NFC tap, calendar, chart, device). Each SVG should be simple — 24x24 viewBox, stroke-based, using `currentColor`.
- Icon container: `h-12 w-12 rounded-xl bg-brand-light text-brand-primary flex items-center justify-center`
- Title: `text-lg font-semibold text-brand-text`
- Description: `text-sm leading-6 text-brand-text-secondary`
- Card: `rounded-2xl bg-white p-8 shadow-sm ring-1 ring-brand-border hover:shadow-md transition-shadow`

Grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8`
Section background: `bg-brand-surface py-20 lg:py-28`
Section header: "Fitur Unggulan" + subtitle "Semua yang dibutuhkan untuk mengelola kehadiran karyawan restoran."

---

**Component 4: `src/components/HowItWorks.astro`** (WEB-03)

Section with `id="cara-kerja"`.

3 steps:
```
Step 1: "Pasang Tablet" — "Letakkan tablet Android di area masuk restoran. Buka ABSENKOK dalam mode kiosk."
Step 2: "Tap Kartu NFC" — "Karyawan tap kartu NFC masing-masing ke tablet saat masuk dan pulang kerja."
Step 3: "Pantau & Kelola" — "Admin lihat dashboard kehadiran real-time, atur jadwal shift, dan export laporan PDF."
```

Each step:
- Number circle: `h-16 w-16 rounded-full bg-brand-primary text-white text-2xl font-bold shadow-lg mx-auto flex items-center justify-center`
- Title: `text-xl font-semibold text-brand-text mt-6`
- Description: `text-base leading-7 text-brand-text-secondary mt-3`
- Layout: `text-center`

Grid: `grid-cols-1 md:grid-cols-3 gap-12`
Section: `bg-white py-20 lg:py-28`
Section header: "Cara Kerja" centered

---

**Component 5: `src/components/Download.astro`** (WEB-04)

Section with `id="download"`.

Full-width CTA section:
- Background: `bg-brand-primary py-20 lg:py-28`
- Large headline (white): `"Siap Digitalisasi Absensi Restoran Anda?"`
- Subtitle (white/70): `"Download ABSENKOK gratis dan mulai kelola kehadiran karyawan dengan NFC."`
- CTA button: `bg-white text-brand-primary rounded-full px-10 py-4 text-lg font-bold shadow-xl hover:bg-gray-50 transition-colors`
- Button text: `"Download APK Gratis"` with download arrow icon
- Button link: `https://github.com/user/absenkok/releases/latest` (placeholder)
- Container: `max-w-3xl mx-auto px-6 text-center`

---

**Component 6: `src/components/Footer.astro`** (WEB-05)

- Dark footer: `bg-gray-900 py-12`
- Copyright: `"© {currentYear} ABSENKOK. Sistem Absensi NFC untuk Restoran."`  — `text-sm text-gray-400`
- Developer credit: `"Dibuat oleh Akmal"` — `text-xs text-gray-500` with "Akmal" in `font-semibold text-gray-400`
- Use `new Date().getFullYear()` in frontmatter for dynamic year
- Layout: `flex flex-col items-center gap-4`

---

**Update: `src/pages/index.astro`**

Replace the placeholder content with the full page assembly:
```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import Features from '../components/Features.astro';
import HowItWorks from '../components/HowItWorks.astro';
import Download from '../components/Download.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout
  title="ABSENKOK — Sistem Absensi NFC untuk Restoran"
  description="Kelola kehadiran karyawan restoran dengan NFC tap, jadwal shift otomatis, dan laporan real-time. Download gratis."
>
  <Header />
  <main>
    <Hero />
    <Features />
    <HowItWorks />
    <Download />
  </main>
  <Footer />
</BaseLayout>
```

---

After creating all files, run build and verify:

```powershell
Set-Location "C:\Users\HYPE R Series\Desktop\projekan\absenkok-website"
npx astro build
```

Then verify all content sections exist in built HTML:
```powershell
Select-String -Path "dist\index.html" -Pattern "Restoran Modern" -SimpleMatch
Select-String -Path "dist\index.html" -Pattern "Fitur Unggulan" -SimpleMatch
Select-String -Path "dist\index.html" -Pattern "Cara Kerja" -SimpleMatch
Select-String -Path "dist\index.html" -Pattern "github.com" -SimpleMatch
Select-String -Path "dist\index.html" -Pattern "Akmal" -SimpleMatch
```

Verify zero JS shipped:
```powershell
$jsFiles = Get-ChildItem "dist\_astro\*.js" -ErrorAction SilentlyContinue
if ($jsFiles) { Write-Error "JS files found — should be zero!" } else { Write-Host "✓ Zero JS shipped" }
```

Verify sitemap:
```powershell
Test-Path "dist\sitemap-index.xml"
```

Git commit:
```powershell
git add -A
git commit -m "feat(18-02): add all content sections + page assembly"
```
  </action>
  <verify>
    <automated>Set-Location "C:\Users\HYPE R Series\Desktop\projekan\absenkok-website"; npx astro build; if ($LASTEXITCODE -eq 0) { $html = Get-Content "dist\index.html" -Raw; $checks = @("Restoran Modern","Fitur Unggulan","Cara Kerja","github.com","Akmal","lang=""id"""); $allPass = $true; foreach ($c in $checks) { if ($html -notmatch [regex]::Escape($c)) { Write-Host "MISSING: $c"; $allPass = $false } else { Write-Host "FOUND: $c" } }; $js = Get-ChildItem "dist\_astro\*.js" -ErrorAction SilentlyContinue; if ($js) { Write-Host "FAIL: JS files found"; $allPass = $false } else { Write-Host "OK: Zero JS" }; if ($allPass) { Write-Host "ALL CHECKS PASSED" } else { Write-Error "Some checks failed" } } else { Write-Error "Build failed" }</automated>
  </verify>
  <done>
    - Build succeeds with all 6 section components + page assembly
    - dist/index.html contains: "Restoran Modern" (hero), "Fitur Unggulan" (features), "Cara Kerja" (how-it-works), "github.com" (download link), "Akmal" (footer credit), lang="id"
    - Zero .js files in dist/_astro/
    - Sitemap-index.xml exists
    - All copy in Bahasa Indonesia
    - Responsive Tailwind classes applied (grid-cols-1, sm:, md:, lg: breakpoints)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete ABSENKOK marketing landing website with all 5 content sections:
1. Header with sticky navigation and anchor links
2. Hero section with CSS tablet mockup frame, headline, and download CTA
3. Features section with 4 feature cards and inline SVG icons
4. How It Works section with 3 numbered steps
5. Download CTA section with GitHub Releases link
6. Footer with "Akmal" developer credit

All copy in Bahasa Indonesia, clean white minimalist design (Apple/Stripe style), responsive layout.
  </what-built>
  <how-to-verify>
1. Start the dev server:
   ```powershell
   Set-Location "C:\Users\HYPE R Series\Desktop\projekan\absenkok-website"
   npx astro dev
   ```

2. Open http://localhost:4321 in Chrome

3. **Desktop check (1440px):**
   - Header: ABSENKOK logo + Fitur/Cara Kerja/Download nav links visible
   - Hero: Two-column layout — text left, tablet mockup right
   - Features: 4 cards in a row
   - How It Works: 3 steps in a row
   - Download: Full-width red CTA section
   - Footer: Dark footer with "Akmal" credit

4. **Mobile check (375px) — Chrome DevTools → Toggle device toolbar:**
   - Header: Logo only, nav links hidden
   - Hero: Stacked — text on top, tablet below
   - Features: Single column cards
   - How It Works: Single column steps
   - All text readable, no horizontal overflow

5. **Tablet check (768px):**
   - Features: 2 columns
   - Other sections adapt gracefully

6. **Functional checks:**
   - Click "Fitur" nav link → scrolls to features section
   - Click "Download APK" button → opens GitHub Releases URL (will 404 — that's expected for placeholder)
   - Scroll all the way down → see "Dibuat oleh Akmal" in footer

7. **Performance:** Page should feel instant — zero JS loading, just static HTML + CSS

8. **(Optional) Deploy to Vercel:**
   If you want to deploy now, run:
   ```powershell
   npx vercel --yes
   ```
   Verify the live URL loads correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if the design looks good, or describe any issues to fix (layout problems, copy changes, color adjustments, etc.)</resume-signal>
</task>

</tasks>

<verification>
Run from `C:\Users\HYPE R Series\Desktop\projekan\absenkok-website\`:

1. `npx astro build` — exit 0
2. `Select-String -Path "dist\index.html" -Pattern "Restoran Modern"` — hero present (WEB-01)
3. `Select-String -Path "dist\index.html" -Pattern "Fitur Unggulan"` — features present (WEB-02)
4. `Select-String -Path "dist\index.html" -Pattern "Cara Kerja"` — how-it-works present (WEB-03)
5. `Select-String -Path "dist\index.html" -Pattern "github.com"` — download link present (WEB-04)
6. `Select-String -Path "dist\index.html" -Pattern "Akmal"` — footer credit present (WEB-05)
7. `Get-ChildItem "dist\_astro\*.js" -ErrorAction SilentlyContinue` — empty (WEB-08)
8. `Test-Path "dist\sitemap-index.xml"` — True (WEB-09)
9. Visual check in browser at 375px, 768px, 1440px — responsive (WEB-06)
10. `Select-String -Path "dist\index.html" -Pattern 'lang="id"'` — Bahasa Indonesia (WEB-07)
</verification>

<success_criteria>
- All 6 section components created with full Bahasa Indonesia marketing copy
- Page assembles correctly — Header → Hero → Features → HowItWorks → Download → Footer
- Build produces clean static HTML with zero JavaScript
- Responsive layout verified at mobile, tablet, and desktop breakpoints
- Download buttons link to GitHub Releases placeholder URL
- "Akmal" developer credit visible in footer
- Human approves visual design quality
- Website ready for Vercel deployment (WEB-10)
</success_criteria>

<output>
After completion, create `.planning/phases/18-absenkok-landing-website/18-02-SUMMARY.md`
</output>
