---
phase: 48-secure-signing-artifact-discipline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - android/key.properties.example
  - android/app/build.gradle.kts
  - docs/android-release-contract.md
autonomous: false
requirements: [REL-01]
must_haves:
  truths:
    - "The `release` build no longer depends on the debug signing config anywhere in tracked code."
    - "Private signing inputs live in `android/key.properties` plus a private upload keystore, and neither secret is tracked in git."
    - "A release build fails with a clear message when private signing inputs are missing instead of silently falling back to debug signing."
  artifacts:
    - path: ".gitignore"
      provides: "Privacy guardrails for `android/key.properties` and common keystore file patterns."
      contains: "android/key.properties"
    - path: "android/key.properties.example"
      provides: "Tracked placeholder schema for the private release-signing inputs."
      contains: "storePassword="
    - path: "android/app/build.gradle.kts"
      provides: "Release signing wired through `key.properties` and a dedicated `release` signing config."
      contains: "key.properties"
    - path: "docs/android-release-contract.md"
      provides: "Operator-facing note that v7.0 release signing uses a private upload key instead of debug signing."
      contains: "upload key"
  key_links:
    - from: ".gitignore"
      to: "android/key.properties.example"
      via: "the tracked example is safe to commit while the real signing file must stay private"
      pattern: "android/key.properties"
    - from: "android/app/build.gradle.kts"
      to: "android/key.properties.example"
      via: "Gradle and the example file must agree on the same signing property names"
      pattern: "keyAlias"
---

<objective>
Replace the current debug-signing fallback with a private upload-key scaffold that is safe to keep in a shared repo.

Purpose: REL-01 is about changing the release-signing contract itself before any artifact-lane work starts.
Output: Tracked privacy guards, a tracked signing-input example file, and Gradle release-signing wiring that fails clearly when private inputs are absent.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-secure-signing-artifact-discipline/48-RESEARCH.md
@.planning/phases/48-secure-signing-artifact-discipline/48-VALIDATION.md
@.gitignore
@android/app/build.gradle.kts
@docs/android-release-contract.md

<interfaces>
Current measured gaps on 2026-03-23:
```text
android/key.properties -> missing
android/key.properties.example -> missing
release buildType -> signingConfig = signingConfigs.getByName("debug")
```

Current shrink baseline that must stay intact:
```kotlin
isMinifyEnabled = true
isShrinkResources = true
```
</interfaces>

<constraints>
- do not create or commit a real keystore or real `android/key.properties`
- do not change app id, version metadata, or the pinned Phase 47 toolchain versions
- do not require operators to edit tracked Gradle code with machine-local paths
- keep this plan focused on signing safety and wiring; full release-lane commands belong to later plans
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the repo-safe private-signing scaffold</name>
  <files>.gitignore, android/key.properties.example, docs/android-release-contract.md</files>
  <read_first>
    - .planning/phases/48-secure-signing-artifact-discipline/48-RESEARCH.md
    - .gitignore
    - docs/android-release-contract.md
  </read_first>
  <action>
    Add the tracked files and ignore rules needed before wiring release signing.

    Required behavior:
    1. Extend `.gitignore` to ignore `android/key.properties` plus common upload-keystore file names or extensions used by Android release signing.
    2. Create `android/key.properties.example` with placeholder values for `storePassword`, `keyPassword`, `keyAlias`, and `storeFile`.
    3. Update `docs/android-release-contract.md` with a concise note that Phase 48 uses a private upload key and the real `key.properties` must stay out of source control.
    4. Keep the tracked example intentionally minimal; exact operator bootstrap steps belong to Phase 49.

    Scope guard:
    - do not add any real passwords, aliases, or machine-local keystore paths
    - do not add a full release runbook yet
    - do not touch preflight or artifact-generation scripts in this task
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path '.gitignore','android/key.properties.example','docs/android-release-contract.md' -Pattern 'android/key.properties','storePassword','keyAlias','upload key'"</automated>
  </verify>
  <done>The repo documents the signing-input shape safely and protects the real signing files from accidental commits.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire Gradle release signing to the private upload key and remove the debug fallback</name>
  <files>android/app/build.gradle.kts</files>
  <read_first>
    - android/app/build.gradle.kts
    - android/key.properties.example
    - .planning/phases/48-secure-signing-artifact-discipline/48-RESEARCH.md
  </read_first>
  <action>
    Update `android/app/build.gradle.kts` so tracked code only supports private release signing.

    Required behavior:
    1. Load `android/key.properties` before the `android {}` block using the Kotlin DSL pattern from current Flutter Android release docs.
    2. Create a `signingConfigs.release` entry that reads `keyAlias`, `keyPassword`, `storeFile`, and `storePassword` from the private properties file.
    3. Change `buildTypes.release` to use the `release` signing config instead of `signingConfigs.getByName("debug")`.
    4. Throw a clear error for release builds when the private properties file or referenced keystore file is missing, instead of silently falling back to debug signing.
    5. Preserve the current shrink settings and APK output naming rule unless they are directly incompatible with release signing.

    Scope guard:
    - do not add machine-local secret defaults
    - do not change debug/profile signing behavior
    - do not add build commands or release staging logic in this plan
  </action>
  <verify>
    <automated>powershell -Command "$content = Get-Content 'android/app/build.gradle.kts' -Raw; if ($content -notmatch 'key\\.properties' -or $content -notmatch 'create\\(\"release\"\\)' -or $content -notmatch 'GradleException') { throw 'missing release signing wiring' }; if ($content -match 'signingConfigs\\.getByName\\(\"debug\"\\)') { throw 'release still uses debug signing' }"</automated>
  </verify>
  <done>The tracked Gradle config now requires a private upload key for `release` and can no longer succeed on the debug keystore.</done>
</task>

</tasks>

<verification>
- `.gitignore` protects the real signing files while `android/key.properties.example` captures the required schema
- `android/app/build.gradle.kts` loads private signing inputs and no longer references debug signing for `release`
- a missing signing file now produces a clear failure instead of an unsafe fallback
</verification>

<success_criteria>
- REL-01 is satisfied at the wiring level: `release` is structurally tied to a private upload key
- the repo remains safe to share because only placeholder signing files are tracked
- later release-lane work can assume secure signing inputs instead of debug-key behavior
</success_criteria>

<output>
After completion, create `.planning/phases/48-secure-signing-artifact-discipline/48-01-SUMMARY.md`
</output>
