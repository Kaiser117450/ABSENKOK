---
phase: 03-overlay-pill-implementation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/models/overlay_pill_state.dart
  - test/models/overlay_pill_state_test.dart
autonomous: true
requirements:
  - REQ-M2-01

must_haves:
  truths:
    - "Overlay payload supports explicit `idle` and `event` modes with a stable, versioned schema"
    - "Legacy `outlet|HH:mm` payloads still render safely as idle state during migration"
    - "Event payloads include expiry metadata so UI can return to persistent idle without dismissing overlay"
  artifacts:
    - path: "lib/models/overlay_pill_state.dart"
      provides: "Typed overlay payload contract + parser/serializer helpers"
      contains: "OverlayPillMode"
    - path: "test/models/overlay_pill_state_test.dart"
      provides: "Executable contract tests for v1 JSON + legacy payload fallback"
      contains: "group('OverlayPillState'"
  key_links:
    - from: "OverlayPillState.fromRaw"
      to: "overlay_task.dart overlayListener payload decode"
      via: "JSON-v1 first, legacy delimiter fallback"
      pattern: "fromRaw"
    - from: "OverlayPillState.toWirePayload"
      to: "KioskBackgroundService FlutterOverlayWindow.shareData()"
      via: "Serialized string payload transport"
      pattern: "toWirePayload"
---

<objective>
Create the overlay state contract before UI/service refactors so all downstream plans implement against one payload schema.

Purpose: REQ-M2-01 requires persistent idle/event transitions and permission-safe behavior. A typed contract prevents schema drift between app isolate and overlay isolate.

Output: New `OverlayPillState` model with parser/serializer plus unit tests covering v1 JSON payloads and legacy delimiter fallback.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overlay-pill-implementation/03-CONTEXT.md
@.planning/phases/03-overlay-pill-implementation/03-RESEARCH.md
@lib/overlay_task.dart
@lib/services/kiosk_background_service.dart

<interfaces>
Current payload transport (legacy, delimiter string):
```dart
// kiosk_background_service.dart
await FlutterOverlayWindow.shareData('$name|$t');

// overlay_task.dart
final parts = raw.toString().split('|');
if (parts.isNotEmpty && parts[0].isNotEmpty) _outletName = parts[0];
if (parts.length > 1 && parts[1].isNotEmpty) _timeStr = parts[1];
```

Target contract from research:
```json
{
  "v": 1,
  "mode": "idle|event",
  "outlet": "Outlet Name",
  "time": "HH:mm",
  "attendanceType": "masuk|break|kembali|pulang|sakit|izin",
  "accentHex": "#22C55E",
  "eventUntilEpochMs": 0,
  "expanded": true
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write contract tests for overlay payload parsing and serialization</name>
  <files>test/models/overlay_pill_state_test.dart</files>
  <action>
Create a dedicated test file for overlay payload contract behavior. Write tests first (RED) for:
1. Parsing valid v1 JSON payload into typed state (idle + event variants).
2. Parsing legacy `outlet|HH:mm` payload into safe idle fallback state.
3. Handling malformed JSON without throwing (fallback to safe defaults).
4. Serializing typed state back to wire payload string with `v:1` fields.
5. Event timeout helper behavior (event considered expired when `eventUntilEpochMs` is in the past).

Keep tests pure Dart/unit-level (no widget pump, no platform channels).
  </action>
  <verify>
    <automated>flutter test test/models/overlay_pill_state_test.dart</automated>
  </verify>
  <done>
    - Test file exists with coverage for JSON v1 parse, legacy fallback, malformed input, and serialization
    - Tests fail before model implementation (expected RED state)
    - Test names clearly map to REQ-M2-01 transition requirements
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement OverlayPillState model to satisfy contract tests</name>
  <files>lib/models/overlay_pill_state.dart</files>
  <action>
Add a new model file defining:
- `enum OverlayPillMode { idle, event }`
- Immutable `OverlayPillState` with fields for outlet, time, attendance type, accent color hex, mode, event expiry, and expanded hint
- `OverlayPillState.fromRaw(String raw)` that supports JSON v1 and legacy delimiter fallback
- `toMap()` / `toWirePayload()` for outbound transport
- Safe defaults (`Absensi Enakko`, `--:--`, idle mode) when payload is missing/invalid
- Small helper for event expiry checks used by overlay state-machine logic

Keep this model free of Flutter UI dependencies so both service and overlay isolate can use it directly.
  </action>
  <verify>
    <automated>flutter test test/models/overlay_pill_state_test.dart</automated>
  </verify>
  <done>
    - All tests in `overlay_pill_state_test.dart` pass
    - `OverlayPillState.fromRaw` accepts both JSON v1 and legacy delimiter payloads
    - Serialization output includes `v:1` and mode/type/accent/time fields
    - No UI-specific imports are introduced in the model file
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `flutter test test/models/overlay_pill_state_test.dart` passes.
2. `flutter analyze lib/models/overlay_pill_state.dart` reports no errors.
3. `rg -n "OverlayPillMode|fromRaw|toWirePayload" lib/models/overlay_pill_state.dart` returns expected symbols.
</verification>

<success_criteria>
- A single shared payload contract exists and is tested.
- Legacy payload callers remain backward-compatible during migration.
- Downstream plans can implement persistent idle/event behavior without redefining schema.
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-pill-implementation/03-01-SUMMARY.md`
with tests added, model API surface, and verification results.
</output>
