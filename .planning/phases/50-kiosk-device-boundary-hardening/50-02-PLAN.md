---
phase: 50-kiosk-device-boundary-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/models/kiosk_device.dart
  - lib/screens/admin/admin_dashboard_screen.dart
  - test/phase50/kiosk_device_model_test.dart
autonomous: true
requirements: [SECSAFE-01]
must_haves:
  truths:
    - "Malformed, null, empty, or short kiosk UUID data does not crash the device-card or dashboard UI."
    - "Nickname remains the first-choice device label when present."
    - "The dashboard can skip or degrade gracefully on malformed kiosk rows instead of failing the whole section."
  artifacts:
    - path: "lib/models/kiosk_device.dart"
      provides: "Safe kiosk-device parsing and display fallback logic"
      exports: ["KioskDevice"]
    - path: "lib/screens/admin/admin_dashboard_screen.dart"
      provides: "Defensive kiosk-device row loading that does not let one bad row break the section"
      contains: "_loadKioskDevices"
    - path: "test/phase50/kiosk_device_model_test.dart"
      provides: "Regression coverage for malformed or short kiosk UUID inputs"
      contains: "KioskDevice Phase 50 safety"
  key_links:
    - from: "lib/screens/admin/admin_dashboard_screen.dart"
      to: "lib/models/kiosk_device.dart"
      via: "Row-by-row KioskDevice.fromJson parsing in _loadKioskDevices()"
      pattern: "KioskDevice.fromJson"
    - from: "lib/widgets/kiosk_device_card.dart"
      to: "lib/models/kiosk_device.dart"
      via: "Device name rendering through device.displayName"
      pattern: "device.displayName"
    - from: "test/phase50/kiosk_device_model_test.dart"
      to: "lib/models/kiosk_device.dart"
      via: "Focused regression coverage for malformed UUID parsing and fallback labels"
      pattern: "displayName|fromJson"
---

<objective>
Make kiosk-device parsing and display fallbacks impossible to crash on malformed UUID data.

Purpose: Close the remaining client-side stability gap in the dashboard/device surfaces while keeping the existing label contract recognizable.
Output: Safe model parsing, defensive dashboard row loading, and focused regression tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-kiosk-device-boundary-hardening/50-RESEARCH.md
@.planning/phases/50-kiosk-device-boundary-hardening/50-VALIDATION.md
@lib/models/kiosk_device.dart
@lib/screens/admin/admin_dashboard_screen.dart
@lib/widgets/kiosk_device_card.dart
@test/phase32/kiosk_device_model_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression coverage and safe parsing for malformed kiosk UUID data</name>
  <files>lib/models/kiosk_device.dart, test/phase50/kiosk_device_model_test.dart</files>
  <behavior>
    - `KioskDevice.fromJson` does not throw when `device_uuid` is null, empty, or non-UUID text; it degrades to a safe string value.
    - `KioskDevice.fromJson` does not throw when `last_heartbeat_at` is malformed; it degrades to `null`.
    - `displayName` prefers a trimmed nickname when present.
    - `displayName` returns a safe fallback for short or empty device UUID values and never calls `substring` out of range.
    - Existing long device UUID values still render a recognizable short label.
  </behavior>
  <action>
    1. Create `test/phase50/kiosk_device_model_test.dart` with group name `KioskDevice Phase 50 safety` and cover:
       - null `device_uuid` -> no throw, safe fallback label
       - empty `device_uuid` -> no throw, safe fallback label
       - short `device_uuid` such as `abc` -> no throw, safe fallback label
       - malformed `last_heartbeat_at` string -> parses as `null` instead of crashing
       - nickname with surrounding whitespace -> trimmed nickname wins over the UUID fallback
    2. Update `lib/models/kiosk_device.dart` to support those tests:
       - Parse `device_uuid` defensively with `toString()` fallback instead of a hard cast
       - Add a small safe timestamp parser that returns `null` on `FormatException`
       - Replace the raw `substring(0, 8)` path with a bounds-safe helper that preserves the familiar prefix when enough characters exist, but never throws on short values
    3. Run the new focused test file RED first, then GREEN after implementation.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/phase50/kiosk_device_model_test.dart</automated>
  </verify>
  <done>The kiosk-device model safely parses and labels malformed UUID/timestamp data, and the regression test file proves the crash path is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Harden dashboard kiosk-device loading so one bad row cannot break the whole section</name>
  <files>lib/screens/admin/admin_dashboard_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_dashboard_screen.dart (_loadKioskDevices and the kiosk-device state wiring)
    - lib/models/kiosk_device.dart (safe parsing contract from Task 1)
  </read_first>
  <action>
    In `_loadKioskDevices()`:
    - Replace the single bulk `.map(...).toList()` parse with row-by-row parsing.
    - Wrap each `KioskDevice.fromJson(...)` call in `try/catch`.
    - On a malformed row, log it with `debugPrint('[Dashboard] Skipping malformed kiosk device row: ...')` and continue loading the rest of the list.
    - Keep the existing section alive even when one row is bad.
    - Do not change the UI contract or realtime subscription structure outside the defensive parsing path.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/models/kiosk_device.dart lib/screens/admin/admin_dashboard_screen.dart</automated>
  </verify>
  <done>The admin dashboard can continue rendering the kiosk-device section even when Supabase returns one malformed kiosk row.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/phase50/kiosk_device_model_test.dart` passes.
- `C:\flutter\bin\flutter.bat analyze lib/models/kiosk_device.dart lib/screens/admin/admin_dashboard_screen.dart` reports no issues.
- The only device-label fallback path in the model is bounds-safe.
</verification>

<success_criteria>
- Short, empty, null, or malformed kiosk UUID data does not crash the dashboard/device surface.
- Existing nickname behavior remains intact.
- A single malformed row no longer breaks the full kiosk-device list load.
</success_criteria>

<output>
After completion, create `.planning/phases/50-kiosk-device-boundary-hardening/50-02-SUMMARY.md`
</output>
