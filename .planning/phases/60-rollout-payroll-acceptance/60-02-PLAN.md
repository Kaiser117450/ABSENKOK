---
phase: 60-rollout-payroll-acceptance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/models/payroll_rollout_acceptance.dart"
  - "lib/services/payroll_rollout_acceptance_service.dart"
  - "lib/services/payroll_validation_bundle_service.dart"
  - "test/services/payroll_rollout_acceptance_service_test.dart"
  - "test/services/payroll_validation_bundle_service_test.dart"
autonomous: true
requirements: ["OPS-01"]
must_haves:
  truths:
    - "Phase 60 has one in-memory acceptance domain that can answer readiness, scenario status, blocked parity rows, and the exact reason the primary CTA is still disabled."
    - "The acceptance domain uses the existing payroll matrix / spreadsheet / PDF semantics instead of inventing new label or severity mappings."
    - "The validation bundle exporter produces a salary-facing artifact that excludes GPS and other low-signal technical fields."
  artifacts:
    - path: "lib/models/payroll_rollout_acceptance.dart"
      provides: "Typed rollout scenario IDs, evidence sources, summary states, and parity row contracts."
      contains: "PayrollRolloutScenarioId"
    - path: "lib/services/payroll_rollout_acceptance_service.dart"
      provides: "Readiness builder that aggregates required scenarios and blocked parity rows."
      contains: "buildPayrollRolloutAcceptanceSummary"
    - path: "lib/services/payroll_validation_bundle_service.dart"
      provides: "Shareable validation bundle output for rollout evidence."
      contains: "buildPayrollValidationBundle"
  key_links:
    - from: "lib/services/payroll_rollout_acceptance_service.dart"
      to: "lib/services/payroll_matrix_semantics.dart"
      via: "the readiness service must reuse the shipped payroll label and severity contract"
      pattern: "PayrollMatrixSemantics"
    - from: "lib/services/payroll_rollout_acceptance_service.dart"
      to: "lib/services/payroll_pdf_matrix_export_service.dart"
      via: "the readiness layer must compare the existing PDF parity path rather than a placeholder export"
      pattern: "PayrollPdfMatrixExportService"
    - from: "lib/services/payroll_validation_bundle_service.dart"
      to: "lib/services/payroll_spreadsheet_export_service.dart"
      via: "the bundle exporter must honor the same salary-facing field exclusions as the spreadsheet contract"
      pattern: "PayrollSpreadsheetExportService"
---

<objective>
Build the typed Phase 60 acceptance domain before integrating it into the admin recap shell.

Purpose: the app currently knows how to export payroll artifacts, but it cannot express rollout readiness or package operator evidence. This plan adds the models and services needed for Phase 60 without adding database persistence or changing payroll logic.
Output: one typed acceptance model, one readiness service, one validation bundle exporter, and service-level regression coverage.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-rollout-payroll-acceptance/60-RESEARCH.md
@.planning/phases/60-rollout-payroll-acceptance/60-VALIDATION.md
@.planning/phases/60-rollout-payroll-acceptance/60-UI-SPEC.md
@.planning/phases/59-pdf-portal-parity/59-PARITY-FIXTURES.md
@lib/models/payroll_matrix_row.dart
@lib/services/payroll_matrix_builder.dart
@lib/services/payroll_matrix_semantics.dart
@lib/services/payroll_pdf_matrix_export_service.dart
@lib/services/payroll_spreadsheet_export_service.dart
@lib/screens/admin/admin_reports_screen.dart
@test/services/payroll_matrix_semantics_test.dart
@test/services/payroll_pdf_matrix_export_service_test.dart
@test/screens/admin/admin_reports_payroll_matrix_test.dart

<interfaces>
Required scenario IDs:
```dart
fullTime
partTime
overtime
outlet24Hour
outletNormal
breakFirst
noShow
```

Required evidence sources:
```dart
admin
spreadsheet
pdf
portal
```

Required output questions:
```text
Is payroll ready?
Which required scenarios are still pending?
Which parity rows are blocked?
Why is the CTA disabled?
What should the validation bundle contain?
```
</interfaces>

<constraints>
- do not add database writes, RPCs, or persistence tables
- keep acceptance state serializable and in-memory
- reuse the shipped payroll semantics and export boundaries
- exclude GPS, queue-order, capture-mode, and raw debug fields from the validation bundle
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the typed rollout acceptance model and readiness service</name>
  <files>lib/models/payroll_rollout_acceptance.dart, lib/services/payroll_rollout_acceptance_service.dart, test/services/payroll_rollout_acceptance_service_test.dart</files>
  <read_first>
    - lib/models/payroll_matrix_row.dart
    - lib/services/payroll_matrix_builder.dart
    - lib/services/payroll_matrix_semantics.dart
    - lib/screens/admin/admin_reports_screen.dart
    - .planning/phases/60-rollout-payroll-acceptance/60-RESEARCH.md
    - .planning/phases/60-rollout-payroll-acceptance/60-UI-SPEC.md
    - test/services/payroll_matrix_semantics_test.dart
  </read_first>
  <action>
    Create the typed readiness domain for Phase 60.

    Required changes:
    1. Create `lib/models/payroll_rollout_acceptance.dart` with concrete types for:
       - `PayrollRolloutScenarioId`
       - `PayrollEvidenceSource`
       - scenario status (`passed`, `pending`, `blocked`)
       - parity evidence row
       - overall readiness summary
    2. Create `lib/services/payroll_rollout_acceptance_service.dart` with a public entry point named `buildPayrollRolloutAcceptanceSummary`.
    3. Make the summary include:
       - passed / pending / blocked counts
       - the seven required scenarios above
       - explicit `canMarkPayrollReady` gating
       - a reason string for disabled CTA states
       - blocked parity row details
       - a separate local confirmation flag for the manual database-review step
    4. Reuse `PayrollMatrixSemantics` and the existing matrix/export models for labels, tags, and severity families. Do not create a second payroll meaning map.
    5. Keep all state in memory and serializable. Do not introduce Supabase writes, local databases, or network calls.
    6. Add `test/services/payroll_rollout_acceptance_service_test.dart` covering:
       - all seven scenario IDs are present
       - CTA stays disabled while any scenario is pending
       - blocked parity rows force `blocked` readiness
       - manual database confirmation must be true before readiness passes

    Scope guard:
    - do not change strict recap logic
    - do not modify spreadsheet or PDF export behavior here
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/payroll_rollout_acceptance_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/models/payroll_rollout_acceptance.dart` exists
    - `lib/models/payroll_rollout_acceptance.dart` contains `PayrollRolloutScenarioId`
    - `lib/models/payroll_rollout_acceptance.dart` contains `outlet24Hour`
    - `lib/models/payroll_rollout_acceptance.dart` contains `breakFirst`
    - `lib/services/payroll_rollout_acceptance_service.dart` exists
    - `lib/services/payroll_rollout_acceptance_service.dart` contains `buildPayrollRolloutAcceptanceSummary`
    - `lib/services/payroll_rollout_acceptance_service.dart` contains `canMarkPayrollReady`
    - `lib/services/payroll_rollout_acceptance_service.dart` contains `database`
    - `test/services/payroll_rollout_acceptance_service_test.dart` contains `blocked`
    - `C:\flutter\bin\flutter.bat test test/services/payroll_rollout_acceptance_service_test.dart` exits 0
  </acceptance_criteria>
  <done>The app now has one typed readiness domain for the Phase 60 rollout workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the validation bundle exporter and its regression coverage</name>
  <files>lib/services/payroll_validation_bundle_service.dart, test/services/payroll_validation_bundle_service_test.dart</files>
  <read_first>
    - lib/services/payroll_spreadsheet_export_service.dart
    - lib/services/payroll_pdf_matrix_export_service.dart
    - lib/services/payroll_rollout_acceptance_service.dart
    - .planning/phases/60-rollout-payroll-acceptance/60-UI-SPEC.md
    - .planning/phases/60-rollout-payroll-acceptance/60-VALIDATION.md
    - test/services/payroll_pdf_matrix_export_service_test.dart
  </read_first>
  <action>
    Create the validation bundle export path for Phase 60.

    Required behavior:
    1. Create `lib/services/payroll_validation_bundle_service.dart` with a public entry point named `buildPayrollValidationBundle`.
    2. The service must generate one shareable salary-facing artifact that includes:
       - validation timestamp
       - outlet and date-range context
       - overall readiness result
       - the seven scenario statuses
       - blocked parity rows and next-action notes
       - evidence labels for Admin / Spreadsheet / PDF / Portal
       - the additive-only reminder
    3. The bundle must explicitly exclude low-signal technical fields:
       - `latitude`
       - `longitude`
       - `capture_mode`
       - `queue_order`
       - `detail_note`
    4. Add `test/services/payroll_validation_bundle_service_test.dart` covering:
       - the bundle contains the rollout summary and evidence sections
       - forbidden fields do not appear
       - blocked rows render as blocked follow-up items
    5. Keep the implementation local to Flutter. Do not call external services or require a backend upload.

    Scope guard:
    - do not add zip packaging or cloud storage
    - do not read or write production data directly
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/payroll_validation_bundle_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/payroll_validation_bundle_service.dart` exists
    - `lib/services/payroll_validation_bundle_service.dart` contains `buildPayrollValidationBundle`
    - `lib/services/payroll_validation_bundle_service.dart` contains `Admin`
    - `lib/services/payroll_validation_bundle_service.dart` contains `Spreadsheet`
    - `lib/services/payroll_validation_bundle_service.dart` contains `Portal`
    - `lib/services/payroll_validation_bundle_service.dart` does not contain `latitude`
    - `lib/services/payroll_validation_bundle_service.dart` does not contain `longitude`
    - `test/services/payroll_validation_bundle_service_test.dart` contains `blocked`
    - `C:\flutter\bin\flutter.bat test test/services/payroll_validation_bundle_service_test.dart` exits 0
  </acceptance_criteria>
  <done>Phase 60 can now export one operator-ready validation bundle without leaking low-signal technical fields.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/payroll_rollout_acceptance_service_test.dart` passes
- `C:\flutter\bin\flutter.bat test test/services/payroll_validation_bundle_service_test.dart` passes
- the new acceptance service and bundle exporter reuse the shipped payroll semantics instead of introducing new parity logic
</verification>

<success_criteria>
- readiness logic is typed, testable, and explainable
- bundle export is salary-facing and shareable
- no new persistence or backend rollout mechanics are introduced
</success_criteria>

<output>
After completion, create `.planning/phases/60-rollout-payroll-acceptance/60-02-SUMMARY.md`
</output>
