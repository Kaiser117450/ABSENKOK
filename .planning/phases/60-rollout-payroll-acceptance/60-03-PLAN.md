---
phase: 60-rollout-payroll-acceptance
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - "lib/screens/admin/admin_reports_screen.dart"
  - "lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart"
  - "test/screens/admin/admin_reports_payroll_rollout_test.dart"
  - "test/screens/admin/admin_reports_payroll_matrix_test.dart"
autonomous: true
requirements: ["OPS-01"]
must_haves:
  truths:
    - "The recap shell shows the Phase 60 rollout acceptance surface with the exact approved copy and does not create a second reporting route."
    - "The primary CTA stays disabled until all seven scenarios pass, blocked parity rows are zero, and the manual database-review confirmation is acknowledged."
    - "The admin UI exposes one readiness banner, one scenario matrix, one evidence table, and one validation-download action."
  artifacts:
    - path: "lib/screens/admin/admin_reports_screen.dart"
      provides: "Final recap-shell integration for the Phase 60 rollout acceptance workflow."
      contains: "Rollout Payroll"
    - path: "lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart"
      provides: "Dedicated widget for the readiness banner, scenario matrix, evidence table, and action bar."
      contains: "Tandai Siap Payroll"
    - path: "test/screens/admin/admin_reports_payroll_rollout_test.dart"
      provides: "Widget regression coverage for the rollout acceptance shell."
      contains: "Mode rollout additive"
  key_links:
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_rollout_acceptance_service.dart"
      via: "the recap shell must derive readiness from the typed service instead of ad hoc widget state"
      pattern: "buildPayrollRolloutAcceptanceSummary"
    - from: "lib/screens/admin/admin_reports_screen.dart"
      to: "lib/services/payroll_validation_bundle_service.dart"
      via: "the secondary action must export the validation bundle rather than invent a second evidence format"
      pattern: "buildPayrollValidationBundle"
---

<objective>
Integrate the Phase 60 rollout acceptance experience into the existing admin recap shell and protect it with widget regression coverage.

Purpose: after the docs and acceptance domain exist, operators still need an actual UI to review readiness and export evidence. This plan wires the approved Phase 60 surface into `AdminReportsScreen` without changing the underlying payroll logic.
Output: one recap-shell rollout panel and final widget coverage for the rollout flow.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-rollout-payroll-acceptance/60-RESEARCH.md
@.planning/phases/60-rollout-payroll-acceptance/60-VALIDATION.md
@.planning/phases/60-rollout-payroll-acceptance/60-UI-SPEC.md
@.planning/phases/60-rollout-payroll-acceptance/60-ACCEPTANCE-FIXTURES.md
@lib/screens/admin/admin_reports_screen.dart
@lib/services/payroll_rollout_acceptance_service.dart
@lib/services/payroll_validation_bundle_service.dart
@test/screens/admin/admin_reports_payroll_matrix_test.dart

<interfaces>
Locked screen copy:
```text
Rollout Payroll
Payroll siap dipakai
Payroll belum aman dipakai
Mode rollout additive
Skenario wajib
Bukti parity
Tandai Siap Payroll
Unduh Bukti Validasi
```

Locked table columns:
```text
Admin
Spreadsheet
PDF
Portal
Status
```

Locked scenario labels:
```text
Full-time
Part-time
Lembur
Outlet 24 jam
Outlet normal
Break-first
No-show
```
</interfaces>

<constraints>
- keep the rollout surface inside the existing admin recap shell
- do not add a new route or portal-facing page
- do not auto-open the destructive confirmation dialog on page load
- keep the validation bundle as the secondary action and `Tandai Siap Payroll` as the primary action
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the rollout acceptance panel to the admin recap shell</name>
  <files>lib/screens/admin/admin_reports_screen.dart, lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart</files>
  <read_first>
    - lib/screens/admin/admin_reports_screen.dart
    - lib/services/payroll_rollout_acceptance_service.dart
    - lib/services/payroll_validation_bundle_service.dart
    - .planning/phases/60-rollout-payroll-acceptance/60-UI-SPEC.md
    - .planning/phases/60-rollout-payroll-acceptance/60-ACCEPTANCE-FIXTURES.md
  </read_first>
  <action>
    Integrate the approved rollout acceptance surface into the recap flow.

    Required behavior:
    1. Create `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart`.
    2. Show the panel inside the existing recap shell with these sections in order:
       - readiness banner
       - additive-only reminder
       - scenario matrix
       - parity evidence table
       - footer action bar
    3. Preserve the exact copy:
       - `Rollout Payroll`
       - `Payroll siap dipakai`
       - `Payroll belum aman dipakai`
       - `Mode rollout additive`
       - `Skenario wajib`
       - `Bukti parity`
       - `Tandai Siap Payroll`
       - `Unduh Bukti Validasi`
    4. Keep the scenario labels exactly:
       - `Full-time`
       - `Part-time`
       - `Lembur`
       - `Outlet 24 jam`
       - `Outlet normal`
       - `Break-first`
       - `No-show`
    5. Use the acceptance service to decide readiness and whether the primary CTA is enabled.
    6. Keep the primary CTA disabled until:
       - all seven scenarios are passed
       - blocked parity rows are zero
       - the manual database-review confirmation is acknowledged
    7. Use a destructive confirmation dialog with the locked title `Terapkan perubahan database` only when the operator triggers the local database-review acknowledgement step. The dialog must not run SQL or mutate production state.
    8. Wire the secondary action to the validation bundle exporter.

    Scope guard:
    - do not change the legacy per-scan export cluster
    - do not create a new admin route
    - do not persist rollout state to Supabase
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_rollout_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart` exists
    - `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart` contains `Rollout Payroll`
    - `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart` contains `Mode rollout additive`
    - `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart` contains `Tandai Siap Payroll`
    - `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart` contains `Unduh Bukti Validasi`
    - `lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart` contains `Terapkan perubahan database`
    - `lib/screens/admin/admin_reports_screen.dart` contains `buildPayrollRolloutAcceptanceSummary`
    - `lib/screens/admin/admin_reports_screen.dart` contains `buildPayrollValidationBundle`
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_rollout_test.dart` exits 0
  </acceptance_criteria>
  <done>The admin recap shell now exposes the approved Phase 60 rollout acceptance workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend widget coverage for the rollout shell and recap integration</name>
  <files>test/screens/admin/admin_reports_payroll_rollout_test.dart, test/screens/admin/admin_reports_payroll_matrix_test.dart</files>
  <read_first>
    - test/screens/admin/admin_reports_payroll_matrix_test.dart
    - lib/screens/admin/admin_reports_screen.dart
    - lib/screens/admin/widgets/payroll_rollout_acceptance_panel.dart
    - .planning/phases/60-rollout-payroll-acceptance/60-VALIDATION.md
  </read_first>
  <action>
    Add the final widget regression coverage for the rollout acceptance shell.

    Required coverage:
    1. `test/screens/admin/admin_reports_payroll_rollout_test.dart` must verify:
       - readiness banner copy appears
       - the seven required scenario labels appear
       - evidence table headers `Admin`, `Spreadsheet`, `PDF`, `Portal`, `Status` appear
       - `Tandai Siap Payroll` starts disabled when readiness is incomplete
       - the destructive confirmation dialog does not appear on initial load
       - `Unduh Bukti Validasi` remains available as the secondary action
    2. `test/screens/admin/admin_reports_payroll_matrix_test.dart` must still prove the recap shell keeps the existing payroll matrix/export UI while the rollout panel is present.
    3. Keep the tests focused on the recap shell; do not turn them into end-to-end export file tests.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_rollout_test.dart test/screens/admin/admin_reports_payroll_matrix_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/screens/admin/admin_reports_payroll_rollout_test.dart` contains `Rollout Payroll`
    - `test/screens/admin/admin_reports_payroll_rollout_test.dart` contains `Full-time`
    - `test/screens/admin/admin_reports_payroll_rollout_test.dart` contains `Unduh Bukti Validasi`
    - `test/screens/admin/admin_reports_payroll_rollout_test.dart` contains `Terapkan perubahan database`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Ekspor PDF Payroll`
    - `test/screens/admin/admin_reports_payroll_matrix_test.dart` contains `Ekspor Spreadsheet`
    - `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_rollout_test.dart test/screens/admin/admin_reports_payroll_matrix_test.dart` exits 0
  </acceptance_criteria>
  <done>The recap-shell rollout acceptance workflow is protected by focused widget regression coverage.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/admin_reports_payroll_rollout_test.dart test/screens/admin/admin_reports_payroll_matrix_test.dart` passes
- the rollout surface uses the exact approved copy and scenario labels
- the primary CTA is gated by readiness instead of being always enabled
</verification>

<success_criteria>
- operators can review rollout readiness where they already review payroll recap data
- the UI cannot approve payroll prematurely
- the secondary validation-bundle action remains available without replacing existing recap exports
</success_criteria>

<output>
After completion, create `.planning/phases/60-rollout-payroll-acceptance/60-03-SUMMARY.md`
</output>
