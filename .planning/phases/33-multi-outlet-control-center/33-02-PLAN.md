---
phase: 33-multi-outlet-control-center
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - lib/screens/admin/central_dashboard_screen.dart
  - lib/widgets/outlet_control_card.dart
  - lib/screens/admin/admin_dashboard_screen.dart
  - lib/app.dart
  - lib/screens/admin/admin_shell.dart
  - test/screens/admin/central_dashboard_screen_test.dart
autonomous: false
requirements: [ADMIN-01, ADMIN-02]
user_setup:
  - service: supabase
    why: "Phase 33 RPCs must exist before the central admin UI can load live aggregate data"
    dashboard_config:
      - task: "Run sql/phase_33_central_dashboard_20260322.sql in Supabase SQL Editor"
        location: "Supabase Dashboard -> SQL Editor -> New Query -> paste and run"
must_haves:
  truths:
    - "Full admins land on a Central Dashboard view at /admin/dashboard"
    - "Central Dashboard shows firm-wide device-health and daily attendance metrics across all active outlets"
    - "Central Dashboard shows one drilldown card per active outlet"
    - "Tapping an outlet card opens an outlet-specific detail dashboard with that outlet preselected"
    - "Kepala gerai continues to use the outlet-scoped AdminDashboardScreen and never sees central-only routing"
  artifacts:
    - path: "lib/screens/admin/central_dashboard_screen.dart"
      provides: "Admin-only chain-wide control center screen"
      exports: ["CentralDashboardScreen"]
    - path: "lib/widgets/outlet_control_card.dart"
      provides: "Outlet rollup card with drilldown action"
      exports: ["OutletControlCard"]
    - path: "lib/app.dart"
      provides: "Role-aware dashboard routing plus outlet-detail route"
    - path: "lib/screens/admin/admin_dashboard_screen.dart"
      provides: "Outlet detail dashboard that accepts a preselected outlet"
    - path: "test/screens/admin/central_dashboard_screen_test.dart"
      provides: "Widget coverage for central screen rendering and drilldown tap"
  key_links:
    - from: "lib/screens/admin/central_dashboard_screen.dart"
      to: "lib/services/analytics_service.dart"
      via: "getCentralDashboardSummary and getOutletControlCenter"
      pattern: "getCentralDashboardSummary|getOutletControlCenter"
    - from: "lib/widgets/outlet_control_card.dart"
      to: "lib/screens/admin/admin_dashboard_screen.dart"
      via: "tap callback pushes /admin/outlet-dashboard?outletId=..."
      pattern: "onTap"
    - from: "lib/app.dart"
      to: "lib/screens/admin/central_dashboard_screen.dart"
      via: "admin-only builder for /admin/dashboard"
      pattern: "CentralDashboardScreen"
    - from: "lib/app.dart"
      to: "lib/screens/admin/admin_dashboard_screen.dart"
      via: "outlet detail route with initialOutletId"
      pattern: "outlet-dashboard"
---

<objective>
Build the central admin UI, wire role-aware routing, and add outlet drilldown into the existing operational dashboard.

Purpose: deliver the actual Multi-Outlet Control Center experience without regressing current kepala-gerai or outlet-detail flows.
Output: CentralDashboardScreen, outlet rollup cards, role-aware routes, and drilldown into an outlet-scoped AdminDashboardScreen.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-multi-outlet-control-center/33-RESEARCH.md
@.planning/phases/32-multi-device-dashboard/32-02-SUMMARY.md

<interfaces>
From lib/services/analytics_service.dart (Plan 01 output):
```dart
class CentralDashboardSummary {
  final int totalOutlets;
  final int connectedDevices;
  final int offlineDevices;
  final int lowBatteryDevices;
  final int pendingSyncDevices;
  final double dailyAttendanceRate;
}

class OutletControlCenterRow {
  final String outletId;
  final String outletName;
  final int connectedDevices;
  final int offlineDevices;
  final int lowBatteryDevices;
  final int pendingSyncDevices;
  final double dailyAttendanceRate;
  final DateTime? lastHeartbeatAt;
}

Future<CentralDashboardSummary?> getCentralDashboardSummary({
  required DateTime date,
});

Future<List<OutletControlCenterRow>> getOutletControlCenter({
  required DateTime date,
});
```

From lib/providers/app_provider.dart (existing):
```dart
class AppState {
  final bool isAdmin;
  final bool isKepalaGerai;
  final String? managedOutletId;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CentralDashboardScreen, outlet cards, and widget tests</name>
  <files>lib/screens/admin/central_dashboard_screen.dart, lib/widgets/outlet_control_card.dart, test/screens/admin/central_dashboard_screen_test.dart</files>
  <read_first>
    - lib/screens/admin/chart_dashboard_screen.dart (dashboard section layout and refresh pattern)
    - lib/screens/admin/admin_dashboard_screen.dart (current admin visual language and role assumptions)
    - lib/widgets/kiosk_device_card.dart (card styling conventions from Phase 32)
    - lib/services/analytics_service.dart (Plan 01 outputs)
    - lib/core/theme.dart
    - test/screens/admin/chart_dashboard_screen_test.dart (existing dashboard widget-test style)
  </read_first>
  <behavior>
    - CentralDashboardScreen shows summary cards for connected devices, offline devices, low battery devices, and firm-wide daily attendance
    - CentralDashboardScreen shows one outlet drilldown card per active outlet row returned by AnalyticsService
    - Tapping an outlet card triggers drilldown with the tapped outletId
    - Screen refresh reloads both summary and outlet rows together
  </behavior>
  <action>
    1. Create `lib/widgets/outlet_control_card.dart` as a reusable tap target for one outlet row:
       - show outlet name, connected/offline/low-battery counts, and daily attendance rate
       - include a clear CTA affordance (icon or label) that communicates drilldown
       - follow the same red/white admin visual language already used in admin screens

    2. Create `lib/screens/admin/central_dashboard_screen.dart`:
       - admin-only central screen
       - `RefreshIndicator` + sectioned layout
       - load both `getCentralDashboardSummary` and `getOutletControlCenter` in one refresh cycle
       - add realtime subscriptions for `attendance_logs`, `employees`, `kiosk_devices`, and `outlets`, reloading the same two fetchers on change
       - expose lightweight optional constructor overrides for tests, such as injected loaders and `onOpenOutlet`, so widget tests can run without real Supabase

    3. Create `test/screens/admin/central_dashboard_screen_test.dart`:
       - renders summary heading/cards with injected fake data
       - renders outlet rollup cards from injected fake rows
       - tapping a card calls the injected `onOpenOutlet` with the correct outletId
       - empty-state case when no rows are returned

    4. Keep the screen focused on chain-wide summary + outlet drilldown only. Do not duplicate the full outlet operational dashboard inside this screen.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/central_dashboard_screen_test.dart</automated>
  </verify>
  <done>CentralDashboardScreen and OutletControlCard exist, load central aggregates, and have widget coverage for render plus drilldown behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire role-aware routing and outlet drilldown into existing dashboards</name>
  <files>lib/app.dart, lib/screens/admin/admin_dashboard_screen.dart, lib/screens/admin/admin_shell.dart</files>
  <read_first>
    - lib/app.dart
    - lib/screens/admin/admin_dashboard_screen.dart
    - lib/screens/admin/admin_shell.dart
    - lib/providers/app_provider.dart
    - .planning/phases/33-multi-outlet-control-center/33-RESEARCH.md
  </read_first>
  <action>
    1. Update `lib/app.dart` so `/admin/dashboard` is role-aware:
       - `admin` => `CentralDashboardScreen`
       - `kepala_gerai` => existing `AdminDashboardScreen`

    2. Add a dedicated outlet-detail route such as `/admin/outlet-dashboard` that builds `AdminDashboardScreen(initialOutletId: queryParamOutletId)`.

    3. Update `lib/screens/admin/admin_dashboard_screen.dart` to accept `initialOutletId`:
       - apply it during initialization for full admins
       - do not override `kepala_gerai`'s locked `managedOutletId`
       - keep the existing per-outlet logs, open shifts, and kiosk-device behavior intact
       - ensure the existing "Lihat Dashboard" button uses the effective outlet id and handles the no-outlet-selected case safely instead of pushing an empty `outletId`

    4. Update `lib/screens/admin/admin_shell.dart` so both `/admin/dashboard` and `/admin/outlet-dashboard` keep the dashboard tab selected.

    5. Wire outlet-card drilldown from `CentralDashboardScreen` to `/admin/outlet-dashboard?outletId=...`.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/central_dashboard_screen.dart lib/widgets/outlet_control_card.dart lib/screens/admin/admin_dashboard_screen.dart lib/app.dart lib/screens/admin/admin_shell.dart</automated>
  </verify>
  <done>Admin routing cleanly separates central and outlet views, central cards drill into an outlet-specific dashboard, and kepala gerai behavior remains unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify central admin flow on device</name>
  <files>None - manual verification checkpoint only</files>
  <what-built>
    CentralDashboardScreen for full admins, outlet rollup drilldown, role-aware routing, and outlet-specific reuse of AdminDashboardScreen.
  </what-built>
  <action>Run the automated preflight command below after Tasks 1 and 2 are complete, then perform the manual verification steps on a real build. Use this checkpoint only to approve the delivered flow or report concrete defects for revision.</action>
  <how-to-verify>
    Prerequisites:
    1. Run `sql/phase_33_central_dashboard_20260322.sql` in Supabase SQL Editor
    2. Build and install the updated APK

    Verification steps:
    1. Sign in as a full admin and confirm `/admin/dashboard` opens the central dashboard, not the old outlet dashboard
    2. Verify summary cards show firm-wide metrics (devices + attendance)
    3. Verify one card appears per active outlet
    4. Tap one outlet card and confirm you land on an outlet-specific detail dashboard with that outlet preselected
    5. From the outlet detail view, confirm device cards and other operational sections reflect only that outlet
    6. Sign in as `kepala_gerai` and confirm `/admin/dashboard` still opens the outlet-scoped dashboard directly, with no central-admin card list
  </how-to-verify>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/screens/admin/central_dashboard_screen_test.dart</automated>
  </verify>
  <done>User confirms the central-admin and outlet-drilldown flow is correct, or returns specific issues for revision.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/screens/admin/central_dashboard_screen_test.dart` passes
- `C:\flutter\bin\flutter.bat analyze ...` reports no errors on the changed dashboard files
- `/admin/dashboard` is admin-central and manager-outlet-specific based on role
- outlet drilldown passes a concrete outlet id into the reused AdminDashboardScreen
</verification>

<success_criteria>
- Full admins see a real chain-wide Central Dashboard as the primary dashboard route
- Central summary cards expose chain-wide device health and today's attendance rate
- Each outlet is represented once in the drilldown list
- Tapping an outlet opens a specific outlet-health/detail view
- Kepala gerai retains the existing outlet-scoped dashboard behavior
</success_criteria>

<output>
After completion, create `.planning/phases/33-multi-outlet-control-center/33-02-SUMMARY.md`
</output>
