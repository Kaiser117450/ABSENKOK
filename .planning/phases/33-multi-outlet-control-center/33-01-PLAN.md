---
phase: 33-multi-outlet-control-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/phase_33_central_dashboard_20260322.sql
  - lib/services/analytics_service.dart
  - test/services/analytics_service_test.dart
autonomous: true
requirements: [ADMIN-01, ADMIN-02]
must_haves:
  truths:
    - "AnalyticsService can parse chain-wide central dashboard summary payloads"
    - "AnalyticsService can parse outlet control-center rollup rows"
    - "AnalyticsService returns null or empty collections safely when Supabase is not ready"
    - "get_central_dashboard_summary RPC exists and returns firm-wide device plus attendance KPIs"
    - "get_outlet_control_center RPC exists and returns one rollup row per active outlet"
  artifacts:
    - path: "sql/phase_33_central_dashboard_20260322.sql"
      provides: "SECURITY DEFINER RPCs for central summary and outlet rollups"
      contains: "CREATE OR REPLACE FUNCTION get_central_dashboard_summary"
    - path: "lib/services/analytics_service.dart"
      provides: "Central dashboard data contracts and service methods"
      contains: "class CentralDashboardSummary"
    - path: "test/services/analytics_service_test.dart"
      provides: "Unit coverage for central analytics parsing and safety guards"
      contains: "group('CentralDashboardSummary.fromJson'"
  key_links:
    - from: "lib/services/analytics_service.dart"
      to: "sql/phase_33_central_dashboard_20260322.sql"
      via: "SupabaseClientFactory.admin.rpc('get_central_dashboard_summary')"
      pattern: "getCentralDashboardSummary"
    - from: "lib/services/analytics_service.dart"
      to: "sql/phase_33_central_dashboard_20260322.sql"
      via: "SupabaseClientFactory.admin.rpc('get_outlet_control_center')"
      pattern: "getOutletControlCenter"
---

<objective>
Create the central-dashboard data layer: SQL RPCs, Dart data contracts, AnalyticsService methods, and unit tests.

Purpose: establish the server-side aggregate foundation that the new admin control center depends on.
Output: RPC migration file, central summary + outlet rollup models in AnalyticsService, and tests proving parsing plus guard behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-multi-outlet-control-center/33-RESEARCH.md
@sql/phase23_rpc_functions.sql
@sql/phase_31_kiosk_devices_20260320.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AnalyticsService with central dashboard contracts and tests</name>
  <files>lib/services/analytics_service.dart, test/services/analytics_service_test.dart</files>
  <read_first>
    - lib/services/analytics_service.dart (existing service/data-class pattern)
    - test/services/analytics_service_test.dart (existing parsing + supabaseReady guard test style)
    - sql/phase23_rpc_functions.sql (naming, auth, and aggregate semantics to stay consistent with)
    - .planning/phases/33-multi-outlet-control-center/33-RESEARCH.md
  </read_first>
  <behavior>
    - CentralDashboardSummary.fromJson parses total_outlets, connected_devices, offline_devices, low_battery_devices, pending_sync_devices, daily_attendance_rate
    - OutletControlCenterRow.fromJson parses outlet_id, outlet_name, connected_devices, offline_devices, low_battery_devices, pending_sync_devices, daily_attendance_rate, last_heartbeat_at
    - getCentralDashboardSummary returns null when supabaseReady is false
    - getOutletControlCenter returns an empty list when supabaseReady is false
    - Numeric JSON values are coerced safely to int or double just like existing AnalyticsService data classes
  </behavior>
  <action>
    1. Extend `lib/services/analytics_service.dart` using the existing file pattern where data contracts live above the `AnalyticsService` class:
       - Add `CentralDashboardSummary`
       - Add `OutletControlCenterRow`
       - Add `Future<CentralDashboardSummary?> getCentralDashboardSummary({required DateTime date})`
       - Add `Future<List<OutletControlCenterRow>> getOutletControlCenter({required DateTime date})`

    2. Keep the existing service conventions:
       - guard with `if (!supabaseReady) return null` / `return []`
       - call `SupabaseClientFactory.admin.rpc(...)`
       - catch exceptions, `debugPrint(...)`, and return null/empty rather than throwing

    3. Use the exact RPC names that Task 2 will create:
       - `get_central_dashboard_summary`
       - `get_outlet_control_center`

    4. Extend `test/services/analytics_service_test.dart` with new groups:
       - `CentralDashboardSummary.fromJson`
       - `OutletControlCenterRow.fromJson`
       - `AnalyticsService central dashboard methods`

    5. Cover these cases explicitly:
       - standard JSON payload parsing
       - int-to-double coercion for `daily_attendance_rate`
       - nullable `last_heartbeat_at`
       - `supabaseReady == false` returns null/empty safely

    Run the tests first, add the new types and methods, then rerun until green.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat test test/services/analytics_service_test.dart</automated>
  </verify>
  <done>AnalyticsService exposes tested, non-throwing central dashboard methods and model parsers that exactly match the new RPC payload shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Add SQL migration for central summary and outlet rollup RPCs</name>
  <files>sql/phase_33_central_dashboard_20260322.sql</files>
  <read_first>
    - sql/phase23_rpc_functions.sql (existing SECURITY DEFINER dashboard RPC style)
    - sql/phase_31_kiosk_devices_20260320.sql (current device-health source table and fields)
    - .planning/phases/33-multi-outlet-control-center/33-RESEARCH.md
  </read_first>
  <action>
    Create `sql/phase_33_central_dashboard_20260322.sql` with two additive SECURITY DEFINER RPCs:

    1. `get_central_dashboard_summary(p_date DATE DEFAULT CURRENT_DATE) RETURNS JSON`
       - admin-only
       - counts active outlets
       - counts active kiosk devices
       - counts offline devices using the existing 30-minute threshold from Phase 32
       - counts low-battery devices with `battery_level < 20`
       - counts devices with `pending_sync_count > 0`
       - computes firm-wide daily attendance rate for the given day using the same active-employee / distinct-masuk semantics as Phase 23

    2. `get_outlet_control_center(p_date DATE DEFAULT CURRENT_DATE) RETURNS JSON`
       - admin-only
       - returns one row per active outlet
       - includes `outlet_id`, `outlet_name`, `connected_devices`, `offline_devices`, `low_battery_devices`, `pending_sync_devices`, `daily_attendance_rate`, `last_heartbeat_at`
       - excludes archived devices by filtering `kiosk_devices.is_active = TRUE`

    Implementation rules:
       - use `SET search_path = public`
       - use `REVOKE ALL ... FROM PUBLIC` and `GRANT EXECUTE ... TO authenticated`
       - keep the migration safe to re-run with `CREATE OR REPLACE FUNCTION`
       - do not modify existing tables or destructive data
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path 'sql/phase_33_central_dashboard_20260322.sql' -Pattern 'CREATE OR REPLACE FUNCTION get_central_dashboard_summary','CREATE OR REPLACE FUNCTION get_outlet_control_center','SECURITY DEFINER' | ForEach-Object { $_.Line }"</automated>
  </verify>
  <done>The repository contains a safe, additive SQL migration that defines the two central dashboard RPCs with admin-only aggregate behavior.</done>
</task>

</tasks>

<verification>
- `C:\flutter\bin\flutter.bat test test/services/analytics_service_test.dart` passes
- `sql/phase_33_central_dashboard_20260322.sql` contains both RPC definitions and SECURITY DEFINER declarations
- AnalyticsService method names and RPC names match exactly
</verification>

<success_criteria>
- CentralDashboardSummary and OutletControlCenterRow are defined and parsed in AnalyticsService
- AnalyticsService safely exposes central summary and outlet rollup fetchers
- A new SQL migration exists for `get_central_dashboard_summary` and `get_outlet_control_center`
- The central data layer is ready for UI wiring without additional backend design work
</success_criteria>

<output>
After completion, create `.planning/phases/33-multi-outlet-control-center/33-01-SUMMARY.md`
</output>
