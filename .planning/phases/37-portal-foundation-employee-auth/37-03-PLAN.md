---
phase: 37-portal-foundation-employee-auth
plan: 03
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/package.json
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-out.ts
autonomous: true
requirements: [AUTH-02, AUTH-03]
user_setup:
  - service: vercel
    why: "The website will need Supabase environment variables available in Vercel for the new portal routes."
    dashboard_config:
      - task: "Add website env vars for Supabase URL and publishable key"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables"
must_haves:
  truths:
    - "Employees can search their own name, choose the matching identity card, and submit password through a dedicated portal login page"
    - "Search requests stay lightweight through debounce, result caps, and a server-side endpoint instead of direct browser-to-Supabase table access"
    - "Protected portal routes refresh and preserve Supabase auth cookies across reloads"
    - "The public marketing site remains static and unchanged outside the new portal routes"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts"
      provides: "SSR-safe Supabase server client helper"
      contains: "createServerClient"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts"
      provides: "Portal route guard and cookie refresh middleware"
      contains: "defineMiddleware"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro"
      provides: "Employee portal login page"
      contains: "name=\"search_name\""
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      provides: "Server-side employee search endpoint"
      contains: "search_portal_employees"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts"
      provides: "Server-side sign-in endpoint"
      contains: "signInWithPassword"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      via: "debounced fetch for login search results"
      pattern: "/portal/auth/search"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts"
      via: "form action POST"
      pattern: "action=\"/portal/auth/sign-in\""
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts"
      via: "shared portal auth helper"
      pattern: "portal"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts"
      to: "sql/phase_37_employee_portal_foundation_20260322.sql"
      via: "RPC call to search_portal_employees"
      pattern: "search_portal_employees"
---

<objective>
Add the website-side auth infrastructure for the employee portal: SSR-safe Supabase utilities, middleware route protection, fast login search, and server-rendered sign-in/sign-out flows.

Purpose: AUTH-02 and AUTH-03 require a real protected portal entry point, not only backend provisioning.
Output: A name-search login page, auth/search endpoints, middleware guard, and cookie-preserving portal auth helpers in the Astro website repo.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/astro.config.mjs
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/package.json
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/BaseLayout.astro
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/Header.astro

<interfaces>
Website build scripts:
```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check"
  }
}
```

Current website output:
```js
export default defineConfig({
  site: 'https://absenkok.vercel.app',
  output: 'static',
  adapter: vercel(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Supabase SSR utilities and portal middleware</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/package.json, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/middleware.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/package.json
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/astro.config.mjs
    - .planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
  </read_first>
  <action>
    Create the portal auth foundation in the website repo.

    Required work:
    1. Add `@supabase/supabase-js` and `@supabase/ssr` to the website repo dependencies.
    2. Create `src/lib/supabase/server.ts` that builds an SSR-safe Supabase server client from Astro request/response cookies.
    3. Create `src/lib/portal/auth.ts` for shared portal auth helpers:
       - normalize human search text for the login page
       - build the hidden portal auth email from the stable employee id
       - define which routes count as protected portal routes
    4. Create `src/middleware.ts`:
       - refresh/update auth cookies using the SSR client
       - treat `/portal` and `/portal/*` as protected routes except login and auth endpoints
       - redirect unauthenticated users to `/portal/login`
       - avoid touching marketing routes so the public site behavior stays unchanged

    Keep the site in static output mode. Do not switch the whole repo to `output: 'server'`; use route-level on-demand rendering instead.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The Astro website has SSR-safe Supabase helpers and middleware that can protect portal routes without making the whole site dynamic.</done>
</task>

<task type="auto">
  <name>Task 2: Build fast name-search login and server-rendered auth endpoints</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/search.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-in.ts, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-out.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/BaseLayout.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/components/Header.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
    - .planning/phases/37-portal-foundation-employee-auth/37-01-PLAN.md
  </read_first>
  <action>
    Add the first employee portal login/search pages and endpoints.

    Required behavior:
    1. `src/pages/portal/login.astro`
       - `export const prerender = false`
       - render a phone-friendly login card using the site's existing visual language
       - fields: searchable name input, hidden `employee_id`, and `password`
       - use a lightweight inline script instead of a client framework
       - only search after at least 2 characters
       - debounce requests around 200-300ms and cancel stale requests
       - cache recent search results in page memory so repeated prefix edits do not refetch unnecessarily
       - render capped identity cards with name, outlet, position, and photo/initial badge if available
       - selecting a card fills the hidden `employee_id`
       - form posts to `/portal/auth/sign-in`
       - preserve generic error copy on failure; do not leak whether a name or account exists

    2. `src/pages/portal/auth/search.ts`
       - `export const prerender = false`
       - accept a GET query such as `?q=andi`
       - normalize the query and reject very short searches
       - cap result count to a small value such as 8
       - call the backend RPC from plan 01 (`search_portal_employees`)
       - return only the minimal JSON payload the login chooser needs
       - add conservative response headers if useful, for example private short-lived browser caching, but do not rely on public shared caches

    3. `src/pages/portal/auth/sign-in.ts`
       - `export const prerender = false`
       - accept a POST form submission
       - require a selected `employee_id`, not a free-form name
       - derive the hidden auth email from the shared helper using the stable employee id
       - call Supabase Auth server-side with `signInWithPassword`
       - redirect to `/portal` on success, back to `/portal/login` on failure

    4. `src/pages/portal/auth/sign-out.ts`
       - `export const prerender = false`
       - clear the auth session server-side and redirect back to `/portal/login`

    Keep the implementation server-rendered with only minimal vanilla JS for the search chooser. Do not introduce a client framework or island unless it becomes strictly necessary.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The website has a fast, low-chatter name-search login page and server-rendered sign-in/sign-out endpoints for employee portal authentication.</done>
</task>

</tasks>

<verification>
- `npm run check` passes in the website repo
- portal login page exists with name-search chooser and password form fields
- middleware protects portal routes and redirects unauthenticated requests
- search, sign-in, and sign-out endpoints are on-demand routes
</verification>

<success_criteria>
- Employees can find themselves through fast name search, choose the right card, and sign in with password
- Protected portal routes reject unauthenticated access
- Auth cookies are maintained server-side for subsequent portal requests
- Marketing pages remain static and unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/37-portal-foundation-employee-auth/37-03-SUMMARY.md`
</output>
