---
phase: 37-portal-foundation-employee-auth
plan: 04
type: execute
wave: 3
depends_on: [37-01, 37-03]
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
autonomous: true
requirements: [AUTH-03, LINK-01]
must_haves:
  truths:
    - "An authenticated portal request resolves exactly one employee context before any portal content is shown"
    - "The initial portal home is protected, survives refresh, and shows employee identity context rather than schedule data"
    - "If identity resolution fails, the portal blocks further access instead of rendering a fake empty schedule state"
    - "Phase 38 has a reusable employee-context helper for schedule queries"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      provides: "Server-side employee-context resolver for authenticated portal requests"
      contains: "resolve_portal_employee"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro"
      provides: "Protected portal shell layout"
      contains: "Portal"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro"
      provides: "Initial employee portal home page"
      contains: "Jadwal akan tersedia"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      via: "server-side employee context lookup before render"
      pattern: "resolvePortalEmployee"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      to: "sql/phase_37_employee_portal_foundation_20260322.sql"
      via: "RPC call to resolve_portal_employee"
      pattern: "resolve_portal_employee"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/auth/sign-out.ts"
      via: "logout form action"
      pattern: "/portal/auth/sign-out"
---

<objective>
Create the protected employee portal shell and the first authenticated page that proves session persistence and one-to-one employee identity resolution.

Purpose: Phase 37 is complete only when the portal can render a protected employee-facing surface after auth, even before schedule data arrives in Phase 38.
Output: Portal layout, employee-context helper, and protected portal home page.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
@.planning/phases/37-portal-foundation-employee-auth/37-01-PLAN.md
@.planning/phases/37-portal-foundation-employee-auth/37-03-PLAN.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/BaseLayout.astro
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro

<interfaces>
Resolver contract expected from plan 01:
```sql
SELECT * FROM resolve_portal_employee();
```

Auth routes expected from plan 03:
```text
/portal/login
/portal/auth/search
/portal/auth/sign-in
/portal/auth/sign-out
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a reusable server-side employee-context helper</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts
    - .planning/phases/37-portal-foundation-employee-auth/37-01-PLAN.md
    - .planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
  </read_first>
  <action>
    Create `src/lib/portal/employee.ts` to hold the single server-side identity lookup used by the portal.

    Required behavior:
    1. Build a helper such as `resolvePortalEmployee(Astro)` or `resolvePortalEmployee(context)` using the server Supabase client.
    2. Call the backend resolver from plan 01 (`resolve_portal_employee`).
    3. Return a typed employee context object containing at least:
       - employee id
       - employee name
       - position
       - home outlet id
       - home outlet name
       - photo url
       - active badge id
    4. Return or throw a blocked-state result when no active employee mapping exists.

    This file becomes the single employee-context entry point for Phase 38 schedule queries. Do not duplicate the resolution join inside page files.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The portal repo has one reusable helper that resolves the authenticated employee context before later schedule work.</done>
</task>

<task type="auto">
  <name>Task 2: Build the protected portal shell and initial home page</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro, C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/BaseLayout.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/login.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
  </read_first>
  <action>
    Create the first authenticated portal page.

    Required behavior:
    1. `src/layouts/PortalLayout.astro`
       - lightweight employee-facing shell
       - brand continuity with the existing site, but visually distinct from the marketing homepage
       - logout form posting to `/portal/auth/sign-out`

    2. `src/pages/portal/index.astro`
       - `export const prerender = false`
       - call the employee-context helper before rendering anything meaningful
       - redirect unauthenticated users through existing middleware behavior
       - when the employee context resolves, render:
         - greeting with employee name
         - outlet and position summary
         - optional photo/initial badge presentation if already easy from the context payload
         - placeholder card such as `Jadwal akan tersedia di fase 38`
       - when the employee context does not resolve, render a blocked access card instead of any schedule-like UI

    Keep this page intentionally small. It should prove auth persistence and identity resolution, not implement schedule fetching.
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run build"</automated>
  </verify>
  <done>The portal has a protected, refresh-safe employee home page that only renders after one active employee context resolves successfully.</done>
</task>

</tasks>

<verification>
- `npm run build` passes in the website repo
- `/portal` is a protected, on-demand route
- portal home uses the shared employee resolver instead of ad hoc queries
- blocked identity resolution does not render schedule content
</verification>

<success_criteria>
- Portal sessions survive refresh on protected routes
- Employee identity resolves exactly once through a shared helper before portal content renders
- The initial employee portal shell is ready for schedule data work in Phase 38
</success_criteria>

<output>
After completion, create `.planning/phases/37-portal-foundation-employee-auth/37-04-SUMMARY.md`
</output>
