---
phase: 37-portal-foundation-employee-auth
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - lib/services/employee_portal_provisioning_service.dart
  - lib/screens/admin/admin_employees_screen.dart
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "An admin can provision employee portal access from the existing employee-management flow"
    - "Provisioning is blocked when the employee is inactive, archived, or unsaved, but not when employee_code is blank"
    - "Successful provisioning returns simple login handoff instructions based on name search plus password"
    - "Portal provisioning uses a dedicated service layer instead of embedding business rules directly in the widget"
  artifacts:
    - path: "lib/services/employee_portal_provisioning_service.dart"
      provides: "Flutter service wrapper for the employee portal provisioning Edge Function"
      contains: "provision-employee-portal-user"
    - path: "lib/screens/admin/admin_employees_screen.dart"
      provides: "Admin UI entry point for creating employee portal credentials"
      contains: "Buat Akses Portal"
  key_links:
    - from: "lib/screens/admin/admin_employees_screen.dart"
      to: "lib/services/employee_portal_provisioning_service.dart"
      via: "EmployeePortalProvisioningService.provisionAccess()"
      pattern: "EmployeePortalProvisioningService"
    - from: "lib/services/employee_portal_provisioning_service.dart"
      to: "supabase/functions/provision-employee-portal-user/index.ts"
      via: "functions.invoke('provision-employee-portal-user')"
      pattern: "provision-employee-portal-user"
---

<objective>
Add the admin-facing provisioning flow for employee portal credentials inside the existing employee-management surface.

Purpose: AUTH-01 is not complete until an operator can actually create initial employee portal access without leaving the product workflow.
Output: A Flutter service plus employee-management UI that provisions and reveals employee portal credentials safely.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
@.planning/phases/37-portal-foundation-employee-auth/37-01-PLAN.md
@lib/screens/admin/admin_employees_screen.dart
@supabase/functions/provision-employee-portal-user/index.ts

<interfaces>
Existing employee-management save payload in admin_employees_screen.dart:
```dart
final payload = {
  'name': _nameCtrl.text.trim(),
  'position': _positionCtrl.text.trim().isEmpty ? null : _positionCtrl.text.trim(),
  'employee_code': _empCodeCtrl.text.trim().isEmpty ? null : _empCodeCtrl.text.trim(),
  'home_outlet_id': _selectedOutletId,
  'is_active': _isActive,
};
```

Existing Edge Function invocation pattern:
```dart
final response = await _client.functions.invoke(
  'create-admin-user',
  body: {...},
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter service for portal provisioning</name>
  <files>lib/services/employee_portal_provisioning_service.dart</files>
  <read_first>
    - lib/services/admin_onboarding_service.dart
    - .planning/phases/37-portal-foundation-employee-auth/37-01-PLAN.md
    - supabase/functions/provision-employee-portal-user/index.ts
  </read_first>
  <action>
    Create `lib/services/employee_portal_provisioning_service.dart` following the existing admin-onboarding service pattern.

    Required contents:
    1. A typed result model for the provisioning response, for example `EmployeePortalProvisionResult`.
    2. A service class `EmployeePortalProvisioningService` with a `provisionAccess()` method that:
       - accepts `employeeId` and `password`
       - calls `functions.invoke('provision-employee-portal-user', body: ...)`
       - translates non-200 responses into user-facing exceptions
       - returns a typed result instead of raw JSON
    3. Small helper validation, such as rejecting blank passwords before the network call.

    Keep all function names and payload keys aligned with Plan 01 so the admin UI does not need to know implementation details like hidden auth emails or indexed search rules.
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/services/employee_portal_provisioning_service.dart</automated>
  </verify>
  <done>The Flutter app has a dedicated service that wraps employee portal provisioning cleanly and returns typed results.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the employee-management UI with a portal provisioning flow</name>
  <files>lib/screens/admin/admin_employees_screen.dart</files>
  <read_first>
    - lib/screens/admin/admin_employees_screen.dart
    - lib/services/employee_portal_provisioning_service.dart
    - .planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
  </read_first>
  <action>
    Add the employee portal provisioning entry point directly to `lib/screens/admin/admin_employees_screen.dart`.

    UI requirements:
    1. Surface a secondary action labeled `Buat Akses Portal` for existing employees.
    2. Disable or block the action with a clear reason when:
       - the employee is inactive or archived
       - the employee has not been saved yet
    3. Do not block provisioning just because `employee_code` is empty; portal login no longer depends on it.
    4. On action, open a focused dialog or bottom sheet that:
       - shows the employee name, outlet, and position as read-only context
       - optionally shows photo/initial badge context if already available in the model
       - collects a password and password confirmation
       - validates minimum password hygiene before submit
       - calls `EmployeePortalProvisioningService.provisionAccess()`
    5. After success, show a simple handoff receipt containing:
       - employee name
       - outlet and position summary
       - chosen password
       - copy action for quick sharing
       - short instruction text such as `Cari nama Anda di portal, pilih kartu yang cocok, lalu masukkan password ini`
    6. On failure, show a user-facing error without leaving the employee-management screen.

    Keep the scope tight:
    - do not build schedule UI here
    - do not add WhatsApp/PDF sharing to this phase
    - do not duplicate business rules already enforced in the service or Edge Function
  </action>
  <verify>
    <automated>C:\flutter\bin\flutter.bat analyze lib/screens/admin/admin_employees_screen.dart lib/services/employee_portal_provisioning_service.dart</automated>
  </verify>
  <done>Admins can provision employee portal credentials from the employee-management UI and hand the employee a simple name-search-plus-password login flow.</done>
</task>

</tasks>

<verification>
- `lib/services/employee_portal_provisioning_service.dart` analyzes cleanly
- `lib/screens/admin/admin_employees_screen.dart` analyzes cleanly with the new provisioning flow
- The UI contains an explicit `Buat Akses Portal` action and credential confirmation state
</verification>

<success_criteria>
- Admins can create initial employee portal credentials without leaving employee management
- The UI blocks invalid provisioning attempts early and clearly
- Portal provisioning business logic lives in a service instead of ad hoc widget code
</success_criteria>

<output>
After completion, create `.planning/phases/37-portal-foundation-employee-auth/37-02-SUMMARY.md`
</output>
