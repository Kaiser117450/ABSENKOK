---
phase: 38-employee-schedule-read-model
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts
  - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03]
must_haves:
  truths:
    - "Portal schedule loading stays server-side and reuses the authenticated employee context from Phase 37"
    - "Today's shift and current-week assignments are derived from one typed helper and one RPC call"
    - "The `/portal` placeholder is replaced with basic schedule data without pulling Phase 39's full state-polish work forward"
    - "Overnight rows render as one logical-day assignment with explicit next-day end context"
  artifacts:
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts"
      provides: "Typed server-side portal schedule loader and transformer"
      contains: "get_portal_schedule_week"
    - path: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro"
      provides: "Authenticated portal home with basic schedule visibility"
      contains: "Jadwal hari ini"
  key_links:
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts"
      to: "sql/phase_38_employee_schedule_read_model_20260322.sql"
      via: "RPC call to get_portal_schedule_week"
      pattern: "get_portal_schedule_week"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts"
      via: "reuse resolved portal identity before loading schedule rows"
      pattern: "resolvePortalEmployee"
    - from: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro"
      to: "C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts"
      via: "loadPortalSchedule() result drives the page"
      pattern: "loadPortalSchedule"
---

<objective>
Wire the employee portal to the new schedule read model with one typed server-side helper and one minimal portal page update.

Purpose: Phase 38 is not complete until the protected portal can actually retrieve and render today's shift and the current-week assignments for the authenticated employee.
Output: A portal schedule helper plus a basic `/portal` schedule surface that replaces the Phase 37 placeholder.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-portal-foundation-employee-auth/37-RESEARCH.md
@.planning/phases/38-employee-schedule-read-model/38-RESEARCH.md
@.planning/phases/38-employee-schedule-read-model/38-01-PLAN.md
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
@C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts

<interfaces>
Existing employee resolver:
```ts
const result = await resolvePortalEmployee(Astro);
if (!result.ok) {
  return { ok: false, reason: result.reason, message: result.message };
}
```

Existing protected portal shell:
```astro
<PortalLayout title="Beranda" employee={result.employee}>
  <slot />
</PortalLayout>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the typed server-side portal schedule helper</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/auth.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/supabase/server.ts
    - .planning/phases/38-employee-schedule-read-model/38-RESEARCH.md
    - .planning/phases/38-employee-schedule-read-model/38-01-PLAN.md
  </read_first>
  <action>
    Create `src/lib/portal/schedule.ts` as the single server-side entry point for portal schedule loading.

    Required behavior:
    1. Define typed portal schedule row/model interfaces, for example:
       - `PortalScheduleEntry`
       - `PortalScheduleModel`
       - a non-throwing result union similar to `resolvePortalEmployee()`

    2. Create a helper such as `loadPortalSchedule(Astro)` that:
       - resolves the employee through `resolvePortalEmployee(Astro)` first
       - computes one explicit portal-local `referenceDate` for the RPC call
       - calls `get_portal_schedule_week(reference_date => ...)`
       - never accepts `employeeId` from page params, forms, or query strings

    3. Normalize the RPC rows into page-friendly data:
       - keep `logicalDate`
       - expose `todayAssignment`
       - expose ordered `weekAssignments`
       - expose ordered `upcomingAssignments`
       - preserve `endsNextDay`
       - preserve `isDayOff`

    4. Keep the helper typed and reusable:
       - no raw row casting inside `index.astro`
       - centralize any business-local date assumption in this helper instead of duplicating it across pages
       - return a typed empty-state result when the employee simply has no assignments in the current week
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run check"</automated>
  </verify>
  <done>The website repo has one typed server-side helper that turns the portal schedule RPC into page-ready current-week data.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the portal placeholder with basic schedule visibility</name>
  <files>C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro</files>
  <read_first>
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/pages/portal/index.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/layouts/PortalLayout.astro
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/schedule.ts
    - C:/Users/HYPE R Series/Desktop/projekan/absenkok-website/src/lib/portal/employee.ts
  </read_first>
  <action>
    Update `src/pages/portal/index.astro` so the protected portal home shows real schedule data instead of the Phase 37 placeholder.

    Required behavior:
    1. Keep `export const prerender = false`.
    2. Load the schedule through `loadPortalSchedule(Astro)` and keep the existing unauthenticated / blocked-account protections intact.
    3. Replace the placeholder with a minimal data surface:
       - one "Jadwal hari ini" card showing outlet, shift label, and start/end times when today's assignment exists
       - one current-week list ordered by `logicalDate`
       - overnight rows show explicit next-day end context such as `Selesai besok`
    4. When the employee has no assignments this week:
       - show a simple placeholder card
       - do not build the full empty/error/not-linked state matrix here; Phase 39 owns that UX
    5. Keep the UI lightweight and server-rendered:
       - reuse `PortalLayout.astro`
       - do not reuse the admin schedule grid
       - do not introduce a client framework or a new auth flow

    Scope guard:
    - do not redesign logout in this phase
    - do not add portal request workflows
    - do not turn this into the full phone-polish phase early
  </action>
  <verify>
    <automated>powershell -Command "Set-Location 'C:/Users/HYPE R Series/Desktop/projekan/absenkok-website'; npm run build"</automated>
  </verify>
  <done>The protected portal home renders today's shift and current-week assignments from the new employee-scoped read model.</done>
</task>

</tasks>

<verification>
- `src/lib/portal/schedule.ts` exists and loads the current-week portal schedule through one typed helper
- `/portal` renders real schedule data for the authenticated employee instead of the Phase 37 placeholder
- the page still handles blocked identity resolution safely and renders overnight rows with next-day end context
</verification>

<success_criteria>
- Employee can retrieve today's assigned shift with outlet plus start/end times
- Employee can retrieve upcoming current-week assignments from the same protected page
- Portal schedule loading remains employee-scoped and server-side
</success_criteria>

<output>
After completion, create `.planning/phases/38-employee-schedule-read-model/38-02-SUMMARY.md`
</output>
