---
phase: 25-dashboard-ui-visualization
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - test/services/streak_service_test.dart
  - test/services/streak_badge_service_test.dart
  - test/screens/admin/chart_dashboard_screen_test.dart
  - test/screens/kiosk/kiosk_scan_streak_test.dart
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, GAME-02, GAME-03, GAME-04]

must_haves:
  truths:
    - "All 4 test stub files exist and compile without errors"
    - "Each test file contains at least one failing test per requirement it covers"
    - "Running flutter test on the stubs produces RED results (expected failures), not compilation errors"
  artifacts:
    - path: "test/services/streak_service_test.dart"
      provides: "Failing stubs for GAME-04 (leaderboard top 5 by current_streak)"
    - path: "test/services/streak_badge_service_test.dart"
      provides: "Failing stubs for GAME-02 (streak milestone detection), GAME-03 (auto-badge at 7/30/90)"
    - path: "test/screens/admin/chart_dashboard_screen_test.dart"
      provides: "Failing stubs for DASH-01 (dashboard sections), DASH-02 (fl_chart rendering), DASH-03 (admin-only outlet comparison), DASH-04 (keepalive mixin)"
    - path: "test/screens/kiosk/kiosk_scan_streak_test.dart"
      provides: "Failing stubs for GAME-02 (streak >= 2 visibility on kiosk scan result)"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 25 requirements so that implementation plans (25-01, 25-02) have automated behavioral verification.

Purpose: Nyquist compliance requires test stubs to exist before implementation. Each stub defines the expected behavior as a failing test (RED phase of TDD). Implementation plans then make these tests pass (GREEN).
Output: 4 test files with failing tests covering DASH-01 through DASH-04 and GAME-02 through GAME-04.
</objective>

<execution_context>
@C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v4.0-phases/25-dashboard-ui-visualization/25-RESEARCH.md
@.planning/milestones/v4.0-phases/25-dashboard-ui-visualization/25-VALIDATION.md
@test/services/analytics_service_test.dart

<interfaces>
<!-- Existing test patterns to follow -->

From test/services/analytics_service_test.dart:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:absensi_enakko_flutter/services/analytics_service.dart';

void main() {
  group('AnalyticsService', () {
    group('AttendanceRateData.fromJson', () {
      test('parses standard response correctly', () {
        // ... direct unit test of data class
      });
    });
  });
}
```

From lib/services/analytics_service.dart (existing, tested):
```dart
class AnalyticsService {
  static final instance = AnalyticsService._();
  Future<AttendanceRateData?> getAttendanceRates(...)
  Future<List<OvertimeFlag>> getOvertimeFlags(...)
}
```

From lib/services/badge_service.dart (existing):
```dart
class BadgeService {
  static final instance = BadgeService._();
  Future<void> assignBadge(String employeeId, String badgeId)
  Future<EmployeeBadge> createBadge({required String name, ...})
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for StreakService and StreakBadgeService</name>
  <files>test/services/streak_service_test.dart, test/services/streak_badge_service_test.dart</files>
  <read_first>
    - test/services/analytics_service_test.dart (test pattern to match)
    - pubspec.yaml (verify flutter_test in dev_dependencies)
  </read_first>
  <action>
1. Create `test/services/streak_service_test.dart` with stubs for GAME-04:

```dart
import 'package:flutter_test/flutter_test.dart';

/// Wave 0 stubs for StreakService (GAME-04: leaderboard top 5 by current_streak)
/// These tests will FAIL until 25-01 creates lib/services/streak_service.dart
void main() {
  group('StreakService', () {
    group('StreakLeaderEntry.fromJson', () {
      test('parses employee streak data correctly', () {
        // GAME-04: leaderboard entry deserialization
        // Will fail until StreakLeaderEntry class exists
        fail('WAVE 0 STUB: StreakLeaderEntry not yet implemented');
      });

      test('handles missing employee_name gracefully', () {
        // GAME-04: defensive parsing for null name
        fail('WAVE 0 STUB: StreakLeaderEntry not yet implemented');
      });
    });

    group('getLeaderboard', () {
      test('returns top 5 entries ordered by current_streak descending', () {
        // GAME-04: leaderboard ordering verification
        fail('WAVE 0 STUB: StreakService.getLeaderboard not yet implemented');
      });

      test('returns empty list when supabaseReady is false', () {
        // GAME-04: guard behavior
        fail('WAVE 0 STUB: StreakService.getLeaderboard not yet implemented');
      });
    });
  });
}
```

2. Create `test/services/streak_badge_service_test.dart` with stubs for GAME-02 and GAME-03:

```dart
import 'package:flutter_test/flutter_test.dart';

/// Wave 0 stubs for StreakBadgeService (GAME-02: milestone detection, GAME-03: auto-badge)
/// These tests will FAIL until 25-02 creates lib/services/streak_badge_service.dart
void main() {
  group('StreakBadgeService', () {
    group('checkAndAwardMilestone', () {
      test('returns 7 when currentStreak is exactly 7', () {
        // GAME-03: 7-day milestone detection
        fail('WAVE 0 STUB: StreakBadgeService not yet implemented');
      });

      test('returns 30 when currentStreak is exactly 30', () {
        // GAME-03: 30-day milestone detection
        fail('WAVE 0 STUB: StreakBadgeService not yet implemented');
      });

      test('returns 90 when currentStreak is exactly 90', () {
        // GAME-03: 90-day milestone detection
        fail('WAVE 0 STUB: StreakBadgeService not yet implemented');
      });

      test('returns null when currentStreak is not a milestone (e.g. 8)', () {
        // GAME-02: non-milestone streaks do not trigger badge
        fail('WAVE 0 STUB: StreakBadgeService not yet implemented');
      });

      test('returns null when currentStreak is 1 (below threshold)', () {
        // GAME-02: streak of 1 is not a milestone
        fail('WAVE 0 STUB: StreakBadgeService not yet implemented');
      });
    });
  });
}
```

Both files should compile cleanly (only use flutter_test, no app imports yet) and produce clear RED failures with descriptive messages.
  </action>
  <verify>
    <automated>cd "C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter" && C:\flutter\bin\flutter.bat test test/services/streak_service_test.dart test/services/streak_badge_service_test.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>Both test files exist, compile, and produce RED failures (not compilation errors). Each test has a descriptive WAVE 0 STUB message. streak_service_test.dart covers GAME-04, streak_badge_service_test.dart covers GAME-02 and GAME-03.</done>
</task>

<task type="auto">
  <name>Task 2: Create widget test stubs for ChartDashboardScreen and kiosk streak display</name>
  <files>test/screens/admin/chart_dashboard_screen_test.dart, test/screens/kiosk/kiosk_scan_streak_test.dart</files>
  <read_first>
    - test/screens/admin/admin_login_screen_test.dart (widget test pattern)
    - test/screens/admin/rekap_harian_test.dart (another widget test pattern)
  </read_first>
  <action>
1. Create `test/screens/admin/chart_dashboard_screen_test.dart` with stubs for DASH-01 through DASH-04:

```dart
import 'package:flutter_test/flutter_test.dart';

/// Wave 0 stubs for ChartDashboardScreen (DASH-01 through DASH-04)
/// These tests will FAIL until 25-01 creates lib/screens/admin/chart_dashboard_screen.dart
void main() {
  group('ChartDashboardScreen', () {
    group('DASH-01: Dashboard sections', () {
      test('renders attendance donut chart section', () {
        // DASH-01: donut chart section visible in scrollable dashboard
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });

      test('renders weekly trend bar chart section', () {
        // DASH-01: weekly trend section visible
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });

      test('renders overtime alerts section', () {
        // DASH-01: overtime section visible
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });

      test('renders streak leaderboard section with top 5', () {
        // DASH-01 + GAME-04: leaderboard section visible
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });
    });

    group('DASH-02: fl_chart rendering', () {
      test('uses PieChart widget for attendance donut', () {
        // DASH-02: fl_chart PieChart used (not CustomPainter)
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });

      test('uses BarChart widget for weekly trend', () {
        // DASH-02: fl_chart BarChart used
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });
    });

    group('DASH-03: Admin-only outlet comparison', () {
      test('shows outlet comparison section for admin role', () {
        // DASH-03: admin sees grouped bar chart
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });

      test('hides outlet comparison section for kepala_gerai role', () {
        // DASH-03: kepala_gerai does NOT see outlet comparison
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });
    });

    group('DASH-04: Memory safety', () {
      test('uses AutomaticKeepAliveClientMixin', () {
        // DASH-04: mixin applied for kiosk 24/7 stability
        fail('WAVE 0 STUB: ChartDashboardScreen not yet implemented');
      });
    });
  });
}
```

2. Create directory `test/screens/kiosk/` if it does not exist, then create `test/screens/kiosk/kiosk_scan_streak_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';

/// Wave 0 stubs for kiosk scan streak display (GAME-02)
/// These tests will FAIL until 25-02 modifies lib/screens/kiosk/kiosk_scan_screen.dart
void main() {
  group('KioskScanScreen streak display', () {
    test('shows streak count when streak >= 2 after masuk scan', () {
      // GAME-02: streak counter visible on kiosk scan result
      fail('WAVE 0 STUB: Streak display not yet added to KioskScanScreen');
    });

    test('hides streak count when streak < 2', () {
      // GAME-02: streak of 0 or 1 should not show fire icon
      fail('WAVE 0 STUB: Streak display not yet added to KioskScanScreen');
    });

    test('hides streak count for pulang scan type', () {
      // GAME-02: streak only shown for masuk, not pulang
      fail('WAVE 0 STUB: Streak display not yet added to KioskScanScreen');
    });

    test('shows milestone celebration text at 7-day streak', () {
      // GAME-03: milestone celebration text visible
      fail('WAVE 0 STUB: Streak display not yet added to KioskScanScreen');
    });

    test('shows milestone celebration text at 30-day streak', () {
      // GAME-03: 30-day milestone
      fail('WAVE 0 STUB: Streak display not yet added to KioskScanScreen');
    });

    test('shows special celebration text at 90-day streak', () {
      // GAME-03: 90-day milestone with "Luar Biasa!" text
      fail('WAVE 0 STUB: Streak display not yet added to KioskScanScreen');
    });
  });
}
```

Ensure `test/screens/kiosk/` directory exists before writing the file:
```bash
mkdir -p test/screens/kiosk
```

Both files should compile cleanly (only use flutter_test, no app imports yet) and produce clear RED failures.
  </action>
  <verify>
    <automated>cd "C:/Users/HYPE R Series/Desktop/projekan/absensi apk/absensi_enakko_flutter" && C:\flutter\bin\flutter.bat test test/screens/admin/chart_dashboard_screen_test.dart test/screens/kiosk/kiosk_scan_streak_test.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>Both widget test files exist, compile, and produce RED failures. chart_dashboard_screen_test.dart covers DASH-01 through DASH-04 with 10 stubs. kiosk_scan_streak_test.dart covers GAME-02 and GAME-03 with 6 stubs. test/screens/kiosk/ directory created.</done>
</task>

</tasks>

<verification>
1. `C:\flutter\bin\flutter.bat test test/services/streak_service_test.dart --no-pub` -- runs and shows RED failures (not compilation errors)
2. `C:\flutter\bin\flutter.bat test test/services/streak_badge_service_test.dart --no-pub` -- runs and shows RED failures
3. `C:\flutter\bin\flutter.bat test test/screens/admin/chart_dashboard_screen_test.dart --no-pub` -- runs and shows RED failures
4. `C:\flutter\bin\flutter.bat test test/screens/kiosk/kiosk_scan_streak_test.dart --no-pub` -- runs and shows RED failures
5. All 4 files contain only `flutter_test` imports (no app imports that would break before implementation)
</verification>

<success_criteria>
- 4 test stub files exist in the correct directories
- All files compile and run (RED failures, not compilation errors)
- Test stubs cover all 7 requirements: DASH-01, DASH-02, DASH-03, DASH-04, GAME-02, GAME-03, GAME-04
- Each test has a descriptive WAVE 0 STUB message indicating what needs to be implemented
- No app code imports (stubs are pure flutter_test until implementation plans wire them up)
</success_criteria>

<output>
After completion, create `.planning/milestones/v4.0-phases/25-dashboard-ui-visualization/25-00-SUMMARY.md`
</output>
