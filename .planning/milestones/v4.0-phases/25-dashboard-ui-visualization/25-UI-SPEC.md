---
phase: 25
slug: dashboard-ui-visualization
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-19
---

# Phase 25 — UI Design Contract

> Visual and interaction contract for the Dashboard UI + Visualization phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Flutter native — not a web project) |
| Preset | not applicable |
| Component library | Material 3 (via `ThemeData` with `useMaterial3: true`) |
| Icon library | Material Icons (existing convention — no iconsax, no third-party icon packages) |
| Font | Plus Jakarta Sans via `google_fonts` (already configured in `lib/core/theme.dart`) |

**Source:** Existing `lib/core/theme.dart` — `buildAppTheme()` + `AppColors` class.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, chip internal padding, inline spacing between icon + text |
| sm | 8px | Compact element spacing (chip row separators, overtime chip gaps, intra-card gaps between label and content) |
| md | 16px | Default card padding, section internal padding (matches existing `AppCard` default) |
| lg | 24px | Section padding between dashboard sections (rate card -> trend chart -> leaderboard) |
| xl | 32px | Top/bottom page padding for the scrollable dashboard |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Chart container height: 200px for donut chart, 180px for bar charts (not spacing — these are component dimensions)
- Leaderboard row height: 56px (comfortable touch target with avatar + text)
- Streak badge on kiosk scan: 44px diameter circle (touch-target compliant)

**Source:** Existing patterns — `AppCard` uses 16px padding, card margin is `EdgeInsets.symmetric(horizontal: 16, vertical: 4)`. All intra-card gaps standardized to 8px (sm token).

---

## Typography

| Role | Size | Weight | Line Height | Usage in This Phase |
|------|------|--------|-------------|---------------------|
| Body | 14px | 400 (regular) | 1.5 | Leaderboard employee names, chart labels, descriptive text |
| Label | 12px | 700 (bold) | 1.4 | Section headers ("Tingkat Kehadiran", "Tren Mingguan"), chart axis labels, overtime chip text |
| Heading | 20px | 700 (bold) | 1.2 | Screen title ("Dashboard"), section titles |
| Display | 28px | 700 (bold) | 1.1 | Hero metric numbers (attendance rate percentage, streak count on kiosk) |

**Weights used: 2 (regular 400 + bold 700).** Label role uses bold (700) instead of semibold (600) to stay within the 2-weight constraint. This is consistent with existing `AttendanceRateCard` which uses w700 for section headers.

**Source:** Existing `AttendanceRateCard` uses 28px/w700 for rate display, 12px/w700 for section headers, 14px for body text.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FFFFFF` (AppColors.background) | Scaffold background, card backgrounds |
| Secondary (30%) | `#F9FAFB` (AppColors.surface) / `#F3F4F6` (AppColors.surfaceVariant) | Card fill, chart background area, leaderboard row alternating fill |
| Accent (10%) | `#DC2626` (AppColors.primary) | See reserved-for list below |
| Destructive | `#DC2626` (AppColors.danger) | Not applicable this phase — no destructive actions |

### Accent Reserved For (10% Budget)

1. **AppBar background** — red header bar (existing pattern)
2. **Donut chart filled arc** — attendance rate percentage arc
3. **Selected period chip** — active choice chip in rate card (Hari/Minggu/Bulan)
4. **"Lihat Dashboard" CTA** — navigation button from admin dashboard to chart screen

### Semantic Colors for Charts

| Color | Hex | AppColors Field | Chart Usage |
|-------|-----|-----------------|-------------|
| Success green | `#16A34A` | `AppColors.success` | Attendance rate >= 80%, streak fire icon, "hadir" bar segments |
| Warning amber | `#F59E0B` | `AppColors.warning` | Overtime chips, attendance rate < 80% but >= 60%, streak milestone badge glow (7-day, 30-day, 90-day) |
| Danger red | `#DC2626` | `AppColors.danger` | Attendance rate < 60%, late arrival indicators |
| Blue info | `#3B82F6` | (local constant) | Cross-outlet comparison: Outlet B bar color (Outlet A uses primary red) |
| Purple | `#8B5CF6` | (local constant) | Cross-outlet comparison: Outlet C bar color |
| Teal | `#14B8A6` | (local constant) | Cross-outlet comparison: Outlet D bar color |

**`#F59E0B` consolidation:** A single `AppColors.warning` field serves both the "warning" semantic role (overtime chips, mid-range attendance) and the streak milestone badge glow. There is no separate `AppColors.accent` field at `#F59E0B` — all amber uses reference `AppColors.warning`. The 10% accent role in the 60/30/10 split is fulfilled by `AppColors.primary` (`#DC2626` red), not by amber.

**Multi-outlet bar chart color assignment:** Outlets are assigned colors in alphabetical order from the palette: `[#DC2626, #3B82F6, #8B5CF6, #14B8A6]`. Maximum 4 outlets (production constraint).

**Source:** AppColors class in `lib/core/theme.dart`. Semantic colors already defined. Chart-specific multi-outlet colors are new additions scoped to `chart_dashboard_screen.dart` only — not added to AppColors.

---

## Component Inventory

### New Widgets to Create

| Widget | Location | Consumes |
|--------|----------|----------|
| `ChartDashboardScreen` | `lib/screens/admin/chart_dashboard_screen.dart` | AnalyticsService, fl_chart |
| `AttendanceDonutChart` | inline in ChartDashboardScreen | `AttendanceRateData` |
| `WeeklyTrendBarChart` | inline in ChartDashboardScreen | `get_weekly_trend` RPC result |
| `StreakLeaderboardCard` | inline in ChartDashboardScreen | `employee_streaks` table data |
| `OutletComparisonChart` | inline in ChartDashboardScreen (admin-only) | `get_outlet_comparison` RPC result |
| `StreakBadgeOverlay` | added to kiosk scan success screen | `employee_streaks` current_streak |

### Existing Widgets to Reuse (No Modification)

| Widget | Usage in This Phase |
|--------|---------------------|
| `AppCard` | Container for each dashboard section |
| `AppBadge` | Status badges in leaderboard rows |
| `AppEmptyState` | Empty states for charts with no data |
| `BadgeAvatar` | Employee avatars in leaderboard |
| `AttendanceRateCard` | Embedded in chart dashboard (already exists from Phase 24) |
| `OvertimeAlertRow` | Embedded in chart dashboard (already exists from Phase 24) |
| `ShimmerSkeleton` | Loading states for chart sections |

### Existing Widgets to Modify

| Widget | Modification |
|--------|-------------|
| `KioskScanScreen` success section | Add streak count display after successful masuk scan |
| `AdminDashboardScreen` | Add "Lihat Dashboard" navigation button to chart dashboard |

---

## Screen Layout: ChartDashboardScreen

Single scrollable `ListView` with the following sections in order:

```
[AppBar: "Dashboard Kehadiran" — red, back arrow]
|  (Back arrow uses Flutter default semantic label:
|   MaterialLocalizations.of(context).backButtonTooltip)
|
+-- [16px top padding]
|
+-- Section 1: AttendanceRateCard (existing widget, reused as-is)
|   +-- Card with period toggle (Hari/Minggu/Bulan) + rate display
|
+-- [24px gap]
|
+-- Section 2: Donut Chart — "Ringkasan Kehadiran"
|   +-- AppCard containing:
|       +-- Label: "Ringkasan Kehadiran" (12px, w700, textSecondary)
|       +-- [8px gap]
|       +-- PieChart (fl_chart) — 200px height, centered
|       |   +-- Hadir arc: AppColors.success
|       |   +-- Tidak Hadir arc: AppColors.danger
|       |   +-- Center text: "{rate}%" (28px, w700)
|       +-- Legend row below chart: [green dot] Hadir  [red dot] Tidak Hadir
|
+-- [24px gap]
|
+-- Section 3: Weekly Trend — "Tren Mingguan"
|   +-- AppCard containing:
|       +-- Label: "Tren Mingguan" (12px, w700, textSecondary)
|       +-- [8px gap]
|       +-- BarChart (fl_chart) — 180px height
|           +-- X-axis: Sen, Sel, Rab, Kam, Jum, Sab, Min (last 7 days)
|           +-- Y-axis: employee count (integer)
|           +-- Bar color: AppColors.success (single color, one outlet)
|
+-- [24px gap]
|
+-- Section 4: Overtime Alerts — "Lembur Hari Ini"
|   +-- AppCard containing:
|       +-- Label: "Lembur Hari Ini" (12px, w700, textSecondary)
|       +-- [8px gap]
|       +-- OvertimeAlertRow (existing widget, reused as-is)
|
+-- [24px gap]
|
+-- Section 5: Streak Leaderboard — "Top 5 Streak Kehadiran"
|   +-- AppCard containing:
|       +-- Label: "Top 5 Streak Kehadiran" (12px, w700, textSecondary)
|       +-- [8px gap]
|       +-- Column of 5 LeaderboardRow widgets:
|           +-- Row: [Rank badge] [BadgeAvatar 40px] [Name 14px] [Spacer] [fire icon + streak count]
|           +-- Rank 1: gold background (#FEF3C7), rank text amber
|           +-- Rank 2: light gray background (#F3F4F6), rank text gray
|           +-- Rank 3: light gray background (#F3F4F6), rank text gray
|           +-- Rank 4-5: no background highlight
|
+-- [24px gap]
|
+-- Section 6 (ADMIN ONLY): Cross-Outlet Comparison — "Perbandingan Outlet"
|   +-- AppCard containing:
|       +-- Label: "Perbandingan Outlet" (12px, w700, textSecondary)
|       +-- [8px gap]
|       +-- BarChart (fl_chart) — 200px height, grouped bars
|           +-- X-axis: outlet names (abbreviated if > 10 chars)
|           +-- Y-axis: attendance rate percentage (0-100%)
|           +-- Bar colors: per-outlet from palette [red, blue, purple, teal]
|           +-- Legend row below chart with outlet color dots
|
+-- [32px bottom padding]
+-- End
```

**Visibility rule:** Section 6 is rendered only when `appState.role == 'admin'`. Kepala Gerai sees sections 1-5 scoped to their own outlet.

---

## Screen Layout: Kiosk Scan Success — Streak Addition

After successful masuk scan, the existing success screen adds:

```
[Existing success content: checkmark + confetti + employee name + "Masuk berhasil"]
|
+-- [16px gap]
|
+-- Streak Row (new):
    +-- Fire icon (Icons.local_fire_department, 24px, AppColors.warning amber)
    +-- [8px gap]
    +-- Text: "{N} hari berturut-turut!" (16px, w700, AppColors.textPrimary)
    +-- Visibility: only shown when streak >= 2 days
```

**Streak milestone celebration (GAME-03):**
- At 7-day milestone: show "Streak 7 Hari!" text in amber with a subtle scale animation (300ms, Curves.easeOutBack)
- At 30-day milestone: show "Streak 30 Hari!" with confetti burst (reuse existing `_confettiCtrl`)
- At 90-day milestone: show "Streak 90 Hari! Luar Biasa!" with confetti burst
- Auto-badge award happens silently in the service layer (BadgeService.awardBadge) — no UI confirmation needed beyond the streak display

---

## Copywriting Contract

| Element | Copy (Bahasa Indonesia) |
|---------|-------------------------|
| Primary CTA | "Lihat Dashboard" (button on admin dashboard navigating to chart screen) |
| Screen title | "Dashboard Kehadiran" |
| Donut section label | "Ringkasan Kehadiran" |
| Trend section label | "Tren Mingguan" |
| Overtime section label | "Lembur Hari Ini" |
| Leaderboard section label | "Top 5 Streak Kehadiran" |
| Outlet comparison label | "Perbandingan Outlet" |
| Empty state heading (charts) | "Belum Ada Data" |
| Empty state body (charts) | "Data kehadiran akan muncul setelah karyawan mulai absen di kiosk." |
| Empty state heading (leaderboard) | "Belum Ada Streak" |
| Empty state body (leaderboard) | "Streak akan terhitung setelah karyawan absen beberapa hari berturut-turut." |
| Error state | "Gagal memuat data. Periksa koneksi internet lalu tarik ke bawah untuk coba lagi." |
| Streak on kiosk (singular) | Not shown (streak < 2 hidden) |
| Streak on kiosk (plural) | "{N} hari berturut-turut!" |
| Streak milestone 7 | "Streak 7 Hari!" |
| Streak milestone 30 | "Streak 30 Hari!" |
| Streak milestone 90 | "Streak 90 Hari! Luar Biasa!" |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

**Language rule:** All user-facing copy in Bahasa Indonesia, matching existing app convention. No English strings in UI.

**Source:** Existing patterns from `AttendanceRateCard` ("Tingkat Kehadiran", "Belum Ada Data Kehadiran") and `AppEmptyState` ("Tidak ada data").

---

## Interaction Contract

### Pull-to-Refresh
- `RefreshIndicator` wraps the entire `ListView` in `ChartDashboardScreen`
- On pull: reload all sections in parallel (`Future.wait`)
- Spinner color: `AppColors.primary` (red)

### Loading States
- Each section shows `ShimmerSkeleton` placeholder matching the section's approximate height
- Sections load independently — a slow RPC does not block other sections from rendering

### Error States
- Per-section error: show inline error text (red, 12px) with "Tarik ke bawah untuk coba lagi"
- Full-screen error: only if the outlet ID is missing/invalid — show `AppEmptyState` with retry button

### Chart Interactions (fl_chart)
- Donut chart: static (no tap interaction) — center text shows current rate
- Bar charts: tap on bar shows tooltip with exact value (fl_chart `BarTouchData` with `touchTooltipData`)
- Tooltip style: dark background (`#111827`), white text (12px, w700), rounded corners (8px), auto-dismiss after 2s

### Navigation
- `AdminDashboardScreen` -> "Lihat Dashboard" button -> `ChartDashboardScreen` via GoRouter push
- `ChartDashboardScreen` AppBar back arrow -> pop back to admin dashboard

### Accessibility
- AppBar back arrow uses Flutter's built-in semantic label: `MaterialLocalizations.of(context).backButtonTooltip` (resolves to localized "Back" string). No custom `Semantics` wrapper needed.

### Memory Safety (DASH-04)
- `ChartDashboardScreen` uses `AutomaticKeepAliveClientMixin` to survive tab switches without rebuilding
- All `Timer` and `StreamSubscription` instances disposed in `dispose()`
- fl_chart widgets must NOT use `withOpacity()` in `paint()` — use pre-defined `Color` constants (matches existing `AppColors` pattern with pre-computed opacity variants)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| pub.dev: fl_chart | `fl_chart: ^0.69.0` | not required (pub.dev official, 5000+ GitHub stars, pure Dart) |
| shadcn | not applicable | not applicable (Flutter project, not web) |

**Note:** `fl_chart` is the only new dependency for this phase, as declared in REQUIREMENTS.md (DASH-02) and STATE.md. It is a pure Dart package with no native code, suitable for 24/7 kiosk operation.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
