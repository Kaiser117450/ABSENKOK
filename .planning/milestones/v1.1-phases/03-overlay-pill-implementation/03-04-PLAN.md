---
phase: 03-overlay-pill-implementation
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - lib/core/constants.dart
  - lib/providers/app_provider.dart
  - lib/app.dart
  - lib/services/kiosk_background_service.dart
  - lib/screens/kiosk/kiosk_idle_screen.dart
  - lib/screens/kiosk/kiosk_scan_screen.dart
  - .planning/phases/03-overlay-pill-implementation/03-04-OEM-VALIDATION.md
autonomous: false
requirements:
  - REQ-M2-01
  - REQ-M2-02

must_haves:
  truths:
    - "When app goes inactive/hidden/paused with kiosk session active, persistent idle overlay is requested"
    - "When app resumes, overlay hides only if foreground visibility toggle is OFF"
    - "Foreground overlay visibility preference is user-configurable and persisted across app restarts"
    - "Overlay remains non-blocking so taps/scrolls continue in the foreground app while the pill is visible"
    - "Overlay permission is re-checked on kiosk start with guided re-enable flow when denied"
    - "Overlay show/render failures surface warning toast feedback (not silent)"
    - "Kiosk reset stops background service and hides overlay to prevent stale floating UI"
    - "OEM validation checklist is completed on real devices for tap-through behavior and readability on light/dark backgrounds"
  artifacts:
    - path: "lib/providers/app_provider.dart"
      provides: "Persisted foreground overlay visibility preference in AppState"
      contains: "keepOverlayInForeground"
    - path: "lib/app.dart"
      provides: "Lifecycle policy wiring using overlay controller result handling"
      contains: "didChangeAppLifecycleState"
    - path: "lib/services/kiosk_background_service.dart"
      provides: "Overlay show path configured for non-blocking interaction/tap-through behavior"
      contains: "ensureOverlayVisible"
    - path: "lib/screens/kiosk/kiosk_idle_screen.dart"
      provides: "UI toggle + guided permission handling + reset teardown integration"
      contains: "_requestOverlayPermission"
    - path: "lib/screens/kiosk/kiosk_scan_screen.dart"
      provides: "Attendance success hook to push temporary event overlay state"
      contains: "_submitAttendance"
    - path: ".planning/phases/03-overlay-pill-implementation/03-04-OEM-VALIDATION.md"
      provides: "Real-device OEM validation checklist and pass/fail evidence"
      contains: "Device Matrix"
  key_links:
    - from: "AppState.keepOverlayInForeground"
      to: "app.dart resumed behavior"
      via: "conditional hideOverlayPill"
      pattern: "keepOverlayInForeground"
    - from: "_startBackgroundService"
      to: "Overlay permission + ensure visible call path"
      via: "requestOverlayPermission + guided dialog"
      pattern: "_startBackgroundService"
    - from: "ensureOverlayVisible showOverlay config"
      to: "foreground app interaction continuity"
      via: "non-blocking overlay flag and tap-through behavior"
      pattern: "OverlayFlag"
    - from: "_submitAttendance success branch"
      to: "KioskBackgroundService event update"
      via: "temporary event payload with attendance type"
      pattern: "_submitAttendance"
---

<objective>
Wire lifecycle policy, user preference, and failure UX so persistent overlay behavior is reliable in real kiosk usage.

Purpose: REQ-M2-01 requires lifecycle-triggered persistence, configurable foreground behavior, guided permission recovery, and non-silent failure feedback.

Output: App/provider/kiosk-screen/service integration that applies lifecycle policy, non-blocking overlay behavior, persisted foreground toggle, and real-device OEM validation evidence.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overlay-pill-implementation/03-CONTEXT.md
@.planning/phases/03-overlay-pill-implementation/03-RESEARCH.md
@.planning/phases/03-overlay-pill-implementation/03-02-SUMMARY.md
@.planning/phases/03-overlay-pill-implementation/03-03-SUMMARY.md
@lib/app.dart
@lib/providers/app_provider.dart
@lib/screens/kiosk/kiosk_idle_screen.dart
@lib/screens/kiosk/kiosk_scan_screen.dart
@lib/services/kiosk_background_service.dart

<interfaces>
Current lifecycle wiring:
```dart
if (state == AppLifecycleState.paused) {
  KioskBackgroundService.showOverlayPill(outletName: session?.outletName);
} else if (state == AppLifecycleState.resumed) {
  KioskBackgroundService.hideOverlayPill();
}
```

Current kiosk reset path:
```dart
await ref.read(appProvider.notifier).clearKioskSession();
if (mounted) context.go('/setup');
```

Current app state persistence:
```dart
final raw = prefs.getString(AppConstants.kioskSessionKey);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persisted foreground visibility preference in app state and constants</name>
  <files>lib/core/constants.dart, lib/providers/app_provider.dart</files>
  <action>
Introduce a persisted boolean preference for overlay visibility while app is foregrounded:
1. Add new key constant in `AppConstants` (e.g., `overlayKeepForegroundKey`).
2. Extend `AppState` with `keepOverlayInForeground` default `false`.
3. Load/store this preference in `AppNotifier` using SharedPreferences alongside kiosk session load.
4. Add a notifier setter method (e.g., `setKeepOverlayInForeground(bool value)`) that persists immediately.

Keep default `false` to preserve current behavior unless user explicitly opts in.
  </action>
  <verify>
    <automated>flutter analyze lib/core/constants.dart lib/providers/app_provider.dart</automated>
  </verify>
  <done>
    - App state contains `keepOverlayInForeground` with default false
    - Preference persists across app restarts
    - Setter method exists and is callable from UI layer
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply lifecycle policy, non-blocking overlay behavior, and runtime integration wiring</name>
  <files>lib/app.dart, lib/services/kiosk_background_service.dart, lib/screens/kiosk/kiosk_idle_screen.dart, lib/screens/kiosk/kiosk_scan_screen.dart, .planning/phases/03-overlay-pill-implementation/03-04-OEM-VALIDATION.md</files>
  <action>
Update runtime wiring to match locked decisions:
- In `didChangeAppLifecycleState`, treat `inactive`, `hidden`, and `paused` as background triggers for persistent idle overlay.
- On `resumed`, hide overlay only when `keepOverlayInForeground == false`.
- In `ensureOverlayVisible`, replace focus-grabbing overlay config with the package-supported non-blocking/tap-through configuration so foreground app interactions continue while overlay is visible.
- Consume `OverlayShowResult` from service calls; show toast warning for `permissionDenied` and `showFailed` outcomes.
- In kiosk start flow, re-check overlay permission each start; if denied, show guided re-enable dialog (reuse existing MIUI-aware path) and warning toast.
- Add a visible toggle UI in kiosk idle screen for "Tampilkan overlay saat aplikasi aktif" bound to provider setter.
- In `_submitAttendance` success branch, push an event overlay update (attendance type + accent + outlet + local time + short expiry), allowing automatic return to idle.
- In kiosk reset (`_confirmLogoutGerai` confirm path), call `KioskBackgroundService.stop()` before clearing session and routing to setup.
- Create `.planning/phases/03-overlay-pill-implementation/03-04-OEM-VALIDATION.md` with a concrete device matrix/checklist template covering tap-through, lifecycle, permission denial recovery, and light/dark readability checks.

Keep toast wording concise and operational (no silent logs-only failures). Do not alter attendance persistence flow logic.
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart lib/services/kiosk_background_service.dart lib/screens/kiosk/kiosk_idle_screen.dart lib/screens/kiosk/kiosk_scan_screen.dart</automated>
  </verify>
  <done>
    - Lifecycle policy handles inactive/hidden/paused/resumed with foreground toggle logic
    - Overlay show path is explicitly configured for non-blocking/tap-through behavior
    - Permission denied and show failure paths trigger toasts
    - Kiosk idle screen has functional foreground visibility toggle bound to persisted state
    - Permission is explicitly re-checked on kiosk start
    - Scan success updates overlay to temporary event state and returns to idle via existing timer contract
    - Reset path stops background service and overlay before session clear
    - OEM validation checklist artifact exists with concrete pass/fail rows
    - No new analyzer errors in touched files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run real-device OEM validation gate (tap-through + readability + lifecycle)</name>
  <what-built>Lifecycle integration, non-blocking overlay configuration, permission/failure UX, event-to-idle flow, and OEM checklist artifact.</what-built>
  <how-to-verify>
Use `.planning/phases/03-overlay-pill-implementation/03-04-OEM-VALIDATION.md` as the execution sheet and record actual device/OS/build for each run.

Required matrix:
1. At least 2 real devices with different OEM skins (one MIUI/HyperOS preferred + one Samsung/Pixel/Realme/ColorOS).
2. Background/minimize flow: overlay appears and remains persistent idle.
3. Tap-through flow: open another app and confirm taps/scrolls still work while overlay is visible (outside the pill hit area).
4. Overlay interaction flow: tap pill toggles expanded/minimized without trapping global interaction.
5. Readability flow: verify text/accent readability on both light and dark foreground app backgrounds.
6. Permission denial flow: deny overlay permission, confirm guided re-enable path + warning toast.
7. Failure feedback flow: trigger overlay show failure path (if reproducible) and confirm warning toast.

Gate rule: all critical rows in checklist must be marked PASS, or failures must be documented with blocking notes before continuing.
  </how-to-verify>
  <resume-signal>Type "approved" with checklist file updated, or provide failing checklist rows to continue with fixes.</resume-signal>
</task>

</tasks>

<verification>
After all tasks:
1. `flutter analyze lib/core/constants.dart lib/providers/app_provider.dart lib/app.dart lib/services/kiosk_background_service.dart lib/screens/kiosk/kiosk_idle_screen.dart lib/screens/kiosk/kiosk_scan_screen.dart` reports no errors.
2. `rg -n "keepOverlayInForeground|overlayKeepForegroundKey" lib/core/constants.dart lib/providers/app_provider.dart lib/screens/kiosk/kiosk_idle_screen.dart` confirms preference wiring.
3. `rg -n "AppLifecycleState\\.inactive|AppLifecycleState\\.hidden|AppLifecycleState\\.paused|AppLifecycleState\\.resumed" lib/app.dart` confirms lifecycle coverage.
4. `rg -n "OverlayFlag|ensureOverlayVisible" lib/services/kiosk_background_service.dart` confirms explicit non-blocking overlay configuration exists in show path.
5. `rg -n "KioskBackgroundService\\.stop\\(\\)" lib/screens/kiosk/kiosk_idle_screen.dart` confirms reset teardown safety.
6. `.planning/phases/03-overlay-pill-implementation/03-04-OEM-VALIDATION.md` is filled with real-device PASS/FAIL evidence and approved in Task 3 checkpoint.
</verification>

<success_criteria>
- Background/minimize reliably triggers persistent overlay without silent failure.
- Foreground behavior is user-controlled and persisted.
- Foreground app interaction stays usable (tap-through/non-blocking) while overlay remains visible.
- Permission denial and render failure paths are user-visible and guided.
- Kiosk reset no longer leaves stale background overlay/service artifacts.
- OEM validation is explicitly completed on real devices, including light/dark readability checks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-pill-implementation/03-04-SUMMARY.md`
with integration points, lifecycle behavior changes, non-blocking overlay notes, and OEM validation results.
</output>
