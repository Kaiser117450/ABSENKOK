---
phase: 03-overlay-pill-implementation
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/overlay_task.dart
  - test/widgets/overlay_pill_widget_test.dart
autonomous: true
requirements:
  - REQ-M2-01
  - REQ-M2-02

must_haves:
  truths:
    - "Overlay isolate renders persistent idle pill with outlet, attendance type/accent, and local `HH:mm`"
    - "Tap interaction toggles expanded/minimized states while keeping pill active"
    - "Temporary event state automatically returns to idle state without full overlay dismiss"
    - "Visual design remains compact dark premium pill (not notification-like) in both expanded and minimized variants"
    - "Pill content remains readable against both light and dark foreground-app backgrounds"
    - "State transitions keep slide/fade + subtle spring behavior"
  artifacts:
    - path: "lib/overlay_task.dart"
      provides: "Overlay isolate state-machine + premium pill renderer using typed payload model"
      contains: "KioskOverlayUI"
    - path: "test/widgets/overlay_pill_widget_test.dart"
      provides: "Widget-level checks for idle/event rendering, expand/minimize behavior, and light/dark readability"
      contains: "group('KioskOverlayUI'"
  key_links:
    - from: "overlayListener payload handler"
      to: "OverlayPillState.fromRaw"
      via: "Typed decode with legacy fallback"
      pattern: "fromRaw"
    - from: "event mode handler"
      to: "idle mode renderer"
      via: "eventUntilEpochMs timer-driven revert"
      pattern: "eventUntilEpochMs"
---

<objective>
Upgrade the overlay isolate UI (`overlay_task.dart`) to implement persistent idle/event state rendering with a premium compact pill design.

Purpose: REQ-M2-01 and REQ-M2-02 require the overlay itself (not just service triggers) to show type/accent/time, preserve expand/minimize interaction, and return event state back to persistent idle.

Output: Refactored `KioskOverlayUI` state-machine and visuals plus widget tests for key behaviors.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-overlay-pill-implementation/03-CONTEXT.md
@.planning/phases/03-overlay-pill-implementation/03-RESEARCH.md
@.planning/phases/03-overlay-pill-implementation/03-01-SUMMARY.md
@lib/models/overlay_pill_state.dart
@lib/overlay_task.dart
@lib/models/attendance_log.dart

<interfaces>
Current isolate state (simplified):
```dart
String _outletName = 'Kiosk Aktif';
String _timeStr = '--:--';
bool _isExpanded = true;
```

Current listener (legacy split):
```dart
final parts = raw.toString().split('|');
```

Color source of truth for attendance type:
```dart
extension AttendanceTypeExt on AttendanceType {
  Color get color { ... }
  String get label { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor overlay isolate state to typed idle/event model with timed revert</name>
  <files>lib/overlay_task.dart</files>
  <action>
Refactor `KioskOverlayUI` state management to consume `OverlayPillState`:
- Replace raw `_outletName/_timeStr` fields with current typed state object.
- Decode incoming payloads via `OverlayPillState.fromRaw`.
- Track event timer (`Timer? _eventResetTimer`) so event payloads auto-revert to idle when expired.
- Preserve tap-to-toggle expand/minimize behavior independent of mode.
- Keep legacy payload compatibility through the model parser (do not duplicate split logic in UI layer).

Do not remove the fallback clock update; keep local `HH:mm` fallback when external payload is stale/missing.
  </action>
  <verify>
    <automated>flutter analyze lib/overlay_task.dart</automated>
  </verify>
  <done>
    - Overlay listener no longer directly splits delimiter payloads
    - Event mode has explicit timer-based return to idle
    - Expand/minimize interaction still works
    - Dispose cleans both listener and event timer resources
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement premium compact two-line pill UI for expanded/minimized variants</name>
  <files>lib/overlay_task.dart</files>
  <action>
Update overlay visual structure to match locked decisions and REQ-M2-02:
- Expanded mode: compact 2-line density, dark semi-opaque background, rounded pill, micro-logo, outlet, attendance type with accent indicator, local time.
- Minimized mode: compact chip that still communicates active status (accent + time).
- Keep width near 90% of screen and height around 56dp for expanded state.
- Use smooth slide/fade + subtle spring transitions for mode/state changes.
- Keep text, accent chips, and logo contrast legible on both bright and dark underlying app surfaces.

Avoid heavy animation redesign; prioritize persistent stability and readability over decorative motion.
  </action>
  <verify>
    <automated>rg -n "_buildExpanded|_buildCollapsed|Animated|Color\\(0xFF1C1C1E\\)|attendanceType|accent" lib/overlay_task.dart</automated>
  </verify>
  <done>
    - Expanded and minimized variants both retain premium visual quality
    - Attendance type + accent remain visible in persistent state
    - Time remains local `HH:mm`
    - Foreground content stays readable on both light and dark backgrounds
    - UI no longer resembles a plain system notification card
  </done>
</task>

<task type="auto">
  <name>Task 3: Add widget tests for render states and expand/minimize interaction</name>
  <files>test/widgets/overlay_pill_widget_test.dart</files>
  <action>
Create widget tests validating critical overlay behavior:
1. Idle payload renders outlet + time + type/accent indicators.
2. Event payload reverts to idle after configured timeout.
3. Tap interaction toggles expanded/minimized layouts.
4. Legacy delimiter payload still renders without crash.
5. Readability validation runs for light and dark parent backgrounds (at minimum white and near-black), asserting key text/accent elements remain visible.

If direct plugin stream wiring is hard to mock, extract testable presenter/widget methods within `overlay_task.dart` and test those directly.
  </action>
  <verify>
    <automated>flutter test test/widgets/overlay_pill_widget_test.dart --plain-name "readability"</automated>
  </verify>
  <done>
    - Widget tests exist and pass for idle/event/toggle/fallback behavior
    - Readability checks exist for both light and dark background variants
    - Tests run without requiring Android overlay runtime
    - Regressions in overlay rendering/state-machine are catchable in CI
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `flutter analyze lib/overlay_task.dart` reports no errors.
2. `flutter test test/widgets/overlay_pill_widget_test.dart` passes.
3. `rg -n "OverlayPillState|eventUntilEpochMs|_eventResetTimer" lib/overlay_task.dart` confirms state-machine integration.
4. `flutter test test/widgets/overlay_pill_widget_test.dart --plain-name "readability"` passes for light/dark readability assertions.
</verification>

<success_criteria>
- Overlay UI now expresses persistent live-activity identity (type/accent/time) in both sizes.
- Event state always falls back to persistent idle without overlay dismissal.
- Critical renderer behavior is covered by automated widget tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-overlay-pill-implementation/03-03-SUMMARY.md`
with UI before/after notes, state-machine behavior, and test outcomes.
</output>
